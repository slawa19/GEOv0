# Negative fixtures (invalid on purpose)

These files are intentionally invalid and are meant for schema/validator tests.

- `missing_scenario_id.json` — omits required `scenario_id`.
- `missing_equivalent_fields.json` — omits both `equivalents` and `baseEquivalent`.

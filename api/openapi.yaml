openapi: 3.0.3
info:
  title: GEO Hub API
  version: 0.1
  description: |
    OpenAPI spec for GEO Hub (MVP v0.1). Derived from docs/ru/04-api-reference.md.
servers:
  - url: /api/v1
tags:
  - name: Auth
  - name: Participants
  - name: TrustLines
  - name: Payments
  - name: Balance
  - name: Clearing
  - name: Equivalents
  - name: Health
  - name: Admin
  - name: Simulator
security:
  - BearerAuth: []

paths:
  /health:
    get:
      tags: [Health]
      summary: Health check
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                required: [status, version, uptime_seconds, timestamp]
                properties:
                  status:
                    type: string
                    enum: [ok, degraded]
                  version:
                    type: string
                  environment:
                    type: string
                    nullable: true
                  uptime_seconds:
                    type: integer
                    minimum: 0
                  timestamp:
                    type: string
        '503':
          description: Service degraded
          content:
            application/json:
              schema:
                type: object
                required: [status, version, uptime_seconds, timestamp]
                properties:
                  status:
                    type: string
                    enum: [ok, degraded]
                  version:
                    type: string
                  environment:
                    type: string
                    nullable: true
                  uptime_seconds:
                    type: integer
                    minimum: 0
                  timestamp:
                    type: string
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /healthz:
    get:
      tags: [Health]
      summary: Health check (alias)
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
                    enum: [ok]
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /health/db:
    get:
      tags: [Health]
      summary: Database health check
      security: []
      responses:
        '200':
          description: DB OK
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
                    enum: [ok]
        '503':
          description: DB unavailable
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
                    enum: [error]
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/health/db:
    get:
      tags: [Health, Admin]
      summary: Authenticated database diagnostic
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
      responses:
        '200':
          description: DB OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDbHealth'
        '403':
          description: Admin token required
          content:
            application/json:
              schema:
                type: object
                required: [error]
                properties:
                  error:
                    type: object
                    required: [code, message]
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                      details:
                        type: object
                        nullable: true
        '503':
          description: Database unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDbHealth'
        '422':
          description: Validation error
          content:
            application/json:
              schema:
                type: object
                required: [error]
                properties:
                  error:
                    type: object
                    required: [code, message]
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                      details:
                        type: object
                        nullable: true
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/config:
    get:
      tags: [Admin]
      summary: Get runtime config (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Config items
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminConfigResponse'
        '403':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      tags: [Admin]
      summary: Patch runtime config subset (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminConfigPatchRequest'
      responses:
        '200':
          description: Updated keys
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminConfigPatchResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/whoami:
    get:
      tags: [Admin]
      summary: Who am I (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Current admin role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminWhoAmIResponse'
        '403':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/feature-flags:
    get:
      tags: [Admin]
      summary: Get feature flags (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Flags
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminFeatureFlags'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    patch:
      tags: [Admin]
      summary: Patch feature flags (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminFeatureFlagsPatchRequest'
      responses:
        '200':
          description: Flags
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminFeatureFlags'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/participants:
    get:
      tags: [Admin]
      summary: List participants (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: q
          schema:
            type: string
        - in: query
          name: status
          schema:
            type: string
        - in: query
          name: type
          schema:
            type: string
            enum: [person, business, hub]
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: per_page
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
      responses:
        '200':
          description: Participants list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminParticipantsListResponse'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/participants/stats:
    get:
      tags: [Admin]
      summary: Participants stats (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Participants stats
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminParticipantsStatsResponse'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/participants/{pid}/freeze:
    post:
      tags: [Admin]
      summary: Freeze participant (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/PidPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminParticipantActionRequest'
      responses:
        '200':
          description: Status updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminParticipantStatusResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/participants/{pid}/unfreeze:
    post:
      tags: [Admin]
      summary: Unfreeze participant (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/PidPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminParticipantActionRequest'
      responses:
        '200':
          description: Status updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminParticipantStatusResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/participants/{pid}/ban:
    post:
      tags: [Admin]
      summary: Ban participant (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/PidPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminParticipantActionRequest'
      responses:
        '200':
          description: Status updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminParticipantStatusChange'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/participants/{pid}/unban:
    post:
      tags: [Admin]
      summary: Unban participant (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/PidPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminParticipantActionRequest'
      responses:
        '200':
          description: Status updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminParticipantStatusChange'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/audit-log:
    get:
      tags: [Admin]
      summary: List audit log (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: q
          schema:
            type: string
        - in: query
          name: action
          schema:
            type: string
        - in: query
          name: object_type
          schema:
            type: string
        - in: query
          name: object_id
          schema:
            type: string
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: per_page
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        '200':
          description: Audit log items
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminAuditLogListResponse'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/equivalents:
    get:
      tags: [Admin]
      summary: List equivalents (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: include_inactive
          required: false
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: Equivalents list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquivalentsList'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      tags: [Admin]
      summary: Create equivalent (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminEquivalentCreateRequest'
      responses:
        '200':
          description: Equivalent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Equivalent'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/equivalents/{code}:
    patch:
      tags: [Admin]
      summary: Patch equivalent (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: path
          name: code
          required: true
          schema:
            $ref: '#/components/schemas/EquivalentCode'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminEquivalentUpdateRequest'
      responses:
        '200':
          description: Equivalent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Equivalent'
        '409':
          $ref: '#/components/responses/Conflict'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

    delete:
      tags: [Admin]
      summary: Delete equivalent (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: path
          name: code
          required: true
          schema:
            $ref: '#/components/schemas/EquivalentCode'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDeleteResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/equivalents/{code}/usage:
    get:
      tags: [Admin]
      summary: Equivalent usage (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: path
          name: code
          required: true
          schema:
            $ref: '#/components/schemas/EquivalentCode'
      responses:
        '200':
          description: Usage summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminEquivalentUsageResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/migrations:
    get:
      tags: [Admin]
      summary: Migrations status (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Migration status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminMigrationsStatus'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/trustlines:
    get:
      tags: [Admin]
      summary: List trustlines (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: equivalent
          required: false
          schema:
            type: string
          description: Equivalent code filter
        - in: query
          name: creditor
          required: false
          schema:
            type: string
          description: Creditor PID (trustline 'from')
        - in: query
          name: debtor
          required: false
          schema:
            type: string
          description: Debtor PID (trustline 'to')
        - in: query
          name: status
          required: false
          schema:
            type: string
            enum: [active, frozen, closed]
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: per_page
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
      responses:
        '200':
          description: Trustlines list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminTrustLinesListResponse'
        '403':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/trustlines/bottlenecks:
    get:
      tags: [Admin]
      summary: Trustlines bottlenecks (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: threshold
          required: false
          schema:
            type: number
            minimum: 0
            maximum: 1
            default: 0.10
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
        - in: query
          name: equivalent
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Bottlenecks list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminTrustLinesBottlenecksResponse'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/liquidity/summary:
    get:
      tags: [Admin]
      summary: Liquidity summary (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: equivalent
          required: false
          schema:
            type: string
        - in: query
          name: threshold
          required: false
          schema:
            type: number
            minimum: 0
            maximum: 1
            default: 0.10
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
      responses:
        '200':
          description: Liquidity summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminLiquiditySummaryResponse'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/incidents:
    get:
      tags: [Admin]
      summary: List incidents (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: per_page
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
      responses:
        '200':
          description: Incidents list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminIncidentsListResponse'
        '403':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/transactions/{tx_id}/abort:
    post:
      tags: [Admin]
      summary: Abort transaction (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: path
          name: tx_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Aborted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminAbortTxResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/graph/snapshot:
    get:
      tags: [Admin]
      summary: Graph snapshot (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: equivalent
          required: false
          schema:
            type: string
        - in: query
          name: include
          required: false
          schema:
            type: string
          description: Comma-separated extras to include (incidents,audit_log,transactions)
      responses:
        '200':
          description: Graph snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminGraphSnapshotResponse'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/graph/ego:
    get:
      tags: [Admin]
      summary: Ego graph snapshot (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: pid
          required: true
          schema:
            type: string
        - in: query
          name: depth
          required: false
          schema:
            type: integer
            enum: [1, 2]
            default: 1
        - in: query
          name: equivalent
          required: false
          schema:
            type: string
        - in: query
          name: status
          required: false
          schema:
            type: array
            items:
              type: string
          style: form
          explode: true
        - in: query
          name: include
          required: false
          schema:
            type: string
          description: Comma-separated extras to include (incidents,audit_log,transactions)
      responses:
        '200':
          description: Ego snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminGraphEgoResponse'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/graph/snapshot:
    get:
      tags: [Simulator]
      summary: Graph snapshot (simulator)
      parameters:
        - in: query
          name: equivalent
          required: true
          schema:
            $ref: '#/components/schemas/EquivalentCode'
      responses:
        '200':
          description: Graph snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorGraphSnapshot'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/graph/ego:
    get:
      tags: [Simulator]
      summary: Ego graph snapshot (simulator)
      parameters:
        - in: query
          name: equivalent
          required: true
          schema:
            $ref: '#/components/schemas/EquivalentCode'
        - in: query
          name: pid
          required: true
          schema:
            type: string
        - in: query
          name: depth
          required: false
          schema:
            type: integer
            enum: [1, 2]
            default: 1
      responses:
        '200':
          description: Ego snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorGraphSnapshot'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/events:
    get:
      tags: [Simulator]
      summary: Events stream (SSE)
      description: |
        Server-Sent Events stream. Each message contains one JSON event.

        This endpoint is modeled as `text/event-stream` in OpenAPI.
      parameters:
        - in: query
          name: equivalent
          required: true
          schema:
            $ref: '#/components/schemas/EquivalentCode'
        - in: header
          name: Last-Event-ID
          required: false
          description: |
            Resume after this previously observed SSE event ID. The server returns
            `410` when the cursor is malformed, belongs to another run, is ahead of
            the run, has been pruned, or cannot fit in the subscriber bootstrap queue.
          schema:
            type: string
      responses:
        '200':
          description: SSE stream
          content:
            text/event-stream:
              schema:
                type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '410':
          description: Replay cursor is invalid or cannot be delivered completely
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/events/poll:
    get:
      tags: [Simulator]
      summary: Events poll (fallback) - always empty in the MVP
      description: |
        The polling fallback for clients that cannot hold an SSE connection. It has never had a
        replay buffer: the handler returns an empty array and reads neither query parameter
        (`app/api/v1/simulator.py`, `# MVP: no replay buffer.`).

        Until this version the canon promised an array of `SimulatorEvent` - six variants a client
        would write parsing code for and never receive (`F-011-7`). The contract now states what
        exists. When a replay buffer is built, lifting `maxItems: 0` will be a deliberate contract
        change, not the belated delivery of this old promise.
      parameters:
        - in: query
          name: equivalent
          required: true
          schema:
            $ref: '#/components/schemas/EquivalentCode'
          description: >-
            Ignored in the MVP - the handler returns an empty array without reading it. Required so
            that the eventual replay buffer keeps the same call shape.
        - in: query
          name: after
          required: false
          schema:
            type: string
          description: >-
            Ignored in the MVP. Reserved for the cursor semantics a replay buffer would need; the
            previous wording, "Return events strictly after this event_id", described behaviour
            that does not exist.
      responses:
        '200':
          description: >-
            Always an empty array: the MVP has no replay buffer, so no event is ever returned.
          content:
            application/json:
              schema:
                # `maxItems: 0` makes the item schema unreachable, which is why it is left empty
                # rather than pointing at SimulatorEvent. That schema stays in this document - it
                # still describes the SSE stream, which is the debt recorded under RT-011-5.
                type: array
                maxItems: 0
                items: {}
              example: []
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/scenarios:
    get:
      tags: [Simulator]
      summary: List simulator scenarios (Real Mode control plane)
      responses:
        '200':
          description: Scenarios list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScenariosListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
    post:
      tags: [Simulator]
      summary: Upload a simulator scenario (Real Mode control plane)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScenarioUploadRequest'
      responses:
        '200':
          description: Scenario summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScenarioSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/scenarios/{scenario_id}:
    get:
      tags: [Simulator]
      summary: Get simulator scenario summary (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/ScenarioIdPath'
      responses:
        '200':
          description: Scenario summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScenarioSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/scenarios/{scenario_id}/graph/preview:
    get:
      tags: [Simulator]
      summary: Preview scenario graph topology (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/ScenarioIdPath'
        - in: query
          name: equivalent
          required: true
          schema:
            $ref: '#/components/schemas/EquivalentCode'
        - in: query
          name: mode
          required: false
          schema:
            type: string
            enum: [fixtures, real]
            default: fixtures
      responses:
        '200':
          description: Scenario graph snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorGraphSnapshot'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/session/ensure:
    post:
      tags: [Simulator]
      summary: Ensure anonymous session
      description: |
        If a valid `geo_sim_sid` cookie exists, returns actor info.
        Otherwise, issues a new cookie session and returns actor info.

        This endpoint is intentionally unauthenticated to support anonymous visitors.
      security: []
      responses:
        '200':
          description: Actor info (anonymous)
          content:
            application/json:
              schema:
                type: object
                required: [actor_kind, owner_id]
                properties:
                  actor_kind:
                    type: string
                    enum: [anon]
                  owner_id:
                    type: string

  /simulator/admin/runs:
    get:
      tags: [Simulator]
      summary: List all runs (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: state
          required: false
          schema:
            type: string
          description: Optional state filter (e.g. running|paused|stopping|stopped|error)
        - in: query
          name: owner_id
          required: false
          schema:
            type: string
          description: Optional owner_id exact match filter
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            default: 50
            minimum: 1
            maximum: 200
        - in: query
          name: offset
          required: false
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        '200':
          description: Paginated list of runs
          content:
            application/json:
              schema:
                type: object
                required: [items, total, limit, offset]
                properties:
                  items:
                    type: array
                    items:
                      type: object
                      required: [run_id, scenario_id, mode, state, owner_id]
                      properties:
                        run_id:
                          type: string
                        scenario_id:
                          type: string
                        mode:
                          type: string
                        state:
                          type: string
                        owner_id:
                          type: string
                        owner_kind:
                          type: string
                          nullable: true
                        intensity_percent:
                          type: integer
                        started_at:
                          type: string
                          nullable: true
                        stopped_at:
                          type: string
                          nullable: true
                        sim_time_ms:
                          type: integer
                        ops_sec:
                          type: number
                        errors_total:
                          type: integer
                        committed_total:
                          type: integer
                        rejected_total:
                          type: integer
                        attempts_total:
                          type: integer
                  total:
                    type: integer
                  limit:
                    type: integer
                  offset:
                    type: integer
        '403':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/admin/runs/stop-all:
    post:
      tags: [Simulator]
      summary: Stop all active runs (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: state
          required: false
          schema:
            type: string
            enum: [running, paused, stopping, '*']
            default: '*'
          description: State filter for stopping runs
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Stop-all result
          content:
            application/json:
              schema:
                type: object
                required: [stopped]
                properties:
                  stopped:
                    type: integer
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs:
    post:
      tags: [Simulator]
      summary: Start a simulator run (Real Mode control plane)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RunCreateRequest'
      responses:
        '200':
          description: Run created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunCreateResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/active:
    get:
      tags: [Simulator]
      summary: Get active simulator run id (Real Mode control plane)
      responses:
        '200':
          description: Active run response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActiveRunResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}:
    get:
      tags: [Simulator]
      summary: Get simulator run status (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      responses:
        '200':
          description: Run status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/pause:
    post:
      tags: [Simulator]
      summary: Pause simulator run (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      responses:
        '200':
          description: Run status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/resume:
    post:
      tags: [Simulator]
      summary: Resume simulator run (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      responses:
        '200':
          description: Run status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/stop:
    post:
      tags: [Simulator]
      summary: Stop simulator run (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
        - in: query
          name: source
          required: false
          schema:
            type: string
        - in: query
          name: reason
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Run status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/restart:
    post:
      tags: [Simulator]
      summary: Restart simulator run (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      responses:
        '200':
          description: Run status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/intensity:
    post:
      tags: [Simulator]
      summary: Set simulator run intensity (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetIntensityRequest'
      responses:
        '200':
          description: Run status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunStatus'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/events:
    get:
      tags: [Simulator]
      summary: Run events stream (SSE) (Real Mode control plane)
      description: |
        Server-Sent Events stream. Each message contains one JSON event.

        This endpoint is modeled as `text/event-stream` in OpenAPI.
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
        - $ref: '#/components/parameters/EquivalentQuery'
        - in: header
          name: Last-Event-ID
          required: false
          description: |
            Resume after this previously observed SSE event ID. If the cursor is
            malformed, belongs to another run, is ahead of the run, has been pruned,
            or cannot fit in the subscriber bootstrap queue, the server returns `410`.
          schema:
            type: string
      responses:
        '200':
          description: SSE stream
          content:
            text/event-stream:
              schema:
                type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '410':
          description: Replay cursor is invalid or cannot be delivered completely
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/actions/tx-once:
    post:
      tags: [Simulator]
      summary: Emit one debug transaction event (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TxOnceRequestBody'
      responses:
        '200':
          description: Transaction emitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TxOnceResponseBody'
        '400':
          description: Simulator action validation error (flat envelope)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionError'
        '403':
          description: Simulator actions disabled or access denied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          description: Invalid simulator identity transport (for example, X-Simulator-Owner)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/actions/clearing-once:
    post:
      tags: [Simulator]
      summary: Emit one debug clearing event (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClearingOnceRequestBody'
      responses:
        '200':
          description: Clearing emitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClearingOnceResponseBody'
        '400':
          description: Simulator action validation error (flat envelope)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionError'
        '403':
          description: Simulator actions disabled or access denied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          description: Invalid simulator identity transport (for example, X-Simulator-Owner)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/graph/snapshot:
    get:
      tags: [Simulator]
      summary: Graph snapshot for a run (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
        - $ref: '#/components/parameters/EquivalentQuery'
      responses:
        '200':
          description: Graph snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorGraphSnapshot'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/metrics:
    get:
      tags: [Simulator]
      summary: Metrics time-series for a run (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
        - $ref: '#/components/parameters/EquivalentQuery'
        - in: query
          name: from_ms
          required: true
          schema:
            type: integer
            minimum: 0
        - in: query
          name: to_ms
          required: true
          schema:
            type: integer
            minimum: 0
        - in: query
          name: step_ms
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Metrics time-series
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetricsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/bottlenecks:
    get:
      tags: [Simulator]
      summary: Bottlenecks for a run (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
        - $ref: '#/components/parameters/EquivalentQuery'
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
        - in: query
          name: min_score
          required: false
          schema:
            type: number
            minimum: 0
            maximum: 1
      responses:
        '200':
          description: Bottlenecks list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BottlenecksResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/artifacts:
    get:
      tags: [Simulator]
      summary: Artifacts index for a run (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      responses:
        '200':
          description: Artifacts index
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtifactIndex'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/artifacts/{name}:
    get:
      tags: [Simulator]
      summary: Download a specific artifact for a run (Real Mode control plane)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
        - $ref: '#/components/parameters/ArtifactNamePath'
      responses:
        '200':
          description: Artifact content
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/clearing/cycles:
    get:
      tags: [Admin]
      summary: Find clearing cycles (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - in: query
          name: participant_pid
          required: false
          schema:
            type: string
        - in: query
          name: equivalent
          required: false
          schema:
            type: string
        - in: query
          name: max_depth
          required: false
          schema:
            type: integer
            minimum: 3
            maximum: 10
            default: 6
      responses:
        '200':
          description: Clearing cycles
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminClearingCyclesResponse'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /admin/participants/{pid}/metrics:
    get:
      tags: [Admin]
      summary: Participant analytics/metrics (admin)
      security: []
      parameters:
        - in: header
          name: X-Admin-Token
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/PidPath'
        - in: query
          name: equivalent
          required: false
          schema:
            type: string
        - in: query
          name: threshold
          required: false
          schema:
            type: number
            minimum: 0
            maximum: 1
      responses:
        '200':
          description: Metrics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminParticipantMetricsResponse'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /equivalents:
    get:
      tags: [Equivalents]
      summary: List active equivalents
      responses:
        '200':
          description: Equivalents list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquivalentsList'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /auth/challenge:
    post:
      tags: [Auth]
      summary: Start challenge-response auth flow
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChallengeRequest'
      responses:
        '200':
          description: Challenge created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChallengeResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /auth/login:
    post:
      tags: [Auth]
      summary: Complete challenge-response and issue JWT tokens
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Tokens issued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenPair'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /auth/refresh:
    post:
      tags: [Auth]
      summary: Exchange refresh token for a new token pair
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshRequest'
      responses:
        '200':
          description: Tokens issued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenPair'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /participants:
    get:
      tags: [Participants]
      summary: Search participants
      parameters:
        - in: query
          name: q
          schema:
            type: string
          description: Search query
        - in: query
          name: type
          schema:
            type: string
            enum: [person, business, hub]
          description: Type filter
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
          description: Max results
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
          description: Page number (optional; used with per_page)
        - in: query
          name: per_page
          schema:
            type: integer
            minimum: 1
            maximum: 200
          description: Items per page (optional; used with page)
      responses:
        '200':
          description: Participants list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParticipantsList'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

    post:
      tags: [Participants]
      summary: Register participant
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ParticipantCreateRequest'
      responses:
        '201':
          description: Participant created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Participant'
        '400':
          $ref: '#/components/responses/BadRequest'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /participants/search:
    get:
      tags: [Participants]
      summary: Search participants (alias)
      parameters:
        - in: query
          name: q
          schema:
            type: string
          description: Search query
        - in: query
          name: type
          schema:
            type: string
            enum: [person, business, hub]
          description: Type filter
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
          description: Max results
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
          description: Page number (optional; used with per_page)
        - in: query
          name: per_page
          schema:
            type: integer
            minimum: 1
            maximum: 200
          description: Items per page (optional; used with page)
      responses:
        '200':
          description: Participants list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParticipantsList'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /participants/me:
    get:
      tags: [Participants]
      summary: Get current participant profile with stats
      responses:
        '200':
          description: Current participant with stats
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParticipantWithStats'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

    patch:
      tags: [Participants]
      summary: Update current participant profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ParticipantUpdateRequest'
      responses:
        '200':
          description: Updated participant
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Participant'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /participants/{pid}:
    get:
      tags: [Participants]
      summary: Get participant
      parameters:
        - $ref: '#/components/parameters/PidPath'
      responses:
        '200':
          description: Participant
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Participant'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /trustlines:
    get:
      tags: [TrustLines]
      summary: List trustlines
      parameters:
        - in: query
          name: direction
          schema:
            type: string
            enum: [outgoing, incoming, all]
            default: all
        - in: query
          name: equivalent
          schema:
            type: string
        - in: query
          name: status
          schema:
            type: string
            enum: [active, frozen, closed]
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: per_page
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
      responses:
        '200':
          description: Trustlines list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrustLinesList'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

    post:
      tags: [TrustLines]
      summary: Create trustline
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrustLineCreateRequest'
      responses:
        '201':
          description: Trustline created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrustLine'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /trustlines/{id}:
    get:
      tags: [TrustLines]
      summary: Get trustline
      parameters:
        - $ref: '#/components/parameters/TrustLineIdPath'
      responses:
        '200':
          description: Trustline
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrustLine'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

    patch:
      tags: [TrustLines]
      summary: Update trustline
      parameters:
        - $ref: '#/components/parameters/TrustLineIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrustLineUpdateRequest'
      responses:
        '200':
          description: Trustline updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrustLine'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

    delete:
      tags: [TrustLines]
      summary: Close trustline
      parameters:
        - $ref: '#/components/parameters/TrustLineIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrustLineCloseRequest'
      responses:
        '200':
          description: Closed
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /clearing/cycles:
    get:
      tags: [Clearing]
      summary: List debt cycles for an equivalent
      parameters:
        - in: query
          name: equivalent
          required: true
          schema:
            type: string
        - in: query
          name: max_depth
          required: false
          schema:
            type: integer
            minimum: 3
            maximum: 10
            default: 6
      responses:
        '200':
          description: Cycles
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClearingCyclesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /clearing/auto:
    post:
      tags: [Clearing]
      summary: Auto-clear cycles for an equivalent
      parameters:
        - in: query
          name: equivalent
          required: true
          schema:
            type: string
        - in: query
          name: max_depth
          required: false
          schema:
            type: integer
            minimum: 3
            maximum: 10
            default: 6
      responses:
        '200':
          description: Clearing result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClearingAutoResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /payments/capacity:
    get:
      tags: [Payments]
      summary: Check capacity for a concrete payment amount
      parameters:
        - in: query
          name: to
          required: true
          schema:
            type: string
          description: Recipient PID
        - in: query
          name: equivalent
          required: true
          schema:
            type: string
        - in: query
          name: amount
          required: true
          schema:
            type: string
            pattern: '^[0-9]+(\.[0-9]+)?$'
          description: Amount as decimal string
      responses:
        '200':
          description: Capacity result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CapacityResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /payments/max-flow:
    get:
      tags: [Payments]
      summary: Estimate maximum transferable amount and diagnostics
      parameters:
        - in: query
          name: to
          required: true
          schema:
            type: string
          description: Recipient PID
        - in: query
          name: equivalent
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Max-flow result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaxFlowResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /payments:
    post:
      tags: [Payments]
      summary: Create payment
      parameters:
        - in: header
          name: Idempotency-Key
          required: false
          deprecated: true
          schema:
            type: string
            maxLength: 128
          description: >
            **Deprecated.** This header is accepted but ignored. Use the mandatory `tx_id` field
            in the request body for idempotency instead. Will be removed in a future API version.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentCreateRequest'
      responses:
        '200':
          description: Payment created (committed or aborted)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentResult'
        '409':
          $ref: '#/components/responses/Conflict'
        '401':
          description: Missing or invalid bearer authentication
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/ErrorEnvelope'
                  - type: object
                    required: [detail]
                    properties:
                      detail:
                        type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          description: Participant is not allowed to create payments
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '429':
          description: Payment rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Payment processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '504':
          description: Payment operation timed out
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'

    get:
      tags: [Payments]
      summary: List payments
      parameters:
        - in: query
          name: direction
          schema:
            type: string
            enum: [sent, received, all]
            default: all
        - in: query
          name: equivalent
          schema:
            type: string
        - in: query
          name: status
          schema:
            type: string
            enum: [COMMITTED, ABORTED, all]
            default: all
          description: Final payment outcome filter. This API exposes only final statuses (`COMMITTED` or `ABORTED`).
        - in: query
          name: from_date
          schema:
            type: string
            format: date-time
        - in: query
          name: to_date
          schema:
            type: string
            format: date-time
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: per_page
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
      responses:
        '200':
          description: Payments list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentsList'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /payments/{tx_id}:
    get:
      tags: [Payments]
      summary: Get payment by tx_id
      parameters:
        - in: path
          name: tx_id
          required: true
          schema:
            type: string
          description: Transaction id
      responses:
        '200':
          description: Payment details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentResult'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /balance:
    get:
      tags: [Balance]
      summary: Get aggregated balance by equivalents
      responses:
        '200':
          description: Balance summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BalanceSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /balance/debts:
    get:
      tags: [Balance]
      summary: Get debts details
      parameters:
        - in: query
          name: equivalent
          required: true
          schema:
            type: string
        - in: query
          name: direction
          schema:
            type: string
            enum: [outgoing, incoming, all]
            default: all
      responses:
        '200':
          description: Debts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebtsDetails'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /integrity/status:
    get:
      tags: [Integrity]
      summary: Get integrity status
      responses:
        '200':
          description: Current integrity status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrityStatusResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /integrity/checksum/{equivalent}:
    get:
      tags: [Integrity]
      summary: Get latest integrity checksum for equivalent
      parameters:
        - in: path
          name: equivalent
          required: true
          schema:
            $ref: '#/components/schemas/EquivalentCode'
      responses:
        '200':
          description: Latest integrity checkpoint checksum
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrityChecksumResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /integrity/verify:
    post:
      tags: [Integrity]
      summary: Run invariant verification
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IntegrityVerifyRequest'
      responses:
        '200':
          description: Verification results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrityVerifyResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /integrity/repair/net-mutual-debts:
    post:
      tags: [Integrity]
      summary: Repair net mutual debts
      responses:
        '200':
          description: Repair result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrityNetMutualDebtsRepairResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /integrity/repair/cap-debts-to-trust-limits:
    post:
      tags: [Integrity]
      summary: Repair cap debts to trust limits
      responses:
        '200':
          description: Repair result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrityCapDebtsRepairResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /integrity/audit-log:
    get:
      tags: [Integrity]
      summary: List integrity verification audit log entries
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: per_page
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
      responses:
        '200':
          description: Audit log items
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrityAuditLogResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  # Interact Mode control plane (011/T1101, published 2026-08-23).
  # These eight operations were registered and callable all along; only their schema
  # visibility was tied to SIMULATOR_ACTIONS_ENABLE, which gated nothing - the runtime
  # guard returns 403 ACTIONS_DISABLED independently of it. Errors here use the flat
  # SimulatorActionError envelope, not ErrorEnvelope: they are assembled by _action_error,
  # which returns a raw JSONResponse, so response_model of the route does not apply.

  /simulator/runs/{run_id}/actions/trustline-create:
    post:
      tags: [Simulator]
      summary: Create a trust line inside a run (Interact Mode)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SimulatorActionTrustlineCreateRequest'
      responses:
        '200':
          description: Trust line created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionTrustlineCreateResponse'
        '400':
          $ref: '#/components/responses/SimulatorActionBadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/SimulatorActionForbidden'
        '404':
          $ref: '#/components/responses/SimulatorActionNotFound'
        '409':
          $ref: '#/components/responses/SimulatorActionConflict'
        '422':
          $ref: '#/components/responses/SimulatorActionUnprocessable'
        '503':
          description: Action unavailable - RUN_PERIMETER_UNAVAILABLE, TRUSTLINE_USED_UNAVAILABLE or SEEDING_FAILED (flat envelope)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionError'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/actions/trustline-update:
    post:
      tags: [Simulator]
      summary: Change the limit of a trust line inside a run (Interact Mode)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SimulatorActionTrustlineUpdateRequest'
      responses:
        '200':
          description: Trust line limit changed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionTrustlineUpdateResponse'
        '400':
          $ref: '#/components/responses/SimulatorActionBadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/SimulatorActionForbidden'
        '404':
          $ref: '#/components/responses/SimulatorActionNotFound'
        '409':
          $ref: '#/components/responses/SimulatorActionConflict'
        '422':
          $ref: '#/components/responses/SimulatorActionUnprocessable'
        '503':
          description: Action unavailable - RUN_PERIMETER_UNAVAILABLE, TRUSTLINE_USED_UNAVAILABLE or SEEDING_FAILED (flat envelope)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionError'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/actions/trustline-close:
    post:
      tags: [Simulator]
      summary: Close a trust line inside a run (Interact Mode)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SimulatorActionTrustlineCloseRequest'
      responses:
        '200':
          description: Trust line closed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionTrustlineCloseResponse'
        '400':
          $ref: '#/components/responses/SimulatorActionBadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/SimulatorActionForbidden'
        '404':
          $ref: '#/components/responses/SimulatorActionNotFound'
        '409':
          $ref: '#/components/responses/SimulatorActionConflict'
        '422':
          $ref: '#/components/responses/SimulatorActionUnprocessable'
        '503':
          description: Action unavailable - RUN_PERIMETER_UNAVAILABLE, TRUSTLINE_USED_UNAVAILABLE or SEEDING_FAILED (flat envelope)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionError'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/actions/payment-real:
    post:
      tags: [Simulator]
      summary: Execute a real payment inside a run (Interact Mode)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SimulatorActionPaymentRealRequest'
      responses:
        '200':
          description: Payment executed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionPaymentRealResponse'
        '400':
          $ref: '#/components/responses/SimulatorActionBadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/SimulatorActionForbidden'
        '404':
          $ref: '#/components/responses/SimulatorActionNotFound'
        '409':
          $ref: '#/components/responses/SimulatorActionConflict'
        '422':
          $ref: '#/components/responses/SimulatorActionUnprocessable'
        '503':
          description: Action unavailable - RUN_PERIMETER_UNAVAILABLE, SEEDING_FAILED or ENGINE_TIMEOUT (flat envelope)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionError'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/actions/clearing-real:
    post:
      tags: [Simulator]
      summary: Execute a real clearing round inside a run (Interact Mode)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SimulatorActionClearingRealRequest'
      responses:
        '200':
          description: Clearing executed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionClearingRealResponse'
        '400':
          $ref: '#/components/responses/SimulatorActionBadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/SimulatorActionForbidden'
        '404':
          $ref: '#/components/responses/SimulatorActionNotFound'
        '409':
          $ref: '#/components/responses/SimulatorActionConflict'
        '422':
          $ref: '#/components/responses/SimulatorActionUnprocessable'
        '500':
          description: Clearing execution failed (CLEARING_FAILED)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionError'
        '503':
          description: Action unavailable - RUN_PERIMETER_UNAVAILABLE or SEEDING_FAILED (flat envelope)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionError'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/actions/participants-list:
    get:
      tags: [Simulator]
      summary: List participants visible inside a run (Interact Mode)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
      responses:
        '200':
          description: Participants of the run
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionParticipantsListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/SimulatorActionForbidden'
        '404':
          $ref: '#/components/responses/SimulatorActionNotFound'
        '422':
          $ref: '#/components/responses/SimulatorActionUnprocessable'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/actions/trustlines-list:
    get:
      tags: [Simulator]
      summary: List trust lines inside a run (Interact Mode)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
        - in: query
          name: equivalent
          required: false
          schema:
            type: string
        - in: query
          name: participant_pid
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Trust lines of the run
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionTrustlinesListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/SimulatorActionForbidden'
        '404':
          $ref: '#/components/responses/SimulatorActionNotFound'
        '422':
          $ref: '#/components/responses/SimulatorActionUnprocessable'
        '503':
          description: Action unavailable - SEEDING_FAILED (flat envelope)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionError'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /simulator/runs/{run_id}/payment-targets:
    get:
      tags: [Simulator]
      summary: List reachable payment targets for a participant (Interact Mode)
      parameters:
        - $ref: '#/components/parameters/RunIdPath'
        - in: query
          name: equivalent
          required: true
          schema:
            type: string
        - in: query
          name: from_pid
          required: true
          schema:
            type: string
        - in: query
          name: max_hops
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 8
            default: 6
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 200
        - in: query
          name: include_max_available
          required: false
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: Reachable payment targets
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorPaymentTargetsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/SimulatorActionForbidden'
        '404':
          $ref: '#/components/responses/SimulatorActionNotFound'
        '422':
          $ref: '#/components/responses/SimulatorActionUnprocessable'
        '503':
          description: Action unavailable - SEEDING_FAILED (flat envelope)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimulatorActionError'
        '429':
          $ref: '#/components/responses/TooManyRequests'

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    PidPath:
      in: path
      name: pid
      required: true
      schema:
        type: string
    TrustLineIdPath:
      in: path
      name: id
      required: true
      schema:
        type: string
        format: uuid
        description: TrustLine UUID

    ScenarioIdPath:
      in: path
      name: scenario_id
      required: true
      schema:
        type: string

    RunIdPath:
      in: path
      name: run_id
      required: true
      schema:
        type: string

    ArtifactNamePath:
      in: path
      name: name
      required: true
      schema:
        type: string

    EquivalentQuery:
      in: query
      name: equivalent
      required: true
      schema:
        $ref: '#/components/schemas/EquivalentCode'

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    Conflict:
      description: Conflict
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    TooManyRequests:
      description: Rate limit exceeded
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
    ServiceUnavailable:
      description: >-
        Measured data is unavailable and the endpoint refuses to substitute
        synthetic data (real-mode simulator analytics).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'

    SimulatorActionBadRequest:
      description: Invalid request payload for the action (flat envelope)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SimulatorActionError'
    SimulatorActionForbidden:
      description: >-
        Actions disabled or access denied (flat SimulatorActionError), or the identity was
        rejected before the handler ran (ErrorEnvelope). Both shapes are reachable: the guard
        inside the handler answers flat, while require_simulator_actor raises before it and the
        global handler wraps the result.
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/SimulatorActionError'
              - $ref: '#/components/schemas/ErrorEnvelope'
    SimulatorActionNotFound:
      description: Run, participant, equivalent or trustline not found (flat envelope)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SimulatorActionError'
    SimulatorActionConflict:
      description: Run is terminal, or the action conflicts with current state (flat envelope)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SimulatorActionError'
    SimulatorActionUnprocessable:
      description: Invalid simulator identity transport (for example, X-Simulator-Owner)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'

  schemas:
    # Interact Mode control plane payloads (011/T1101, added 2026-08-23).
    # Money-carrying fields (limit, used, available, amount, cleared amounts) are exact
    # decimal strings, per AGENTS.md section 8 - never floats.
    SimulatorActionTrustlineCreateRequest:
      type: object
      additionalProperties: false
      required: [from_pid, to_pid, equivalent, limit]
      properties:
        from_pid:
          type: string
        to_pid:
          type: string
        equivalent:
          type: string
        limit:
          type: string
        client_action_id:
          type: string
          nullable: true

    SimulatorActionTrustlineCreateResponse:
      type: object
      additionalProperties: false
      required: [trustline_id, from_pid, to_pid, equivalent, limit]
      properties:
        ok:
          type: boolean
          default: true
        trustline_id:
          type: string
        from_pid:
          type: string
        to_pid:
          type: string
        equivalent:
          type: string
        limit:
          type: string
        client_action_id:
          type: string
          nullable: true

    SimulatorActionTrustlineUpdateRequest:
      type: object
      additionalProperties: false
      required: [from_pid, to_pid, equivalent, new_limit]
      properties:
        from_pid:
          type: string
        to_pid:
          type: string
        equivalent:
          type: string
        new_limit:
          type: string
        client_action_id:
          type: string
          nullable: true

    SimulatorActionTrustlineUpdateResponse:
      type: object
      additionalProperties: false
      required: [trustline_id, old_limit, new_limit]
      properties:
        ok:
          type: boolean
          default: true
        trustline_id:
          type: string
        old_limit:
          type: string
        new_limit:
          type: string
        client_action_id:
          type: string
          nullable: true

    SimulatorActionTrustlineCloseRequest:
      type: object
      additionalProperties: false
      required: [from_pid, to_pid, equivalent]
      properties:
        from_pid:
          type: string
        to_pid:
          type: string
        equivalent:
          type: string
        client_action_id:
          type: string
          nullable: true

    SimulatorActionTrustlineCloseResponse:
      type: object
      additionalProperties: false
      required: [trustline_id]
      properties:
        ok:
          type: boolean
          default: true
        trustline_id:
          type: string
        client_action_id:
          type: string
          nullable: true

    SimulatorActionPaymentRealRequest:
      type: object
      additionalProperties: false
      required: [from_pid, to_pid, equivalent, amount]
      properties:
        from_pid:
          type: string
        to_pid:
          type: string
        equivalent:
          type: string
        amount:
          type: string
        client_action_id:
          type: string
          nullable: true

    SimulatorActionPaymentRealResponse:
      type: object
      additionalProperties: false
      required: [payment_id, from_pid, to_pid, equivalent, amount, status]
      properties:
        ok:
          type: boolean
          default: true
        payment_id:
          type: string
        from_pid:
          type: string
        to_pid:
          type: string
        equivalent:
          type: string
        amount:
          type: string
        status:
          type: string
        client_action_id:
          type: string
          nullable: true

    SimulatorActionClearingRealRequest:
      type: object
      additionalProperties: false
      required: [equivalent]
      properties:
        equivalent:
          type: string
        max_depth:
          type: integer
          minimum: 1
          maximum: 12
          default: 6
        client_action_id:
          type: string
          nullable: true

    SimulatorActionClearingRealResponse:
      type: object
      additionalProperties: false
      required: [equivalent, cleared_cycles, total_cleared_amount, cycles]
      properties:
        ok:
          type: boolean
          default: true
        equivalent:
          type: string
        cleared_cycles:
          type: integer
          minimum: 0
        total_cleared_amount:
          type: string
        cycles:
          type: array
          items:
            $ref: '#/components/schemas/SimulatorActionClearingCycle'
        client_action_id:
          type: string
          nullable: true

    SimulatorActionClearingCycle:
      type: object
      additionalProperties: false
      required: [cleared_amount, edges]
      properties:
        cleared_amount:
          type: string
        edges:
          type: array
          items:
            $ref: '#/components/schemas/SimulatorActionEdgeRef'

    SimulatorActionParticipantItem:
      type: object
      additionalProperties: false
      required: [pid, name, type, status]
      properties:
        pid:
          type: string
        name:
          type: string
        type:
          type: string
        status:
          type: string

    SimulatorActionParticipantsListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SimulatorActionParticipantItem'

    SimulatorActionTrustlineListItem:
      type: object
      additionalProperties: false
      required: [from_pid, from_name, to_pid, to_name, equivalent, limit, used, reverse_used, available, status]
      properties:
        from_pid:
          type: string
        from_name:
          type: string
        to_pid:
          type: string
        to_name:
          type: string
        equivalent:
          type: string
        limit:
          type: string
        used:
          type: string
        reverse_used:
          type: string
        available:
          type: string
        status:
          type: string

    SimulatorActionTrustlinesListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SimulatorActionTrustlineListItem'

    SimulatorPaymentTargetsItem:
      type: object
      additionalProperties: false
      required: [to_pid, hops]
      properties:
        to_pid:
          type: string
        hops:
          type: integer
          minimum: 1
        max_available:
          type: string
          nullable: true

    SimulatorPaymentTargetsResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SimulatorPaymentTargetsItem'

    SimulatorActionEdgeRef:
      type: object
      additionalProperties: false
      required: [from, to]
      properties:
        from:
          type: string
        to:
          type: string
    SimulatorActionError:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          nullable: true

    TxOnceRequestBody:
      type: object
      required: [equivalent]
      properties:
        equivalent:
          type: string
        from:
          type: string
          nullable: true
        to:
          type: string
          nullable: true
        amount:
          type: string
          nullable: true
        ttl_ms:
          type: integer
          nullable: true
        intensity_key:
          type: string
          nullable: true
        seed:
          # Deliberately an EMPTY schema. The field is `Optional[Any]`
          # (app/api/v1/simulator.py:944) and the runtime only interpolates it into a hash seed
          # (`f"{run_id}:{eq}:{seed}"`, app/core/simulator/runtime_impl.py:539), so any JSON
          # value is accepted, null included, and an empty schema states exactly that. What stood
          # here was a lone `nullable: true`, which in OpenAPI 3.0.3 modifies a sibling `type`
          # and nothing else - with no `type` to modify it stated nothing at all (`F-011-10`).
          {}
        client_action_id:
          type: string
          nullable: true

    TxOnceResponseBody:
      type: object
      required: [emitted_event_id]
      properties:
        ok:
          type: boolean
          default: true
        emitted_event_id:
          type: string
        client_action_id:
          type: string
          nullable: true

    ClearingOnceRequestBody:
      type: object
      required: [equivalent]
      properties:
        equivalent:
          type: string
        cycle_edges:
          type: array
          nullable: true
          items:
            type: object
            additionalProperties:
              type: string
        cleared_amount:
          type: string
          nullable: true
        seed:
          # Empty schema, same reason as TxOnceRequestBody.seed above: `Optional[Any]`
          # (app/api/v1/simulator.py:960), interpolated into a hash seed and nothing more.
          {}
        client_action_id:
          type: string
          nullable: true

    ClearingOnceResponseBody:
      type: object
      required: [plan_id, done_event_id]
      properties:
        ok:
          type: boolean
          default: true
        plan_id:
          type: string
        done_event_id:
          type: string
        client_action_id:
          type: string
          nullable: true

    Equivalent:
      type: object
      required: [code, precision, is_active, created_at, updated_at]
      properties:
        code:
          $ref: '#/components/schemas/EquivalentCode'
        symbol:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        precision:
          type: integer
          minimum: 0
          maximum: 18
        metadata:
          type: object
          nullable: true
          additionalProperties: true
        is_active:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    StoredEquivalent:
      description: Read projection that keeps pre-contract rows visible to operators.
      type: object
      required: [code, precision, is_active, created_at, updated_at]
      properties:
        code:
          type: string
        symbol:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        precision:
          type: integer
        metadata:
          type: object
          nullable: true
          additionalProperties: true
        is_active:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    EquivalentsList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/StoredEquivalent'

    EquivalentCode:
      type: string
      pattern: '^[A-Z0-9_]{1,16}$'
      description: Equivalent code (1-16 chars; A-Z, 0-9, underscore)
    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
            message:
              type: string
            details:
              type: object
              nullable: true

    AdminConfigItem:
      type: object
      required: [key, value, mutable]
      properties:
        key:
          type: string
        value:
          description: Any JSON value
        mutable:
          type: boolean

    AdminConfigResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminConfigItem'

    AdminConfigPatchRequest:
      type: object
      required: [updates]
      properties:
        updates:
          type: object
          additionalProperties: true
        reason:
          type: string
          nullable: true

    AdminConfigPatchResponse:
      type: object
      required: [updated]
      properties:
        updated:
          type: array
          items:
            type: string

    AdminFeatureFlags:
      type: object
      required: [multipath_enabled, full_multipath_enabled, clearing_enabled]
      properties:
        multipath_enabled:
          type: boolean
        full_multipath_enabled:
          type: boolean
        clearing_enabled:
          type: boolean
      additionalProperties: false

    AdminFeatureFlagsPatchRequest:
      type: object
      description: Partial patch; only provided fields are updated.
      properties:
        multipath_enabled:
          type: boolean
          nullable: true
        full_multipath_enabled:
          type: boolean
          nullable: true
        clearing_enabled:
          type: boolean
          nullable: true
        reason:
          type: string
          nullable: true

    AdminWhoAmIResponse:
      type: object
      required: [role]
      properties:
        role:
          type: string
          enum: [admin]

    AdminParticipantActionRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string

    # 011/T1102 (2026-08-23): five operations whose bodies the application already described
    # and the canon did not. Transcribed by hand from the response models, in the style of this
    # document - the canon is authority number one, not a build artifact of the code.
    AdminClearingCycleEdge:
      type: object
      required: [equivalent, debtor, creditor, amount]
      properties:
        equivalent:
          type: string
        debtor:
          type: string
        creditor:
          type: string
        amount:
          # Money stays an exact decimal string (AGENTS.md section 8), never a float.
          type: string

    AdminClearingCyclesForEquivalent:
      type: object
      required: [cycles]
      properties:
        cycles:
          # A cycle is an ordered list of edges, so this is an array of arrays.
          type: array
          items:
            type: array
            items:
              $ref: '#/components/schemas/AdminClearingCycleEdge'

    AdminClearingCyclesResponse:
      type: object
      required: [equivalents]
      properties:
        equivalents:
          # Keyed by equivalent code; the key set is data, not contract.
          type: object
          additionalProperties:
            $ref: '#/components/schemas/AdminClearingCyclesForEquivalent'

    # 011/T1102 (2026-08-23): the two admin graph reads, whose 200 bodies the canon declared as a
    # bare object. Transcribed by hand from AdminGraphSnapshotResponse and its ego subclass
    # (app/schemas/graph.py:12-63) and from the two handlers that fill them. The participant block
    # and the whole viz computation are duplicated line for line between the handlers, so one set
    # of schemas describes both.
    AdminGraphParticipant:
      type: object
      required: [pid, display_name, type, status]
      properties:
        pid:
          type: string
        display_name:
          type: string
        type:
          type: string
          enum: [person, business, hub]
        status:
          # Lower-cased on the way out, so this is the DB vocabulary
          # (app/db/models/participant.py:22), not the filter words the UI sends in.
          type: string
          enum: [active, suspended, left, deleted]
        # The four viz fields are filled only when `equivalent` is supplied AND names an existing
        # equivalent AND the participant set is non-empty; otherwise all four stay null. The keys
        # are always present either way. `?equivalent=NOSUCH` is a 200 with four nulls, because
        # validate_equivalent_code checks the format only.
        net_balance_atoms:
          # Integer atoms via str(int): no decimal point, negative for a net debtor. NOT a money
          # amount - the major-unit value is this divided by 10 ** precision.
          type: string
          nullable: true
        net_sign:
          type: integer
          nullable: true
          enum: [-1, 0, 1]
        viz_color_key:
          type: string
          nullable: true
          enum:
            - suspended
            - left
            - deleted
            - business
            - person
            - debt-0
            - debt-1
            - debt-2
            - debt-3
            - debt-4
            - debt-5
            - debt-6
            - debt-7
            - debt-8
        viz_size:
          # Node dimensions in pixels. The field is typed dict[str, int] (app/schemas/graph.py:22)
          # and the only writer builds exactly {"w", "h"}: the two real keys are named, and the
          # map stays as open as the type rather than being closed to a shape the annotation does
          # not promise.
          type: object
          nullable: true
          properties:
            w:
              type: integer
            h:
              type: integer
          additionalProperties:
            type: integer

    AdminGraphDebt:
      # Structurally the same four fields as AdminClearingCycleEdge, but a different model on a
      # different read; kept separate so one response cannot silently retype the other.
      type: object
      required: [equivalent, debtor, creditor, amount]
      properties:
        equivalent:
          type: string
        debtor:
          type: string
        creditor:
          type: string
        amount:
          # Money stays an exact decimal string (AGENTS.md section 8), never a float.
          type: string

    AdminGraphTransactionItem:
      # Built as a plain dict by _graph_fetch_transactions and placed in a list[Any] field, so no
      # response model shapes it.
      type: object
      required: [tx_id, type, state, initiator_pid, created_at, updated_at]
      properties:
        tx_id:
          type: string
        type:
          type: string
          enum:
            - TRUST_LINE_CREATE
            - TRUST_LINE_UPDATE
            - TRUST_LINE_CLOSE
            - PAYMENT
            - CLEARING
            - COMPENSATION
            - COMMODITY_REDEMPTION
        state:
          type: string
          enum:
            - NEW
            - ROUTED
            - PREPARE_IN_PROGRESS
            - PREPARED
            - COMMITTED
            - ABORTED
            - PROPOSED
            - WAITING
            - REJECTED
        initiator_pid:
          type: string
        created_at:
          # Deliberately NOT `format: date-time` - see `F-011-9`. These values are copied raw from
          # the column into a list[Any] field, so the UTC-attaching validator never runs, and
          # under SQLite they carry no offset. The same row served by GET /admin/incidents does
          # carry one. Declaring a format the service does not guarantee is the defect this
          # program exists to remove.
          type: string
        updated_at:
          type: string
        equivalent:
          # payload.get("equivalent"); both writers store a string, null when a row has no key.
          type: string
          nullable: true
        error:
          # NULL is the common case on the wire: `tx.error` is copied raw out of the column
          # (app/api/v1/admin.py:255) and only an aborted transaction ever has one. The seed
          # pack ships the null explicitly - most of its rows carry `"error": null`
          # (admin-fixtures/packs/greenfield-village-100-v2/v1/datasets/transactions.json).
          #
          # The null is a BRANCH, not a sibling `nullable: true`. In OpenAPI 3.0.3 `nullable`
          # modifies a sibling `type` and nothing else, so beside a bare `oneOf` it is inert and
          # the schema rejected the null it claimed to allow (`F-011-10`).
          oneOf:
            - $ref: '#/components/schemas/AdminGraphTransactionError'
            - nullable: true
              enum: [null]

    AdminGraphTransactionError:
      # The abort payload the payment engine writes into the transactions.error JSON column.
      # There is no reader-side validation, so nothing is required and the tail stays open.
      type: object
      additionalProperties: true
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true

    AdminGraphSnapshotResponse:
      type: object
      required: [participants, trustlines, equivalents, debts, incidents, audit_log, transactions]
      properties:
        participants:
          type: array
          items:
            $ref: '#/components/schemas/AdminGraphParticipant'
        trustlines:
          # Direction is from -> to = creditor -> debtor. One edge per (equivalent, from, to).
          type: array
          items:
            $ref: '#/components/schemas/TrustLine'
        equivalents:
          # Always the full table on both reads - the dropdown reads this list, so the ego read
          # deliberately does not narrow it.
          type: array
          items:
            $ref: '#/components/schemas/StoredEquivalent'
        debts:
          type: array
          items:
            $ref: '#/components/schemas/AdminGraphDebt'
        # The three below are list[Any] on the model, so their item shape comes from the fetch
        # helper. Each is present and empty unless named in the `include` CSV.
        incidents:
          type: array
          items:
            # NOT AdminIncidentItem: same row, same keys, different wire. See F-011-9.
            $ref: '#/components/schemas/AdminGraphIncidentItem'
        audit_log:
          type: array
          items:
            $ref: '#/components/schemas/AdminAuditLogItem'
        transactions:
          type: array
          items:
            $ref: '#/components/schemas/AdminGraphTransactionItem'

    AdminGraphEgoResponse:
      # The ego read returns the snapshot body plus the root; the subclass appends its field, so
      # root_pid is the last key on the wire.
      allOf:
        - $ref: '#/components/schemas/AdminGraphSnapshotResponse'
        - type: object
          properties:
            root_pid:
              type: string
              nullable: true

    AdminGraphIncidentItem:
      # 011/`F-011-9`: the very same stuck-payment row as AdminIncidentItem, with the very same
      # keys, but served through the graph reads - and one field differs on the wire.
      # `_graph_fetch_incidents` puts `tx.created_at` into a plain dict (app/api/v1/admin.py:217)
      # which lands in a `list[Any]` field (app/schemas/graph.py:32), so the UTC-attaching
      # validator on AdminIncidentItem never runs. Under SQLite the column comes back naive and
      # the value goes out with no offset, while GET /admin/incidents serves the same row with a
      # trailing Z. Proved by running the production models on one naive datetime: the item model
      # yields `2026-08-23T12:00:00Z`, the graph response `2026-08-23T12:00:00`.
      #
      # Hence no `format: date-time` here. Declaring one would make authority number one promise
      # something the service does not deliver, which is the defect this programme removes. The
      # behavioural fix - making both paths agree - changes the wire and is therefore out of scope
      # for 011 by its own Non-goals; it is recorded as a debt with no owner in this programme.
      type: object
      required: [tx_id, state, initiator_pid, equivalent, age_seconds, sla_seconds]
      properties:
        tx_id:
          type: string
        state:
          type: string
        initiator_pid:
          type: string
        equivalent:
          type: string
        age_seconds:
          type: integer
          minimum: 0
        sla_seconds:
          type: integer
          minimum: 0
        created_at:
          # Deliberately without `format: date-time` - the offset is missing here. Same reasoning
          # as AdminGraphTransactionItem.created_at, reached by a different route.
          type: string
          nullable: true

    AdminIncidentItem:
      # Served by GET /admin/incidents, where the response model does run and its validator
      # attaches UTC. The graph reads use AdminGraphIncidentItem instead - see F-011-9.
      type: object
      required: [tx_id, state, initiator_pid, equivalent, age_seconds, sla_seconds]
      properties:
        tx_id:
          type: string
        state:
          type: string
        initiator_pid:
          type: string
        equivalent:
          type: string
        age_seconds:
          type: integer
          minimum: 0
        sla_seconds:
          type: integer
          minimum: 0
        created_at:
          type: string
          format: date-time
          nullable: true

    AdminIncidentsListResponse:
      type: object
      required: [page, per_page, total, items]
      properties:
        page:
          type: integer
          minimum: 1
        per_page:
          type: integer
          minimum: 1
          maximum: 200
        total:
          type: integer
          minimum: 0
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminIncidentItem'

    AdminParticipantListItem:
      type: object
      required: [pid, display_name, type, status, verification_level, created_at]
      properties:
        pid:
          type: string
        display_name:
          type: string
        type:
          type: string
        status:
          type: string
        verification_level:
          type: integer
        created_at:
          type: string
          format: date-time

    AdminParticipantsListResponse:
      type: object
      required: [page, per_page, total, items]
      properties:
        page:
          type: integer
          minimum: 1
        per_page:
          type: integer
          minimum: 1
          maximum: 200
        total:
          type: integer
          minimum: 0
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminParticipantListItem'

    AdminParticipantsStatsResponse:
      type: object
      properties:
        participants_by_status:
          type: object
          additionalProperties:
            type: integer
        participants_by_type:
          type: object
          additionalProperties:
            type: integer
        total_participants:
          type: integer
          minimum: 0
          default: 0

    ActiveRunResponse:
      type: object
      additionalProperties: false
      properties:
        api_version:
          type: string
          default: simulator-api/1
        run_id:
          type: string
          nullable: true

    AdminDbHealth:
      # 011/T1102. Built literally by the handler (app/api/v1/health.py:132-140 on success,
      # :143-151 on failure), so the key set is fixed rather than serialized from a model.
      # This body was declared `schema: {}` until 2026-08-23 - the emptiest possible description,
      # and one the opacity guard could not even see until its predicate learned to look for it.
      type: object
      required: [status, db, timestamp]
      properties:
        status:
          type: string
          enum: [ok, error]
        db:
          type: object
          required: [dialect, reachable, latency_ms]
          properties:
            dialect:
              # SQLAlchemy backend name, e.g. `sqlite` or `postgresql`.
              type: string
            reachable:
              type: boolean
            latency_ms:
              # Null on the failure path, where no round trip completed.
              type: integer
              nullable: true
              minimum: 0
        details:
          # Present only on the 503 body, carrying the exception text.
          type: string
          nullable: true
        timestamp:
          type: string
          format: date-time

    AdminParticipantStatusChange:
      # 011/T1102. What `_set_participant_status` really emits (app/api/v1/admin.py:859,
      # returned unchanged at :876): exactly two keys, both always present, both non-null.
      # All four callers share this shape and differ only in the status literal they write -
      # freeze -> suspended (:892), unfreeze -> active (:912), ban -> deleted (:929),
      # unban -> active (:946). The pre-existing AdminParticipantStatusResponse cannot be
      # reused for ban: it permits only active|suspended, and ban writes deleted.
      type: object
      additionalProperties: false
      required: [pid, status]
      properties:
        pid:
          type: string
        status:
          type: string
          enum: [active, suspended, deleted]

    AdminParticipantStatusResponse:
      type: object
      required: [pid, status]
      properties:
        pid:
          type: string
        status:
          type: string
          enum: [active, suspended]
      additionalProperties: false

    AdminAbortTxResponse:
      type: object
      required: [tx_id, status]
      properties:
        tx_id:
          type: string
        status:
          type: string
          pattern: '^aborted$'
      additionalProperties: false

    AdminAuditLogItem:
      type: object
      required: [id, timestamp, action]
      properties:
        id:
          type: string
          # UUID columns, and the
          # model types them as UUID (app/schemas/admin.py:94-96), so the wire carries the
          # canonical hyphenated form. The canon left the format off both of them.
          format: uuid
        timestamp:
          type: string
          format: date-time
        actor_id:
          type: string
          format: uuid
          nullable: true
        actor_role:
          type: string
          nullable: true
        action:
          type: string
        object_type:
          type: string
          nullable: true
        object_id:
          type: string
          nullable: true
        reason:
          type: string
          nullable: true
        before_state:
          # Open by design and listed in ACCEPTED_FREE_FORM with the
          # reason; `additionalProperties` is not spelled out because true is the OpenAPI
          # default, the same correction slice 3 made to the error envelopes.
          type: object
          nullable: true
        after_state:
          type: object
          nullable: true
        request_id:
          type: string
          nullable: true
        ip_address:
          type: string
          nullable: true
        user_agent:
          type: string
          nullable: true

    AdminAuditLogResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminAuditLogItem'

    AdminMigrationsStatus:
      type: object
      required: [is_up_to_date]
      properties:
        current_revision:
          type: string
          nullable: true
        head_revision:
          type: string
          nullable: true
        is_up_to_date:
          type: boolean

    AdminEquivalentCreateRequest:
      type: object
      required: [code]
      properties:
        code:
          $ref: '#/components/schemas/EquivalentCode'
        symbol:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        precision:
          type: integer
          default: 2
          minimum: 0
          maximum: 18
        metadata:
          type: object
          nullable: true
          additionalProperties: true
        is_active:
          type: boolean
          default: true
        reason:
          type: string
          nullable: true

    AdminEquivalentUpdateRequest:
      type: object
      properties:
        symbol:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        precision:
          type: integer
          nullable: true
          minimum: 0
          maximum: 18
        metadata:
          type: object
          nullable: true
          additionalProperties: true
        is_active:
          type: boolean
          nullable: true
        reason:
          type: string
          nullable: true

    AdminEquivalentUsageResponse:
      type: object
      required: [code, trustlines, debts, integrity_checkpoints]
      properties:
        code:
          type: string
        trustlines:
          type: integer
        debts:
          type: integer
        integrity_checkpoints:
          type: integer
      additionalProperties: false

    AdminDeleteResponse:
      type: object
      required: [deleted]
      properties:
        deleted:
          type: string
      additionalProperties: false

    SignedRequest:
      type: object
      properties:
        signature:
          type: string
          description: base64 signature

    ChallengeRequest:
      type: object
      required: [pid]
      properties:
        pid:
          type: string

    ChallengeResponse:
      type: object
      required: [challenge, expires_at]
      properties:
        challenge:
          type: string
        expires_at:
          type: string
          format: date-time

    LoginRequest:
      type: object
      required: [pid, challenge, signature]
      properties:
        pid:
          type: string
        challenge:
          type: string
        signature:
          type: string
        device_info:
          $ref: '#/components/schemas/DeviceInfo'

    DeviceInfo:
      type: object
      properties:
        platform:
          type: string
          nullable: true
        app_version:
          type: string
          nullable: true

    RefreshRequest:
      type: object
      required: [refresh_token]
      properties:
        refresh_token:
          type: string

    TokenPair:
      type: object
      required: [access_token, refresh_token, expires_in, participant]
      properties:
        access_token:
          type: string
        refresh_token:
          type: string
        expires_in:
          type: integer
          description: Access token TTL in seconds
          example: 3600
        participant:
          $ref: '#/components/schemas/ParticipantPublic'
        token_type:
          type: string
          example: Bearer

    ParticipantPublic:
      type: object
      required: [pid, display_name, status]
      properties:
        pid:
          type: string
        display_name:
          type: string
        status:
          type: string
          enum: [active, suspended, left, deleted]

    Participant:
      type: object
      required: [pid, status]
      properties:
        pid:
          type: string
          description: Participant identifier (derived from public_key)
        display_name:
          type: string
        type:
          type: string
          enum: [person, business, hub]
        status:
          type: string
          enum: [active, suspended, left, deleted]
          description: |
            Participant status:
            - active: normal operation
            - suspended: temporarily frozen by admin (can be unfrozen)
            - left: participant voluntarily left the community
            - deleted: participant removed from system
        verification_level:
          type: integer
          minimum: 0
          maximum: 3
          description: Verification level (0=unverified, 3=fully verified)
        public_key:
          type: string
          description: Ed25519 public key (base64)
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        profile:
          $ref: '#/components/schemas/ParticipantProfile'
        public_stats:
          $ref: '#/components/schemas/ParticipantPublicStats'

    ParticipantProfile:
      type: object
      additionalProperties: true
      properties:
        type:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        contacts:
          type: object
          nullable: true
          additionalProperties: true

    ParticipantPublicStats:
      type: object
      required: [total_incoming_trust, member_since]
      properties:
        total_incoming_trust:
          type: string
        member_since:
          type: string
          format: date-time

    ParticipantCreateRequest:
      type: object
      required: [display_name, public_key, signature]
      properties:
        display_name:
          type: string
        type:
          type: string
          enum: [person, business, hub]
          default: person
        public_key:
          type: string
        profile:
          $ref: '#/components/schemas/ParticipantProfile'
        signature:
          type: string
          description: |
            base64 Ed25519 signature of canonical JSON of this request body **excluding** `signature`.
            Signed payload keys: `display_name`, `type`, `public_key`, and optional `profile` if provided.

    ParticipantsList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Participant'

    ParticipantStats:
      type: object
      required: [total_incoming_trust, total_outgoing_trust, total_debt, total_credit, net_balance]
      properties:
        total_incoming_trust:
          type: string
        total_outgoing_trust:
          type: string
        total_debt:
          type: string
        total_credit:
          type: string
        net_balance:
          type: string

    ParticipantWithStats:
      allOf:
        - $ref: '#/components/schemas/Participant'
        - type: object
          required: [stats]
          properties:
            stats:
              $ref: '#/components/schemas/ParticipantStats'

    ParticipantUpdateRequest:
      type: object
      required: [signature]
      properties:
        display_name:
          type: string
          nullable: true
        profile:
          $ref: '#/components/schemas/ParticipantProfile'
        signature:
          type: string

    TrustLine:
      type: object
      # 011/T1102 (2026-08-23): `required` named six of the ten fields the model declares
      # without a default, and `updated_at` was missing from `properties` altogether even
      # though every trustline read emits it (app/schemas/trustline.py:24). The three fields
      # left out - policy, from_display_name, to_display_name - default to null in the model,
      # so nothing guarantees them; no read sets response_model_exclude_none, so in practice
      # all three do reach the wire.
      #
      # Present, but not null, and the first version of this note said null on all three. The
      # wire test corrected it: `policy` is `{}`, never null, because TrustLineService.create
      # writes `policy=data.policy or {}` (app/core/trustlines/service.py:190) - which also means
      # the ORM column's five-key default (app/db/models/trustline.py:15-21) never fires for an
      # API-created line. The two display names are non-null wherever the query joins the
      # participant rows, and null only on the metrics capacity path, exactly as
      # AdminParticipantCapacityBottleneck says of itself.
      required: [id, from, to, equivalent, limit, used, available, status, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
          description: TrustLine UUID
        from:
          type: string
          description: PID of trustline creator (creditor)
        to:
          type: string
          description: PID of trustee (potential debtor)
        from_display_name:
          type: string
          nullable: true
        to_display_name:
          type: string
          nullable: true
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        limit:
          type: string
          description: Maximum debt allowed (decimal string)
        used:
          type: string
          description: Current debt against this line (decimal string)
        available:
          type: string
          description: Available credit (limit - used)
        policy:
          type: object
          additionalProperties: true
          # 011/T1102: the five keys were named in the description but never declared, so a
          # generated client got nothing typed. `additionalProperties` stays true on
          # purpose (`F-011-8`): validate_trustline_policy rejects unknown keys on
          # POST/PATCH /trustlines, but the simulator scenario seeder writes arbitrary
          # JSON into the same column without calling it, and those rows are served by
          # these same responses.
          properties:
            auto_clearing:
              type: boolean
              nullable: true
            can_be_intermediate:
              type: boolean
              nullable: true
            max_hop_usage:
              # The validator parses with Decimal(str(value)) and requires >= 0, so a numeric
              # string and a number are both accepted (app/utils/validation.py:212-219).
              #
              # An explicit null is ACCEPTED too, and that is why the null branch is spelled out
              # here on the read schema and on both write schemas alike.
              # `validate_trustline_policy` skips the key outright when the value is null -
              # `if key not in policy or policy[key] is None: continue`
              # (app/utils/validation.py:210-211) - and the service then stores the policy dict
              # verbatim into the JSON column (app/core/trustlines/service.py:190, :334-336), so
              # a null written through POST/PATCH /trustlines is read straight back out by every
              # TrustLine response.
              #
              # A BRANCH, not a sibling `nullable: true`: in OpenAPI 3.0.3 `nullable` modifies a
              # sibling `type` and nothing else, so beside a bare `oneOf` it was inert and this
              # node forbade the null it claimed to allow (`F-011-10`).
              oneOf:
                - type: string
                - type: number
                - nullable: true
                  enum: [null]
            daily_limit:
              # Same validator branch, same accepted-and-persisted null.
              oneOf:
                - type: string
                - type: number
                - nullable: true
                  enum: [null]
            blocked_participants:
              type: array
              nullable: true
              items:
                type: string
          description: |
            TrustLine policy settings.

            Allowed keys (MVP): `auto_clearing`, `can_be_intermediate`, `max_hop_usage`, `daily_limit`, `blocked_participants`.

            Note: `daily_limit` is **informational only** and is **not enforced in MVP**.
        status:
          type: string
          enum: [active, frozen, closed]
          description: TrustLine status
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    TrustLinesList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/TrustLine'

    TrustLineCreateRequest:
      type: object
      required: [to, equivalent, limit, signature]
      properties:
        to:
          type: string
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        limit:
          type: string
        policy:
          type: object
          additionalProperties: true
          # 011/T1102: the five keys were named in the description but never declared, so a
          # generated client got nothing typed. `additionalProperties` stays true on
          # purpose (`F-011-8`): validate_trustline_policy rejects unknown keys on
          # POST/PATCH /trustlines, but the simulator scenario seeder writes arbitrary
          # JSON into the same column without calling it, and those rows are served by
          # these same responses.
          properties:
            auto_clearing:
              type: boolean
              nullable: true
            can_be_intermediate:
              type: boolean
              nullable: true
            max_hop_usage:
              # The validator parses with Decimal(str(value)) and requires >= 0, so a numeric
              # string and a number are both accepted (app/utils/validation.py:212-219).
              #
              # An explicit null is ACCEPTED too, and that is why the null branch is spelled out
              # here on the read schema and on both write schemas alike.
              # `validate_trustline_policy` skips the key outright when the value is null -
              # `if key not in policy or policy[key] is None: continue`
              # (app/utils/validation.py:210-211) - and the service then stores the policy dict
              # verbatim into the JSON column (app/core/trustlines/service.py:190, :334-336), so
              # a null written through POST/PATCH /trustlines is read straight back out by every
              # TrustLine response.
              #
              # A BRANCH, not a sibling `nullable: true`: in OpenAPI 3.0.3 `nullable` modifies a
              # sibling `type` and nothing else, so beside a bare `oneOf` it was inert and this
              # node forbade the null it claimed to allow (`F-011-10`).
              oneOf:
                - type: string
                - type: number
                - nullable: true
                  enum: [null]
            daily_limit:
              # Same validator branch, same accepted-and-persisted null.
              oneOf:
                - type: string
                - type: number
                - nullable: true
                  enum: [null]
            blocked_participants:
              type: array
              nullable: true
              items:
                type: string
          description: |
            Optional TrustLine policy.

            Allowed keys (MVP): `auto_clearing`, `can_be_intermediate`, `max_hop_usage`, `daily_limit`, `blocked_participants`.

            Note: `daily_limit` is **informational only** and is **not enforced in MVP**.
        signature:
          type: string
          description: |
            base64 Ed25519 signature of canonical JSON of this request body **excluding** `signature`.
            Signed payload keys: `to`, `equivalent`, `limit`, and optional `policy` if provided.

    TrustLineUpdateRequest:
      type: object
      required: [signature]
      properties:
        limit:
          type: string
        policy:
          type: object
          additionalProperties: true
          # 011/T1102: the five keys were named in the description but never declared, so a
          # generated client got nothing typed. `additionalProperties` stays true on
          # purpose (`F-011-8`): validate_trustline_policy rejects unknown keys on
          # POST/PATCH /trustlines, but the simulator scenario seeder writes arbitrary
          # JSON into the same column without calling it, and those rows are served by
          # these same responses.
          properties:
            auto_clearing:
              type: boolean
              nullable: true
            can_be_intermediate:
              type: boolean
              nullable: true
            max_hop_usage:
              # The validator parses with Decimal(str(value)) and requires >= 0, so a numeric
              # string and a number are both accepted (app/utils/validation.py:212-219).
              #
              # An explicit null is ACCEPTED too, and that is why the null branch is spelled out
              # here on the read schema and on both write schemas alike.
              # `validate_trustline_policy` skips the key outright when the value is null -
              # `if key not in policy or policy[key] is None: continue`
              # (app/utils/validation.py:210-211) - and the service then stores the policy dict
              # verbatim into the JSON column (app/core/trustlines/service.py:190, :334-336), so
              # a null written through POST/PATCH /trustlines is read straight back out by every
              # TrustLine response.
              #
              # A BRANCH, not a sibling `nullable: true`: in OpenAPI 3.0.3 `nullable` modifies a
              # sibling `type` and nothing else, so beside a bare `oneOf` it was inert and this
              # node forbade the null it claimed to allow (`F-011-10`).
              oneOf:
                - type: string
                - type: number
                - nullable: true
                  enum: [null]
            daily_limit:
              # Same validator branch, same accepted-and-persisted null.
              oneOf:
                - type: string
                - type: number
                - nullable: true
                  enum: [null]
            blocked_participants:
              type: array
              nullable: true
              items:
                type: string
          description: |
            Optional TrustLine policy update.

            Allowed keys (MVP): `auto_clearing`, `can_be_intermediate`, `max_hop_usage`, `daily_limit`, `blocked_participants`.

            Note: `daily_limit` is **informational only** and is **not enforced in MVP**.
        signature:
          type: string
          description: |
            base64 Ed25519 signature of canonical JSON of this request body **excluding** `signature`.
            Signed payload keys: `id` (path param), optional `limit` if provided, optional `policy` if provided.

    TrustLineCloseRequest:
      type: object
      required: [signature]
      properties:
        signature:
          type: string
          description: |
            base64 Ed25519 signature of canonical JSON payload **excluding** `signature`.
            Signed payload keys: `id` (path param).

    ClearingCycleEdge:
      type: object
      required: [debt_id, debtor, creditor, amount]
      properties:
        debt_id:
          type: string
        debtor:
          type: string
        creditor:
          type: string
        amount:
          type: string

    ClearingCyclesResponse:
      type: object
      required: [cycles]
      properties:
        cycles:
          type: array
          items:
            type: array
            items:
              $ref: '#/components/schemas/ClearingCycleEdge'

    ClearingAutoResponse:
      type: object
      required: [equivalent, cleared_cycles]
      properties:
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        cleared_cycles:
          type: integer

    CapacityResponse:
      type: object
      required: [can_pay, max_amount, routes_count, estimated_hops]
      properties:
        can_pay:
          type: boolean
        max_amount:
          type: string
        routes_count:
          type: integer
        estimated_hops:
          type: integer

    MaxFlowPath:
      type: object
      required: [path, capacity]
      properties:
        path:
          type: array
          items:
            type: string
        capacity:
          type: string

    Bottleneck:
      type: object
      required: [from, to, limit, used, available]
      properties:
        from:
          type: string
        to:
          type: string
        limit:
          type: string
        used:
          type: string
        available:
          type: string

    MaxFlowResponse:
      type: object
      required: [max_amount, paths, bottlenecks, algorithm, computed_at]
      properties:
        max_amount:
          type: string
        paths:
          type: array
          items:
            $ref: '#/components/schemas/MaxFlowPath'
        bottlenecks:
          type: array
          items:
            $ref: '#/components/schemas/Bottleneck'
        algorithm:
          type: string
        computed_at:
          type: string
          format: date-time

    PaymentCreateRequest:
      type: object
      required: [tx_id, to, equivalent, amount, signature]
      properties:
        tx_id:
          type: string
          description: Client-generated transaction id for mandatory idempotency.
          minLength: 1
          maxLength: 64
          pattern: '^[A-Za-z0-9._:-]+$'
        to:
          type: string
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        amount:
          type: string
        description:
          type: string
        constraints:
          $ref: '#/components/schemas/PaymentConstraints'
        signature:
          type: string
          description: |
            base64 Ed25519 signature of canonical JSON of this request body **excluding** `signature`.
            Signed payload keys: `tx_id`, `to`, `equivalent`, `amount`, optional `description`, optional `constraints`.

    PaymentConstraints:
      type: object
      additionalProperties: false
      properties:
        max_hops:
          type: integer
          minimum: 1
          maximum: 10
          nullable: true
        max_paths:
          type: integer
          minimum: 1
          maximum: 10
          nullable: true
        timeout_ms:
          type: integer
          minimum: 1
          maximum: 120000
          nullable: true
        avoid:
          type: array
          items:
            type: string
          nullable: true

    PaymentRoute:
      type: object
      required: [path, amount]
      properties:
        path:
          type: array
          items:
            type: string
        amount:
          type: string

    PaymentError:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
        details:
          # Read back verbatim from the transactions.error JSON column
          # (app/core/payments/service.py:1282-1286). The payment engine's abort writer coerces a
          # missing payload to {} (app/core/payments/engine.py:1761-1778), so a row it wrote
          # always carries an object.
          #
          # It is NOT the only writer, which is why the null branch below is real rather than a
          # concession to hypothetical legacy rows: `scripts/seed_db.py:459` stores
          # `error=item.get("error")` straight from a fixtures pack, bypassing that normalizer
          # entirely, and the shipped pack's ABORTED rows carry `{"code", "message"}` with no
          # `details` key at all (admin-fixtures/.../transactions.json:29, :346). For those rows
          # `.get("details")` yields None and `PaymentError` serializes `"details": null`
          # (executed; `details` is `Optional[Dict[str, Any]] = None` on app/schemas/payment.py:57
          # and no read on this route sets `exclude_none`).
          #
          # The tail is OPEN, and that is the point: both abort paths forward the `.details` of
          # whatever 4xx GeoException prepare or commit raised (app/core/payments/service.py:896-903),
          # so adding a `details=` to any 4xx raise changes this object with no schema change.
          # The branches below therefore DOCUMENT what the code emits today. `anyOf`, never
          # `oneOf`: a closed union would make an unlisted-but-real response non-conforming, and
          # a `oneOf` with a catch-all is worse still - every documented payload would match two
          # branches and be rejected for matching more than one.
          #
          # The null is the last branch rather than a sibling `nullable: true`. In OpenAPI 3.0.3
          # `nullable` modifies a sibling `type` and nothing else, so beside a bare `anyOf` it was
          # inert and this node rejected the null it claimed to allow (`F-011-10`).
          anyOf:
            # Plain abort with no diagnostic payload - cancellation and timeout (E007), the
            # default abort (E010), and the E002 raised when routing finds no capacious path.
            - type: object
              additionalProperties: false
            # E002, insufficient capacity on one hop (app/core/payments/engine.py:767, :949).
            # available / needed / reserved are money, so exact decimal strings.
            - type: object
              required: [available, needed, reserved, from, to]
              properties:
                available:
                  type: string
                needed:
                  type: string
                reserved:
                  type: string
                from:
                  type: string
                to:
                  type: string
              additionalProperties: false
            # E008 from RetryablePaymentConflictException (app/utils/exceptions.py:78-81):
            # a retryable database serialization failure. `retryable` is the client-facing
            # signal that the same payment may be resubmitted.
            - type: object
              required: [retryable, conflict_kind]
              properties:
                retryable:
                  type: boolean
                conflict_kind:
                  type: string
              additionalProperties: false
            # E008 from the post-apply per-participant delta check
            # (app/core/payments/engine.py:1594). `equivalent` is deliberately a plain string,
            # NOT EquivalentCode: the producer falls back to the equivalent UUID when the code
            # cannot be read, and the code pattern would reject a real response.
            - type: object
              required: [invariant, source, equivalent, equivalent_id, total_drift, drifts]
              properties:
                invariant:
                  type: string
                  enum: [PAYMENT_DELTA_DRIFT]
                source:
                  type: string
                equivalent:
                  type: string
                equivalent_id:
                  type: string
                total_drift:
                  type: string
                drifts:
                  type: array
                  items:
                    type: object
                    required: [participant_id, participant_uuid, expected_delta, actual_delta, drift]
                    properties:
                      participant_id:
                        type: string
                      participant_uuid:
                        type: string
                      expected_delta:
                        type: string
                      actual_delta:
                        type: string
                      drift:
                        type: string
                    additionalProperties: false
              additionalProperties: false
            # E008 from the three protocol invariants re-checked before commit - the same three
            # shapes the integrity endpoints return, from the same producers.
            - $ref: '#/components/schemas/InvariantViolationDetails'
            # The open tail. Any other 4xx GeoException raised in prepare or commit reaches this
            # node with its own details; this branch is what keeps such a response conforming.
            - type: object
            # The seeded abort rows described above, which carry no `details` key at all.
            - nullable: true
              enum: [null]

    PaymentResult:
      type: object
      required: [tx_id, status, from, to, equivalent, amount, created_at]
      properties:
        tx_id:
          type: string
        status:
          type: string
          enum: [COMMITTED, ABORTED]
          description: Final payment outcome. Internal transaction states are not exposed via the Payments API.
        from:
          type: string
        to:
          type: string
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        amount:
          type: string
        routes:
          type: array
          items:
            $ref: '#/components/schemas/PaymentRoute'
        error:
          $ref: '#/components/schemas/PaymentError'
        created_at:
          type: string
          format: date-time
        committed_at:
          type: string
          format: date-time

    PaymentsList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PaymentResult'

    BalanceEquivalent:
      type: object
      required: [code, total_debt, total_credit, net_balance, available_to_spend, available_to_receive]
      properties:
        code:
          $ref: '#/components/schemas/EquivalentCode'
        total_debt:
          type: string
        total_credit:
          type: string
        net_balance:
          type: string
        available_to_spend:
          type: string
        available_to_receive:
          type: string

    BalanceSummary:
      type: object
      required: [equivalents]
      properties:
        equivalents:
          type: array
          items:
            $ref: '#/components/schemas/BalanceEquivalent'

    DebtsDetails:
      type: object
      required: [outgoing, incoming]
      properties:
        outgoing:
          type: array
          items:
            type: object
            required: [creditor, equivalent, amount]
            properties:
              creditor:
                type: string
              creditor_name:
                type: string
              equivalent:
                $ref: '#/components/schemas/EquivalentCode'
              amount:
                type: string
        incoming:
          type: array
          items:
            type: object
            required: [debtor, equivalent, amount]
            properties:
              debtor:
                type: string
              debtor_name:
                type: string
              equivalent:
                $ref: '#/components/schemas/EquivalentCode'
              amount:
                type: string

    IntegrityInvariantChecks:
      # All three checks always run and always record an outcome, so all three keys are present
      # whenever `checks` is (app/core/integrity.py:79-114). The value shapes deliberately are not
      # one shared schema: zero_sum has no violation count to report and never writes a
      # `violations` key - not on the pass path and not on the failure path either.
      type: object
      required: [zero_sum, trust_limits, debt_symmetry]
      additionalProperties: true
      properties:
        zero_sum:
          type: object
          required: [passed]
          additionalProperties: true
          properties:
            passed:
              type: boolean
            details:
              # Absent or an object - never null, unlike the `details` on InvariantResult below.
              # This node is a plain dict literal, not a pydantic model, so an unset key is simply
              # missing: the pass path writes `{"passed": true}` / `{"passed": true,
              # "violations": 0}` and no `details` at all, and the failure path writes
              # `exc.details`, which every raise in app/core/invariants.py supplies as a dict
              # (app/core/integrity.py:79-114). The `nullable: true` that stood here was untrue,
              # and inert besides - in OpenAPI 3.0.3 `nullable` modifies a sibling `type` and
              # nothing else, so beside a bare `oneOf` it stated nothing (`F-011-10`).
              oneOf:
                - $ref: '#/components/schemas/InvariantViolationDetails'
        trust_limits:
          type: object
          required: [passed, violations]
          additionalProperties: true
          properties:
            passed:
              type: boolean
            violations:
              type: integer
              minimum: 0
            details:
              # Absent or an object - never null, unlike the `details` on InvariantResult below.
              # This node is a plain dict literal, not a pydantic model, so an unset key is simply
              # missing: the pass path writes `{"passed": true}` / `{"passed": true,
              # "violations": 0}` and no `details` at all, and the failure path writes
              # `exc.details`, which every raise in app/core/invariants.py supplies as a dict
              # (app/core/integrity.py:79-114). The `nullable: true` that stood here was untrue,
              # and inert besides - in OpenAPI 3.0.3 `nullable` modifies a sibling `type` and
              # nothing else, so beside a bare `oneOf` it stated nothing (`F-011-10`).
              oneOf:
                - $ref: '#/components/schemas/InvariantViolationDetails'
        debt_symmetry:
          type: object
          required: [passed, violations]
          additionalProperties: true
          properties:
            passed:
              type: boolean
            violations:
              type: integer
              minimum: 0
            details:
              # Absent or an object - never null, unlike the `details` on InvariantResult below.
              # This node is a plain dict literal, not a pydantic model, so an unset key is simply
              # missing: the pass path writes `{"passed": true}` / `{"passed": true,
              # "violations": 0}` and no `details` at all, and the failure path writes
              # `exc.details`, which every raise in app/core/invariants.py supplies as a dict
              # (app/core/integrity.py:79-114). The `nullable: true` that stood here was untrue,
              # and inert besides - in OpenAPI 3.0.3 `nullable` modifies a sibling `type` and
              # nothing else, so beside a bare `oneOf` it stated nothing (`F-011-10`).
              oneOf:
                - $ref: '#/components/schemas/InvariantViolationDetails'

    IntegrityInvariantsStatus:
      # The `invariants_status` JSON column, handed back verbatim by
      # GET /integrity/checksum/{equivalent} (app/api/v1/integrity.py:178). Sole writer:
      # compute_integrity_checkpoint_for_equivalent (app/core/integrity.py:58-124).
      #
      # Only the five keys of the opening literal are required. The status/checks/alerts/passed
      # block was added later, so rows written before it carry the five base keys and nothing
      # else - and a JSON column keeps whatever it was given, which is also why this stays open.
      type: object
      required: [computed_at, debts_count, trustlines_count, debts_non_negative, debts_negative_count]
      additionalProperties: true
      properties:
        computed_at:
          # `datetime.now(timezone.utc).isoformat()` frozen into JSON, not a datetime rendered by
          # the response model - so it ends in `+00:00`, unlike every timestamp pydantic emits.
          type: string
          format: date-time
        debts_count:
          type: integer
          minimum: 0
        trustlines_count:
          type: integer
          minimum: 0
        debts_non_negative:
          type: boolean
        debts_negative_count:
          type: integer
          minimum: 0
        status:
          type: string
          enum: [healthy, warning, critical]
        checks:
          $ref: '#/components/schemas/IntegrityInvariantChecks'
        alerts:
          # Bare check names, one per failed check - a different vocabulary from the
          # sentence-style alerts on IntegrityStatusResponse.
          type: array
          items:
            type: string
            enum: [zero_sum, trust_limits, debt_symmetry]
        passed:
          type: boolean

    IntegrityAuditLogAfterState:
      # Not a stored blob: the handler builds this dict field by field from an
      # integrity_audit_log row (app/api/v1/integrity.py:504-514), so all nine keys are always
      # present however old the row is.
      type: object
      additionalProperties: true
      required:
        - operation_type
        - tx_id
        - equivalent
        - state_checksum_before
        - state_checksum_after
        - affected_participants
        - invariants_checked
        - verification_passed
        - error_details
      properties:
        operation_type:
          # The seven writers. This value selects the shape of the three open objects below.
          type: string
          enum:
            - TRUST_LINE_CREATE
            - TRUST_LINE_UPDATE
            - TRUST_LINE_CLOSE
            - INTEGRITY_VERIFY
            - PAYMENT
            - CLEARING
            - SIMULATOR_AUDIT_DRIFT
        tx_id:
          # Only PAYMENT and CLEARING run inside a transaction; the other five write null.
          type: string
          nullable: true
        equivalent:
          # An equivalent code in practice, but deliberately not EquivalentCode: three writers
          # fall back to a stringified UUID when the code lookup misses, which the code pattern
          # would reject.
          type: string
        state_checksum_before:
          # sha-256 hex, or the empty string: the column is NOT NULL, so "no prior checkpoint"
          # is spelled "" rather than null.
          type: string
          pattern: '^([0-9a-f]{64})?$'
        state_checksum_after:
          type: string
          pattern: '^([0-9a-f]{64})?$'
        affected_participants:
          # Shape chosen by the writer, keyed off operation_type: {from, to} for TRUST_LINE_CREATE,
          # plus trustline_id for UPDATE/CLOSE, {participants} for PAYMENT, {participants, edges}
          # for CLEARING, {drifts, tick_index, source} for SIMULATOR_AUDIT_DRIFT, and {} for
          # INTEGRITY_VERIFY, which audits an equivalent rather than a set of participants.
          type: object
          additionalProperties: true
        invariants_checked:
          # Usually the `checks` node above; INTEGRITY_VERIFY stores dumped InvariantResult
          # objects instead, and SIMULATOR_AUDIT_DRIFT a fourth vocabulary entirely.
          type: object
          additionalProperties: true
        verification_passed:
          type: boolean
        error_details:
          # Null exactly when verification_passed is true.
          type: object
          nullable: true
          additionalProperties: true

    InvariantViolationDetails:
      # The payload IntegrityViolationException carries (app/core/invariants.py).
      # Shared because two surfaces return exactly these three shapes: the integrity
      # endpoints via InvariantResult.details, and a payment abort via
      # PaymentError.details when the engine re-checks invariants before commit
      # (app/core/payments/engine.py:1248-1255).
      oneOf:
      # check_zero_sum. Keys are equivalent ids, values the signed imbalance - money, so
      # exact decimal strings (AGENTS.md section 8), never numbers.
      - type: object
        required: [invariant, violations]
        properties:
          invariant:
            type: string
            enum: [ZERO_SUM_VIOLATION]
          violations:
            type: object
            additionalProperties:
              type: string
        additionalProperties: false
      # check_trust_limits, item built at app/core/invariants.py:136-145.
      - type: object
        required: [invariant, violations]
        properties:
          invariant:
            type: string
            enum: [TRUST_LIMIT_VIOLATION]
          violations:
            type: array
            items:
              type: object
              required: [debtor_id, creditor_id, equivalent_id, debt_amount, trust_limit, violation_amount]
              properties:
                debtor_id:
                  type: string
                creditor_id:
                  type: string
                equivalent_id:
                  type: string
                debt_amount:
                  type: string
                trust_limit:
                  type: string
                violation_amount:
                  type: string
              additionalProperties: false
        additionalProperties: false
      # check_debt_symmetry, item built at app/core/invariants.py:218-227.
      - type: object
        required: [invariant, violations]
        properties:
          invariant:
            type: string
            enum: [DEBT_SYMMETRY_VIOLATION]
          violations:
            type: array
            items:
              type: object
              required: [participant_a, participant_b, equivalent_id, debt_a_to_b, debt_b_to_a, net_debt]
              properties:
                participant_a:
                  type: string
                participant_b:
                  type: string
                equivalent_id:
                  type: string
                debt_a_to_b:
                  type: string
                debt_b_to_a:
                  type: string
                net_debt:
                  type: string
              additionalProperties: false
        additionalProperties: false

    InvariantResult:
      type: object
      required: [passed]
      properties:
        passed:
          type: boolean
        value:
          type: string
          nullable: true
        violations:
          type: integer
          nullable: true
        details:
          # Null when `passed` is true; otherwise the details of the IntegrityViolationException,
          # copied through unchanged by the two endpoints that build this object
          # (app/api/v1/integrity.py:108, :121, :135 and :213, :226, :240). So the shape here is
          # exactly what app/core/invariants.py raises: three variants, told apart by `invariant`.
          #
          # `violations` is an OBJECT in the zero-sum variant and an ARRAY in the other two. That
          # asymmetry is in the producers, not a transcription slip - which is why this is a
          # oneOf of three whole objects rather than one object with a loose `violations`.
          #
          # CLEARING_NEUTRALITY_VIOLATION (app/core/invariants.py:284-287) is deliberately absent:
          # verify_clearing_neutrality has one caller, the clearing service, and its exception
          # never becomes an InvariantResult. Declaring a shape this endpoint cannot return would
          # be the mirror of the defect program 011 exists to fix.
          #
          # The null is a BRANCH here, and it is the ordinary case: this IS a pydantic model
          # (`details: Optional[Dict[str, Any]] = None`, app/schemas/integrity.py:19) and no read
          # on either endpoint sets `exclude_none`, so every PASSING invariant emits
          # `"details": null` (executed). A sibling `nullable: true` could not say that - in
          # OpenAPI 3.0.3 it modifies a sibling `type` and nothing else, so beside a bare `oneOf`
          # it was inert and rejected the null it claimed to allow (`F-011-10`).
          oneOf:
            - $ref: '#/components/schemas/InvariantViolationDetails'
            - nullable: true
              enum: [null]
      additionalProperties: false

    EquivalentIntegrityStatus:
      type: object
      required: [status, checksum, invariants]
      properties:
        status:
          type: string
          enum: [healthy, warning, critical]
        checksum:
          type: string
        last_verified:
          type: string
          format: date-time
          nullable: true
        invariants:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/InvariantResult'
      additionalProperties: false

    IntegrityStatusResponse:
      type: object
      required: [status, last_check, equivalents, alerts]
      properties:
        status:
          type: string
          enum: [healthy, warning, critical]
        last_check:
          type: string
          format: date-time
        equivalents:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/EquivalentIntegrityStatus'
        alerts:
          type: array
          items:
            type: string
      additionalProperties: false

    IntegrityChecksumResponse:
      type: object
      required: [equivalent, checksum, created_at, invariants_status]
      properties:
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        checksum:
          type: string
        created_at:
          type: string
          format: date-time
        invariants_status:
          $ref: '#/components/schemas/IntegrityInvariantsStatus'

    IntegrityVerifyRequest:
      type: object
      properties:
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
      additionalProperties: false

    IntegrityVerifyResponse:
      type: object
      required: [status, checked_at, equivalents, alerts]
      properties:
        status:
          type: string
          enum: [healthy, warning, critical]
        checked_at:
          type: string
          format: date-time
        equivalents:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/EquivalentIntegrityStatus'
        alerts:
          type: array
          items:
            type: string
      additionalProperties: false

    IntegrityNetMutualDebtsRepairResponse:
      type: object
      required: [ok, action, netted_pairs, updated, deleted]
      properties:
        ok:
          type: boolean
          enum: [true]
        action:
          type: string
          enum: [net-mutual-debts]
        netted_pairs:
          type: integer
          minimum: 0
        updated:
          type: integer
          minimum: 0
        deleted:
          type: integer
          minimum: 0
      additionalProperties: false

    IntegrityCapDebtsRepairResponse:
      type: object
      required: [ok, action, scanned, updated, deleted]
      properties:
        ok:
          type: boolean
          enum: [true]
        action:
          type: string
          enum: [cap-debts-to-trust-limits]
        scanned:
          type: integer
          minimum: 0
        updated:
          type: integer
          minimum: 0
        deleted:
          type: integer
          minimum: 0
      additionalProperties: false

    IntegrityAuditLogItem:
      type: object
      required: [timestamp, action]
      properties:
        timestamp:
          type: string
          format: date-time
        actor_id:
          type: string
          nullable: true
        action:
          type: string
        object_type:
          type: string
          nullable: true
        object_id:
          type: string
          nullable: true
        after_state:
          $ref: '#/components/schemas/IntegrityAuditLogAfterState'

    IntegrityAuditLogResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/IntegrityAuditLogItem'

    SimulatorVizSize:
      type: object
      required: [w, h]
      properties:
        w:
          type: number
        h:
          type: number
      additionalProperties: false

    SimulatorGraphNode:
      type: object
      required: [id]
      properties:
        id:
          type: string
        name:
          type: string
          nullable: true
        type:
          type: string
          nullable: true
        status:
          type: string
          nullable: true
        links_count:
          type: integer
          nullable: true
        net_balance:
          type: string
          nullable: true
        net_balance_atoms:
          type: string
          nullable: true
        net_sign:
          type: integer
          nullable: true
          enum: [-1, 0, 1]
        viz_color_key:
          type: string
          nullable: true
        viz_shape_key:
          type: string
          nullable: true
        viz_size:
          $ref: '#/components/schemas/SimulatorVizSize'
        viz_badge_key:
          type: string
          nullable: true
      additionalProperties: true

    SimulatorGraphLink:
      type: object
      required: [source, target]
      properties:
        id:
          type: string
          nullable: true
        source:
          type: string
        target:
          type: string
        # None of the three is ever null on the wire, so none of them carries a null branch.
        # The `nullable: true` that stood beside each `oneOf` was inert anyway - in OpenAPI 3.0.3
        # `nullable` modifies a sibling `type` and nothing else (`F-011-10`) - but it was also
        # untrue, on both schemas, for two different reasons.
        #
        # On the SNAPSHOT link: `scenario_to_snapshot` sets `used=0` unconditionally and takes
        # `trust_limit`/`available` from the scenario's `limit`
        # (app/core/simulator/snapshot_builder.py:373-384), and `limit` is `required` on every
        # trustline in fixtures/simulator/scenario.schema.json under
        # `anyOf: [integer, number, string]`, which rejects a missing key and an explicit null
        # alike - verified by feeding both through `validate_scenario_or_400`. Real mode then
        # overwrites all three with money strings (snapshot_builder.py:232-234).
        #
        # On the PATCH: there is no pydantic model at all, only two hand-built dicts.
        # `build_edge_patch_for_equivalent` sets all three to money strings
        # (app/core/simulator/edge_patch_builder.py:161-163); `build_edge_patch_for_pairs` sets
        # `used`/`available` and OMITS `trust_limit` entirely (edge_patch_builder.py:269-277).
        # Absent, or a value - never null.
        trust_limit:
          oneOf:
            - type: string
            - type: number
        used:
          oneOf:
            - type: string
            - type: number
        available:
          oneOf:
            - type: string
            - type: number
        status:
          type: string
          nullable: true
        viz_color_key:
          type: string
          nullable: true
        viz_width_key:
          type: string
          nullable: true
        viz_alpha_key:
          type: string
          nullable: true
      additionalProperties: true

    SimulatorPaletteEntry:
      type: object
      required: [color]
      properties:
        color:
          type: string
        label:
          type: string
          nullable: true
      additionalProperties: false

    SimulatorGraphLimits:
      type: object
      properties:
        max_nodes:
          type: integer
          nullable: true
        max_links:
          type: integer
          nullable: true
        max_particles:
          type: integer
          nullable: true
      additionalProperties: false

    SimulatorGraphSnapshot:
      type: object
      required: [equivalent, generated_at, nodes, links]
      properties:
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        generated_at:
          type: string
          format: date-time
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/SimulatorGraphNode'
        links:
          type: array
          items:
            $ref: '#/components/schemas/SimulatorGraphLink'
        palette:
          type: object
          nullable: true
          additionalProperties:
            $ref: '#/components/schemas/SimulatorPaletteEntry'
        limits:
          $ref: '#/components/schemas/SimulatorGraphLimits'
      additionalProperties: true

    SimulatorGraphNodePatch:
      type: object
      required: [id]
      properties:
        id:
          type: string
        net_balance:
          type: string
          nullable: true
        net_balance_atoms:
          type: string
          nullable: true
        net_sign:
          type: integer
          nullable: true
          enum: [-1, 0, 1]
        viz_color_key:
          type: string
          nullable: true
        viz_shape_key:
          type: string
          nullable: true
        viz_size:
          $ref: '#/components/schemas/SimulatorVizSize'
        viz_badge_key:
          type: string
          nullable: true
      additionalProperties: true

    SimulatorGraphLinkPatch:
      type: object
      required: [source, target]
      properties:
        source:
          type: string
        target:
          type: string
        # None of the three is ever null on the wire, so none of them carries a null branch.
        # The `nullable: true` that stood beside each `oneOf` was inert anyway - in OpenAPI 3.0.3
        # `nullable` modifies a sibling `type` and nothing else (`F-011-10`) - but it was also
        # untrue, on both schemas, for two different reasons.
        #
        # On the SNAPSHOT link: `scenario_to_snapshot` sets `used=0` unconditionally and takes
        # `trust_limit`/`available` from the scenario's `limit`
        # (app/core/simulator/snapshot_builder.py:373-384), and `limit` is `required` on every
        # trustline in fixtures/simulator/scenario.schema.json under
        # `anyOf: [integer, number, string]`, which rejects a missing key and an explicit null
        # alike - verified by feeding both through `validate_scenario_or_400`. Real mode then
        # overwrites all three with money strings (snapshot_builder.py:232-234).
        #
        # On the PATCH: there is no pydantic model at all, only two hand-built dicts.
        # `build_edge_patch_for_equivalent` sets all three to money strings
        # (app/core/simulator/edge_patch_builder.py:161-163); `build_edge_patch_for_pairs` sets
        # `used`/`available` and OMITS `trust_limit` entirely (edge_patch_builder.py:269-277).
        # Absent, or a value - never null.
        trust_limit:
          oneOf:
            - type: string
            - type: number
        used:
          oneOf:
            - type: string
            - type: number
        available:
          oneOf:
            - type: string
            - type: number
        status:
          type: string
          nullable: true
        viz_color_key:
          type: string
          nullable: true
        viz_width_key:
          type: string
          nullable: true
        viz_alpha_key:
          type: string
          nullable: true
      additionalProperties: true

    SimulatorEventEdgeRef:
      type: object
      required: [from, to]
      properties:
        from:
          type: string
        to:
          type: string
      additionalProperties: false

    SimulatorEventEdgeStyle:
      type: object
      properties:
        viz_width_key:
          type: string
          nullable: true
        viz_alpha_key:
          type: string
          nullable: true
      additionalProperties: true

    SimulatorTxUpdatedEvent:
      type: object
      required: [event_id, ts, type, equivalent]
      properties:
        event_id:
          type: string
        ts:
          type: string
          format: date-time
        type:
          type: string
          enum: [tx.updated]
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        from:
          type: string
          nullable: true
        to:
          type: string
          nullable: true
        amount:
          type: string
          nullable: true
        amount_flyout:
          type: boolean
          nullable: true
        ttl_ms:
          type: integer
          nullable: true
        intensity_key:
          type: string
          nullable: true
        edges:
          type: array
          nullable: true
          items:
            type: object
            required: [from, to]
            properties:
              from:
                type: string
              to:
                type: string
              style:
                $ref: '#/components/schemas/SimulatorEventEdgeStyle'
            additionalProperties: true
        node_badges:
          type: array
          nullable: true
          items:
            type: object
            required: [id]
            properties:
              id:
                type: string
              viz_badge_key:
                type: string
                nullable: true
            additionalProperties: true
        node_patch:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/SimulatorGraphNodePatch'
        edge_patch:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/SimulatorGraphLinkPatch'
      additionalProperties: true

    SimulatorTxFailedEvent:
      type: object
      required: [event_id, ts, type, equivalent, error]
      properties:
        event_id:
          type: string
        ts:
          type: string
          format: date-time
        type:
          type: string
          enum: [tx.failed]
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        from:
          type: string
          nullable: true
        to:
          type: string
          nullable: true
        error:
          type: object
          required: [code, message, at]
          properties:
            code:
              type: string
            message:
              type: string
            at:
              type: string
              format: date-time
            details:
              type: object
              nullable: true
              additionalProperties: true
          additionalProperties: true
      additionalProperties: true

    SimulatorClearingDoneEvent:
      type: object
      required: [event_id, ts, type, equivalent, plan_id]
      properties:
        event_id:
          type: string
        ts:
          type: string
          format: date-time
        type:
          type: string
          enum: [clearing.done]
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        plan_id:
          type: string
        cleared_cycles:
          type: integer
          nullable: true
        cleared_amount:
          type: string
          nullable: true
        cycle_edges:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/SimulatorEventEdgeRef'
        node_patch:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/SimulatorGraphNodePatch'
        edge_patch:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/SimulatorGraphLinkPatch'
      additionalProperties: true

    SimulatorAuditDriftEvent:
      type: object
      required: [event_id, ts, type, equivalent, tick_index, severity, total_drift, drifts, source]
      properties:
        event_id:
          type: string
        ts:
          type: string
          format: date-time
        type:
          type: string
          enum: [audit.drift]
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        tick_index:
          type: integer
        severity:
          type: string
          enum: [warning, critical]
        total_drift:
          type: string
        drifts:
          type: array
          items:
            type: object
            additionalProperties: true
        source:
          type: string
          enum: [post_tick_audit, delta_check]
      additionalProperties: true

    TopologyChangedNodeRef:
      type: object
      required: [pid]
      properties:
        pid:
          type: string
        name:
          type: string
          nullable: true
        type:
          type: string
          nullable: true
      additionalProperties: false

    TopologyChangedEdgeRef:
      type: object
      required: [from_pid, to_pid, equivalent_code]
      properties:
        from_pid:
          type: string
        to_pid:
          type: string
        equivalent_code:
          $ref: '#/components/schemas/EquivalentCode'
        limit:
          type: string
          nullable: true
      additionalProperties: false

    TopologyChangedPayload:
      type: object
      properties:
        added_nodes:
          type: array
          items:
            $ref: '#/components/schemas/TopologyChangedNodeRef'
        removed_nodes:
          type: array
          items:
            type: string
        frozen_nodes:
          type: array
          items:
            type: string
        added_edges:
          type: array
          items:
            $ref: '#/components/schemas/TopologyChangedEdgeRef'
        removed_edges:
          type: array
          items:
            $ref: '#/components/schemas/TopologyChangedEdgeRef'
        frozen_edges:
          type: array
          items:
            $ref: '#/components/schemas/TopologyChangedEdgeRef'
        node_patch:
          type: array
          nullable: true
          items:
            type: object
            additionalProperties: true
        edge_patch:
          type: array
          nullable: true
          items:
            type: object
            additionalProperties: true
      additionalProperties: false

    SimulatorTopologyChangedEvent:
      type: object
      required: [event_id, ts, type, equivalent, payload]
      properties:
        event_id:
          type: string
        ts:
          type: string
          format: date-time
        type:
          type: string
          enum: [topology.changed]
        equivalent:
          $ref: '#/components/schemas/EquivalentCode'
        payload:
          $ref: '#/components/schemas/TopologyChangedPayload'
        reason:
          type: string
          nullable: true
      additionalProperties: true

    SimulatorEvent:
      oneOf:
        - $ref: '#/components/schemas/SimulatorTxUpdatedEvent'
        - $ref: '#/components/schemas/SimulatorTxFailedEvent'
        - $ref: '#/components/schemas/SimulatorClearingDoneEvent'
        - $ref: '#/components/schemas/SimulatorAuditDriftEvent'
        - $ref: '#/components/schemas/SimulatorTopologyChangedEvent'
        - $ref: '#/components/schemas/SimulatorRunStatusEvent'

    SimulatorRunStatusEvent:
      type: object
      required: [event_id, ts, type, run_id, scenario_id, state]
      properties:
        event_id:
          type: string
        ts:
          type: string
          format: date-time
        type:
          type: string
          enum: [run_status]
        run_id:
          type: string
        scenario_id:
          type: string
        state:
          $ref: '#/components/schemas/RunState'
        sim_time_ms:
          type: integer
          nullable: true
          minimum: 0
        intensity_percent:
          type: integer
          nullable: true
          minimum: 0
          maximum: 100
        ops_sec:
          type: number
          nullable: true
          minimum: 0
        queue_depth:
          type: integer
          nullable: true
          minimum: 0
        attempts_total:
          type: integer
          nullable: true
          minimum: 0
        committed_total:
          type: integer
          nullable: true
          minimum: 0
        rejected_total:
          type: integer
          nullable: true
          minimum: 0
        errors_total:
          type: integer
          nullable: true
          minimum: 0
        timeouts_total:
          type: integer
          nullable: true
          minimum: 0
        consec_all_rejected_ticks:
          type: integer
          nullable: true
          minimum: 0
        last_event_type:
          type: string
          nullable: true
        current_phase:
          type: string
          nullable: true
        last_error:
          type: object
          nullable: true
          required: [code, message, at]
          properties:
            code:
              type: string
            message:
              type: string
            at:
              type: string
              format: date-time
          additionalProperties: false
      additionalProperties: true

    ScenarioUploadRequest:
      type: object
      required: [scenario]
      properties:
        scenario:
          type: object
          description: Scenario JSON payload (validated separately via scenario schema)
          additionalProperties: true
      additionalProperties: false

    ScenarioSummary:
      type: object
      required: [api_version, scenario_id, participants_count, trustlines_count, equivalents]
      properties:
        api_version:
          type: string
          example: simulator-api/1
        scenario_id:
          type: string
        name:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
          nullable: true
        participants_count:
          type: integer
          minimum: 0
        trustlines_count:
          type: integer
          minimum: 0
        equivalents:
          type: array
          items:
            type: string
        clusters_count:
          type: integer
          nullable: true
        hubs_count:
          type: integer
          nullable: true
        tags:
          type: array
          nullable: true
          items:
            type: string
      additionalProperties: false

    ScenariosListResponse:
      type: object
      required: [api_version, items]
      properties:
        api_version:
          type: string
          example: simulator-api/1
        items:
          type: array
          items:
            $ref: '#/components/schemas/ScenarioSummary'
      additionalProperties: false

    RunCreateRequest:
      type: object
      required: [scenario_id, mode, intensity_percent]
      properties:
        scenario_id:
          type: string
        mode:
          type: string
          enum: [fixtures, real]
        intensity_percent:
          type: integer
          minimum: 0
          maximum: 100
      additionalProperties: false

    RunCreateResponse:
      type: object
      required: [api_version, run_id]
      properties:
        api_version:
          type: string
          example: simulator-api/1
        run_id:
          type: string
      additionalProperties: false

    RunState:
      type: string
      enum: [idle, running, paused, stopping, stopped, error]

    RunStatus:
      type: object
      required: [api_version, run_id, scenario_id, mode, state]
      properties:
        api_version:
          type: string
          example: simulator-api/1
        run_id:
          type: string
        scenario_id:
          type: string
        mode:
          type: string
          enum: [fixtures, real]
        state:
          $ref: '#/components/schemas/RunState'
        started_at:
          type: string
          format: date-time
          nullable: true
        stopped_at:
          type: string
          format: date-time
          nullable: true
        stop_requested_at:
          type: string
          format: date-time
          nullable: true
        stop_source:
          type: string
          nullable: true
        stop_reason:
          type: string
          nullable: true
        stop_client:
          type: string
          nullable: true
        sim_time_ms:
          type: integer
          nullable: true
          minimum: 0
        intensity_percent:
          type: integer
          nullable: true
          minimum: 0
          maximum: 100
        ops_sec:
          type: number
          nullable: true
          minimum: 0
        queue_depth:
          type: integer
          nullable: true
          minimum: 0
        errors_total:
          type: integer
          nullable: true
          minimum: 0
        committed_total:
          type: integer
          nullable: true
          minimum: 0
        rejected_total:
          type: integer
          nullable: true
          minimum: 0
        attempts_total:
          type: integer
          nullable: true
          minimum: 0
        timeouts_total:
          type: integer
          nullable: true
          minimum: 0
        errors_last_1m:
          type: integer
          nullable: true
          minimum: 0
        consec_all_rejected_ticks:
          type: integer
          nullable: true
          minimum: 0
        last_error:
          type: object
          nullable: true
          required: [code, message, at]
          properties:
            code:
              type: string
            message:
              type: string
            at:
              type: string
              format: date-time
          additionalProperties: false
        last_event_type:
          type: string
          nullable: true
        current_phase:
          type: string
          nullable: true
      additionalProperties: false

    SetIntensityRequest:
      type: object
      required: [intensity_percent]
      properties:
        intensity_percent:
          type: integer
          minimum: 0
          maximum: 100
      additionalProperties: false

    MetricPoint:
      type: object
      required: [t_ms, v]
      properties:
        t_ms:
          type: integer
          minimum: 0
        v:
          # 2026-08-20 / p007_t715: decimal string, not a number. Two of the
          # seven series (total_debt, clearing_volume) are amounts in the
          # selected equivalent, and money stays exact decimal. One type for all
          # seven series: the values come from one column, so a union would only
          # force consumers to branch.
          # The form is fixed and identical for the DB-backed and the synthetic
          # producer: the eight fractional digits of the Numeric(20, 8) column,
          # plain decimal notation, never exponential.
          # null means "no measurement at/before this timestamp"; it is
          # intentionally distinguishable from a measured zero ("0.00000000").
          type: string
          nullable: true
          description: Metric value as decimal string
      additionalProperties: false

    MetricSeriesKey:
      type: string
      enum:
        - success_rate
        - avg_route_length
        - total_debt
        - clearing_volume
        - bottlenecks_score
        # Network cardinalities. Measured per tick and persisted since the
        # simulator storage MVP; exposed here by spec 007 / T713 so the canon,
        # the pydantic model, the reader and the writer describe one set.
        - active_participants
        - active_trustlines

    MetricSeries:
      type: object
      required: [key, points]
      properties:
        key:
          $ref: '#/components/schemas/MetricSeriesKey'
        unit:
          type: string
          nullable: true
          enum: ['%', count, amount]
        points:
          type: array
          items:
            $ref: '#/components/schemas/MetricPoint'
      additionalProperties: false

    MetricsResponse:
      type: object
      required: [api_version, run_id, equivalent, from_ms, to_ms, step_ms, series]
      properties:
        api_version:
          type: string
          example: simulator-api/1
        run_id:
          type: string
        equivalent:
          type: string
        from_ms:
          type: integer
          minimum: 0
        to_ms:
          type: integer
          minimum: 0
        step_ms:
          type: integer
          minimum: 1
        series:
          type: array
          items:
            $ref: '#/components/schemas/MetricSeries'
      additionalProperties: false

    BottleneckTarget:
      oneOf:
        - type: object
          required: [kind, from, to]
          properties:
            kind:
              type: string
              enum: [edge]
            from:
              type: string
            to:
              type: string
          additionalProperties: false
        - type: object
          required: [kind, id]
          properties:
            kind:
              type: string
              enum: [node]
            id:
              type: string
          additionalProperties: false

    BottleneckReasonCode:
      type: string
      enum:
        - LOW_AVAILABLE
        - HIGH_USED
        - FREQUENT_ABORTS
        - TOO_MANY_TIMEOUTS
        - ROUTING_TOO_DEEP
        - CLEARING_PRESSURE

    BottleneckItem:
      type: object
      required: [target, score, reason_code]
      properties:
        target:
          $ref: '#/components/schemas/BottleneckTarget'
        score:
          type: number
        reason_code:
          $ref: '#/components/schemas/BottleneckReasonCode'
        label:
          type: string
          nullable: true
        suggested_action:
          type: string
          nullable: true
      additionalProperties: false

    BottlenecksResponse:
      type: object
      required: [api_version, run_id, equivalent, items]
      properties:
        api_version:
          type: string
          example: simulator-api/1
        run_id:
          type: string
        equivalent:
          type: string
        items:
          type: array
          items:
            $ref: '#/components/schemas/BottleneckItem'
      additionalProperties: false

    ArtifactItem:
      type: object
      required: [name, url]
      properties:
        name:
          type: string
        content_type:
          type: string
          nullable: true
        size_bytes:
          type: integer
          nullable: true
          minimum: 0
        sha256:
          type: string
          nullable: true
        url:
          type: string
      additionalProperties: false

    ArtifactIndex:
      type: object
      required: [api_version, run_id, items]
      properties:
        api_version:
          type: string
          example: simulator-api/1
        run_id:
          type: string
        artifact_path:
          type: string
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/ArtifactItem'
        bundle_url:
          type: string
          nullable: true
      additionalProperties: false

    # 011/T1102 (2026-08-23): the five remaining admin reads, whose 200 bodies the canon declared
    # as a bare object. Every key below was read off the response models (app/schemas/admin.py,
    # app/schemas/metrics.py, app/schemas/trustline.py) AND observed on the wire by calling the
    # five routes against a seeded SQLite database.
    #
    # A note that applies to all of them: none of these routes sets response_model_exclude_none or
    # response_model_exclude_unset, so FastAPI emits EVERY declared field on every response. A
    # field that is "optional" in Python is present-and-null on the wire, which is why the
    # `required:` lists below are exhaustive and the optionality shows up as `nullable: true`.

    AdminTrustLinesListResponse:
      # GET /admin/trustlines (app/api/v1/admin.py:1396). AdminPaginatedMeta + items, the same
      # four-key shape as AdminParticipantsListResponse and AdminIncidentsListResponse.
      type: object
      required: [page, per_page, total, items]
      properties:
        page:
          type: integer
          minimum: 1
        per_page:
          type: integer
          minimum: 1
          maximum: 200
        total:
          # The unpaginated count from TrustLineService.count_all with the same filters.
          type: integer
          minimum: 0
        items:
          type: array
          items:
            $ref: '#/components/schemas/TrustLine'

    AdminAuditLogListResponse:
      # GET /admin/audit-log (app/api/v1/admin.py:960). Distinct from the existing
      # AdminAuditLogResponse, which carries `items` alone and is not referenced by any operation.
      type: object
      required: [page, per_page, total, items]
      properties:
        page:
          type: integer
          minimum: 1
        per_page:
          type: integer
          minimum: 1
          maximum: 200
        total:
          type: integer
          minimum: 0
        items:
          # Rows are ORM AuditLog objects validated straight into AdminAuditLogItem, newest
          # timestamp first. before_state/after_state stay open - see ACCEPTED_FREE_FORM.
          type: array
          items:
            $ref: '#/components/schemas/AdminAuditLogItem'

    AdminTrustLinesBottlenecksResponse:
      # GET /admin/trustlines/bottlenecks (app/api/v1/admin.py:693).
      type: object
      required: [threshold, items]
      properties:
        threshold:
          # A NUMBER here, not a decimal string: the handler parses the query into a Decimal and
          # then calls float() on it before building the response
          # (app/api/v1/admin.py:709, AdminTrustLinesBottlenecksResponse.threshold: float).
          # It is a ratio, not money, so AGENTS.md section 8 does not apply. Contrast
          # AdminParticipantMetricsResponse.meta.threshold, which is the same query parameter
          # served as a decimal string.
          type: number
          minimum: 0
          maximum: 1
        items:
          # Active trustlines with limit > 0 whose available/limit ratio is strictly below
          # `threshold`, ordered by available ascending then created_at ascending, capped at the
          # `limit` query parameter. `used` is the matching Debt row (0 when there is none) and
          # `available` is limit - used, both computed in the query, not stored.
          type: array
          items:
            $ref: '#/components/schemas/TrustLine'

    AdminLiquidityNetRow:
      # One ranked participant in the three top-N lists of the liquidity summary.
      type: object
      required: [pid, display_name, net]
      properties:
        pid:
          type: string
        display_name:
          type: string
        net:
          # Money: total credited minus total debited across the selected equivalent(s), as an
          # exact decimal string (AGENTS.md section 8). Negative for a net debtor. NOT atoms -
          # this is the raw Debt.amount sum in major units.
          type: string

    AdminLiquiditySummaryResponse:
      # GET /admin/liquidity/summary (app/api/v1/admin.py:712).
      type: object
      required:
        - equivalent
        - threshold
        - updated_at
        - active_trustlines
        - bottlenecks
        - incidents_over_sla
        - total_limit
        - total_used
        - total_available
        - top_creditors
        - top_debtors
        - top_by_abs_net
        - top_bottleneck_edges
      properties:
        equivalent:
          # The `equivalent` query parameter, trimmed and upper-cased, or null when it was omitted
          # or blank (app/api/v1/admin.py:719-720). Echoed with no lookup: an unknown code is a 200
          # with this field set and every aggregate at zero, not a 404. Deliberately NOT
          # $ref EquivalentCode - nothing validates the pattern on this route.
          type: string
          nullable: true
        threshold:
          # A NUMBER, via float(Decimal(...)) - see the note on
          # AdminTrustLinesBottlenecksResponse.threshold.
          type: number
          minimum: 0
          maximum: 1
        updated_at:
          # datetime.now(timezone.utc) at handler entry, so it is always offset-aware and the
          # format is honest here (unlike the raw column copies in AdminGraphTransactionItem).
          type: string
          format: date-time
        active_trustlines:
          # COUNT over trustlines with status = 'active', filtered by equivalent when given.
          type: integer
          minimum: 0
        bottlenecks:
          # The TOTAL number of edges below the threshold, not the length of
          # top_bottleneck_edges: the helper counts every match and only then applies `limit`
          # (app/api/v1/admin.py:667-668).
          type: integer
          minimum: 0
        incidents_over_sla:
          # PAYMENT transactions still in a non-terminal state whose updated_at is older than
          # PAYMENT_TX_STUCK_TIMEOUT_SECONDS. When `equivalent` is given the filter is applied in
          # Python against payload["equivalent"], so a row with no equivalent in its payload is
          # excluded from a filtered count but included in the unfiltered one.
          type: integer
          minimum: 0
        total_limit:
          # Money, exact decimal string: SUM of TrustLine.limit over active lines.
          type: string
        total_used:
          # Money: SUM of coalesce(Debt.amount, 0) over the same lines.
          type: string
        total_available:
          # Money: SUM of (limit - used). Equals total_limit - total_used.
          type: string
        top_creditors:
          # Net > 0, descending, capped at the `limit` query parameter.
          type: array
          items:
            $ref: '#/components/schemas/AdminLiquidityNetRow'
        top_debtors:
          # Net < 0, ascending (most negative first), same cap.
          type: array
          items:
            $ref: '#/components/schemas/AdminLiquidityNetRow'
        top_by_abs_net:
          # Ranked by abs(net) with no sign filter, so its rows overlap the two lists above.
          type: array
          items:
            $ref: '#/components/schemas/AdminLiquidityNetRow'
        top_bottleneck_edges:
          # The same rows GET /admin/trustlines/bottlenecks returns for this threshold, limit and
          # equivalent - one shared helper builds both (_load_admin_trustline_bottlenecks).
          type: array
          items:
            $ref: '#/components/schemas/TrustLine'

    AdminParticipantBalanceRow:
      # One equivalent's aggregates for the participant. All six amounts are money as exact
      # decimal strings. Rows count LIVE trustlines only (status != 'closed'): since migration 019
      # a closed incarnation can coexist with the live one and summing both double-counts
      # (app/core/admin/metrics.py:196-199).
      type: object
      required:
        - equivalent
        - outgoing_limit
        - outgoing_used
        - incoming_limit
        - incoming_used
        - total_debt
        - total_credit
        - net
      properties:
        equivalent:
          type: string
        outgoing_limit:
          # Participant is the creditor (trustline `from`).
          type: string
        outgoing_used:
          type: string
        incoming_limit:
          # Participant is the debtor (trustline `to`).
          type: string
        incoming_used:
          type: string
        total_debt:
          # SUM of Debt.amount where the participant is the debtor. Unlike the four limit/used
          # figures this is not restricted to live trustlines - it reads the debts table.
          type: string
        total_credit:
          type: string
        net:
          # total_credit - total_debt; negative for a net debtor.
          type: string

    AdminParticipantCounterpartySplitRow:
      type: object
      required: [pid, display_name, amount, share]
      properties:
        pid:
          type: string
        display_name:
          # Never null: falls back to the pid when the column is blank
          # (app/core/admin/metrics.py:324).
          type: string
        amount:
          # Money, exact decimal string.
          type: string
        share:
          # amount / total as a float, or 0.0 when the total is zero. A ratio, not money - the one
          # place in this family where float(Decimal / Decimal) is applied to money-derived values.
          type: number

    AdminParticipantCounterpartySplit:
      # Who the participant owes and who owes them, for the requested equivalent only.
      type: object
      required: [eq, totalDebt, totalCredit, creditors, debtors]
      properties:
        eq:
          type: string
        totalDebt:
          # camelCase on the wire: the field is total_debt in Python with
          # serialization_alias="totalDebt" (app/schemas/metrics.py:31-32), and FastAPI serializes
          # responses with by_alias=True. Money, exact decimal string.
          type: string
        totalCredit:
          # Same aliasing as totalDebt.
          type: string
        creditors:
          # Counterparties the participant owes (participant is the debtor), sorted by amount
          # descending then pid. `share` is of totalDebt.
          type: array
          items:
            $ref: '#/components/schemas/AdminParticipantCounterpartySplitRow'
        debtors:
          # Counterparties who owe the participant; `share` is of totalCredit.
          type: array
          items:
            $ref: '#/components/schemas/AdminParticipantCounterpartySplitRow'

    AdminParticipantConcentrationSide:
      type: object
      required: [top1, top5, hhi]
      properties:
        top1:
          # Largest single counterparty share, 0.0 when there are none.
          type: number
        top5:
          # Sum of the five largest shares.
          type: number
        hhi:
          # Herfindahl-Hirschman index as the sum of squared shares, so it is on the 0..1 scale,
          # not the 0..10000 scale the name usually implies (app/core/admin/metrics.py:74-75).
          type: number

    AdminParticipantConcentration:
      # Computed purely from the counterparty split - no extra query.
      type: object
      required: [eq, outgoing, incoming]
      properties:
        eq:
          type: string
        outgoing:
          # Shares of the `creditors` list, i.e. concentration of what the participant owes.
          $ref: '#/components/schemas/AdminParticipantConcentrationSide'
        incoming:
          # Shares of the `debtors` list.
          $ref: '#/components/schemas/AdminParticipantConcentrationSide'

    AdminParticipantNetDistributionBin:
      type: object
      required: [from_atoms, to_atoms, count]
      properties:
        from_atoms:
          # Integer atoms via str(int): no decimal point, negative allowed. NOT a money amount -
          # the major-unit value is this divided by 10 ** equivalent precision. Bin bounds are
          # half-open [from_atoms, to_atoms) except the last bin, whose to_atoms is the maximum.
          type: string
        to_atoms:
          type: string
        count:
          type: integer
          minimum: 0

    AdminParticipantNetDistribution:
      # Net position of EVERY participant in the system for this equivalent, bucketed - the
      # requested participant is one point in it, not the subject.
      type: object
      required: [eq, min_atoms, max_atoms, bins]
      properties:
        eq:
          type: string
        min_atoms:
          # Same atoms-as-string convention as the bin bounds.
          type: string
        max_atoms:
          type: string
        bins:
          # Exactly 20 bins of equal integer width, or a single degenerate bin holding every
          # participant when max == min (app/core/admin/metrics.py:405-411).
          type: array
          items:
            $ref: '#/components/schemas/AdminParticipantNetDistributionBin'

    AdminParticipantRank:
      type: object
      required: [eq, rank, n, percentile, net]
      properties:
        eq:
          type: string
        rank:
          # 1-based, 1 = largest net. The code keeps a 0 branch for a pid missing from the
          # participants table, which the handler's own earlier lookup makes unreachable.
          type: integer
          minimum: 0
        n:
          # Total participants ranked, i.e. every row of the participants table.
          type: integer
          minimum: 0
        percentile:
          # (n - rank) / (n - 1), and exactly 1.0 when n <= 1.
          type: number
        net:
          # Money, exact decimal string - but it has been through atoms and back
          # (_decimal_to_atoms truncates toward zero at the equivalent's precision, then
          # _atoms_to_decimal quantizes), so it can differ from AdminParticipantBalanceRow.net in
          # the sub-precision digits and it carries exactly `precision` decimal places.
          type: string

    AdminParticipantCapacitySide:
      type: object
      required: [limit, used, pct]
      properties:
        limit:
          # Money, exact decimal string; live trustlines only (status != 'closed').
          type: string
        used:
          type: string
        pct:
          # used / limit as a float, 0.0 when limit is 0. A ratio, not money.
          type: number

    AdminParticipantCapacityBottleneck:
      type: object
      required: [dir, other, trustline]
      properties:
        dir:
          # Direction relative to the requested participant: "out" = they are the creditor.
          type: string
          enum: [out, in]
        other:
          # The counterparty PID, or the raw participant UUID as a string if the pid lookup misses
          # (app/core/admin/metrics.py:503-504).
          type: string
        trustline:
          # from_display_name and to_display_name are hard-coded to null on this path
          # (app/core/admin/metrics.py:511-512) - unlike the same schema served by
          # /admin/trustlines/bottlenecks, which joins the names.
          $ref: '#/components/schemas/TrustLine'

    AdminParticipantCapacity:
      type: object
      required: [eq, out, inc, bottlenecks]
      properties:
        eq:
          type: string
        out:
          # Lines where the participant is the creditor.
          $ref: '#/components/schemas/AdminParticipantCapacitySide'
        inc:
          # Lines where the participant is the debtor. Note the key is `inc`, not `in`.
          $ref: '#/components/schemas/AdminParticipantCapacitySide'
        bottlenecks:
          # ALWAYS EMPTY unless the `threshold` query parameter is supplied: the guard is
          # `thr is not None`, and threshold has no default on this route
          # (app/api/v1/admin.py:2227, app/core/admin/metrics.py:527). Sorted by dir, then
          # counterparty pid, then trustline id. A line the participant sits on both ends of
          # would appear twice, once per direction.
          type: array
          items:
            $ref: '#/components/schemas/AdminParticipantCapacityBottleneck'

    AdminParticipantActivity:
      # Event counts in trailing windows. "Now" is not the clock: it is the maximum timestamp
      # found among the participant's trustlines, their audit rows and ALL transactions, falling
      # back to utcnow only when none exists (app/core/admin/metrics.py:608).
      type: object
      required:
        - windows
        - trustline_created
        - trustline_closed
        - incident_count
        - participant_ops
        - payment_committed
        - clearing_committed
        - has_transactions
      properties:
        windows:
          # Always literally [7, 30, 90]; no parameter changes it.
          type: array
          items:
            type: integer
        trustline_created:
          # Keys are the window sizes as decimal strings - "7", "30", "90" - because the Python
          # type is dict[int, int] and JSON object keys are strings. Windows are cumulative and
          # nested, so the 90 count includes the 30 count.
          type: object
          additionalProperties:
            type: integer
        trustline_closed:
          type: object
          additionalProperties:
            type: integer
        incident_count:
          # Stuck PAYMENT transactions the participant initiated. Not filtered by equivalent.
          type: object
          additionalProperties:
            type: integer
        participant_ops:
          # Audit rows against this pid whose action starts with PARTICIPANT_ or
          # admin.participants. - two vocabularies, both matched.
          type: object
          additionalProperties:
            type: integer
        payment_committed:
          type: object
          additionalProperties:
            type: integer
        clearing_committed:
          type: object
          additionalProperties:
            type: integer
        has_transactions:
          # True when the pre-filter query returned any committed PAYMENT/CLEARING row in the
          # widest window AT ALL - it is not about this participant, so it can be true while every
          # count above is zero (app/core/admin/metrics.py:683).
          type: boolean

    AdminParticipantMetricsResponse:
      # GET /admin/participants/{pid}/metrics (app/api/v1/admin.py:2223), assembled by
      # compute_participant_metrics. Six of the nine keys are filled only when `equivalent` is
      # supplied; without it the handler returns early and they are present-and-null
      # (app/core/admin/metrics.py:120).
      type: object
      required:
        - pid
        - equivalent
        - balance_rows
        - counterparty
        - concentration
        - distribution
        - rank
        - capacity
        - activity
        - meta
      properties:
        pid:
          # The path parameter, trimmed. A blank or unknown pid is a 404, so this is never empty.
          type: string
        equivalent:
          # Trimmed and upper-cased, null when omitted. Unlike the liquidity summary this route
          # DOES validate it: validate_equivalent_code enforces the format and an unknown code is
          # a 404, so a non-null value here always names a real equivalent.
          type: string
          nullable: true
        balance_rows:
          # One row per equivalent, ordered by code, when `equivalent` is omitted; exactly one row
          # when it is given. Equivalents the participant has no exposure to still get a row of
          # zeros.
          type: array
          items:
            $ref: '#/components/schemas/AdminParticipantBalanceRow'
        counterparty:
          # All six of these are `required` AND present-and-null whenever `equivalent` was
          # omitted: compute_participant_metrics returns at app/core/admin/metrics.py:120 before
          # any of them is computed, and each is `Optional[...] = None` on the response model
          # (app/schemas/metrics.py:107-112) with no `exclude_none` anywhere on the route.
          #
          # `allOf` with a sibling `nullable: true` did NOT say that. In OpenAPI 3.0.3 `nullable`
          # modifies a sibling `type` and nothing else, so beside a bare `allOf` it was inert and
          # all six rejected the null they claimed to allow: a real 200 body from
          # GET /admin/participants/{pid}/metrics failed validation against this document on all
          # six properties, twice each - "None for not nullable" and "None is not of type
          # 'object'" (`F-011-10`). The composition keyword therefore changes from `allOf` to
          # `oneOf`: a null branch cannot be added to an `allOf`, where every branch must match.
          oneOf:
            - $ref: '#/components/schemas/AdminParticipantCounterpartySplit'
            - nullable: true
              enum: [null]
        concentration:
          oneOf:
            - $ref: '#/components/schemas/AdminParticipantConcentration'
            - nullable: true
              enum: [null]
        distribution:
          oneOf:
            - $ref: '#/components/schemas/AdminParticipantNetDistribution'
            - nullable: true
              enum: [null]
        rank:
          oneOf:
            - $ref: '#/components/schemas/AdminParticipantRank'
            - nullable: true
              enum: [null]
        capacity:
          oneOf:
            - $ref: '#/components/schemas/AdminParticipantCapacity'
            - nullable: true
              enum: [null]
        activity:
          oneOf:
            - $ref: '#/components/schemas/AdminParticipantActivity'
            - nullable: true
              enum: [null]
        meta:
          # Declared dict[str, Any] "for forward compatibility", but there is exactly one writer
          # and it stores exactly one key (app/core/admin/metrics.py:163-165). The object is empty
          # when `equivalent` was omitted, because that path returns before meta is built.
          # additionalProperties stays true to match the annotation.
          type: object
          additionalProperties: true
          properties:
            threshold:
              # A decimal STRING here - the raw Decimal from the query parameter, serialized by
              # pydantic's Any-field inference. The SAME query parameter is a `number` on
              # /admin/liquidity/summary and /admin/trustlines/bottlenecks, which call float() on
              # it. Null when the caller supplied `equivalent` but not `threshold`; the key is
              # absent entirely when `equivalent` was omitted, so it is not required.
              type: string
              nullable: true

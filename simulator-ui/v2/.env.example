# Demo Fast Mock v2
# Vite only exposes vars prefixed with VITE_

# When true, the UI must load from /simulator-fixtures/ (no backend).
VITE_DEMO_FIXTURES=1

# Base path for fixtures (served by Vite from /public)
VITE_FIXTURES_BASE=/simulator-fixtures/v1

# Strict data/visual contract: unknown viz keys are a hard error.
VITE_STRICT_VIZ_KEYS=1

# Test-mode: disable animations/particles for stable screenshots
VITE_TEST_MODE=0

# When true, the Demo UI button is visible even in production (outside dev tools).
VITE_ALLOW_DEMO_UI=false

# --- Optional: Real mode (backend) ---
# Enable real mode explicitly (or use URL ?mode=real)
# VITE_API_MODE=real
# Backend base for API calls (default: /api/v1)
# VITE_GEO_API_BASE=/api/v1
# Dev convenience token used only in `vite dev` when no token is set
# (treated as X-Admin-Token by the UI)
# VITE_GEO_DEV_ACCESS_TOKEN=dev-admin-token-change-me
# If you prefer JWT participant auth, set it here (treated as Authorization: Bearer <jwt>)
# VITE_GEO_ACCESS_TOKEN=

# Admin UI environment (example)
#
# Real-mode: calls the backend API directly from the browser.
# Requires backend running (default local: http://127.0.0.1:18000)
#
VITE_API_MODE=real
VITE_API_BASE_URL=http://127.0.0.1:18000

# Notes:
# - If you want fixtures/mock mode instead:
#     VITE_API_MODE=mock
# - Vite dev proxy is NOT configured by default in this repo, so prefer VITE_API_BASE_URL.

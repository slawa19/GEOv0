"""The debt ledger's own machinery: the operation journal (programme 015, phase B step 4).

`journal` is the only module here today. It is INERT until something installs it on an engine and a
session target - step 4 slice A builds the mechanism and registers nothing globally, so no existing
behaviour changes.
"""

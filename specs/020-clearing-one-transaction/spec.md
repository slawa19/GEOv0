# 020 — Клиринг: один детектор и одно правило выбора

- **Date:** 2026-09-21 (черновик); переписана 2026-09-25 (`T2000`) по консультации Codex и ответу владельца, против `main` на `5934e24`.
- **Status:** AUTHORIZED WITH CONDITIONS 2026-09-25 — по второму кругу консультации (`T2000-DECISION: AUTHORIZE-WITH-CONDITIONS`, раздел «Решения консультации, второй круг»). Стадии начинаются **по порядку**, как в 019: стадия 1 в работе, каждая следующая — после слияния предыдущей с её гейтом и §15-ревью. Условия (дословно из маркера `CONDITIONS`): *define per-detection full-depth amount ordering; move semantic auto_clear change and documentation into stage 3; strengthen R-020-1 and R-020-2 anti-vacuum controls; require equivalent-comparator and repeated-call cost evidence before switch or deletion; reconcile 021 ordering and durability handoff; correct three factual statements; canonical gates and exact-HEAD §15 reviews per stage*.
- **Status authority:** метка описательная; завершённость устанавливают success criteria и записанные evidence, а не поле статуса.
- **Owner surface:** `app/core/clearing/service.py` (детекторы `:471-1460`, `auto_clear` `:2252-2300`, разрешение исхода коммита `:2199-2250`); новые тесты `tests/**/test_p020_*` и перенос ассертов из селекторов раздела «Verification plan», п. 3; детерминированный генератор и раннер замера детектора в `scripts/` (новый файл по образцу `scripts/measure_clearing_min_amount_plan.py`); реализационные примечания `docs/ru/02-protocol-spec.md` §7.2 (`:991-1045`) и `docs/ru/09-decisions-and-defaults.md` §1.12.1 (`:308-343`). **Исключено:** `app/core/money_boundary.py` (019; сохраняется как есть), `app/core/ledger/book.py` (018), `app/core/invariants.py` (нейтральность — без изменений), `app/core/payments/`, `app/api/v1/clearing.py` (контракт вызова не меняется), `api/openapi.yaml` (read-only: `max_depth` 3–10, по умолчанию 6, `:2719-2723`, `:2753-2757`), драйвер клиринга симулятора `app/core/simulator/real_clearing_engine.py` и соседи (021) — при этом `_SQL_DETECTOR_MAX_CYCLE_LENGTH` и `ClearingCommittedAfterCancellation` остаются импортируемыми (`real_clearing_engine.py:16-18`), пока их потребитель не мигрирует в 021.
- **Origin:** решение владельца 2026-09-21 (последовательность 018–021); решение владельца 2026-09-24 «упрощение ядра — цель» (`specs/README.md:106`). Консультация Codex 2026-09-25: `gpt-6-astra` (requested), high, read-only, замороженный `5934e24`; вывод `.local-run/codex-review/2026-09-25-020/final.md` (не коммитится). Маркеры: `VERDICT-020: ACCEPT-WITH-CHANGES`, `T2000-DECISION: NARROW`, `SPEC-FRESH: PARTLY`, `SCOPE-020: TOO-BIG`, P1 = 1, P2 = 7, P3 = 1, `PRODUCT-QUESTIONS: 1`.
- **Depends on:** 018 (мутация долга через `Book`, `ClearingReduction`), 019 (закрыта 2026-09-25: одна транзакция `SERIALIZABLE` на попытку, исключительный **сессионный** лок эквивалента у клиринга, владелец повторов `_run_attempts`).
- **Blocks:** 021 (граница уточнена там же, см. Changelog 021 от 2026-09-25).

## Problem

**Историческое.** `T1549`-находка 2 (`40001` на `INSERT INTO transactions` → `500`) закрыта программой 019: ошибки вставки и flush внутри денежного блока доходят до `_end_attempt_on_error` и `_run_attempts` (`service.py:2084`, `:2247`, `:1462`); реальный `40001` держит регрессия `tests/integration/test_clearing_commit_replay_postgres.py:458`. Interlock резервирований и `prepare_locks` сняты 019. Основанием этой программы они больше не являются.

**Что остаётся.**

1. **Три детектора с разной семантикой и усечением.** Треугольники `find_triangles_sql` (`service.py:635`, `LIMIT 100` на `:760`), четырёхугольники `find_quadrangles_sql` (`:813`, `LIMIT 50` на `:888`), Python DFS (`:1315`, выход по `len(cycles) > 50` только между корнями, `:1372-1375`), слияние `:1433-1446`. SQL ограничивает **ротации** до дедупликации: при **различных** суммах циклов ротации одного треугольника идут подряд, и 100 строк — не более 34 разных треугольников; при равных суммах `ORDER BY clear_amount DESC` (`:759`) ротации не группирует, и 100 строк могут нести больше уникальных циклов. SQL фильтрует допуск в `JOIN` (`:741`), DFS — после обхода (`:1274`, `:1412`); оба ответа дополнительно проходят Python-проверку политики (`:1203`, `:1412`). Итог — какие циклы попадут в выборку при переполнении, зависит от обхода и повторов одного цикла, а не от объявленного правила.
2. **Остаток старшинства неизвестного коммита** (`specs/BACKLOG.md:95`, получатель 020): провал чтения-разрешения после неизвестного исхода `COMMIT` может разрешить второй денежный заход (F-020-3).

## Findings

Проверено оркестратором по `5934e24` 2026-09-25. Находки 1, 2, 4–9 — против черновика программы; находка 3 — дефект текущего кода.

| ID | Sev | Находка | Evidence |
|---|---|---|---|
| F-020-1 | P1 | Черновик брал строку эквивалента до advisory-лока и заменял сессионный лок транзакционным. Это обращает порядок 019 (advisory → строка эквивалента → долги; возможна взаимоблокировка с платежом) и теряет снимок **после** ожидания: транзакционный лок первого оператора не гарантирует свежий снимок и не переживает повтор всей транзакции | `money_boundary.py:26`, `:181`; `service.py:1698-1710`, `:1830`; 019 `spec.md:443`; протокол `02:1154` |
| F-020-2 | P2 | Новый владелец повторов и общий с платежом предикат не нужны: `_run_attempts` уже владеет повторами на цикл; предикат клиринга — `40001/40P01`; `23505` платежа относится к `uq_debts_debtor_creditor_equivalent`, а не к конверту/вхождению | `service.py:405`, `:1462-1500`; `payments/service.py:129` |
| F-020-3 | P2 | Неизвестный коммит: `40001/40P01` чтения-разрешения пробрасывает уже `_drain_task` (`task.result()` на `:202`) — исключение выходит из `await self._drain_task(...)` на `:2212`, строка `:2215` не достигается; внешний `except` (`:2247`) отдаёт его `_end_attempt_on_error`, тот не находит вхождения и поднимает `_ClearingAttemptConflict` (`:184`) — повтор рождается из вторичной ошибки. Дубля клиринга нет (детерминированный `tx_id`, `:275`, `:2062`), но нарушено «unknown commit is never retried» (docstring `:171-172`, `09:327`). Не воспроизведено | `service.py:2199-2250`, `:156-186`; `BACKLOG.md:95-101` |
| F-020-4 | P2 | Правило выбора шире, чем признавал черновик: порядок по сумме меняет исход на общих рёбрах и ниже лимита; лимит уникальных циклов меняет покрытие против лимита ротаций; минимальный `debt.id` не полный ключ равенства | `service.py:559-581`, `:760`, `:1374`; `tests/unit/test_p012_t1210_detector_union_default_tier.py:206` |
| F-020-5 | P2 | Черновик исключал глубины 7–10 и обещал «пропуск при изменённой сумме». Канон: 3–10, по умолчанию 6; исполнение берёт `min` **текущих залоченных** сумм; изменённая положительная сумма сама по себе пропуска не вызывает, прежние условия пропуска (пропавшие строки, неположительная сумма, отказ политики) остаются | `openapi.yaml:2721-2723`, `:2755-2757`; `service.py:1851`, `:1907-1911`, `:1926` |
| F-020-6 | P2 | Граница 020/021 противоречива: 021 планировал клиринг внутри транзакции тика, а контракт клиринга — положительная сумма уже долговечна, `commit=False` у вызывающего исключён; симулятор импортирует константу глубины и ловит `ClearingCommittedAfterCancellation` | `09:310`, `:327`; `real_clearing_engine.py:16-18`, `:384-389`; 021 `spec.md:34` (до правки) |
| F-020-7 | P2 | План проверки не отделял новое от уже верного: два «красных» репродьюсера зелёные сегодня; модуль «только объединения» несёт ассерты остатка долга на общем ребре и достижимости длинного цикла, а другой модуль импортирует его фикстуры | `test_clearing_commit_replay_postgres.py:458`; `test_p012_t1210_…:206`, `:288`; `test_p012_t1211_shared_edge_order_postgres.py:6` (import) |
| F-020-8 | P2 | Замер «граф 200 участников» не задаёт ветвления и числа путей; `ORDER BY … LIMIT` ограничивает результат, а не рекурсивное раскрытие; подсчёт запросов не включал реплей, стоп/hold, нейтральность, `Book` | `invariants.py:237`; `book.py:943` |
| F-020-9 | P3 | Карта источников и реестр устарели: 2 369 строк сервиса (не 2 349), `engine.py` удалён, §7.6.1 переписан (а не снят), решения UoW/реплея на `09:308` | `specs/README.md:43` |

**Свежесть черновика (сжато).** Верно: три детектора; один CTE как предложение; `UUIDv5` вхождения; нейтральность; одна атомарная денежная попытка на цикл; non-goals. Устарело: якоря, «восемь строк клиринга» (мутация в `Book`, `service.py:2130` → `book.py:429`), `_debt_id_key` (`:539`, расхождение написания UUID было SQLite-only), `T1549`-2, оценка 400–500 строк. Неверно: «`auto_clear(6)` всегда в DFS» (лестница `[4, 6]` сначала пробует 4, `:2273`); «разные правила финального порядка» (оба подмножества сортируются `_cycle_order_key`: длина ↑, сумма ↓, id ↑ — различается отбор кандидатов); «периметр вершин ≠ периметр рёбер» (на простом цикле эквивалентны; DFS уже исключает чужие вершины на длинах 5–6, `:1274`); `max_cycles` (параметра нет; `auto_clear` останавливается на `count > 100`, `:2290`, то есть допускает **101** успех); общий `23505`; снятие пиннинга и `ClearingCommittedAfterCancellation`.

## Current / Intended / Optimal

- **Current.** Выборка кандидатов определяется тремя детекторами с разными лимитами (100 ротаций / 50 ротаций / >50 сырых циклов между корнями) и слиянием; финальный порядок — длина ↑, сумма ↓, отсортированные id ↑ (`:577-581`); `auto_clear` идёт лестницей `[min(depth,4), depth]` (`:2273-2275`), то есть исполняет короткий цикл раньше длинного. Исполнение — под исключительным сессионным локом 019, `min` залоченных сумм, `Book`, нейтральность, реплей по `UUIDv5`. После неизвестного коммита ошибка чтения-разрешения может разрешить повтор (F-020-3).
- **Intended.** До 100 **уникальных** допустимых циклов, выбранных одним правилом (решение владельца 2026-09-25, ниже); повтор разрешён только при подтверждённом откате (предусловие 3 четвёртого круга 019; `09:327`).
- **Optimal.** Один детектор над одним отношением допустимых рёбер с канонизацией до лимита; одна граница исполнения 019 без изменений; локальная правка старшинства исхода коммита без нового слоя повторов и без новой долговечной сущности.

## Решения

**Детектор — один рекурсивный CTE, условно.** Выбран по консультации при условии прохождения замера (Verification plan, п. 5) **до** включения. CTE перечисляет простые направленные циклы над отношением допустимых рёбер: один эквивалент, `amount > 0`, направление линии кредитор → должник, статус линии active/frozen (`T1551`), действующий консент `auto_clearing` (включая поддерживаемые legacy-значения, `service.py:982`; SQL-версия не вправе молча расширить консент), оба конца ребра в периметре (`None` — глобально; пустой или неразрешимый периметр — никого). Ротации канонизируются один раз (старт с наименьшего UUID долга), лимит применяется к уникальным циклам **после** порядка. Эта же допустимость перепроверяется при авторитетном исполнении — ранее найденный цикл допустимым не считается. Внутри — native UUID; наружу — прежние PID, канонические строки UUID и формат денег.

- **Сравнитель — один DFS над одним SQL-отфильтрованным графом** с тем же контрактом (допустимость, канонизация, порядок, top-100). Это настоящая более дешёвая альтернатива (отказ черновика от неё недействителен) и обязательный экспериментальный сравнитель замера; если он даёт то же поведение проще и в пределах порогов — выбирается он (второй круг, P2-4). Основания выбрать CTE — одно отношение допуска в БД, исчезновение слияния детекторов и меньше передачи графа и ORM-обвязки, а не «SQL быстрее».
- **Без runtime-fallback CTE → DFS.** Провал приёмки останавливает стадию 3 и возвращает выбор детектора на консультацию.
- Оставить только два фиксированных SQL-запроса — отвергнуто: теряются поддерживаемые циклы длиннее 4.

**Правило выбора — решение владельца 2026-09-25: «Большая сумма первой».** Правило определено **на одно обнаружение**. Если допустимых циклов больше лимита, выбираются до 100 уникальных допустимых циклов в порядке **суммы погашения по убыванию, независимо от длины**; при равенстве — по полной канонической идентичности (отсортированный кортеж всех UUID долгов цикла, а не только минимальный); лимит выборки считается отдельно от числа исполненных циклов. Рекомендация консультации была иной — «сначала короткие, внутри длины — большая сумма»; владелец выбрал иначе. **Это намеренное изменение поведения:** длинный цикл с большей суммой вытесняет короткий и **ниже лимита** — на общем ребре остаются другие долги, чем сегодня (Codex указал на это; `test_p012_t1210_…:206` — случай одной длины, он не меняется). **Лестница — решение второго круга консультации (`LADDER: FULL-DEPTH REQUEST ON EVERY DETECTION; RE-DETECT AFTER EACH SUCCESS`).** Сегодня `auto_clear` исполняет успешный кандидат короткой ступени, не заглядывая в широкую (`service.py:2273-2285`); сортировка каждой ступени по сумме этого не исправляет. Поэтому каждое обнаружение запрашивает **полную глубину вызова**, и порядок по сумме действует глобально по допустимым циклам длин 3…`max_depth` этого обнаружения:

1. сумма каждого кандидата вычисляется по графу этого обнаружения;
2. порядок — сумма ↓, затем полный канонический кортеж UUID долгов; не более 100 уникальных кандидатов;
3. кандидаты пробуются по порядку до первого успеха;
4. после успеха список отбрасывается, и обнаружение повторяется на полной глубине.

Это сохраняет нынешнее повторное обнаружение и применяет правило владельца. Обещаний **нет**: замороженной партии, оптимизации итогового объёма клиринга, глобально убывающих закоммиченных сумм при конкурентных изменениях; исполнение по-прежнему берёт текущие залоченные суммы (`:1907`). Лестница, собирающая обе ступени до общего порядка, отвергнута: запрос полной глубины уже содержит короткие циклы, а оптимизация, пропускающая глубокое перечисление, потребовала бы доказанной границы «невидимый цикл не обгонит выбранный» — такой механизм не оправдан. Достижимость длинного цикла, когда короткие не исполняются (`test_p012_t1210_…:288`), сохраняется по построению. Лимит 100 кандидатов — отдельно от потолка 101 успеха. Нового продуктового вопроса нет (`PRODUCT-QUESTIONS: 0`).

**Лок — форма и порядок 019 без изменений** (`service.py:1541`, протокол `02:1154`): (1) завершить/сбросить неавторитетное обнаружение; (2) закрепить соединение и взять ту же двухчисловую advisory-идентичность исключительно (`money_boundary.py:181`); (3) откатить транзакцию взятия, сохранив сессионное исключение; (4) авторитетная попытка `SERIALIZABLE`: проверка фактической изоляции, реплей, стоп/hold `row_lock=True` (`:1830`), `FOR UPDATE` долгов; (5) коммит или разрешение исхода; исключение держится через разрешённые повторы; (6) точное освобождение или инвалидация соединения. Пиннинг, ручная сборка engine-bound сессии (`:1568`, `:1686`; `09:287`) — действующие требования, не наследие резервирований.

**Повторы — `_run_attempts` остаётся** (он уже на цикл): откат всей транзакции, свежий снимок, сброс ORM (`:1500`), ограниченный бюджет, исходный SQLSTATE, исчерпание — повторяемый `409/E008`. Классификатор платежа не заимствуется; `23505` конверта/вхождения в список повторов не входит (разрешение идентичности — отдельно). Весь `auto_clear` в цикл повтора не оборачивается: более ранние циклы уже закоммичены.

**Неизвестный коммит — локальная правка потока управления**, первая кодовая стадия: исход исходного `COMMIT` старше ошибки его разрешения. Подтверждённый откат (`40001/40P01` самого коммита) — повтор разрешён; неизвестный исход — не становится повторяемым из-за провала разрешителя; подтверждённое вхождение возвращает долговечную сумму. Без нового реестра результатов и новой долговечной сущности.

**Сохраняется:** глубина 3–10, по умолчанию 6; `min` залоченных сумм: изменённая положительная сумма сама по себе пропуска не вызывает, исполнение пересчитывает минимум, прежние условия пропуска остаются (пропавшие строки `:1851`, неположительная сумма `:1909`, отказ политики `:1926`); допуск замороженных линий; периметр (исполнение и реплей в периметре); фильтрация политики **до** перечисления (отклонённый цикл не занимает лимит); потолок `auto_clear` 101 успех (`count > 100`) — отдельно от лимита кандидатов, смена на 100 была бы отдельным продуктовым решением; `UUIDv5` вхождения над неупорядоченным множеством UUID долгов с прежним namespace (`:275`); `NEW` → flush до конверта → `COMMITTED` в той же транзакции (`:2064`, `:2197`); `Book.operation`/`posting.apply(ClearingReduction(...))` (`book.py:895`, `service.py:2101-2130`); нейтральность (`:2025`, `:2189`; `invariants.py:260`); `ClearingCommittedAfterCancellation` и её потребитель (`real_clearing_engine.py:389`); вызываемые сигнатуры `find_cycles`, `execute_clearing_with_amount`, `auto_clear`.

## Решения консультации, второй круг (2026-09-25)

Codex, `gpt-6-astra` (requested), high, read-only, замороженный `8956bae`; вывод `.local-run/codex-review/2026-09-25-020b/final.md` (не коммитится). Статический разбор; тесты, замеры и код не запускались. Маркеры: `VERDICT-020: ACCEPT-WITH-CHANGES`, `T2000-DECISION: AUTHORIZE-WITH-CONDITIONS`, `FIRST-ROUND-ADDRESSED: PARTLY`, `LADDER: FULL-DEPTH REQUEST ON EVERY DETECTION; RE-DETECT AFTER EACH SUCCESS`, `OWNER-RULE-FAITHFUL: PARTLY`, `STAGE-PLAN: WEAK`, `PRODUCT-QUESTIONS: 0`, P1 = 0, P2 = 5, P3 = 3.

**Первый круг по оценке второго:** F-020-1, 2, 3, 5, 9 — `ADDRESSED`; F-020-4, 6, 7, 8 — `PARTLY` (доводятся находками ниже). «Addressed» — в спеке, не в коде и не в runtime.

| Находка | Суть | Диспозиция — где внесено |
|---|---|---|
| P2-1 | Стадия 3 вводила правило, а лестницу снимала стадия 4 — между ними правильный детектор исполнял бы короткий цикл первым | Принято. Семантика `auto_clear` и документация — в стадии 3 с полным backend milestone; стадия 4 — только чистка, сохраняющая поведение; стадия 5 сводит evidence («Стадии», «Tasks») |
| P2-2 | R-020-1 допускал 4-цикл (виден на короткой ступени) и не гарантировал наблюдаемого различия долгов | Принято. 5-цикл на глубине 6 с суммами 100/10/100, ассерт на остаток и число вхождений; тай-брейк с общим минимальным UUID; точный упорядоченный top-100 («Verification plan», п. 1) |
| P2-3 | R-020-2 проходил на базовой линии при неверном расписании отказов | Принято. Предписанное расписание, наблюдение входов в `_execute_clearing_with_amount`, оба положительных контроля (п. 1) |
| P2-4 | Замер ячеек авторизует эксперимент, а не переключение: не измерены повторные вызовы полной глубины и совместимость вызывающих | Принято. Требования «до переключения и до удаления», эквивалентный сравнитель, выбор DFS при равном поведении (п. 5; «Решения» → детектор) |
| P2-5 | 021 одновременно коммитил платежи до клиринга и обещал атомарность всего тика; лестница симулятора не станет amount-first от смены `find_cycles` | Принято. Non-goal о симуляторе как временном исключении; 021 правится в передаче (атомарность платёжной фазы, отдельно долговечные исходы клиринга, замена лестницы и ротации) |
| P3-1 | «100 строк — не более 34 треугольников» верно только при различных суммах | Исправлено («Problem», п. 1) |
| P3-2 | Исключение разрешителя выходит из `_drain_task` (`:202`) на `:2212`, а не на `:2215` | Исправлено (F-020-3) |
| P3-3 | Пропавшие строки — не единственное условие пропуска (`:1909`, `:1926`) | Исправлено (F-020-5, «Сохраняется») |

## Non-goals

- Не меняет правило протокола: `c = min`, каждое ребро минус `c`, нейтральность, консент через `auto_clearing`; явный консент (`CLEARING_PROPOSE/ACCEPT/REJECT`) остаётся зарезервированным.
- Не реализует клиринг по расписанию и по триггеру: режим решён 2026-08-11 (`09:42-45`), не реализован, и это продуктовая работа. `POST /clearing/auto` — действие хаба; симулятор вызывает сервис напрямую (`real_clearing_engine.py:384`).
- Не меняет глубину по умолчанию, диапазон 3–10, статусы клирящихся линий, потолок 101 успеха.
- Не вводит новый HTTP-контракт идемпотентности для `/clearing/auto` (решение 2026-08-11).
- Не трогает границу исполнения 019 (лок, пиннинг, освобождение, отмена) и не строит новый слой повторов или транзакций.
- Не трогает драйвер клиринга симулятора — 021. Его собственная короткая лестница (`real_clearing_engine.py:211-222`) и ротация приоритета по тикам (`:246`) — **временное исключение совместимости**, а не свидетельство, что каждый вызывающий следует правилу «большая сумма первой»: смена `find_cycles` не делает этого вызывающего amount-first на всей его глубине. Замена лестницы и ротации на обнаружение полной глубины с порядком по сумме — задача 021 (передача записана в 021). `_SQL_DETECTOR_MAX_CYCLE_LENGTH` сохраняет прежнее значение и смысл до миграции: перенастройка его значения молча изменила бы старого вызывающего.

## Проверка на переусложнение (§19.2) — обновлено 2026-09-25

1. **Какую наблюдаемую потерю это предотвращает?** Новой воспроизведённой потери денег нет. Основание — цель владельца «упрощение ядра» для 018–021 (`specs/README.md:106`), плюс снятие непоследовательной выборки кандидатов и правка назначенного дефекта классификации исхода (F-020-3). `T1549` — история, не основание.
2. **Что говорит протокол?** Консент, точное погашение цикла, нейтральность, атомарность эффектов и конкурентная граница/исход должны выжить (`02:1063`, `:1120`, `:1140`, `:1185`). CTE, размер сервиса и новый слой повторов протокол не требует. Пример протокола §7.2.3 упорядочивает треугольники `ORDER BY clear_amount DESC LIMIT 100` — правило владельца с ним согласуется.
3. **Кто противник?** Поддерживаемые конкурентные платежи, инжекты, конкурирующий клиринг и писатели стопа/hold; отмена и прерванная связь с БД. Произвольный враждебный код в процессе — вне заявления программы.
4. **Что дешевле и чего ему не хватает?** (а) Оставить нынешний исполнитель — дешевле, и он уже держит денежную границу: поэтому он **не** переписывается. (б) Один DFS над SQL-отфильтрованным графом — настоящая альтернатива детектору; дублирование политики ему **не** присуще. Сравнивается честно в замере; CTE выбирается, только если проходит пороги, и за одно отношение допуска в БД. (в) Только SQL 3/4 — теряет поддерживаемые длины 5–10. (г) Оставить все три — сохраняет расхождение выборки.
5. **Цена, числом.** Новых таблиц, воркеров и настроек — 0; новых файлов — генератор/раннер замера и тесты `test_p020_*`. Оценка: около 850–950 строк детекторной области (`service.py:471-1460`, ~990 физических строк с комментариями) заменяются 250–350, **нетто минус ~500–700 строк**; сервис около 1 650–1 900 строк, не 400–500. Это оценка, не цель удаления; исполняемые строки и комментарии учитываются раздельно. Историческое evidence 019: зонд клиринга — **47 SQL-операторов** без `BEGIN`/`COMMIT` (019 `spec.md:500`). Число запросов и время нового детектора не измерены — это стадия 2.
6. **Крайние случаи.** Берём достижимые: пересечение циклов по ребру, переполнение лимита, глубины до 10, изменённые залоченные суммы, исключения по политике/статусу/периметру, реплей, конфликт всей транзакции, стоп/hold, отмена, неизвестный коммит, неясное освобождение лока — через существующие границы. Инфраструктуру ради проверки инфраструктуры не строим.

## Verification plan

### 1. Репродьюсеры, обязанные падать на `5934e24` (red-first)

Только два. Новых красных тестов на `T1549` и на периметр **нет**: они уже держатся и остаются зелёными как retention (п. 3). Если красная характеризация сливается раньше своей правки — узкий `xfail(strict=True, raises=TargetMismatch)` по образцу 019, снимается правкой-владельцем.

- **R-020-1, выбор уникальных циклов по правилу владельца** (`tests/integration/test_p020_selection_amount_first_unique_cycles_postgres.py`), три части:
  - *Переполнение.* 101 непересекающийся треугольник с фиксированными UUID и **различными** суммами → `find_cycles` при глубине 4 **и** 6 возвращает **точный упорядоченный** список 100 канонических идентичностей с наибольшими суммами (не только число). Сегодня: глубина 4 — 34 уникальных треугольника (`:760`), DFS на глубине 6 добавляет не более 51 сырого цикла (`:1374`), и даже объединение не доходит до 100.
  - *Снятие лестницы, наблюдаемое по долгам.* Треугольник и **5-цикл** делят ребро, запрошенная глубина 6. Суммы: общее ребро 100, остальные рёбра треугольника 10, остальные рёбра 5-цикла 100. По правилу владельца исполняется 5-цикл и выбирает общее ребро целиком — остаются исключительные рёбра треугольника по 10; при «сначала короткие» остались бы исключительные рёбра 5-цикла по 10. Ассерт — точные идентичности и суммы оставшихся долгов и ожидаемое число закоммиченных вхождений. Сегодня исполняется треугольник (лестница `[4, 6]`). 4-цикл на той же схеме — дополнительное покрытие, но не репродьюсер: он виден уже на короткой ступени, и одна смена сортировки прошла бы его при оставленной лестнице.
  - *Полный ключ равенства.* Циклы с равной суммой, у которых **совпадает минимальный UUID долга**, но различается дальнейший канонический кортеж, — реализация с ключом «только минимальный id» проходить не должна.
- **R-020-2, неизвестный коммит → провал разрешителя → нет второй попытки** (`tests/integration/test_p020_unknown_commit_is_not_retried_postgres.py`). Расписание отказов предписано, потому что другое проходит на базовой линии вхолостую: разрешитель сам повторяет чтение трижды (`service.py:428-447`) и один провал чтения проглатывает, а всегда падающий разрешитель валит и вторую сверку классификатора (`:176-181`) — ошибка без второй попытки, ложный зелёный. Расписание:
  1. внедрить неизвестный исход исходного `COMMIT`;
  2. провалить **первый полный вызов** `_reconcile_committed_execution` (все его чтения) с `40001` или `40P01`;
  3. дать последующей сверке классификатора установить настоящее отсутствие вхождения;
  4. бюджет — не меньше двух попыток исполнения и достаточный дедлайн.

  Наблюдается вход в авторитетный `_execute_clearing_with_amount`, а не вызовы внешнего сервиса. На базовой линии вторая попытка достигается через `:184` и `:1497` (тест красный). После правки: одна попытка, терминальный отказ, вхождения нет, сохранённые долги не изменились. Оба положительных контроля обязательны — подтверждённый откат самого коммита разрешает повтор; подтверждённое вхождение возвращает долговечную сумму: они не дают «правке» отключить все повторы или всю сверку. Это честно названная инъекция отказов, а не evidence реального расписания потери соединения; рядом остаются настоящие Postgres-тесты конкурентности.

### 2. Инварианты и анти-вакуум

- Нейтральность (`invariants.py:260`) и точное `c = min` залоченных сумм на каждом ребре.
- Идентичность вхождения: цикл, найденный новым детектором, даёт тот же `UUIDv5`, что построенный из ORM-объектов (`test_clearing_commit_replay_postgres.py`).
- Допуск до перечисления: граф, где отклонённые по консенту/статусу/периметру циклы превышают лимит, — допустимый цикл всё равно попадает в выборку (анти-вакуум фильтра: исключение не съедает лимит); контрпроверка — каждый класс исключения на графе действительно отбрасывает свой цикл, а допустимый соседний находится.
- Глубины 3, 4, 6, 7, 10 находят заложенные циклы своих длин; глубина `d` не возвращает циклов длиннее `d`.
- Детерминизм равенства: равные суммы упорядочены полной канонической идентичностью, одинаково на глубинах 4 и 6.
- Граница 019 без изменений: `test_p019_*` из п. 3 зелёные; стенд замера не меняет форму лока.

### 3. Существующие селекторы, обязанные остаться зелёными

Поведенческие ассерты сохраняются; тесты приватных API могут меняться только по карте переноса (стадия 2): для каждого удаляемого теста — какой ассерт куда перенесён. Модуль `test_p012_t1210_detector_union_default_tier.py` целиком **не удаляется**: он несёт ассерты остатка долга на общем ребре (`:206`) и достижимости длинного цикла (`:288`), а `test_p012_t1211_shared_edge_order_postgres.py` импортирует его фикстуры.

- Integration: `test_clearing_commit_replay_postgres.py`, `test_clearing_skip_releases_locks_postgres.py`, `test_concurrent_clearing_payment_lost_update_postgres.py`, `test_clearing_max_depth_controls_long_cycles.py`, `test_p012_t1211_shared_edge_order_postgres.py`, `test_p012_money_form_and_detector_reach_postgres.py`, `test_p1_clearing_run_perimeter_postgres.py`, `test_p015_t1551_clearing_reduces_debt_on_a_frozen_line_postgres.py`, `test_scenarios.py`; защита 019: `test_p019_clearing_attempt_conflicts_reach_the_retry_owner_postgres.py`, `test_p019_money_writers_refuse_non_serializable_postgres.py`, `test_p019_clearing_holds_the_stop_for_share_postgres.py`, `test_p019_equivalent_lock_modes_postgres.py`.
- Unit: `test_clearing_sql_cycle_detection.py`, `test_clearing_auto_clearing_policy.py`, `test_clearing_additional_cases.py`, `test_zero_debt_policy.py`, переносимые ассерты `test_p012_t1210_detector_union_default_tier.py`.

Запуск: `scripts/verify_local.ps1 -TaskSlug <slug> -BackendOnly -BackendSelector …`; для медленных случаев 019 — `-IncludeExpensive`. Число собранных тестов сверяется с ожидаемым.

### 4. Запрещено

- Тестировать конкурентность клиринга подменой сессии или заглушкой детектора.
- Принимать «новый детектор нашёл то же, что старый» как критерий: старые расходились; критерий — протокол и объявленное правило выбора.
- Принимать стенд «устаревший снимок после ожидания» как целевое поведение: позитивный ассерт — свежий авторитетный снимок **после** ожидания; устаревший снимок — только отрицательный контроль.
- Ослаблять `FOR UPDATE`, стоп/hold `row_lock` или форму лока ради производительности.
- Ослаблять пороги замера задним числом, усреднять провалившиеся ячейки с прошедшими, считать таймаут пустым результатом.
- Удалять тест без записи о переносе его поведенческого ассерта.

### 5. Протокол замера CTE (пороги зафиксированы этой редакцией, до замера)

Инженерные пороги приёмки, не измеренные результаты и не SLA продукта.

- В репозитории — детерминированный генератор и точный раннер запроса: фиксированные seed и UUID; мигрированная изолированная база; манифест графа (вершины/рёбра, распределение степеней, число допустимых/исключённых, заложенные циклы).
- Семейства графов: **200 вершин / 2 000 рёбер долга**, историческое **400 / 4 000**, перекошенный граф с вершинами высокой степени, плотный стресс **200 / 8 000**. Запросы глобальные и в периметре; линии active/frozen/closed, отказ в консенте, гарантированные допустимые и исключённые циклы.
- Заложены длины 3–10, переполнение, циклы с общим ребром, граф без коротких циклов. Глубины **3, 4, 6, 7, 10** — замер только на 6 не авторизует нынешний API.
- Снимаются: фактический сгенерированный SQL и `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)`, число строк рекурсии, временный I/O, число операторов, идентичности на выходе, сквозная задержка детектора.
- После прогрева — **не менее 20 замеров на ячейку**; p50/p95 и холодный запуск отдельно. Сравнение со старой реализацией и с одним DFS над отфильтрованным графом, с указанием, где их выборки различаются.
- Приёмка: **p95 ≤ 500 мс** для ячеек 200/2 000 при глубине ≤ 6; **p95 ≤ 2 с** для больших и более глубоких объявленных ячеек; **ни один стресс-вызов > 10 с**. Таймаут бенчмарка обязателен и засчитывается провалом. В ячейках с сопоставимым результатом регресс больше **2×** базовой линии расследуется до удаления.
- Провал останавливает включение и удаление; пороги задним числом не ослабляются; тихий runtime-fallback не включается.

**Протокол выше авторизует эксперимент, а не продовое переключение.** До переключения (стадия 3) и до любого удаления требуется ещё:

- генератор, seed, распределения топологии и степеней, допустимое ветвление и точные ячейки бенчмарка **заморожены до замера времени**;
- DFS-сравнитель реализует **тот же** контракт: полная допустимость, канонизация, порядок по сумме, top-100. Старый DFS с обрывом таким сравнителем не является;
- повторное обнаружение полной глубины на **меняющемся** графе через представительные прогоны `auto_clear`, включая сохранённый потолок успехов: отдельно суммарное время обнаружений, суммарное число SQL и задержка всего вызова. Ограничения вызывающих: `/clearing/auto` держит Redis-аренду на 30 с (`app/api/v1/clearing.py:53`) без продления (`app/utils/distributed_lock.py:54`, `:70`); у симулятора свой бюджет клиринга (`real_clearing_engine.py:291`). Это требование замера, а не наблюдённый таймаут или потерянное денежное исключение;
- замер существующих схем вызова симулятора и ревью совместимости с его бюджетом и отменой;
- ревью сравнительной задержки, роста промежуточных путей и размера реализации. **Если отфильтрованный DFS даёт то же поведение проще и в пределах порогов, выбирается он** (§19.2), а не CTE лишь потому, что CTE прошёл.

До удаления evidence покрывает: точные идентичности и порядок кандидатов, исключения по допуску, поддерживаемые глубины, остаточные долги, идентичность вхождения, сохранённое поведение 019, стоимость сравнителя, стоимость повторных вызовов, совместимость вызывающих и место назначения каждого удаляемого поведенческого ассерта. Это точечные артефакты приёмки, а не основание для нового фреймворка.
- Если меняется длительность или место взятия лока (не ожидается): повторный замер живучести по протоколу 019 (`spec.md:452`) — **4 партии по 15, ≥ 90 % успеха в каждой партии**, прежний бюджет повторов, не агрегат.

## Стадии

Каждая стадия сливается отдельно, с каноническим гейтом и своим §15-ревью на точном HEAD (деньги, конкурентность).

| Стадия | Работа и приёмка | Гейт и ревью |
|---|---|---|
| 0 | Эта редакция: current/intended/optimal, суженный объём, решения по локу и повторам, решение владельца, граница 021, пороги замера | Повторная консультация по редакции до авторизации; runtime-утверждений нет |
| 1 | Неизвестный коммит: R-020-2 красным по предписанному расписанию отказов, затем локальная правка старшинства исхода. Идентичность, реплей и отмена не меняются | Канонический целевой гейт: R-020-2 + реплей/повторы/отмена (`test_clearing_commit_replay_postgres.py`, `test_p019_clearing_attempt_conflicts_…`); §15 на точном HEAD до слияния |
| 2 | Характеризация и замер: R-020-1, карта переноса ассертов, замороженный генератор, экспериментальный детектор и эквивалентный DFS-сравнитель **вне** продового пути; замер по п. 5, включая повторные вызовы и схемы вызова симулятора. Узкий strict-xfail допустим | Гейт характеризации; команда и замороженные артефакты замера; §15 замера и генератора до опоры на результаты |
| 3 | Переключение (только после приёмки замера): один детектор **и** смена семантики `auto_clear` — полная глубина на каждом обнаружении, повторное обнаружение после успеха, лестница снимается; перенос ассертов; заменённые детекторы удаляются только после доказанного переноса поведения и совместимости вызывающих. В том же срезе — документация поведения: §7.2 протокола и `09` §1.12.1 | Целевые гейты (детектор, политика, периметр, глубины, денежный формат, общее ребро, auto-clear/реплей/пропуск), **полный backend milestone** до слияния; §15 на точном HEAD |
| 4 | Необязательная чистка, сохраняющая поведение: устаревшие адаптеры и fallback. Сигнатуры, импорты, поведение исключений и порядок сохраняются | Целевые гейты затронутого; §15 на любой отдельный кодовый срез |
| 5 | Закрытие: сведение evidence (удалённое, фактические строки/запросы/задержка), оставшиеся пределы, передача контракта 021 — документирует уже доставленное, а не впервые | Закрывающее ревью по §19.5 без новой программы инфраструктуры от ревью; названные CI jobs и непройденные поверхности; класс 2 — в BACKLOG |

## Tasks

| ID | Задача | Статус |
|---|---|---|
| `T2000` | Стадия 0: переписать спеку против `5934e24` по консультации Codex 2026-09-25 и ответу владельца; граница в 021; строка реестра | `[x]` |
| `T2001` | Стадия 1: R-020-2 красным по предписанному расписанию, правка старшинства исхода коммита, гейт, §15 | `[ ]` |
| `T2002` | Стадия 2: R-020-1, карта переноса ассертов, замороженный генератор и раннер, экспериментальный детектор и эквивалентный DFS-сравнитель, замер по п. 5 (ячейки, повторные вызовы, симулятор), §15 замера | `[!]` |
| `T2003` | Стадия 3: переключение на один детектор и полная глубина `auto_clear` с повторным обнаружением, перенос ассертов, удаление заменённых детекторов и слияния, документация §7.2/§1.12.1, полный backend milestone, §15 | `[!]` |
| `T2004` | Стадия 4: необязательная чистка, сохраняющая поведение; §15 при отдельном срезе | `[!]` |
| `T2005` | Стадия 5: закрытие по §19.5, сведение evidence | `[!]` |

Легенда: `[x]` выполнено, `[ ]` в работе, `[!]` заблокировано или не авторизовано. Программа авторизована с условиями 2026-09-25; `T2002`–`T2005` стоят `[!]`, пока не начнётся их стадия — по порядку.

## Changelog

**2026-09-25 — второй круг консультации (`8956bae`): авторизована с условиями.** `AUTHORIZE-WITH-CONDITIONS`, P1 = 0, P2 = 5, P3 = 3 — все внесены (раздел «Решения консультации, второй круг»). Лестница: полная глубина на каждом обнаружении, повторное обнаружение после успеха; правило владельца определено на обнаружение. Смена `auto_clear` и её документация перенесены в стадию 3. R-020-1 и R-020-2 ужесточены против холостого прохода. Добавлены требования к замеру до переключения и до удаления (эквивалентный сравнитель, повторные вызовы, вызывающие). Три фактические правки. Граница 021 доведена. `T2001` в работе; стадии начинаются по порядку.

**2026-09-25 — `T2000`: спека переписана против `5934e24`.** Консультация Codex (`gpt-6-astra` requested, high, read-only, замороженный `5934e24`): `ACCEPT-WITH-CHANGES`, `NARROW`, P1 = 1, P2 = 7, P3 = 1 — все девять внесены как F-020-1…9; якоря перепроверены оркестратором по коду. Объём сужен до одного детектора, одного правила выбора и правки неизвестного коммита; граница исполнения 019 (исключительный сессионный лок, пиннинг, `_run_attempts`, `ClearingCommittedAfterCancellation`) сохраняется, а не переписывается. `T1549`-2 признан закрытым 019. Решение владельца 2026-09-25 по единственному продуктовому вопросу: **«Большая сумма первой»** — вопреки рекомендации «сначала короткие»; записано как намеренное изменение поведения. Пороги замера CTE зафиксированы до замера. Граница с 021 уточнена там же. Прежняя редакция и её записи ниже — история.

**2026-09-21 — заведена как черновик.** Утверждение «interlock — обход отмены» опровергнуто: он защищает взаимное исключение с резервированиями платежа; поэтому его удаление отнесено к 019. Утверждение «три детектора → один CTE» держится частично: DFS оказался основным путём, а не fallback, и семантики сторон расходятся — единый детектор объявлен изменением поведения с решением владельца, а не рефакторингом.

**2026-09-21, позже — внутренний adversarial-проход, пять правок.** (1) Периметр был описан «по трём колонкам» — это только треугольники; задан как предикат на обоих концах каждого ребра, добавлен репродьюсер длины 5–6. (2) `_debt_id_key` снимался на неверной посылке (ORM-путь) — нормализация задана как native `uuid` из CTE. (3) Non-goal о клиринге по расписанию цитировал решение как нерешённое — переформулирован: решён, не реализован; два режима `09:41-42` выразимы `max_depth`. (4) §7.6.1 протокола не был в owner surface — отнесён к 019, где снимается. (5) Якоря нейтральности (`:1840-1879` — протокол; в коде `:1924`, `:2161-2162`), cap 50 (`:1402`), `T1210-bis` (`:739`, `:1187-1194`, `:1453`) исправлены. *(Якоря этой записи — на дереве 2026-09-21; пункт (1) о различии периметров и пункт (4) о снятии §7.6.1 опровергнуты консультацией 2026-09-25.)*

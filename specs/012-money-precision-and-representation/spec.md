# 012 — Деньги: precision, scale и представление

- **Date:** 2026-08-21
- **Status:** **РЕАЛИЗАЦИЯ АВТОРИЗОВАНА** — владелец авторизовал реализацию программ 009–014
  2026-08-21, запись в `4a995e9`. Первая редакция спеки писала «реализация не авторизована»; это
  устарело, и статусы задач ниже исправлены 2026-08-24.
  Якоря и все замеры выполнены на HEAD `0ebffcf` 2026-08-21 — **новая сессия перепроверяет их
  сама**: между тем срезом и началом реализации прошли волны 010 и 011.
- **Status authority:** метка описательная; завершённость устанавливают success criteria и записанные
  evidence, а не поле статуса.
- **Owner surface:** ведётся **двумя последовательными слайсами**, а не одним набором.
  **Слайс A (сразу):** `app/utils/validation.py`, `app/core/simulator/**` в части денежного
  представления, `simulator-ui/v2/src/**`, `admin-ui/src/**`, их тесты.
  **Слайс B (только после закрытия 010):** `app/core/payments/`, `app/core/clearing/`,
  `app/core/trustlines/`, `app/core/balance/` — исключительно в части чтения `Equivalent.precision`.
  **Запрещено всегда:** `app/core/recovery.py`; `api/openapi.yaml` — владелец 011.
  Основание для слайса B — в `## Owner surface`.
- **Origin:** маршрут `008/tasks.md`: строки 7 и 11 триажа волн 1–3; группы `A1`, `B`, `C`, `D`, `E`,
  `F`; `PAR-A2b-001`; группа `Q` и денежная часть группы `S`.
- **Depends on:** 010 — слайс B ждёт его закрытия (те же файлы). 011 — **только** в части
  непрозрачных денежных ответов (`F-011-1`), но **не** как источник `precision`: см. `F-012-4`.
- **Почему это одна программа:** предмет один — **какое число считается деньгами и как оно
  доезжает до экрана**. Разрезав по поверхностям, мы получим бэкенд, уважающий точность, и фронт,
  печатающий два знака, — то есть сегодняшнее состояние.

## Problem

У системы нет единого понятия точности денежной величины. `Equivalent.precision` существует, хранится
и редактируется через админский API — и **не читается ни одним денежным модулем**:

| Модуль | Вхождений `precision` |
|---|---|
| `app/core/payments/` | **0** |
| `app/core/clearing/` | **0** |
| `app/core/trustlines/` | **0** |
| `app/core/balance/` | **0** |
| `simulator-ui/v2/src` (без тестов) | **0** |
| `admin-ui/src` (без тестов) | 105 строк / **130 вхождений** |

Замерено 2026-08-21. Асимметрия между двумя фронтендами — не стилистика: одни и те же деньги один
интерфейс форматирует с учётом точности эквивалента, второй **не знает о её существовании**.

При этом `seeds/equivalents.json:10` поставляет `HOUR` с `precision: 1`, то есть конфигурация, в
которой два знака после запятой неверны, — не гипотетическая, а поставляемая в коробке.

## Owner surface

**Граница перекроена 2026-08-21 по внешнему ревью — прежняя делала программу неисполнимой.**
Первая редакция требовала (`F-012-2`), чтобы денежные модули читали `Equivalent.precision`, и тут же
запрещала себе `payments/` и `clearing/`, отправляя правку «записью для 010». Проверено: у 010 в
owner surface ровно четыре файла и **нет** precision-задачи, а `trustlines/` и `balance/` не
принадлежат ни одной программе вовсе. То есть требование не имело исполнителя ни при каком маршруте.
Механизм «запись для другой программы» здесь не работает: он передаёт задачу тому, кто её не брал.

**Слайс A** — разрешено сразу: валидация величин (`app/utils/validation.py`), денежное представление
в `app/core/simulator/**`, форматтеры и типы обоих фронтендов, тесты, закрепляющие старое
форматирование.

**Слайс B** — `app/core/payments/`, `app/core/clearing/`, `app/core/trustlines/`,
`app/core/balance/`, **только** чтение `Equivalent.precision` и ничего больше. Открывается **после
закрытия 010** и не раньше: 010 правит в тех же файлах обработку отказов, а одновременные волны по
одному файлу запрещены (`specs/README.md`, «Пересечение по файлу»). Расширять 010 несвязанной
precision-работой было бы хуже: её предмет — fail-closed, а не точность.

Запрещено всегда: `app/core/recovery.py`; `api/openapi.yaml` — владелец 011, 012 сообщает ему
требуемые формы полей.

## Findings

Проверено по коду 2026-08-21 на HEAD `0ebffcf`. Замеры воспроизводимы командами, приведёнными рядом.

**`F-012-1` — расхождение подписанной и записанной суммы. Severity `P1`, назначена 2026-08-24 по
результату `RT-012-1`. Наблюдено на живой БД, а затем подтверждено сквозным прогоном.**

Валидация принимает суммы со `scale` до 18 (`app/utils/validation.py:91`,
`DEFAULT_MAX_AMOUNT_SCALE = 18`); хранилище объявлено `Numeric(20, 8)`
(`app/db/models/debt.py:14` — `Debt.amount`, `app/db/models/trustline.py:14` — `TrustLine.limit`).
Проверено вставкой в `NUMERIC(20,8)` на PostgreSQL 16.9:

| Подано | Сохранено | Дельта |
|---|---|---|
| `0.12345678` | `0.12345678` | 0 |
| `0.123456789` | `0.12345679` | **−1e-9 — записано БОЛЬШЕ поданного** |
| `0.123456785` | `0.12345679` | −5e-9 |
| `0.000000001` | `0` | +1e-9 — величина исчезла |
| `0.123456789012345678` | `0.12345679` | −9.87e-10 |

Postgres **округляет, а не отбрасывает**, и в обе стороны. Проверено также, что
`parse_amount_decimal` принимает все эти строки (`scale` 9 и 18 — принимаются, 19 — отвергается).

Следствия:

1. Участник подписал одно число, в леджер попало другое, **большее**. Подпись остаётся верной для
   исходного сообщения (§2 протокола), но состояние ей не соответствует.
2. Случай «в ноль» **закрыт** констрейнтом `chk_debt_amount_positive` (`app/db/models/debt.py:27`) —
   даст `IntegrityError`. Констрейнт ловит любое `amount <= 0`, но штатный платёж принимает только
   положительную сумму, поэтому достижим именно случай «округлилось в ноль». Случай «чуть больше» не
   нарушает **ни одного** констрейнта.
3. `Debt` уникален по тройке (`uq_debts_debtor_creditor_equivalent`, `app/db/models/debt.py:26`),
   поэтому величина перезаписывается в одной строке и округление применяется к уже накопленному
   значению многократно.
4. Инвариант нулевой суммы этого не увидит: он структурно тождественен нулю (`app/core/invariants.py:62-80`).
5. **Проверка дельты платежа этого тоже не увидит.** `app/core/payments/engine.py:1545-1554`:
   `tolerance = Decimal("0.00000001")` при сравнении `abs(drift) > tolerance`. Величина `1e-8` — ровно
   один квант `Numeric(20, 8)`, сравнение строгое, а наблюдённые дельты (`1e-9`, `5e-9`) на порядок
   меньше допуска. То есть барьер, который должен был поймать расхождение, устроен так, что пропускает
   его по построению. Найдено оркестратором при разборе исследования 015 и подтверждено ревьюером.

**Severity назначена: `P1`.** Первая редакция ставила `P1-кандидат`, потому что было измерено
поведение **хранилища**, а не приложения: между валидацией и `INSERT` лежат движок и планировщик, и
любой из них мог бы квантовать. `RT-012-1` замкнул цепочку 2026-08-24 и показал, что **не квантует
никто**. Прогон на PostgreSQL 16.9, воспроизведён оркестратором независимо от автора теста:

| Подписано и подано | Ответ API | В `debts` | Дельта |
|---|---|---|---|
| `0.123456789` | `200`, `amount` эхом | `0.12345679` | **+1e-9, больше подписанного** |
| `0.123456785` | `200`, `amount` эхом | `0.12345679` | +5e-9 |
| `0.123456789012345678` | `200`, `amount` эхом | `0.12345679` | +9.88e-10 |
| `0.000000001` | **`500` `E010`** | — | величина исчезла |
| `0.12345678` (контроль, scale 8) | `200` | `0.12345678` | 0 |

Три случая из четырёх **коммитятся**, и леджер держит **больше**, чем подписано, — при этом API
возвращает клиенту ровно ту строку, которую он подал. Участник видит подтверждение своего числа и
получает обязательство на другое.

**Хуже, чем описывала первая редакция, в одном месте.** Случай «в ноль» не просто даёт
`IntegrityError` — он выходит наружу как **HTTP 500 `E010` «Internal server error»**. Отправителю
сообщают, что сломался сервис, а не что сумма непредставима.

**Якорь подписи в changelog указывал не туда.** Там `payments/service.py:493-511` назван местом,
где подпись считается по исходной строке; на самом деле это вызов `parse_amount_decimal` (`:504`),
а payload подписи строится на `:588-601` и проверяется на `:603-616`. Вывод верен, обоснование
указывало на другой код — характерный промах, записанный в уроках ревью.

**`F-012-2` — строки 7 и 11 триажа, P2. `Equivalent.precision` не читается денежными модулями.**
Ноль вхождений в каждом из четырёх каталогов (таблица в `## Problem`). Намерение проекта установлено
**фактом, а не мнением**: `precision` читается и уважается в `app/core/admin/metrics.py:107,374,378,436`,
`app/api/v1/admin.py:1445,1820`, `app/core/simulator/edge_patch_builder.py:58` и в `admin-ui`.
Значит игнорирование её в денежных модулях — дефект, а не решение.

**`F-012-3` — `Decimal("0.01")` как зашитая единица денег. P3 (понижена по ревью). Двадцать сайтов, два в денежном ядре.**
Замер: 20 вхождений литерала в `app/`, распределение —
`simulator/trust_drift_engine.py` (9), `simulator/runtime_utils.py` (2),
`simulator/real_payment_planner.py` (2), `simulator/real_clearing_engine.py` (2),
`simulator/inject_executor.py` (2), `simulator/real_tick_orchestrator.py` (1) и
**`app/core/clearing/service.py:361,450` (2)**.

Последние два — параметр `min_amount` клиринга (`AND {least_expr} > :min_amount`,
`app/core/clearing/service.py:397-400`, `:485-490`).

**Довод о вреде, приведённый в первой редакции, не выдержал проверки — исправлено.** Я написал, что
при `HOUR` с `precision: 1` порог `0.01` задан в непредставимых единицах и потому вреден. Внешнее
ревью показало: при `precision: 1` минимальная валидная положительная величина — `0.1`, а `0.1 > 0.01`,
поэтому **любой валидный долг порог проходит** и наблюдаемого эффекта нет. Получить его можно лишь
создав непредставимый долг `0.05`, то есть заранее нарушив тот самый инвариант, который программа
и вводит.

Поэтому severity `F-012-3` — **P3, а не P2**: это зашитое предположение о масштабе, доказанное
чтением, без доказанной цены. Класс остаётся реальным (20 литералов, из них 2 в денежном ядре), но
чинится как гигиена и разбором, а не как дефект с эффектом. Два сайта `clearing/` попадают в
**слайс B** этой же программы.

**`F-012-4` — группа `B`, P2. У `simulator-ui/v2` нет источника точности вовсе — хотя он доступен.**
`grep precision simulator-ui/v2/src` без тестов → **0** совпадений (в `admin-ui/src` — 105 строк,
**130 вхождений**: на одной строке бывает несколько, например `useGraphAnalytics.ts:31` содержит три).

**Причина, которую я назвал в первой редакции, была неверной, и это исправлено по внешнему ревью.**
Я написал, что точность нельзя получить, пока канон не описывает денежные поля. Проверено: `precision`
**уже доступен фронту сегодня** — `GET /api/v1/equivalents` (`app/api/v1/equivalents.py:14-27`)
отдаёт `StoredEquivalent` с полем `precision: int` (`app/schemas/equivalents.py:14`), эндпоинт и поле
описаны в каноне (`api/openapi.yaml:1840-1852`), а у simulator UI есть общий authenticated
HTTP-хелпер (`simulator-ui/v2/src/api/http.ts:54`).

Значит дефект хуже, чем описывался: точность **доступна и не запрашивается**. Работа не блокируется
программой 011 — от неё зависит только описание непрозрачных денежных ответов (`F-011-1`).

**Якоря канона перемерены 2026-08-24 и сдвинулись — сам вывод устоял.** Волна 011 переписала
`api/openapi.yaml`, и `:1840-1852` теперь указывает внутрь симуляторного пути. Действующие
координаты: `GET /equivalents` — `api/openapi.yaml:2200`, схема `StoredEquivalent` — `:4101`, её
`precision` — `:4114`, и `precision` входит в `required`. В `app/schemas/equivalents.py` поле
`StoredEquivalent.precision` стоит на строке **15**, а не 14 (`:39` — `Equivalent.precision` с
границами `ge=0, le=18` — верно). Утверждение «точность опубликована, описана в каноне и фронт её
просто не спрашивает» подтверждено на текущем HEAD.

**`F-012-5` — группы `B`/`C`, P2. Деньги как `number` и два знака в константе.**
`simulator-ui/v2/src/utils/numberFormat.ts:73-75`:

```ts
export function formatAmount2(v: number): string {
  ...
  return v.toFixed(2)
}
```

Два дефекта в трёх строках: денежная величина принимается как JS `number` (двоичный float), и число
знаков — константа. Функция применяется к **лимитам доверия**:
`simulator-ui/v2/src/composables/useSelectedNodeEdgeStats.ts:39-40` (`inLimitText`, `outLimitText`).

**`F-012-6` — группа `C`, `B-B3-4-001`. P1.** Владелец решением 2026-08-20 принял вариант **A** по
вопросу 1 триажа T810 (`008/tasks.md:297`) вместе с повышением до P1. Первая редакция писала
«вопрос решён как A», не назначив severity, — исправлено по внешнему ревью: решение принято, значит
в спеке стоит `P1`, а не пересказ процесса.
`simulator-ui/v2/src/components/NodeCardOverlay.vue:72-86`, `netText`: предпочитается мажорное
`net_balance`, а при его отсутствии печатается `net_balance_atoms` **как есть** (`String(atoms)`).
Волна 4 замерила: в поставляемом `snapshot.json` поле `net_balance` есть у **0 из 100** узлов.
Значит на дефолтном режиме печатаются атомы, то есть ошибка ×100 при `precision: 2`.

**`F-012-7` — группа `D`, P3. `admin-ui` печатает два знака явно, а не по умолчанию.**
Первая редакция указывала на дефолт `formatDecimalFixed(input: string, digits = 2)`
(`admin-ui/src/utils/decimal.ts:85`). Внешнее ревью показало, что **ни один продакшен-вызывающий
второй аргумент не опускает**, поэтому тест на дефолт был бы искусственным. Настоящие якоря — явные
двойки у денежных вызывающих: `admin-ui/src/pages/DashboardPage.vue:106-107` и
`admin-ui/src/pages/TrustlinesPage.vue:124-125` (`function money(v: string) { return formatDecimalFixed(v, 2) }`),
`admin-ui/src/pages/graph/graphPageHelpers.ts:22-23`. Проверено.

Контрпример в ту же сторону: `admin-ui/src/pages/LiquidityPage.vue:217-220` уже передаёт `precision`
правильно — то есть верная форма в `admin-ui` тоже существует, и работа состоит в её распространении.

Сама арифметика здесь строковая и точная (`addDecimalStrings`, `compareDecimalStrings`), то есть
`admin-ui` устроен **правильнее** второго фронта.

**`F-012-8` — группа `F`, P3. Сложение величин разных эквивалентов в одно число** (`C-C3-1-007`).
Разбор — `specs/008-surface-code-review/evidence-index.md` (в каталоге 012 такого файла нет; ссылка
исправлена по внешнему ревью). Правка входит в набор, потому что имеет ту же причину: величина без
эквивалента и без точности трактуется как «просто число».

**`F-012-9` — группа `A1`, P3. Форма денежной строки на проводе.**
Правильная форма в системе уже есть: `app/core/simulator/edge_patch_builder.py:69-70` —
`format(v.quantize(money_quant, rounding=ROUND_DOWN), "f")`, где `money_quant` выведен из
`Equivalent.precision` (`:58`). Она квантует и запрещает экспоненту. Задача — распространить её,
а не изобретать: `snapshot_builder.py:168` несёт вторую копию той же функции.

**Побочное наблюдение, требующее решения, а не правки:** этот путь квантует `ROUND_DOWN`, а Postgres
при сохранении округляет **half-up** (`F-012-1`). Два режима округления в одной системе — вопрос к
`015`, здесь фиксируется ссылкой.

**`F-012-10` — `PAR-A2b-001`, P3.** `app/core/simulator/real_payment_planner.py:210` — генерация суммы
платежа сужает денежные входы через `float`. Величина не персистится, а используется для
планирования; цена не измерена, и спека обязана начать с замера.

### Тестовая половина — группа `Q` и денежная часть группы `S`

Ведётся **вместе** с правками выше, а не отдельной программой: эти тесты кодируют именно то
форматирование, которое исправляется. Разведя их, мы получим окно, в котором поведение исправлено, а
набор продолжает утверждать старое и остаётся зелёным.

- **`MISSED-B4-NUMFMT-FIXED2`** — `simulator-ui/v2/src/utils/numberFormat.test.ts:14-18` жёстко
  закрепляет два знака: `expect(formatAmount2(1.234)).toBe('1.23')`. Починка `F-012-5` делает его
  красным — это ожидаемо и правильно, тест меняется тем же коммитом.
- **`C-B4-3-002`** — единственный ассерт набора, наблюдающий рендер баланса, подаёт `net_balance: '42'`,
  то есть форму, которой конвейер фикстур не производит. Следствие: `F-012-6` **ненаблюдаем по
  построению**. Чинится вместе с ним.
- **`C-B4-3-009`** — тест закрепляет денежные `trust_limit` в старом виде.
- Группа `Q` целиком — ассерты, структурно неспособные упасть, на этой же поверхности.

## Current / Intended / Optimal

**Current.** Точность эквивалента известна системе, хранится и редактируется, но денежные модули её
не читают; один фронт не знает о ней вовсе, второй печатает два знака по умолчанию; валидация
принимает больше знаков, чем способно хранить хранилище, и разница молча округляется — в обе стороны.

**Intended.** Денежная величина на всём пути сопровождается своим эквивалентом и его точностью.
Величина, которую нельзя сохранить точно, **отвергается на входе**, а не округляется молча. Экран
показывает столько знаков, сколько объявляет эквивалент.

**Optimal.** Форма решения уже существует внутри репозитория и не изобретается:
`edge_patch_builder.py:69-70` показывает квантование по `precision` с запретом экспоненты,
`admin-ui/src/utils/decimal.ts` — строковую денежную арифметику. Работа состоит в том, чтобы сделать
их **общими**, и в том, чтобы согласовать `DEFAULT_MAX_AMOUNT_SCALE` с фактической ёмкостью
хранилища. Сплошная замена запрещена разбором M20: 833 вхождения `??` в обоих фронтах уже однажды
показали, чем кончается codemod по форме.

## Non-goals

- **Не codemod.** Ни по `Decimal("0.01")`, ни по форматтерам. Каждый сайт получает решение с
  основанием.
- **Не правка денежного ядра сверх чтения `precision`.** Слайс B трогает `payments/`, `clearing/`, `trustlines/`, `balance/` **только** ради чтения точности; обработка отказов, retry и таксономия конфликтов остаются за 010.
- **Не правка канона.** Требуемые формы полей сообщаются 011.
- **Не менять `Numeric(20, 8)`.** Расширение денежной колонки — необратимая миграция и продуктовое
  решение владельца, а не следствие этой работы. 012 приводит **валидацию** к хранилищу, а не
  наоборот, если владелец не решит иначе.
- **Не трогать метрические серии** — группа `X` (`B-D1-002`) закрыта программой 007 срезом T715.
- **Не решать вопрос о режиме округления** (`ROUND_DOWN` против half-up) — он шире денежного
  представления и уходит в `015`.

## Verification plan

**1. Репродьюсеры, обязанные падать на текущем коде.**

| ID | Что обязан показать | Гейт |
|---|---|---|
| `RT-012-1` | **сквозной**: платёж на сумму со `scale` 9 принимается API и оказывается в `debts` с другой величиной. Замыкает `F-012-1` от входа до строки БД | Postgres, обязателен |
| `RT-012-2` | эквивалент с `precision: 1`: величина `0.05` **принимается** `parse_amount_decimal` и сохраняется в `Numeric(20,8)`, но `edge_patch_builder.py:69-70` квантует её `ROUND_DOWN` до `0.0` — две конкретные стадии названы, а не «где-то». **`HOUR` обязан создаваться тестом явно:** `tests/conftest.py:113-119` создаёт пустую схему и `seeds/equivalents.json` не загружает | Postgres |
| `RT-012-3` | **снят как искусственный** по внешнему ревью: при `precision: 1` минимальная валидная величина `0.1` порог `> 0.01` проходит, и наблюдаемого эффекта нет. Взамен — `T1203` разбирает 20 литералов чтением, без репродьюсера, потому что доказанной цены у класса нет | — |
| `RT-012-4` | `formatAmount2` теряет точность на величине с `precision: 4` и искажает на `precision: 1` | дефолтный |
| `RT-012-5` | `NodeCardOverlay` на поставляемых фикстурах печатает атомы: ×100 при `precision: 2` | дефолтный |
| `RT-012-6` | `admin-ui` печатает два знака для эквивалента с `precision: 1` — **на реальных денежных вызывающих** (`DashboardPage.vue:106-107`, `TrustlinesPage.vue:124-125`), а не на дефолтном параметре `formatDecimalFixed`: его ни один продакшен-вызывающий не опускает, и тест на дефолт был бы искусственным | дефолтный |

**2. Инварианты, обязанные выжить, и контрпроверки.**

- **Величина, которую нельзя сохранить точно, не сохраняется молча** — контрпроверка: возврат
  `DEFAULT_MAX_AMOUNT_SCALE` к 18 при хранилище `scale 8` обязан ронять `RT-012-1`.
- Экран показывает ровно `precision` знаков — контрпроверка: подмена `precision` на другое значение
  обязана менять вывод; тест, не реагирующий на подмену, неотличим от своего отсутствия.
- Денежная строка никогда не уходит на провод в экспоненциальной форме — контрпроверка на величине,
  чей `str(Decimal)` даёт `E+`.
- **Ни один денежный путь не принимает `float`** — контрпроверка: аннотация `float` на денежном
  параметре обязана ловиться гардом, а не ревью.
- Правка не меняет уже корректные величины: контрпроверка на `precision: 2`, где старое и новое
  поведение обязаны совпасть **побайтово**.

**3. Существующие селекторы, обязанные остаться зелёными.** Поимённо:

- `tests/integration/test_payments_amount_validation.py` — прямая поверхность `F-012-1` (в первой редакции путь был указан как `tests/unit/...`; такого файла нет)
- `tests/integration/test_payment_prepare_capacity_policy.py`
- `tests/integration/test_payment_inverse_multisegment_postgres.py`
- `tests/unit/test_admin_trustlines_bottlenecks.py`, `tests/unit/test_admin_trustlines_list.py`
- `npm --prefix simulator-ui/v2 run test:unit`
- `npm --prefix admin-ui run test`
- `npm --prefix simulator-ui/v2 run typecheck` и `npm --prefix admin-ui run lint`

Тиры целиком: дефолтный backend; Postgres marker-тир; (замерено 2026-08-21).

**4. Запрещённые способы доказательства.**

- **Запрещено** засчитывать SQLite за гейт по `F-012-1`. Поведение округления при сохранении —
  свойство типа `NUMERIC(20,8)` конкретной СУБД; SQLite хранит иначе, и зелёный там ничего не значит.
  Это же — обязательная проверка: **если SQLite ведёт себя иначе, весь класс невидим для дефолтного
  тира**, и это надо записать явно.
- **Запрещено** доказывать правку форматтера тестом, который сам содержит ожидаемую строку с двумя
  знаками: он проверяет себя. Требуется параметризация по `precision` с контрпроверкой.
- **Запрещено** менять `numberFormat.test.ts` «чтобы стал зелёным» отдельно от правки `F-012-5`.
  Тест и поведение меняются одним коммитом, иначе теряется доказательство.
- **Запрещено** сплошное переписывание по грепу — разбор M20 показал, что регекс по форме даёт
  неверный масштаб (190 против фактических 833).

## Tasks

Легенда: `[x]` выполнено · `[ ]` в работе · `[!]` заблокировано или не авторизовано.

| ID | Задача | Статус |
|---|---|---|
| T1200 | `RT-012-1`…`RT-012-6` — красные на текущем коде; невоспроизводимое получает `REFUTED` с записью. **Выполнена 2026-08-24:** шесть репродьюсеров красные, ни один не переформулирован; `RT-012-3` был снят ещё внешним ревью. `F-012-1` подтверждена сквозным прогоном и получила `P1` | `[x]` |
| T1201 | `F-012-1`: согласовать `DEFAULT_MAX_AMOUNT_SCALE` с ёмкостью хранилища; величина, не сохраняемая точно, отвергается на входе. **Severity назначается здесь** по результату `RT-012-1` | `[ ]` |
| T1202 | **Слайс B**: денежные модули (`payments/`, `clearing/`, `trustlines/`, `balance/`) читают `Equivalent.precision`. Ведётся этой программой, а не передаётся. **Разблокирована:** 010 закрыта 2026-08-22 (`dba860a`), пересечения по файлу больше нет | `[ ]` |
| T1203 | `F-012-3`: разбор двадцати сайтов `Decimal("0.01")` выборкой с основанием по каждому; два сайта клиринга передаются 010 | `[ ]` |
| T1204 | `F-012-4` + `F-012-5`: источник точности во фронте и общий форматтер по образцу `edge_patch_builder.py:69-70`; деньги перестают быть `number` | `[ ]` |
| T1205 | `F-012-6` вместе с `C-B4-3-002`: фикстуры производят мажорное поле, ассерт начинает наблюдать реальную ветку | `[ ]` |
| T1206 | `F-012-7` + `F-012-8`: `admin-ui` перестаёт печатать два знака по умолчанию и складывать разные эквиваленты | `[ ]` |
| T1207 | `F-012-9` + `F-012-10`: единая форма денежной строки; замер цены `float` в планировщике до правки | `[ ]` |
| T1208 | Группа `Q` и денежная часть `S` — синхронно с T1204–T1206, одним коммитом с поведением | `[ ]` |
| T1209 | Запись для 011 (требуемые формы денежных полей канона). Записи для 010 больше нет: precision-работу ведёт слайс B этой программы | `[ ]` |
| T1210 | **Внутренний adversarial review** субагентом (§16.4 `AGENTS.md`) | `[ ]` |
| T1211 | **Независимое внешнее ревью и публикация evidence на точном HEAD** — §15: программа трогает денежную семантику, то есть защищённый контракт §8 | `[ ]` |

### `T1200` — шесть репродьюсеров построены, и три из них нашли то, чего в спеке не было

Выполнена 2026-08-24. Все шесть красные на текущем коде; ни один не потребовалось переформулировать,
в отличие от волны 011, где переписывать пришлось три из шести.

**`F-012-1` подтверждена сквозным прогоном и получила `P1`** — таблица в самой находке.

**`F-012-9` описана неверно, и это нашёл репродьюсер, а не чтение.** Спека называет
`edge_patch_builder.py:69-70` «правильной формой, которая уже есть в репозитории». **В том же файле
живёт второй денежный производитель, и он не правильный:** `build_edge_patch_for_pairs` (`:173`,
путь патчей на транзакцию и на `clearing.done`) отдаёт `"used": str(used_amt)` и
`"available": str(available_amt)` на `:273-274` — без `format(…, "f")` и **без** `helper.precision`,
который у него в руках. Измерено: линия доверия, недоиспользованная на один квант хранилища, кладёт
на провод **`available: '1E-8'`**. Экспоненциальные деньги, с производственного пути, сегодня.

Отсюда же уточнение к инварианту «денежная строка не уходит в экспоненте». **`E+` на бэкенде из
хранилища недостижим:** любое значение из `Numeric(20,8)` имеет экспоненту −8, и вычитание её не
поднимает. Достижимое нарушение — `E-`, и оно проверяется против настоящего производителя; `E+`
проверяется прямым сравнением двух функций форматирования, что и показывает, какая из них держит
инвариант.

**Найден пробел покрытия, который не является предметом 012, но объясняет, почему класс дожил.**
Ни один Postgres-маркированный тест набора **не водит приложение по HTTP** — и с общей фикстурой не
может: `client` привязан к завёрнутой в savepoint `db_session`, `PaymentEngine._apply_flow` входит в
`begin_nested`, слушатель `after_transaction_end` перезапускает savepoint в середине разворачивания,
и **любой платёж на Postgres-тире возвращает 500 независимо от суммы** (измерено на обычных
`10.25`). То есть HTTP-поведение на Postgres сегодня не тестируется вовсе. Обойдено локальной
фикстурой в `tests/integration/p012_pg_http.py`, а не правкой общей `tests/conftest.py`. Кандидат в
014 («ложнозелёные гарды») — там его предмет.

**SQLite дисквалифицирован не по правилу, а по измерению.** Он не округляет при записи вообще:
`NUMERIC(20,8)` — это affinity, и колонка держит `0.123456789` как есть. Три различия делают
дефолтный тир слепым: округление, которое там видно, принадлежит result-процессору SQLAlchemy, а не
СУБД; на «половинке» бэкенды расходятся **в разные стороны** (`0.123456785` → `0.12345678` против
`0.12345679`); и `0.000000001` остаётся `> 0`, поэтому `chk_debt_amount_positive` не срабатывает и
никакого `500` не существует. Записано в докстроке `RT-012-1`, как требует план верификации.

**Восемь якорей спеки сдвинулись** после волн 010 и 011 — перемерены и записаны: `min_amount`
клиринга `:451,557` вместо `:361,450`, предикаты `:498,608` вместо `:397-400,485-490`, вторая копия
`_to_money_str` — `snapshot_builder.py:171-174`, `admin/metrics.py:384,388`,
`schemas/equivalents.py:15`, `_ensure_schema_initialized` — `tests/conftest.py:120-138`, канон —
`api/openapi.yaml:2200` и `:4101`. Ни один вывод от этого не изменился.

**Фронтовая половина.** Счёт «0 из 100» подтверждён и оказался шире: у **всех трёх** поставляемых
эквивалентов `net_balance` отсутствует у 100 узлов из 100. Второй дефект `formatAmount2` отделён от
первого и показан независимо от числа знаков: два лимита, различающиеся на один атом при scale 8,
схлопываются в один double и печатаются одинаково.

## Changelog

- **2026-08-21.** Программа заведена по маршруту 008. Все якоря и замеры выполнены на HEAD `0ebffcf`.

  **Заведена находка, которой в реестре 008 не было — `F-012-1`.** Расхождение подписанной и
  записанной суммы наблюдено на живой PostgreSQL 16.9: валидация принимает `scale` до 18, хранилище
  `Numeric(20,8)`, округление молчаливое и **в обе стороны**, включая случай, когда записанная сумма
  **больше** поданной. Найдено при подготовке брифа 015 по требованию владельца о нулевом балансе;
  здесь получает владельца и репродьюсер.

  **Три записанные формулировки уточнены замером:**
  1. «Источника `precision` во фронте нет вовсе» — подтверждено буквально: `simulator-ui/v2/src` даёт
     **0** вхождений, `admin-ui/src` — **105**. Асимметрия между двумя фронтами измерена.
  2. «13 мест симулятора + места ядра» для `Decimal("0.01")` — фактически **20** вхождений, из них
     **2 в денежном ядре** (`clearing/service.py:361,450`, параметр `min_amount`), что при `HOUR` с
     `precision: 1` задаёт порог в непредставимых единицах.
  3. Группа `A1` описывалась как дефект формы. Найдено, что **правильная форма в репозитории уже
     есть** (`edge_patch_builder.py:69-70`: квантование по `precision` + `format(..., "f")`), и работа
     сводится к её распространению, а не к изобретению.

  **Побочно зафиксировано и передано в 015:** денежный путь квантует `ROUND_DOWN`, а хранилище
  округляет half-up. Два режима округления в одной системе — вопрос шире денежного представления.

- **2026-08-21, внешнее ревью Codex — вердикт `REFUTED`, восемь правок приняты.** Клон на `35e0f67`,
  `codex-cli 0.144.0-alpha.4`, `--sandbox read-only`, exit `0`, маркер `VERDICT-DONE`.

  **Устояло главное:** `F-012-1` подтверждена, причём усилена. Ревьюер прошёл цепочку целиком и
  показал, что **явного `quantize`/`round`/`normalize` на пути платежа нет** ни в одной точке между
  `parse_amount_decimal` и `Debt.amount`: подпись проверяется над исходной строкой
  (`payments/service.py:493-511`), маршрутизатор работает на `Decimal` без округления
  (`router.py:460-549`), сумма едет строкой через `PrepareLock.effects` (`engine.py:958-976`) и
  накапливается как `debt_s_r.amount += remaining_amount` (`:1417-1434`). Существующая строка
  **изменяется**, а не добавляется новой, то есть каждое новое высокоточное приращение складывается с
  уже округлённым значением и округляется повторно. `P1-кандидат` признан корректной дисциплиной.

  **Принято и исправлено:**
  1. **Границы программы делали её неисполнимой.** `F-012-2` требовала, чтобы денежные модули читали
     `precision`, но 012 запрещала себе `payments/`/`clearing/`, у 010 такой задачи нет, а
     `trustlines/` и `balance/` не принадлежали никому. Программа перекроена на **два
     последовательных слайса**; слайс B открывается после закрытия 010.
  2. **Зависимость от 011 обоснована неверно.** `precision` уже доступен фронту:
     `GET /api/v1/equivalents` отдаёт его (`equivalents.py:14-27`, `schemas/equivalents.py:14`), поле
     описано в каноне (`openapi.yaml:1840-1852`). Дефект не в недоступности, а в том, что **доступное
     не запрашивается**. От 011 остаётся зависимость только по непрозрачным денежным ответам.
  3. **`F-012-3` понижена P2 → P3.** Довод «порог `0.01` непредставим при `precision: 1`» не выдержал
     проверки: минимальная валидная величина `0.1` порог проходит, эффекта нет. `RT-012-3` снят как
     искусственный.
  4. **`F-012-6` получила severity `P1`** — владелец принял вариант A с повышением 2026-08-20;
     пересказывать процесс вместо severity было ошибкой.
  5. **`F-012-7` переякорена**: дефолт `digits = 2` ни одним продакшен-вызывающим не используется;
     настоящие сайты — явные двойки в `DashboardPage.vue:106-107`, `TrustlinesPage.vue:124-125`,
     `graphPageHelpers.ts:22-23`. Рядом найден контрпример правильной формы — `LiquidityPage.vue:217-220`.
  6. **`105` — это строки, а не вхождения.** Фактических вхождений `precision` в `admin-ui/src` —
     **130**; ошибка единицы измерения, а не определения «без тестов».
  7. **Селектор `tests/unit/test_payments_amount_validation.py` не существует** — файл лежит в
     `tests/integration/`. Исправлено.
  8. **`HOUR` в тестовой среде недоступен по умолчанию**: `tests/conftest.py:113-119` создаёт пустую
     схему и сиды не загружает. Репродьюсеры обязаны создавать эквивалент явно.

  Плюс уточнены якоря `debt.py` (`:26-27`, а не `:29/:31`) и ссылка на `evidence-index.md`, которого
  в каталоге 012 нет.

  **Добавлено оркестратором по итогам исследования 015:** в `F-012-1` внесён пункт 5 — допуск
  `check_payment_delta` равен `1e-8`, то есть **ровно одному кванту хранилища**, при строгом
  сравнении. Барьер, который должен был поймать расхождение, пропускает его по построению.

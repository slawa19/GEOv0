T1211 evidence ledger
frozen_sha: dd1218d2d76452a19ecb56da867f41e7d67e77aa (branch claude/012-money-s1, clone clean, HEAD verified)
review_root: <TEMP>/geov0-t1211-557fcbee1f624047bf57398d0b6a74bb
codex_path: <LOCALAPPDATA>/OpenAI/Codex\bin\a7c12ebff69fb123\codex.exe
codex_version: codex-cli 0.144.0-alpha.4
model_requested: gpt-5.6-sol, model_reasoning_effort=high; resolved model ID not locally confirmable
credential: ДОСТИЖИМ ревьюеру; риск принят владельцем 2026-08-14 (specs/BACKLOG.md:336-364)
slice A (prompt, whole wave): prompt by geov0-a7 (ask_t1211.md) + scope-exclusion note for specs/015; stdin
slice B (mechanical diff review --base review-base=e45e721): diff INCLUDES docs(015) commits - dilution recorded, findings against 015 docs are out of scope and routed to geov0-a2
stop-line: declared to all four sessions before freeze; acks from 1b, df, a2; a7 committed to silence in advance
postgres gate on frozen SHA: pytest tests/integration -q -m postgres -k "p012 or clearing", run INSIDE the frozen clone, dedicated DB geov0_test_agent_012_s2_signfix: 80 passed, 353 deselected, exit 0 (2026-08-24)

--- ЗАМОРОЗКА КЛОНА (обе итерации; отличаются только FROZEN_SHA и BASE) ---
  git clone --no-hardlinks --quiet D:/www/projects/2025/GEOv0 <reviewRoot>/frozen
  git -C <clone> checkout --detach --quiet <FROZEN_SHA>
  git -C <clone> branch review-base <BASE>
  проверка: git -C <clone> rev-parse HEAD == <FROZEN_SHA>; git -C <clone> status --porcelain пуст
  круг 1: FROZEN_SHA=dd1218d2d76452a19ecb56da867f41e7d67e77aa, BASE=e45e721

--- КОМАНДЫ ПРОГОНОВ (PowerShell 5.1; через cmd /c, потому что 5.1 оборачивает stderr нативного
    exe в NativeCommandError; $codex = <LOCALAPPDATA>/OpenAI/Codex/bin/a7c12ebff69fb123/codex.exe) ---
  срез A (промпт волны, подаётся на stdin; prompt_t1211.md = ask_t1211.md с подстановкой SHA
          и секцией «Границы предмета»):
    cmd /c "type `"<reviewRoot>\prompt_t1211.md`" | `"$codex`" exec --sandbox read-only
            -C `"<clone>`" -m gpt-5.6-sol -c model_reasoning_effort=`"high`"
            -o `"<reviewRoot>\final_wave.md`" - > `"<reviewRoot>\progress_wave.log`" 2>&1"
  срез B (механический диапазон):
    cmd /c "`"$codex`" exec --sandbox read-only -C `"<clone>`" review --base review-base
            -m gpt-5.6-sol -c model_reasoning_effort=`"high`"
            -o `"<reviewRoot>\final_range.md`" > `"<reviewRoot>\progress_range.log`" 2>&1"

--- ПРОВЕРКА ЗАВЕРШЕНИЯ (§15: результат отделён от прогресса и проверяется машинно) ---
  exit code процесса cmd == 0 ($LASTEXITCODE фонового шелла)
  -o файл непуст (wc -c)
  грепом на конце присутствуют все маркеры:
    VERDICT-T1211, VERDICT-SAMPLE-BIAS, VERDICT-READY-TO-CLOSE, VERDICT-DONE

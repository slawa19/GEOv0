# 017 — PostgreSQL как единственный движок

- **Date:** 2026-09-21
- **Status:** AUTHORIZED WITH CONDITIONS 2026-09-21 — по консультации Codex (`VERDICT-017: ACCEPT-WITH-CHANGES`, `T1700-DECISION: AUTHORIZE-WITH-CONDITIONS`, P1 = 0, P2 = 3, P3 = 3 — все внесены) и протоколу решений владельца 2026-09-21 (`specs/README.md`, «Рефакторинг ядра»); код не начат; продуктовый вопрос П1 отвечен владельцем 2026-09-21: SQLite как способ развёртывания или запуска **не нужен**; стадия 3 ждёт только бюджета времени тира (`T1706`)
- **Status authority:** метка описательная; завершённость устанавливают success criteria и записанные evidence, а не поле статуса.
- **Owner surface:** `app/config.py` (`DATABASE_URL` и его умолчание), `app/db/session.py`, `app/db/sqlite_transaction_control.py` (удаление), `app/main.py:323-500` (три SQLite-пробы схемы), dialect-ветки в 8 модулях `app/` (50 вхождений; перечень — задача `T1703`) и упоминания `sqlite` в 17 модулях (109 вхождений, из них часть — датированные записи измерений в docstring, которые сохраняются), `requirements-dev.txt:5` (`aiosqlite`; **у него есть потребитель вне тестов** — генератор демо-фикстур `admin-fixtures/tools/generate_simulator_demo_snapshots.py`, который зовёт `prebuild` сборки Simulator UI v2, измерено 2026-09-21 прогоном разделённого гейта; удаление обязано ответить и за него, иначе UI-сборка теряет фикстуры), `scripts/{run_local,run_full_stack,verify_local}.ps1`, `scripts/{init_sqlite_db,check_sqlite_db,check_real_snapshot_debts_sqlite}.py` (удаление), `scripts/run_simulator_run_and_analyze.py:348` (открывает SQLite напрямую), `scripts/verify_admin_phase4_real_contract.ps1:57` (единственный e2e админки с настоящим бэкендом, жёстко на SQLite-файле), `scripts/validate_test_database_url.py:78,94-120`, `scripts/t1525_savepoint_detector.py` (удаление: измеряет поведение SQLite), `migrations/env.py:26-32,60` (отказ от SQLite и владение bootstrap `alembic_version`), `docker/docker-entrypoint.sh:38` (переходит на общий bootstrap), `.env.example:11-16`, `.gitignore:33-53`, `app/db/types.py:19-151`, `app/schemas/equivalents.py:24-31` и `app/schemas/trustline.py:26-29` (наивные метки времени SQLite), `app/core/simulator/storage.py:14,35,223-224,260-294,372-373,471-484` (dialect-upsert и предупреждение о неточности денег на SQLite), `app/api/v1/health.py:132,143`, около 27 документов (`README.md:250,296-311,364-372,539-556`, `docs/ru/05-deployment.md:100`, `docs/ru/06-contributing.md:240-242`, `docs/ru/config-reference.md`, `docs/ru/testing/*`, `docs/ru/simulator/backend/real-mode-runbook.md`, `admin-ui/docs/real-api-integration.md`, `AGENTS.md:107,202,253,281`, комментарии `api/openapi.yaml:4524,4636,4801,7626`), `tests/conftest.py`, `tests/scratch_db.py`, `tests/migrated_schema.py`, `pytest.ini` (маркер `postgres`), `.github/workflows/quality.yml` (backend-часть `required-quality`, job `postgres`, job `simulator-super-smoke`). **Исключено:** `api/openapi.yaml`, `migrations/versions/` (новых миграций нет; `migrations/env.py` — в scope как владелец bootstrap, см. выше), любая денежная логика: ни одна строка `payments/`, `clearing/`, `ledger/` не меняется по существу, удаляются только ветки `if not self._is_postgres()` с сохранением Postgres-ветки дословно.
- **Origin:** решение владельца 2026-09-21 о рефакторинге ядра (`specs/README.md`, раздел «Рефакторинг ядра — программы 017–022»). Это первая программа последовательности: она не меняет поведение денег, а убирает второй диалект, из-за которого (а) дефолтный тир тестов не видит исходов, ради которых построен денежный путь, и (б) в программе 015 был отвергнут самый дешёвый механизм журнала — триггер БД.
- **Depends on:** закрытие 015 (`T1509`, `READY-TO-CLOSE: YES`, `CLASS-1-COUNT: 0`; закрыта обычным закрытием по §19.5, `origin/main` `6c5e63f`). Ничего из 015 не переоткрывает. **Оговорка о evidence:** гейты 015 внешним ревью **не покрыты** ни на одном шаге закрытия — ревьюер ничего не запускал (утверждения 5 и 8 `NOT-ESTABLISHED`); единственное gate-evidence — локальные прогоны оркестратора в строках `T1548`, `T1535`, `T1523`. Эта программа на «015 проверена внешне» не опирается: её собственная приёмка — числа собранных и прошедших тестов на Postgres до и после (`T1706`).
- **Blocks:** 018 и 019 — **частично** (вердикт внешнего ревью `PRECONDITION-FOR-018-019: PARTIAL`): их механизмам нужны денежные тесты на Postgres, а не удаление каждого SQLite-теста. Точная формулировка: 018 (триггер БД и `SET LOCAL`) и 019 (SSI, `app/db/session.py:67-69`) невозможны в **выбранной** реализации, пока дефолтный тир собирается `create_all` на SQLite (015 `spec.md:670-675`); удаление SQLite целиком — архитектурный выбор этой программы, обоснованный ниже, а не техническая необходимость.

## Problem

Продукт работает на PostgreSQL (`docker-compose.yml:41`; решение `T1544`: «авторитетна PostgreSQL», 015 `spec.md:868`), а по умолчанию всё остальное работает на SQLite: `app/config.py:54-57` (`DEFAULT_SQLITE_DATABASE_URL`), `scripts/run_local.ps1:96-97`, `scripts/run_full_stack.ps1:95-96`, `scripts/verify_local.ps1:86` (`$defaultTestDb = "sqlite+aiosqlite:///..."`), `tests/conftest.py:39-44`. Из этого следуют три дефекта, и важно различать, какой из них **живой**.

1. **Живой дефект — покрытие проверки.** Дефолтный тир тестов не способен увидеть исход, ради которого построен денежный путь. Все advisory-локи короткого замыкаются вне Postgres: `app/core/payments/engine.py:182` (`if not self._is_postgres(): return`), то же на `:237`, `:308`; interlock клиринга — отдельная ветка по диалекту, `app/core/clearing/service.py:1523`; уровень изоляции ставится только для Postgres (`app/db/session.py:67-69`). Это ровно правило §15 AGENTS.md «проверяйте, способен ли стенд увидеть тот исход, ради которого построен»: зелёный дефолтный прогон ничего не говорит о блокировках, SSI и `FOR UPDATE`, а Postgres-тир CI идёт только по расписанию (`.github/workflows/quality.yml:156-159`). Phase B 015 уже записала, что scheduled-CI для этого недостаточен (`spec.md:842-844`).
2. **Исторические потери — закрыты, но их цена осталась.** `app/db/sqlite_transaction_control.py:14-28` перечисляет три денежных дефекта, рождённых семантикой SQLite: платёж `ABORTED` с сохранённым долгом `7.00000000`; тик, сохранивший два `COMMITTED`-платежа и долг `925.31` при отчёте об откате; 113 savepoint'ов в 63 тестах без открытой транзакции БД. Сегодня они **не достижимы**: контроль транзакций установлен в `app/db/session.py:61`, и закрытие `T1525` (015 `spec.md:849`, `:3246`) фиксирует, что подтверждённые дефекты исправлены до принятия остаточного риска. Что осталось — модуль на 182 строки, из которых 60 — docstring о том, почему pysqlite не открывает транзакцию, и класс дефектов, который может вернуться с любой новой точкой записи, потому что второй диалект требует отдельной правильности.
3. **Второй диалект — налог на каждый модуль ядра.** Ветвление по диалекту стоит в 8 модулях `app/` (50 вхождений `_is_sqlite` / `_is_postgres` / `dialect_name` / `get_backend_name`; слово `sqlite` — 109 вхождений в 17 модулях, замер 2026-09-21): `LEAST` против `min()` (`clearing/service.py:658-660`, `:787`), нормализация UUID для raw SQL (`:664`), `_PARAMSTYLES` журнала (`ledger/journal.py:2035-2042`), float-round-trip денег (`journal.py:2084-2101`), три стартовые пробы схемы в `app/main.py:323-500` (около 178 строк, включая regex-разбор DDL SQLite), `sqlite_busy_error_name` в пяти модулях. Оценка объёма — 900–1000 строк, чья единственная цель — эмуляция. Хуже строк — правило «каждый механизм обязан быть верен на обоих диалектах»: из-за него Phase B 015 **отвергла триггер БД** как писатель журнала (`spec.md:670-675`: «дефолтный SQLite-тир, собираемый `create_all`, проверял бы систему без писателя журнала — встроенное ложное зелёное») и построила вместо него 3 078 строк перехвата событий SQLAlchemy.

**Чему SQLite на самом деле служит — полная инвентаризация 2026-09-21.** Для развёртывания он **уже не используется**: `docker/docker-entrypoint.sh:13-19` завершает процесс на любой схеме URL кроме `postgresql`, драйвер `aiosqlite` стоит только в `requirements-dev.txt:5` и ни в один образ не попадает, `docker-compose.yml:41` задаёт Postgres. SQLite держит другое — путь «без установки чего-либо» для разработчика и тестировщика: оба лаунчера (`run_local.ps1:96`, `run_full_stack.ps1:95`; их `-ResetDb`/`reset-db` работают **только** на SQLite-файле, `run_full_stack.ps1:1032`), дефолтный тир тестов (151 файл неявно, 21 строит свой SQLite-движок, 11 названы `*sqlite*`), единственный e2e админки с настоящим бэкендом (`verify_admin_phase4_real_contract.ps1:57`), пять утилит без Postgres-двойника (`init_sqlite_db.py`, `check_sqlite_db.py`, `check_real_snapshot_debts_sqlite.py`, фаза анализа `run_simulator_run_and_analyze.py:348`, `t1525_savepoint_detector.py`) и два блока README «no-Docker quickstart (SQLite)». В CI на SQLite сидят ровно два job'а — `required-quality` и `simulator-super-smoke`; ни один job не поднимает Python-бэкенд за UI. Postgres-лаунчер уже есть, но только для симулятора (`scripts/run_real_simulator.ps1`), и есть runbook переносного Postgres без Docker (`docs/ru/backend/postgres-local-portable.md`). Честный объём: около **97 файлов** должны измениться (25 `app/`, 2 конфигурации, 13 скриптов, 1 workflow, 30 тестов и модулей поддержки, 27 документов). Ответ владельца на П1 («SQLite как способ запуска не нужен») с этой картиной согласуется, но означает: **удалить SQLite можно только после того, как у разработчика и тестировщика появится равноценный путь на Postgres одной командой.**

**Чем SQLite полезен сегодня и что из этого сохраняется.** Внешнее ревью нашло один случай, где SQLite-тест поймал мутацию, пропущенную Postgres-тиром (015 `spec.md:1748`): `tests/unit/test_p015_t1544_inject_refuses_a_deactivated_equivalent.py:51-80,115-124` записывает выполненный SQL и утверждает отказ **до** вставки конверта операции. Ассерт переносим и **сохраняется** в Postgres-форме; проверка только итоговых строк потеряла бы покрытие. Незаменимого второго оракула для Postgres-продукта ревью не нашло.

## Что решает программа

- **Один диалект.** `DATABASE_URL` без схемы `postgresql+asyncpg://` — отказ при старте с внятной ошибкой; умолчание из `app/config.py` удаляется вместе с `DEFAULT_SQLITE_DATABASE_URL`. Локальный запуск требует живой Postgres: `docker-compose.yml` уже поднимает `postgres:16-alpine`; стенд владельца живёт в WSL2. Скрипты запуска перестают подставлять SQLite и требуют URL явно, с подсказкой на `docker compose up db`. **Два URL со стороны хоста, не один:** имя `db` из compose видно только внутри сети контейнеров (`docker-compose.yml:8`, `:41`), а его база `geov0` — не тестовая; для тестов страж требует `geov0_test_*` на `127.0.0.1`. **Существующие файлы `.local-run/*.db` — пользовательские данные:** снятие движка не разрешает их удалять (`docs/ru/09-decisions-and-defaults.md:17-19`); скрипты их не трогают, README называет, что с ними делать.
- **Лаунчер полного стека на Postgres — в стадии 1, до удаления чего-либо** (решение по консультации Codex 2026-09-21, `ANSWER-1: ADOPT-WITH-CHANGES`). `run_full_stack.ps1` и `run_local.ps1` получают Postgres-путь: схема через общий вход миграций вместо `init_sqlite_db.py`, **наполнение настоящими операциями** (следующий пункт), админка и симулятор в real-режиме. **Граница сброса задаётся до замены удаления файла удалением базы** (P2 консультации): сегодня сброс отказывает на любом URL кроме своего SQLite-файла (`run_full_stack.ps1:1032`), а сброс тестовой Postgres-базы требует имени `geov0_test_*` и явного `GEO_TEST_ALLOW_DB_RESET=1` (`validate_test_database_url.py:112-118`). Dev-сброс получает ту же строгость: сбрасывается только база, **принадлежащая лаунчеру**, с проверяемым именем (`geov0_dev_<slug>`), после остановки процессов приложения; произвольный переданный URL сбросить нельзя, но запуститься на нём можно. **Проверка готовности перед стартом бэкенда** (P2): схема на `head`, ожидаемая популяция сида, baseline по каждому эквиваленту; автоматически инициализируется только свежая база лаунчера, незавершённая инициализация — отказ с инструкцией (baseline «усыновляет» необъяснённые остатки, `reconciliation.py:977`, поэтому молчаливый пересев недопустим). **Токен админа** передаётся каждому клиенту его переменной — `VITE_ADMIN_TOKEN` для админки (`realApi.ts:378`), `VITE_GEO_ACCESS_TOKEN` для симулятора (`useSimulatorApp.ts:543`); сегодня не передаётся никому, и при своём `ADMIN_TOKEN` в `.env` каждый вызов админки отвечает `403`. Обещание «без Docker» сохраняется через обновлённый runbook переносного Postgres. Две используемые утилиты осмотра (`check-db`, фаза анализа рана) переносятся на Postgres; остальные SQLite-утилиты удаляются с записью.
- **Данные для проверки админки производит сам продукт, а не заготовленный JSON** (вопрос владельца 2026-09-21: «может, проверять админку на реальных данных, а не тянуть прицеп с фикстурами»; решение по консультации Codex, `SEED-BY-RUNNING-THE-PRODUCT: ADOPT-WITH-CHANGES`, `ADMIN-FIXTURES-JSON: KEEP-SMALL-THROUGH-DOMAIN`, `WHEN: WITH-017-LAUNCHER`). Сегодня `scripts/seed_db.py` кладёт строки **прямо в таблицы** (`:133,146,187,331,359,426,487,597,635`), мимо доменных сервисов; генератор строит долги из линий доверия (`admin-fixtures/tools/seedlib.py:48`), а транзакции — отдельно (`generate_seed_greenfield_village_100.py:541`), то есть 248 засеянных транзакций не породили 374 засеянных долга; долги идут конвертом `SEED`, который критерий (б) сверки не проверяет (`reconciliation.py:38`), а baseline их «усыновляет» (`:977`). Для проекта, чья цель — «каждый долг объяснён записанными операциями», это та же фикстура, только в базе. Замена: **компактный упорядоченный рецепт операций** (участники, линии, платежи — не строки долгов и не выдуманные результаты транзакций), который `seed_db.py` исполняет через уже существующие подписанные доменные пути — `ParticipantService.create_participant` (`participants/service.py:24`), `TrustLineService.create` (`trustlines/service.py:117`), `PaymentService.create_payment` (`payments/service.py:371`), клиринг — с настоящими тестовыми парами ключей, символическими именами, разрешаемыми в PID, последовательно и с явной проверкой завершения каждого шага. Порядок на свежей базе лаунчера: каталог эквивалентов → **пустой baseline по каждому эквиваленту до первого движения денег** → операции → сверка обязана дать `PASSED` с нулевыми остатками baseline и ожидаемым покрытием операций (именно сверка `reconciliation.py:780`, а не `/integrity/verify`, который её не вызывает — `integrity.py:223-230`). Рецепт обязан зажечь экраны: ребро с доступной ёмкостью ниже 10 % (порог bottleneck — `admin.py:757,791`), исполненный клиринг, **отдельный выживший цикл долгов** (эндпоинт циклов показывает клирящиеся сейчас, а не исторические — `admin.py:2376`), активность в каждом выбранном эквиваленте. **Прогон симулятора источником сида не становится** (первая редакция предложения оркестратора это предлагала и опровергнута): сид рана берётся из времени и случайных байт (`runtime_utils.py:128-130`, `run_lifecycle.py:167-169`), такт ждёт одну секунду реального времени (`runtime_impl.py:907`), у Greenfield 50 тактов прогрева (`scenario.json:5533`), первый клиринг возможен с такта 25 и не гарантирован; тест детерминизма планировщика фиксирует сид вручную и отключает БД. Прогоны симулятора остаются для широких демонстраций и интеграционных проверок. Ждать 021 не нужно: подписанные пути уже есть, а зависимость проверки админки от корректности симулятора не вводится. Старый маленький сид `seeds/*.json` тоже пишет мимо домена (`seed_db.py:146,187`) и переписывается на тот же рецепт, а не остаётся как есть.
- **Тестовые сообщества на 50 и 100 участников сохраняются — как описание плюс рецепт, а балансы получаются из операций** (вопрос владельца 2026-09-21; решение по консультации Codex, `COMMUNITIES-PRESERVED: YES-WITH-CHANGES`, `ORDER-BOOK: COMMITTED`, `DEFINITION-FORMAT: OWN-FORMAT`, `DEV-KEYS: GENERATED-PER-RUN`, `BALANCE-POSTCONDITIONS: STRUCTURAL-ONLY`). Сообщество сегодня лежит в четырёх слоях, и смысл живёт в первых трёх. **Замысел** — `docs/ru/seeds/README.md` (370 строк: что такое seed-документ, правило направления линии, ролевые паттерны линий, циклы клиринга, чек-лист качества, анти-паттерны) и `seed-greenfield-village-100.md`, `seed-riverside-town-50.md` с заметками v2: шесть групп — якоря и инфраструктура, производители и ремёсла, розница и еда, услуги, домохозяйства, агенты запуска — с ролевыми набросками и таблицей степеней. **Структура** — ростер и линии доверия с лимитами и политиками; нести вперёд надо **топологии v2 в трёх эквивалентах** (Greenfield: 100 участников, 523 линии — UAH 432, HOUR 87, EUR 4; Riverside: 50 участников, 316 линий — UAH 218, HOUR 96, EUR 2; замер 2026-09-21), а не базовые v1: v2 меняет лимиты, политики маршрутизации и клиринга и связность (`generate_seed_greenfield_village_100_v2.py:163,189,229`, `generate_seed_riverside_town_50_v2.py:491`), и у сообществ разные формулы лимитов (`generate_seed_greenfield_village_100.py:199-208` против `generate_seed_riverside_town_50.py:158-172`). **Поведение** — профили групп, веса получателей, модели сумм, цепочки потоков и стресс-события в сценариях симулятора; они остаются симулятору. **Выдуманный результат** удаляется: долг на линии сегодня — лимит, умноженный на `((n % 17) + 1) / 20` с ручными «бутылочными горлышками» по 93 % (`generate_seed_greenfield_village_100.py:211-215,291-296`), а транзакции ролей не знают вовсе — отправитель `pids[(i+3) % n]`, получатель `pids[(i+17) % n]`, сумма `5 + (i % 37) * 3` (`:541-600`). **Конструкция:** (1) одно описание на сообщество в `seeds/communities/<community>/community.json` с **собственной малой схемой, принадлежащей инструментам сида**: символические идентификаторы участников, **явные** группы (а не роль, выведенная из подстроки PID или имени), эквиваленты с точностью, линии с лимитами десятичной строкой и доменными политиками; формат сценария симулятора для этого не годится — он смешивает структуру с тактами, прогревом и стрессом, допускает числовые лимиты и произвольные объекты политики (`fixtures/simulator/scenario.schema.json`: `limit` — `integer | number | string`, `policy` — `additionalProperties: true`); сценарии симулятора **генерируются из описания** плюс его собственных поведенческих настроек, второго ростера руками не ведётся; структурная логика v2 извлекается из генераторов **до** удаления их зависимостей; (2) один **закоммиченный, написанный руками** рецепт на сообщество — порядка 30–50 платёжных команд (оценка консультации, не проверенный рецепт): символические концы, десятичная сумма, идентификатор команды, короткое экономическое объяснение и **ожидаемый исход на каждую команду**; генератора «профили → рецепт» нет — фиксированное зерно не делает малую стохастическую выборку надёжной демонстрацией, переписанное подмножество планировщика стало бы второй поведенческой политикой, а импорт планировщика вернул бы зависимость от симулятора; направление платежа — покупатель-должник → продавец-кредитор (`real_payment_planner.py:140`), поэтому профиль «производитель → розница» не означает «продал рознице»; (3) для немногих команд, чьи точные рёбра строят «горлышко» и циклы, используется существующее подписанное ограничение `max_hops=1` (`app/schemas/payment.py:33`), потому что маршрутизация сама выбирает путь; исполняется один названный цикл, а **отдельный выживший цикл строится после последней команды клиринга с сохранённым согласием `auto_clearing`** — иначе он выпадает из клирящихся (`clearing/service.py:892,934`, `admin.py:2382`); отказ по ёмкости происходит до создания транзакции и не равен сохранённому `ABORTED` (`payments/service.py:851,901`), поэтому счётчики «принято/отклонено» приёмкой не служат; (4) **ключи генерируются на каждый прогон** существующим `generate_keypair()`, приватные живут только в памяти исполнителя, наружу уходит лишь таблица «символический идентификатор → PID» (`PID = base58(sha256(public_key))`, `crypto.py:37-48`; регистрация проверяет подпись владения, `participants/service.py:24-57`); детерминированно выводимые ключи отвергнуты — это публичные учётные данные, которыми может подписаться кто угодно, где бы ни оказались записи; перед работой с базой исполнитель требует нормализованный `ENV ∈ {dev, test}`, явно выбранную одноразовую базу по контракту имён лаунчера и теста и **пустой** набор данных приложения, падает на частичной инициализации и сбрасывается только явным охраняемым действием; проверка `ENV` защищает от обычной ошибки, а не от оператора, намеренно подавшего доступ к продакшену (образ копирует `scripts/`, `docker/Dockerfile:23`, а `ENV` в compose переопределяем, `docker-compose.yml:40`); (5) тесты админки обращаются к участникам по имени или символическому идентификатору через таблицу, никогда по зашитому PID. **Приёмка:** пустой baseline, нулевые остатки, сверка `PASSED` с полным покрытием поддерживаемых операций; четыре условия админки; структурные утверждения о сообществе — состав ростера и групп, представительные кредитные отношения, сохранённые лимиты и политики, названные циклы в одном эквиваленте, экономический смысл каждой команды. **Знаки нетто-балансов групп не утверждаются и балансы разных эквивалентов не складываются**: seed-документы описывают отношения кредитор/должник и степени, а не итоговые знаки (`seed-greenfield-village-100.md:38,54`, `seed-riverside-town-50.md:41,78`) — первая редакция предложения оркестратора приписала им такие обещания. Два примера в `seed-riverside-town-50.md:97-122` исправляются до того, как станут исполняемыми: первая диаграмма цикла перевёрнута относительно собственной расшифровки, вторая замыкает цикл через UAH и HOUR, а клиринг смешанные циклы отвергает (`clearing/service.py:1587`); обмен между эквивалентами при этом не вводится. **Что сознательно теряется:** сразу насыщенный граф долгов, подобранное распределение балансов, «застрявшие» инциденты и разброс истории на 96 дней; замороженные участники (сегодня пять и два, индексы нарисованы генератором, `:190-194`) получаются настоящей операцией заморозки там, где они нужны ручной демонстрации. Исключительные состояния UI и окна истории 7/30/90 дней проверяются подменой ответа, а расчёт окон на бэкенде (`core/admin/metrics.py:569,610`) — своими тестами: подмена его не проверяет. **Цена:** 523 создания линий и 100 регистраций (или 316 и 50) до платежей, причём создание линии считает чекпоинты до и после изменения и коммитит (`trustlines/service.py:187,249,291`) под `SERIALIZABLE`; сид исполняется один раз на ручной запуск или на изолированный job CI, не на каждый браузерный тест; Greenfield-100 — умолчание для ручного запуска, Riverside-50 — для CI, рецепт сотни идёт в отдельной приёмочной проверке; время регистрации, линий, платежей, клиринга и сверки меряется раздельно до принятия бюджета, и сервисы ради бюджета не обходятся.
- **Один тир тестов.** `TEST_DATABASE_URL` обязателен и обязан быть Postgres; страж `tests/conftest.py:39-48` (только `geov0_test_*`) и `:76-89` сохраняются. Маркер `postgres` теряет смысл: **каждый** DB-тест идёт на Postgres. Маркер удаляется из `pytest.ini`, `scripts/verify_local.ps1` и файлов; суффикс `_postgres` в именах файлов остаётся историческим, переименование не делается (§14: не смешивать классы cleanup). Маркер `slow` остаётся.
- **Две стратегии изоляции, а не одна.** Внешнее ревью показало, что оставить только savepoint-фикстуру нельзя: она даёт сессию, привязанную к соединению (`tests/conftest.py:409-411`), а клиринг на Postgres такую сессию **отвергает** (`app/core/clearing/service.py:1530-1539`, до 019 это поведение живо); тесты снапшотов симулятора ждут чужой коммит, который внутри одного `SERIALIZABLE`-снимка не виден никогда (`tests/integration/test_simulator_real_snapshot_db_enrichment.py:59-69`); тест перезапуска требует настоящих коммитов, видимых другому процессу (`tests/integration/test_p015_t1523_restart_after_commit_postgres.py:174`). Поэтому: режим **A** — внешняя транзакция плюс savepoint, откат в конце, для тестов, где приложение не владеет соединением; режим **B** — одноразовая база с настоящими корневыми коммитами для клиринга, конкурентности, фоновых задач и перезапуска. Потребители каждого режима перечисляются в инвентаре `T1706` **до** авторизации. Семантика транзакций приложения под фикстуры не подгоняется. **Объём, которого маркер не показывает** (находка `T1706`, воспроизведена оркестратором): отказ клиринга на привязанной сессии — не редкий случай, а **13 файлов дефолтного тира**, которые уже сегодня исполняют настоящий клиринг и не помечены `postgres`; перечень — в `t1706-inventory.md`, здесь он не повторяется, потому что за один день разошёлся с кодом дважды (11 → 12 → 13). Сегодня это молчит только потому, что `clearing/service.py:1523` уводит не-Postgres в ранний возврат. **Предикат режима B по клирингу — достижимость, а не слово в тексте:** файл получает B, если его тест доводит исполнение до `execute_clearing_with_amount` на сессии, привязанной к соединению, **с непустым циклом** — та же строка `:1523` уходит в ранний возврат и при `not cycle`, поэтому «файл ходит в `/clearing/auto`» режима B ещё не доказывает, нужен ассерт, что цикл нашёлся. Сервис достигается и через роут: `tests/integration/test_scenarios.py` (`test_clearing` `:562`, `POST /api/v1/clearing/auto` `:709`, фикстура `client` отдаёт приложению тот же `db_session`, `tests/conftest.py:425-449`, эндпоинт строит `ClearingService(db)`, `app/api/v1/clearing.py:51`) и `tests/integration/test_clearing_max_depth_controls_long_cycles.py` (`:96-103`, ассерт `cleared_cycles >= 1`) слова `ClearingService` не содержат вовсе или содержат не там, и поиском токена были пропущены; а `tests/unit/test_interact_actions_backend_p1.py:1077` ходит в клиринговый роут с нулём циклов и отказа не достигает. Клиринг реального тика безопасен: `real_clearing_engine.py:154` берёт `AsyncSessionLocal`, подменённый фикстурой на сессию, привязанную к engine (`tests/conftest.py:181-182`).
- **Схема — один раз, с bootstrap.** Порядок: bootstrap `alembic_version VARCHAR(128)` (`tests/migrated_schema.py:47-50`, `:81`; решение `T1535` 2026-09-20 сохраняется — голый `alembic upgrade head` падает на `010→011`) → `alembic upgrade head` в шаблонную базу → отключение от шаблона → `CREATE DATABASE ... TEMPLATE` на задачу или тест режима B, с очисткой и требованием права `CREATEDB` у тестовой роли. **Bootstrap живёт в одном месте** (P3 консультации): сегодня его копии стоят в `docker/docker-entrypoint.sh:38` и `tests/migrated_schema.py:47`, а сам вход миграций (`migrations/env.py:60`) к нему не готов; вход миграций становится владельцем «создать или расширить `alembic_version`», а лаунчеры, entrypoint и тесты зовут его — третья копия в лаунчере не заводится, тестовая инфраструктура в лаунчеры не импортируется. Провизионирование шаблона живёт в `tests/migrated_schema.py`, а не в `conftest.py`, и ставится **до** массовой миграции тестов. `T1541` (схема строится трижды) закрывается только если консолидируются **и** две pytest-сессии CI (`quality.yml:198-226`), **и** bootstrap entrypoint'а — без потери покрытия самого entrypoint'а (015 `spec.md:865`); шаблон сам по себе этого не делает.
- **CI видит Postgres на каждом PR — это разделение job'а на два, а не правка `runs-on`.** `required-quality` (`quality.yml:20-64`, `windows-latest`) — **один шаг** `:59-64`, один вызов `./scripts/verify_local.ps1 -TaskSlug ci-required` без `-BackendOnly`; backend и UI разведены не в workflow, а внутри скрипта (pytest — `verify_local.ps1:112-141`, UI-блок под `if (-not $BackendOnly)` — `:142-167`). Сервисы GitHub Actions на Windows-раннерах недоступны, поэтому появляются два job'а: **backend** на `ubuntu-latest` с сервисом `postgres:16` (как уже сделано в job `postgres`, `quality.yml:156-178`) и **UI** там, где он есть сегодня. У скрипта сегодня есть только `-BackendOnly`, значит UI-job'у нужен либо переключатель `-UiOnly`, либо вынос UI-блока; и проверка единственной головы Alembic (`check_alembic_heads.py`) сегодня стоит **внутри UI-блока** (`verify_local.ps1:142-145`) — она переезжает в backend-job, иначе потеряется. `main` **не защищена** (`gh api repos/slawa19/GEOv0/branches/main/protection` — `Branch not protected`, проверено 2026-09-21), обязательных status checks нет, поэтому смена имени job'а ничего технически не блокирует; запрет раздела «Запрещено» объявлять CI зелёным по старым именам остаётся. Job `postgres` по расписанию упраздняется, его селекторы входят в обязательный тир. Job `simulator-super-smoke` (`quality.yml:677-708`) сегодня гоняет backend-тесты без Postgres и без URL — он получает сервис и URL в том же срезе, иначе обязательный Postgres его сломает. Это изменение механизма проверки, то есть триггер внешнего ревью по §15.
- **Порядок реализации — три стадии, каждая ревьюема отдельно** (условие внешнего ревью): **(1)** провизионирование, обязательный Postgres-гейт на PR и лаунчер полного стека на Postgres, SQLite ещё жив — это закрывает живой дефект 1 раньше всего остального и даёт тестировщику рабочий путь; **(2)** миграция тестов на два режима фикстур по инвентарю, замер времени тира; **(3)** удаление совместимости — модуль, пробы, ветки, скрипты, зависимость. Стадия 3 не начинается, пока бюджет времени тира (`T1706`) не установлен **консультацией Codex по замеру** — это технический порог, владельцу он не выносится (протокол решений 2026-09-21).
- **Удаление (стадия 3).** `app/db/sqlite_transaction_control.py`; три пробы в `app/main.py:323-500`; `scripts/init_sqlite_db.py`, `scripts/check_sqlite_db.py`, `scripts/check_real_snapshot_debts_sqlite.py`; SQLite-ветка `scripts/run_simulator_run_and_analyze.py:348`; `aiosqlite` из `requirements-dev.txt:5`; `tests/scratch_db.py::install_test_sqlite_pragmas`; все ветки `if not self._is_postgres()` / `_is_sqlite`. Postgres-ветка остаётся **дословно**, чтобы diff денежного кода читался как удаление строк, а не как правка логики.
- **Храповик.** Тест-гард `tests/unit/test_p017_no_second_dialect.py` из двух частей: (а) ветвления по диалекту в исполняемом коде `app/` — константа `0`, сегодня `50`; в перечень входят не только именованные helper'ы, но и прямые обращения `dialect.name` / `engine.dialect` / `url.get_backend_name()` и сравнение со строкой `"postgresql"`/`"sqlite"` — иначе обычная запись `if engine.dialect.name != "postgresql": return` прошла бы мимо; (б) слово `sqlite` в исполняемом коде — константа `0`; docstring отличаются от исполняемых строк **по AST** (`ast.get_docstring`), а не по тройным кавычкам, чтобы SQL в строковых литералах не прятался; датированные записи измерений в docstring (например `app/db/journal_tables.py:270-282`, `T1526`/`T1530`) сохраняются по §1 через узкий allow-list. Контрпроверки (§9 anti-vacuum) — и положительные (сканер находит подложенный фрагмент каждого вида), и отрицательные (сканер не срабатывает на allow-list и на слово внутри docstring). Гард проверяет форму, а не истину: он не докажет, что оставшийся код корректен на Postgres, это делают тесты корзин A/B/C; наблюдение SQL-вызова лока доказывает исполнение ветки, а не исключение — конкурентные расписания корзины B остаются рядом.

## Non-goals

- Не меняет ни одного денежного правила, ни одной схемы, ни одной миграции. `Numeric(20,8)`, CHECK-ограничения, `RESTRICT` — как есть.
- Не убирает advisory-локи, `prepare_locks`, interlock клиринга, `version_id_col` и журнал 015: их судьбу решают 018–020; здесь у них лишь исчезает ветка `if not postgres`.
- Не трогает `api/openapi.yaml` и wire-контракты.
- Не вводит testcontainers, embedded Postgres или иной новый инструмент: стенд — `docker compose` или внешний URL, как сегодня у Postgres-тира.
- Не переименовывает файлы `*_postgres.py` и не чистит архивы и `_audit*`.
- Не удаляет пользовательские SQLite-файлы и не берёт на себя миграцию данных из них: у dev-БД по решению `09:14-19` данных, требующих переноса, нет; если владелец назовёт такой набор, это отдельная задача.

## Проверка на переусложнение (§19.2) — 2026-09-21, уточнено по внешнему ревью

1. **Какую наблюдаемую потерю это предотвращает?** Честный ответ — двухчастный. **Живая** проблема — покрытие: тир, в котором локи, SSI и `FOR UPDATE` не наблюдаемы (`engine.py:182`, `session.py:67-69`), и Postgres только по расписанию; это дефект измерителя, а не движение денег. **Исторические** потери из `sqlite_transaction_control.py:14-28` (`925.31`, `7.00000000`) воспроизведены, закрыты `T1525` и сегодня недостижимы (`session.py:61`); они — цена второго диалекта, а не текущая потеря, и спека их таковой не выдаёт. Отсюда следствие для владельца: программа авторизуется как **устранение ложного зелёного и долга сопровождения**, а не как починка живой денежной потери, и её место в очереди определено тем, что 018/019 без неё невозможны в выбранной форме.
2. **Что говорит протокол?** Атомарность и корректность долга — требования (`02-protocol-spec.md:208-211`); PostgreSQL как единственный движок — **реализационный выбор**, протокол его не требует. Что требует практика проекта: `T1544` («авторитетна PostgreSQL»), advisory-локи, `FOR UPDATE`, `SERIALIZABLE` (`app/config.py:68`) — примитивы решений 2026-08-11 существуют только там.
3. **Кто противник?** Никакого: программа ничего не защищает, она упрощает и чинит измеритель.
4. **Что дешевле и чего ему не хватает?** Три альтернативы, две из них добавлены по внешнему ревью. **(а) Сначала сделать существующий Postgres-job обязательным на PR** (`quality.yml:156`, `:203`, `:218` — job и селекторы уже есть), SQLite оставить: закрывает живой дефект 1 за один срез без миграции 450 тестов. **Это стадия 1 программы**; чего ей не хватает — двух диалектов в ядре остаётся ровно столько же, триггер 018 по-прежнему нельзя объявить обязательным в дефолтном тире, и Phase B `:670-675` остаётся в силе. **(б) Переходный ограниченный SQLite-тир** только для неденежных persistence-тестов (поиск участников `tests/integration/test_participants_search.py:40`, обновление токена `tests/integration/test_auth_refresh.py:15`), всё денежное — на Postgres: остаточная цена — второй драйвер, второй путь построения схемы, вторая стратегия фикстур и граница покрытия, которую надо охранять; спека этот вариант отклоняет как постоянное обязательство, но допускает как **временное состояние между стадиями 1 и 3**. **(в) Полное приложение на обоих диалектах** (сегодня): альтернативные реализации журнала, контекста и конкурентности — самый дорогой вариант, и ревью его не рекомендует.
5. **Цена, числом.** Новых сущностей: 0 (один тест-гард, одна функция провизионирования). Удаляется: 1 модуль (182 строки, точно), около 178 строк `main.py` (`:323-500`), 3 скрипта и одна ветка анализатора, 50 dialect-веток в 8 модулях (точно), одна dev-зависимость, файлы тестов корзины D-SQLite (8 по инвентаризации; файлов со `sqlite` в имени — 11, разница — тесты корзин A/E, которые остаются) и по одному тесту `test_this_modules_engine_has_the_application_sqlite_pragmas` в пяти файлах симулятора. Меняется: `conftest.py` (две стратегии), `migrated_schema.py` (bootstrap → шаблон → клон), `verify_local.ps1`, три job'а `quality.yml`. **Цена, которую надо признать вслух (оценка по подсчёту фикстур, не замер):** порядка 450 тестовых функций в 118 файлах, сегодня не помеченных `postgres`, но трогающих БД, начинают требовать живой Postgres; точное число — в `T1706`. **Не оценённое ранее и теперь названное:** право `CREATEDB` и владение шаблоном; очистка клонов при падении; изоляция по `-TaskSlug` для баз, а не только каталогов; накладные расходы соединений под `NullPool` (`tests/conftest.py:131-140`, обход event-loop на Windows); адаптация SQLite-плейсхолдеров в raw SQL тестов (например `test_simulator_real_snapshot_db_enrichment.py:118-120`); конверсия фикстур в два режима. **Бюджет времени тира** — технический порог, устанавливается консультацией Codex по замеру `T1706`, владельцу не выносится (холодный и тёплый локальный прогон, CI); измеренная вилка 82 с против 669 с (015, `T1523`) шаблоном не объясняется и не закрывается — она про состояние WSL, и стадия 3 не начинается, пока бюджет не принят.
6. **Крайние случаи.** «Приложение стартовало на SQLite» — после стадии 3 пути нет. «У разработчика есть SQLite-файл с данными» — путь есть (`.local-run/geov0.db`), берём: файл не трогается, README называет `docker compose up db` и сидинг; переноса данных нет по решению `09:14-19`.

## Verification plan

### 1. Репродьюсеры, обязанные падать на текущем коде

- `tests/unit/test_p017_no_second_dialect.py` — часть (а): константа `0`, сегодня факт `50` (см. §19.2 п. 5). Падает.
- `tests/unit/test_p017_default_tier_can_see_the_lock.py` — на дефолтном `TEST_DATABASE_URL` из `verify_local.ps1` с **непустым** набором эквивалентов вызов `PaymentEngine._acquire_equivalent_owner_locks` обязан выполнить `pg_advisory_xact_lock` (`engine.py:194`); сегодня на дефолтном тире он возвращается на `engine.py:182`, не выполнив ничего (пустой набор не выполняет лока ни на одном движке — это оговорено в тесте). Это тест **на измеритель** (§15); он доказывает исполнение ветки, а не исключение, поэтому конкурентные расписания корзины B остаются рядом. После 019 тест переписывается под новый механизм, но не удаляется.
- `tests/unit/test_p017_required_gate_runs_on_postgres.py` — гард формы на `quality.yml`: обязательный backend-job несёт сервис Postgres и `TEST_DATABASE_URL`; падал на трёх ассертах до среза стадии 1, зелен после. **Путь исправлен 2026-09-21 с `tests/integration/` на `tests/unit/`; записанная причина исправлена 2026-09-22 по внешнему ревью Codex — решение верное, обоснование под ним было шире правды (§15).** Таксономия репозитория (`tests/unit/test_postgres_test_taxonomy.py:123-152`) проверяет **суффикс имени файла** `_postgres.py`, а не любое упоминание Postgres; путь из первой редакции спеки оканчивался именно на `_postgres.py`, поэтому на нём гард потребовал бы маркер `postgres` и вышел бы из дефолтного тира — единственного, который обязан заметить исчезновение Postgres из гейта (измерено 2026-09-21: «PostgreSQL suffix modules without marker»). **Но «другого пути не было» — неверно:** модуль в `tests/integration/` с именем без этого суффикса, например `test_p017_required_postgres_gate.py`, остался бы без маркера и в дефолтном тире. Решение стоит на существе файла, а не на безвыходности: он читает один YAML, не открывает БД и не поднимает сессию, поэтому живёт рядом с остальными гардами формы в `tests/unit/`.

### 2. Инварианты, обязанные выжить, и контрпроверка

- Тесты с вердиктом `keep` проходят на Postgres без изменения ассертов. **Авторитет по составу, вердикту и режиму фикстуры — per-file инвентарь `specs/017-postgres-only-engine/t1706-inventory.md`** (`T1706`, публикуется исполнителем отдельным PR): 299 файлов `test_*.py` (проверено оркестратором `find tests -name 'test_*.py'`), вердикты keep 260 / transfer 26 / delete 13, режимы A 100 / B 90 / без БД 109 (после двух исправлений A → B 2026-09-21). **Половина «до» для бюджета времени тира снята исполнителем** (раздел 8 инвентаря): дефолтный тир на SQLite — 2447 passed, 283 deselected, холодный прогон 268.42 с, тёплый 268.54 с; прогрева нет вовсе, значит вилка 82 с / 669 с из `T1523` прогревом не объясняется и остаётся за Postgres и состоянием WSL. Числа корзин из первой инвентаризации оркестратора (A 84 / B 14 / C 42 / D 59 / F 41 / G 37 на «около 304» файлов, куда попали модули поддержки) **заменены этим инвентарём** и больше не цитируются. Контрпроверка: число собранных тестов до и после записывается в Changelog; расхождение объясняется поимённо, «0 collected» — провал.
- Ассерт «отказ до вставки конверта» из `test_p015_t1544_inject_refuses_a_deactivated_equivalent.py:51-80,115-124` сохраняется в Postgres-форме вместе с чувствительностью к мутации размещения гарда (015 `spec.md:1748`).
- `tests/integration/test_p015_t1530_delta_arithmetic_postgres.py:233-244,276,294-312` сравнивает схему из `create_all` со схемой из миграций — это **не** SQLite-тест; контракт «метаданные и миграции дают одно ограничение» либо снимается явно с записью, либо остаётся именованным диагностическим исключением из правила «`create_all` в тестах нет». Решение — в `T1706`, не по умолчанию.
- Страж URL (`conftest.py:39-48`) по-прежнему отказывает на URL вне `geov0_test_*`. Контрпроверка: `tests/unit/test_test_database_guard.py` остаётся зелёным.
- `alembic upgrade head` после bootstrap на пустой Postgres-базе — единственная дорога к схеме. Контрпроверка: `tests/integration/test_p015_t1534_the_migrated_schema_flag_is_true_postgres.py`.
- Режим B действительно даёт видимость другому соединению: `test_simulator_real_snapshot_db_enrichment.py`, `test_p015_t1523_restart_after_commit_postgres.py` зелёные на нём; контрпроверка — тот же тест на режиме A краснеет (это и есть измерение, что режим B нужен).

### 3. Существующие селекторы, обязанные остаться зелёными

**У каждого селектора назван режим фикстуры — без режима «остаётся зелёным» не проверяемо** (первая редакция перечисляла только имена, и `tests/unit/test_invariants.py` стоял в списке, хотя в режиме A на Postgres зелёным остаться не может: `db_session` + `ClearingService`, `test_invariants.py:8,25`). Правило: селектор, который исполняет клиринг, ждёт чужого коммита или переживает перезапуск процесса, идёт в режиме B. Проверено оркестратором по коду — **режим B:** `tests/integration/test_scenarios.py` (клиринг через `client`: `test_clearing` `:562`, `POST /api/v1/clearing/auto` `:709`; в первой редакции инвентаря строка была помечена режимом A — режим решался поиском токена по тексту, а не по достижимости; исправлено исполнителем), `tests/unit/test_invariants.py` (клиринг через `db_session`), `tests/integration/test_clearing_commit_replay_postgres.py`, `tests/integration/test_concurrent_clearing_payment_lost_update_postgres.py`, `tests/integration/test_concurrent_prepare_routes_bottleneck_postgres.py`, `tests/integration/test_payment_idempotency_postgres.py`, `tests/integration/test_p015_t1523_restart_after_commit_postgres.py`, `tests/integration/test_simulator_real_snapshot_db_enrichment.py`; **собственная одноразовая база (уже сегодня, через `CREATE DATABASE`):** `test_p015_step5a_reconciliation_postgres.py`, `test_p015_step5b_criterion_b_postgres.py`; остальные перечисленные — по инвентарю `t1706-inventory.md`, и расхождение инвентаря с этим абзацем решается в пользу кода, а не списка.

`tests/integration/test_scenarios.py`, `tests/integration/test_payments_idempotency.py`, `tests/integration/test_payment_idempotency_postgres.py`, `tests/integration/test_clearing_commit_replay_postgres.py`, `tests/integration/test_concurrent_clearing_payment_lost_update_postgres.py`, `tests/integration/test_concurrent_prepare_routes_bottleneck_postgres.py`, `tests/integration/test_p015_t1523_restart_after_commit_postgres.py`, `tests/integration/test_p015_step5a_reconciliation_postgres.py`, `tests/integration/test_p015_step5b_criterion_b_postgres.py`, `tests/integration/test_simulator_real_snapshot_db_enrichment.py`, `tests/integration/test_participants_search.py`, `tests/integration/test_auth_refresh.py`, `tests/contract/test_openapi_contract.py`, `tests/unit/test_payments_2pc.py`, `tests/unit/test_invariants.py`, `tests/unit/test_debt_symmetry.py`, `tests/unit/test_trustline_signatures.py`, `tests/unit/test_p015_t1544_inject_refuses_a_deactivated_equivalent.py`.

### 4. Запрещено

- Засчитывать прогон с `TEST_DATABASE_URL` на SQLite за evidence чего бы то ни было после стадии 2.
- «Переносить» SQLite-тесты корзины D на Postgres правкой ассертов: они проверяют механизм, которого больше нет, и удаляются с записью в Changelog (§11: показать, что тест проверяет удалённый контракт).
- Менять семантику транзакций приложения (например снимать отказ клиринга на привязанной сессии) ради фикстуры.
- Смягчать страж `geov0_test_*` ради удобства локального запуска.
- Объявлять CI зелёным без имени job'а и SHA: после переезда job'ов старые имена не существуют.
- Начинать стадию 3 до того, как бюджет времени тира установлен консультацией Codex по замеру; выносить этот бюджет владельцу — тоже запрещено: он не продуктовый.

## Отменяемые решения владельца

Программа просит снять **одно** датированное решение, уточняет одно правило и объявляет устаревшими две вспомогательные посылки:

- **Снять:** Phase B 015, решение 1 (`spec.md:670-675`) в части «триггер отвергнут, потому что SQLite-тир его не увидит», и шаг 8 порядка первого среза (`:842-844`) «функциональное покрытие на SQLite». Основание — новая находка (дефект 1 в `## Problem`: тир не видит локов), а не новое прочтение.
- **Уточнить:** `docs/ru/09-decisions-and-defaults.md:14-19` (`.local-run/geov0.db` как dev-БД) — заменить на Postgres-URL из `docker-compose.yml`; существующие файлы — данные пользователя, не удаляются.
- **Устаревшие посылки, записываются как устаревшие, а не переписываются:** `T1540` — принятое ограничение SQLite-констрейнтов (015 `spec.md:864`); `T1544` — оговорка «на SQLite advisory-лок no-op» (`:868`). Исторические решения сохраняются в тексте.
- **Сохраняются:** `T1535` (bootstrap `alembic_version`, 2026-09-20) — воспроизводится в провизионировании; принятые риски `T1525` в части обработки ошибок Postgres и границ покрытия (015 `spec.md:3216-3244`) — удаление SQLite их **не закрывает**, и спека этого не заявляет.

Остальные решения 2026-08-11 (идентичность локов, пиннутое соединение клиринга, L1–L3) программа **не трогает**: они принадлежат 019 и 020.

## Внешнее ревью

Триггер §15 — изменение механизма проверки (`verify_local.ps1`, `quality.yml`, `conftest.py`). Задача `T1708`. Ревьюер по матрице runbook (оркестратор Claude Code → Codex). Ревьюеру ставится: (1) найти оставшийся путь, где приложение или тест стартует без Postgres; (2) проверить, что удалённые тесты корзины D не несли поведенческого ассерта, не перенесённого в A (список ⚑ из инвентаризации 2026-09-21 и per-file инвентарь `T1706` — обязательный чек-лист); (3) сравнить число собранных тестов до и после; (4) проверить, что режим B назначен каждому тесту, которому нужен чужой коммит.

**Ревью черновика проведено 2026-09-21** (до авторизации, по просьбе владельца): Codex `codex-cli 0.155.0-alpha.9.2`, requested model `gpt-6-astra`, `model_reasoning_effort=high`, `--sandbox read-only`, замороженный credential-free clone на `80ef001`, промпт на stdin, финальный ответ через `-o`, exit `0`, все девять маркеров на месте. Вердикт: `VERDICT-017: ACCEPT-WITH-CHANGES`, `T1700-DECISION: AUTHORIZE-WITH-CONDITIONS`, `SCOPE-017: RIGHT`, `PRECONDITION-FOR-018-019: PARTIAL`, `DIRECTION-017-022: ON-GOAL`, P1 = 0, P2 = 3, P3 = 3. Ограничение evidence: ревьюер ничего не запускал; числа и время приняты им по записи. Артефакты (промпт, финальный ответ, лог, ledger) — в игнорируемом `.local-run/codex-review/2026-09-21-017/` внутри проекта; временный каталог с клонами удалён.

**Вторая консультация 2026-09-21** — по трём продуктовым вопросам после полных инвентаризаций: тот же бинарник и модель, clone `a2e65c6`, exit `0`, все маркеры на месте; по этой программе `ANSWER-1: ADOPT-WITH-CHANGES` (P2: граница сброса dev-базы; P3: один владелец bootstrap). Оба внесены.

**2026-09-22 — внешнее ревью §15 по `T1701` (Codex), и почему оно шло до конца стадии, а не после.** `T1701` меняет **сам механизм проверки**, а стадии 2 и далее судятся этим гейтом — §15 велит ревьюировать критическую границу **до** перехода, иначе evidence всего построенного сверху стоит на непроверенном основании. Прогон: `codex-cli 0.155.0-alpha.9.2`, requested model `gpt-6-astra`, `model_reasoning_effort=high`, `--sandbox read-only`, credential-free clone на `37fec08` в `.local-run/codex-review/2026-09-22-t1701/`, промпт на stdin, финальный ответ через `-o`, exit `0`, все девять маркеров на месте.

Вердикты: `VERDICT-GATE: BROKEN`, `VERDICT-PROVISION: BROKEN`, `VERDICT-SENTINELS: WEAK`, `VERDICT-BOOTSTRAP: WEAK`, `SCOPE-T1701: RIGHT`, `READY-TO-CLOSE: NO`, P1 = 1, P2 = 5, P3 = 2. **Ограничение evidence:** в клоне нет ни Python, ни БД — ревьюер не запускал ничего, поэтому гейты внешним ревью **не покрыты**, и локальные числа остаются единственным gate-evidence.

Все восемь находок воспроизведены оркестратором по коду до принятия и исправлены одним fix-delta. Две из них были ложным зелёным в собственных гардах волны:

- **P1.** `if sys.exc_info()[0] is None: raise` внутри `except` — `raise` недостижим, потому что внутри обработчика это сама обрабатываемая ошибка. Отказавший `DROP DATABASE` после **успешного** тела становился строкой в stderr и проходящим тестом. Доказано исполнением. Починено флагом, выставляемым после `yield`.
- **Подметание сирот уничтожало базу соседней задачи.** Слаг `a__b` валиден, и задача `a` дропала `geov0_test_a__b` — воспроизведено на живом Postgres. Разделитель `__` **зарезервирован**: имя тира не может его содержать, поэтому `<тир>__<суффикс>` по построению принадлежит `<тиру>`. Владение задаётся пространством имён, а не префиксом строки; сузить сам предикат подметания нельзя — имя `geov0_test_a__b` по одному только имени неразличимо между тиром задачи `a__b` и клоном задачи `a`. Цена названа: слаг для Postgres-тира не может содержать `__`; ни один слаг репозитория и CI не затронут, ограничение записано в `AGENTS.md` §5.
- Гард «отказ, а не skip» **сам скипался** на мутации, которую обещал ловить (`Skipped` наследует `BaseException`). Гарды workflow читали только `job.get("if")` и были слепы к шаговым `if:` и `continue-on-error` — обход защиты, а не её отсутствие (§15).
- «Ровно один владелец» было шире кода: весь `.github` и все `.yml` лежали вне области сканирования **структурно**, так что заявленное исключение по имени не существовало; обычные написания `ALTER TABLE "alembic_version"` и `public.alembic_version` шли мимо шаблонов.
- Инструкция для offline-режима была неверна: Alembic эмитит собственный безусловный `CREATE TABLE`, и ручной прогон statements лишь сдвигает падение на одно вперёд. Заявление сужено, offline-подсистема не строилась.

P3 — две ошибки записи: команда в `BACKLOG.md` со вставленным U+000B и обоснование пути гарда в этой спеке, которое было шире проверенного (таксономия матчит **суффикс** `_postgres.py`, а не любое имя с Postgres, — «другого пути не было» неверно). Обе исправлены; решение по пути гарда оставлено, заменена причина.

**Класс по §19.5 — 2 у всех восьми:** деньги и долг не движутся неправильно, и ревьюер это подтвердил отдельно. Поэтому находки не открыли нового круга и не стали задачами программы; единственный §15-круг по fix-delta израсходован.

## Tasks

| ID | Задача | Статус |
|---|---|---|
| `T1700` | Решение по консультации Codex 2026-09-21: Postgres — единственный движок, `AUTHORIZE-WITH-CONDITIONS`, условия внесены; запись в `specs/README.md` сделана; продуктовый ответ П1 получен 2026-09-21 («нет»); остаток — запись в `docs/ru/09-decisions-and-defaults.md` при стадии 3 | `[ ]` |
| `T1711` | **Стадия 1.** Сид настоящими операциями: компактный рецепт (участники, линии, платежи, клиринг) и его исполнитель в `scripts/seed_db.py` через `ParticipantService` / `TrustLineService` / `PaymentService` / клиринг; **ключи генерируются на прогон и живут только в памяти**, наружу — таблица «символический идентификатор → PID» в `.local-run/`; охрана: `ENV ∈ {dev, test}`, одноразовая база по контракту имён, пустой набор данных, отказ на частичной инициализации; пустой baseline до денег; приёмка — сверка `PASSED` с нулевыми остатками baseline и покрытием операций, ребро ниже 10 % ёмкости, исполненный клиринг, выживший цикл, активность в каждом эквиваленте; прямые вставки `seed_db.py` и путь `--source fixtures` удаляются, когда лаунчер и e2e админки переведены | `[!]` |
| `T1712` | **Стадия 1, до `T1711`.** Описания сообществ `seeds/communities/{greenfield-village-100,riverside-town-50}/community.json` со схемой, принадлежащей инструментам сида: ростер v2 с явными группами, три эквивалента с точностью, линии v2 (523 и 316) с лимитами десятичной строкой и политиками; структурная логика v2 извлечена из `admin-fixtures/tools/` до удаления генераторов; сценарии симулятора генерируются из описания плюс собственных поведенческих настроек — `scripts/generate_simulator_seed_scenarios.py` **переписывается, а не создаётся**: он существует (622 строки) и уже собирает оба сценария `*-realistic-v2` из v2-модулей, поэтому приёмка переписывания — **побайтовое совпадение** закоммиченных `fixtures/simulator/*/scenario.json` до и после; второй ростер не ведётся; исправлены два примера циклов в `docs/ru/seeds/seed-riverside-town-50.md` (до правки блок `:95-130`; якоря сместились, искать по заголовку `## Clearing Cycles Examples`); `docs/ru/seeds/README.md` (разделы о генерации фикстур) переписан под описание и рецепт | `[!]` |
| `T1713` | **Стадия 1, до `T1711`.** Рецепт на сообщество — закоммиченный, написанный руками: порядка 30–50 команд с символическими концами, суммой, идентификатором, экономическим объяснением и ожидаемым исходом на команду; `max_hops=1` для команд, строящих «горлышко» и циклы; один исполненный клиринг; выживший клирящийся цикл строится после последней команды клиринга; настоящая заморозка участника там, где она нужна демонстрации; валидатор ссылок рецепта на описание | `[!]` |
| `T1710` | **Стадия 1.** Лаунчер полного стека на Postgres в `run_full_stack.ps1` / `run_local.ps1`: общий вход миграций, сид рецептом (`T1711`), оба UI в real-режиме; сброс только базы лаунчера `geov0_dev_<slug>` после остановки процессов; проверка готовности (схема, популяция, baseline) с отказом на незавершённой инициализации; токен — каждому клиенту его переменной; `verify_admin_phase4_real_contract.ps1` на одноразовой Postgres-базе; runbook переносного Postgres обновлён; `check-db` и фаза анализа рана перенесены на Postgres | `[!]` |
| `T1701` | **Стадия 1.** Bootstrap `alembic_version` переезжает во вход миграций (`migrations/env.py`), его зовут entrypoint, тесты и лаунчер. Провизионирование в `tests/migrated_schema.py`: bootstrap → `upgrade head` → шаблон → клон на задачу/тест, очистка, требование `CREATEDB`; **в том же срезе `pytest.skip` при отсутствии `CREATEDB` превращается в жёсткий отказ** в четырёх модулях, которые уже сегодня честно пишут «это отсутствующее измерение, а не пройденное» (`test_p015_step5a_reconciliation_postgres.py:206`, `test_p015_step5b_criterion_b_postgres.py:168`, `test_p015_step5c_hold_races_postgres.py:633`, `test_p015_t1530_delta_arithmetic_postgres.py:264-269`) — право становится предусловием тира именно здесь, с появлением провизионирования шаблона, и оставить skip до стадии 2 значило бы окно ложного зелёного из §9; `.github/workflows/quality.yml`: обязательный backend-гейт на ubuntu с сервисом Postgres, `simulator-super-smoke` с сервисом и URL, job `postgres` упразднён. **`required-quality` разделяется на backend-job (ubuntu, сервис Postgres, проверка единственной головы Alembic) и UI-job** (переключатель `-UiOnly` или вынос UI-блока `verify_local.ps1:142-167`). `tests/unit/test_quality_workflow_schedule.py` **приобретает** утверждения о новом обязательном backend-job'е: сегодня он о `required-quality` не утверждает ничего — пиннит только `container-smoke` (`:9-29`) и порядок шагов `simulator-visual-e2e` (`:32-45`), так что это новая работа, а не починка | `[!]` |
| `T1702` | **Стадия 2.** `tests/conftest.py`: режимы A (savepoint) и B (одноразовая база, корневые коммиты); `TEST_DATABASE_URL` обязателен; `scratch_db.py` без SQLite; удаление маркера `postgres` из `pytest.ini`, `verify_local.ps1`, файлов | `[!]` |
| `T1703` | **Стадия 3.** Удаление 50 dialect-веток в 8 модулях `app/` с сохранением Postgres-ветки дословно и `sqlite`-упоминаний в исполняемом коде остальных 9; удаление `sqlite_transaction_control.py`, проб `main.py:323-500`, трёх скриптов, ветки анализатора, `aiosqlite` | `[!]` |
| `T1704` | `app/config.py`: `DATABASE_URL` обязателен и только `postgresql+asyncpg`; `app/db/session.py` без dialect-веток; `scripts/{run_local,run_full_stack,verify_local}.ps1`: URL обязателен, два URL со стороны хоста, подсказка на `docker compose up db`; `README.md` onboarding и судьба существующих `.local-run/*.db` | `[!]` |
| `T1705` | Удаление тестов корзины D-SQLite и пяти pragma-тестов; перенос ⚑-ассертов (`test_an_aborted_payment_commit_leaves_debts_unchanged`, `test_a_stale_writer_cannot_overwrite_the_committed_debt_amount`, `test_a_real_busy_snapshot_replays_the_money_phase_and_commits_once`) в корзину A; ассерт `t1544_inject_refuses…` в Postgres-форме; решение по контракту `create_all` в `t1530` | `[!]` |
| `T1706` | **До стадии 2:** per-file инвентарь keep / delete / transfer с режимом фикстуры A или B; **после стадии 2:** замер времени тира (холодный и тёплый локально, CI) и бюджет, установленный консультацией Codex по замеру (не подпись владельца), — условие входа в стадию 3 | `[!]` |
| `T1707` | Гарды `test_p017_no_second_dialect.py` (AST-docstring, прямые обращения к диалекту, allow-list, положительные и отрицательные контрпроверки), `test_p017_default_tier_can_see_the_lock.py`, `tests/unit/test_p017_required_gate_runs_on_postgres.py` (последний сделан в срезе стадии 1) | `[!]` |
| `T1708` | Внешнее ревью §15 на точном HEAD каждой стадии; evidence с командами и exit code | `[!]` |

Легенда: `[x]` выполнено, `[ ]` в работе, `[!]` заблокировано или не авторизовано.

## Changelog

**2026-09-21 — заведена как черновик** по решению владельца о рефакторинге ядра. Факты собраны тремя read-only обходами и адверсарной перепроверкой (см. `specs/README.md`, раздел «Рефакторинг ядра»). Реализация не авторизована; каждая стадия вычитывается владельцем отдельно.

**2026-09-21, позже — внутренний adversarial-проход, четыре правки.** (1) Счёт «17 модулей» смешивал ветвления по диалекту (8 модулей, 50 вхождений) и слово `sqlite` (17 модулей, 109 вхождений) — разведены, гард разделён на две части с allow-list для датированных измерений в docstring. (2) Пробы `main.py` — `:323-500`, около 178 строк, а не 90. (3) Оценка «450 тестов» помечена как оценка. (4) Якоря 015 переставлены после сдвига на 22 строки коммитом `bf5c620`.

**2026-09-21 — снято противоречие с протоколом решений владельца.** В четырёх местах спека отдавала бюджет времени тира «на подпись владельца», хотя владелец в тот же день постановил: технические решения — консультацией Codex, ему только продуктовые вопросы. Сессия-исполнитель, читая спеку, честно сообщила владельцу, что без него хода нет; владелец спросил, почему его спрашивают. Бюджет — технический порог; все четыре места переписаны.

**2026-09-21 — вторая находка сессии-исполнителя (geov0-63, задача `T1706`): verification plan обещал зелёным селектор, который по механизму зелёным остаться не может.** Перепроверено оркестратором по коду: `test_invariants.py` и `test_scenarios.py` из списка «обязаны остаться зелёными» исполняют настоящий клиринг на общей фикстуре, а клиринг на Postgres сессию, привязанную к соединению, отвергает (`clearing/service.py:1530-1539`) — в режиме A они краснеют безусловно. По §13 это дефект спеки, и он исправлен в спеке: у селекторов раздела 3 названы режимы; объём отказа назван — 11 файлов дефолтного тира, которых маркер `postgres` не показывает; четыре `pytest.skip` при отсутствии `CREATEDB` становятся жёстким отказом (в `T1701`, а не в `T1702`: право становится предусловием с появлением шаблона, и исполнитель указал на окно между стадиями); по замечанию исполнителя перепроверено основание режима B у `test_scenarios.py` — оно независимое (`:562`, `:709`, `conftest.py:425-449`), а инвентарь в этой строке ошибался; скан исполнителя по той же форме нашёл второй такой файл (`test_clearing_max_depth_controls_long_cycles.py`), итого файлов с отказом клиринга **тринадцать**, и предикат режима B переписан с «встречается `ClearingService`» на «достигается исполнение клиринга с непустым циклом на привязанной сессии» — истинное обоснование («токена в файле нет») несло ложный вывод; числа корзин первой инвентаризации заменены per-file инвентарём исполнителя (299 файлов, а не «около 304»).

**2026-09-21, по вопросу владельца «как сохранить сообщества на 50 и 100 участников в новом функционале» — четвёртая консультация Codex.** Проверка по коду: смысл сообщества живёт в замысле (`docs/ru/seeds/`), структуре и поведении, а выдуман только результат — долг как `лимит × ((n % 17) + 1) / 20`, транзакции без ролей. Оркестратор предложил «описание в формате сценария симулятора плюс книга заказов, выведенная из профилей, с детерминированными ключами и проверкой знаков балансов групп». Консультация (`gpt-6-astra` high, read-only, clone `d7ab2c9` в `.local-run/codex-review/2026-09-21-community-recipe/`, exit `0`, P1 = 0, P2 = 6, P3 = 2; все P2 воспроизведены оркестратором по коду) сохранение подтвердила, четыре решения заменила: нести вперёд топологии **v2** (оркестратор сравнил базовую сотню на 439 линий с v2-пятидесяткой; у сотни v2 — 523 линии); описание — в собственном малом формате у инструментов сида, сценарии симулятора генерируются из него; рецепт — закоммиченный и написанный руками, 30–50 команд с ожидаемым исходом на каждую, без генератора; ключи — на прогон, только в памяти; знаки балансов групп не утверждаются, потому что seed-документы их не обещают, а два примера циклов в документе Riverside противоречат сами себе и клирингу. Введены `T1712`, `T1713`, дополнена `T1711`.

**2026-09-21 — находка сессии-исполнителя (geov0-63) по форме `T1701`, перепроверена оркестратором по коду на `4310603`.** Спека говорила «backend-часть `required-quality` переезжает на ubuntu», как будто части уже разделены; `required-quality` — один шаг с одним вызовом `verify_local.ps1`, разделение живёт внутри скрипта, и проверка головы Alembic сидит в UI-блоке. Записано прямо: это разделение job'а на два. Там же: `main` не защищена, так что переименование job'а не блокер; гард workflow о `required-quality` сегодня не утверждает ничего и должен утверждения **приобрести**; маркер `postgres` несут 52 модуля и 206 тестовых функций, все в `tests/integration/`, только модульной формой (подсчёт исполнителя). Заодно снято противоречие внутри front matter: `migrations/` был исключён целиком, а `migrations/env.py` внесён в scope как владелец bootstrap — исключение сужено до `migrations/versions/`.

**2026-09-21, по вопросу владельца «проверять админку на реальных данных, а не тянуть прицеп с фикстурами» — третья консультация Codex.** Проверка по коду показала, что заготовленные наборы попадают в базу мимо домена и с выдуманной историей (долги из линий, транзакции отдельно). Оркестратор предложил сидить прогоном симулятора; консультация (`gpt-6-astra` high, read-only, clone `7483d1f` в `.local-run/codex-review/2026-09-21-admin-seed/`, exit `0`, P1 = 0, P2 = 5, P3 = 2) идею приняла, способ отвергла: прогон неповторяем и медленен (сид из времени, секунда на такт, 50 тактов прогрева) и делал бы проверку админки зависимой от симулятора. Принято: компактный рецепт операций через существующие подписанные доменные пути, пустой baseline до денег, сверка как приёмка, начало — вместе с лаунчером 017, без ожидания 021. Все пять P2 воспроизведены оркестратором по коду. Введена `T1711`.

**2026-09-21, по замечанию владельца о поверхностной проверке — полная инвентаризация потребителей SQLite и вторая консультация Codex.** Инвентаризация (все рантайм-умолчания, все скрипты, все job'ы CI, все конфиги Playwright, хранилище симулятора, тесты, документы, зависимости, данные на диске; несущие факты перепроверены оркестратором) показала то, чего не было в спеке: для развёртывания SQLite уже не используется, зато на нём стоят оба лаунчера с их сбросом базы, единственный e2e админки с настоящим бэкендом и пять утилит без Postgres-двойника; честный объём — около 97 файлов, а не перечисленные раньше полтора десятка. Owner surface дополнен, введена задача `T1710` — лаунчер полного стека на Postgres в стадии 1. Консультация (`ANSWER-1: ADOPT-WITH-CHANGES`) добавила два условия, оба внесены: граница сброса dev-базы задаётся с той же строгостью, что у тестовой (`validate_test_database_url.py:112-118`), и bootstrap `alembic_version` получает одного владельца — вход миграций — вместо третьей копии в лаунчере.

**2026-09-21, ещё позже — внешнее ревью черновика (Codex, см. раздел «Внешнее ревью»), три P2 воспроизведены оркестратором по коду и внесены, три P3 внесены.** P2-1: спека выдавала исторические потери `T1525` за текущие — они закрыты `session.py:61`; §19.2 п. 1 переписан двухчастно (живой дефект — покрытие), п. 4 получил две недостающие альтернативы, из которых «сначала обязательный Postgres-гейт» стала стадией 1; «Blocks» переформулирован с «невозможно» на «невозможно в выбранной форме». P2-2: единственная savepoint-фикстура несовместима с отказом клиринга на привязанной сессии (`clearing/service.py:1530-1539`) и с тестами, ждущими чужой коммит — введены два режима фикстур с инвентарём потребителей до авторизации. P2-3: `alembic upgrade head` требует bootstrap `alembic_version` (`migrated_schema.py:47-50`, `T1535`), шаблон не закрывает `T1541` сам по себе, неоценённые статьи цены названы, бюджет времени тира стал условием входа в стадию 3. P3: контракт `create_all` в `t1530` решается явно; job `simulator-super-smoke`, `requirements-dev.txt:5`, анализатор `:348`, два URL со стороны хоста, судьба `.local-run/*.db`; гард ловит прямые обращения к `dialect.name` и различает docstring по AST. Вспомогательные посылки `T1540`/`T1544` записаны устаревшими, `T1535` и остаточные риски `T1525` — сохранёнными.

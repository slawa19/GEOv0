# 011 — Канон описывает то, что отдаёт

- **Date:** 2026-08-21
- **Status:** **РЕАЛИЗАЦИЯ АВТОРИЗОВАНА** — владелец 2026-08-21 по всей волне 009–014
  (`specs/README.md`, коммит `4a995e9`); начата 2026-08-22 после закрытия 009 и 010. Прежняя строка
  «реализация не авторизована» относилась к более раннему решению того же дня и устарела — та же
  правка, что понадобилась программе 010.
  **Якоря спеки требуют сверки:** HEAD ушёл далеко, волны 009 и 010 закрыты и обе дописали сюда свои
  записи.
- **Status authority:** метка описательная; завершённость устанавливают success criteria и записанные
  evidence, а не поле статуса.
- **Owner surface:** `api/openapi.yaml`, декларации `responses=` в `app/api/v1/**`,
  `app/api/router.py`, `app/api/deps.py` (`_RATE_LIMIT_EXEMPT_PATHS`), `app/main.py` (корневые
  маршруты), `app/core/simulator/sse_broadcast.py` (сериализация события),
  `tests/contract/test_openapi_contract.py`. **Запрещено:** менять поведение эндпоинтов. Программа
  приводит **объявление** в соответствие с тем, что код уже отдаёт, — и наоборот, где отдаёт неверно.
- **Origin:** маршрут `008/tasks.md`, раздел «Маршрутизация класса „в спеку“ в программы 009–014»:
  строка 12 (`C-C2-010`), группы `O` и `Y`, `PAR-A1b-001`, `PAR-A2a-001`, долг `T716(а)` программы 007.
- **Почему это одна программа:** все шесть находок — про один разрыв: **`api/openapi.yaml` объявлен
  авторитетом №1 (§8 `AGENTS.md`), но не описывает того, что приложение реально отдаёт.** Чинить их
  врозь значит несколько раз открывать один файл; §«Пересечение по файлу» `specs/README.md` это
  запрещает.
- **Включающий шаг для 012 и 013.** Пока канон не описывает поля, фронтендовый декодер чинится
  вслепую, а контрактный гейт сверяет пути, но не содержимое. Цена этого уже уплачена трижды
  (`C-C1-002`, `C-C1-012`, `C-C2-006`).

## Problem

Три независимых счёта одной и той же поверхности не сходятся, и расхождение измерено, а не оценено:

| Счёт | Значение | Чем получено |
|---|---|---|
| Операций объявлено в каноне | **88** | разбор `api/openapi.yaml` парсером YAML |
| Операций в схеме, генерируемой FastAPI | **92** | `app.openapi()` |
| Операций **зарегистрировано и вызываемо** | **100** | обход `app.routes`, `APIRoute` |
| Из них скрыто из схемы | **8** | те же маршруты |

**Окружение среза зафиксировано, потому что два из четырёх чисел от него зависят.** Замер сделан при
`SIMULATOR_ACTIONS_ENABLE` **не установленной** и `METRICS_ENABLED` в значении по умолчанию (`true`,
`app/config.py:140`). Восемь маршрутов Interact Mode скрыты не литералом, а вызовом:
`include_in_schema=_actions_enabled()`, где `_actions_enabled()` читает переменную окружения
(`app/api/v1/simulator.py:357-358`). **При включённых actions генерируемая схема содержит 100
операций, а не 92**, и расхождение с каноном становится 100 против 88. Поправка внешнего ревью
2026-08-21: без фиксации окружения числа не воспроизводимы.

Восемь скрытых — не служебные. Это **денежные действия Interact Mode**:

```
POST /api/v1/simulator/runs/{run_id}/actions/trustline-create
POST /api/v1/simulator/runs/{run_id}/actions/trustline-update
POST /api/v1/simulator/runs/{run_id}/actions/trustline-close
POST /api/v1/simulator/runs/{run_id}/actions/payment-real
POST /api/v1/simulator/runs/{run_id}/actions/clearing-real
GET  /api/v1/simulator/runs/{run_id}/actions/participants-list
GET  /api/v1/simulator/runs/{run_id}/actions/trustlines-list
GET  /api/v1/simulator/runs/{run_id}/payment-targets
```

То есть операции, создающие платежи, клиринг и линии доверия, **не описаны авторитетом №1 и не
проверяются контрактным гейтом**. Одна из них — `trustline-create` — несёт подтверждённый P1
`F-009-4` программы 009: дефектный предикат `status != "closed"` и `commit()` без перехвата
`IntegrityError`. Дефект и невидимость сошлись на одном маршруте, и это не совпадение: маршрут,
которого нет в каноне, не имеет и контрактной проверки, способной его поймать.

Второй разрыв — внутри объявленного. Тринадцать 2xx-ответов описаны как непрозрачный объект
(`type: object` + `additionalProperties: true`, без `properties`), то есть авторитет не описывает
**ничего**. Тринадцать — **нижняя граница**: предикат верхнеуровневый, а непрозрачность бывает и
вложенной (см. `F-011-1`).

## Owner surface

Разрешено: править `api/openapi.yaml`, добавлять `responses=` в декларации маршрутов, вносить
корневые маршруты под общий гейт, исправлять сериализацию SSE-события, расширять контрактный тест.

Запрещено: менять поведение эндпоинтов и формы ответов, которые потребители уже читают. Если
объявление и поведение расходятся, программа обязана сперва установить, **которая сторона неверна**,
и только потом править. Прецедент: `R-EXT6-001`, где неверным оказался документ, а не схема.

Запрещено: трогать денежное ядро (`app/core/payments/`, `app/core/clearing/`, `app/core/recovery.py`)
— владелец 010 — и предикаты/обработку конфликтов на маршрутах Interact Mode — владелец 009.
011 описывает **коды и формы ответов**, включая новые коды, которые заведёт 009; сами коды
устанавливает 009 задачей T907.

## Findings

Проверено по коду 2026-08-21 на HEAD `de4dd08`.

**`F-011-1` — `C-C2-010`, P2. Канон не описывает содержимое ответа. Тринадцать операций, а не четыре.**
Записанная находка называла «четыре ответа как `type: object` + `additionalProperties: true`».
Разбор канона парсером даёт **13** 2xx-ответов, объявленных непрозрачным объектом:

`/admin/participants/stats`, `/admin/participants/{pid}/ban`, `/admin/participants/{pid}/unban`,
`/admin/participants/{pid}/metrics`, `/admin/audit-log`, `/admin/trustlines`,
`/admin/trustlines/bottlenecks`, `/admin/liquidity/summary`, `/admin/incidents`,
`/admin/clearing/cycles`, `/admin/graph/snapshot`, `/admin/graph/ego`, `/simulator/runs/active`
— все `GET`, кроме `ban`/`unban` (`POST`).

**Уточнение важно не размером, а составом:** среди них `/admin/trustlines`,
`/admin/liquidity/summary`, `/admin/clearing/cycles` и `/admin/participants/{pid}/metrics` — денежные
поверхности. Первоначальная формулировка «графовые схемы» описывала меньшую и менее рискованную часть
класса.

**13 — это верхний уровень, и класс им не исчерпывается.** Внешнее ревью 2026-08-21 предъявило форму,
которую предикат «верхнеуровневый `type: object` без `properties`» не ловит: `/admin/participants`
(`api/openapi.yaml:328-337`) объявляет `properties` и выглядит описанным, но элемент его массива —
`items: {type: object, additionalProperties: true}`, то есть содержимое каждой записи не описано.
Проверено. Следствие для работы: **гард обязан быть рекурсивным**, а число 13 — нижней границей.
Первым шагом `T1102` пересчитывает класс с рекурсивным предикатом, и только потом описывает.

**`F-011-2` — `C-A1a-001` / `T809`, P2. Знаменатель поверхности: 100, а не 88.**
Воспроизведено: `APIRoute` — 100, вызываемых операций — 100, генерируемая схема — 92, канон — 88,
скрытых из схемы — 8 (перечислены в `## Problem`, окружение зафиксировано там же). Расхождение 100 против 88 уже
называлось в 008 «четвёртым экземпляром метод-дефекта»; здесь оно получает владельца.

Скрытие восьми маршрутов — **решение, а не дефект по построению**: Interact Mode может сознательно
не публиковаться. Программа обязана установить, было ли оно принято, и либо задокументировать его с
датой и причиной, либо снять. Чего быть не должно — **необъявленной денежной операции без причины**.

**`F-011-3` — `PAR-A1b-001`, P2. `429` объявлен один раз при лимитере на всех роутерах.**
`api/openapi.yaml:2402` — единственное вхождение `'429'` во всём каноне. При этом
`app/api/router.py:10-21` подключает `Depends(deps.rate_limit)` ко **всем одиннадцати** HTTP-роутерам
(`auth`, `participants`, `trustlines`, `payments`, `balance`, `clearing`, `integrity`, `equivalents`,
`health`, `admin`, `simulator`); без гейта только `websocket`. Значит около 86 операций способны
вернуть статус, которого канон не объявляет.

**`F-011-4` — `T808-003`, P2. Корневые маршруты вне гейта, а предписанная правка инертна.**
Четыре маршрута объявлены прямо на приложении, минуя `api_router` и его `dependencies`:
`app/main.py:543` (`/metrics`), `:551` (`/health`), `:569` (`/healthz`), `:574` (`/health/db`).
`rate_limit` для них **не вызывается вовсе**.

Отсюда следствие, ради которого находка и заведена: внесение их путей в
`_RATE_LIMIT_EXEMPT_PATHS` (`app/api/deps.py:45-49`, сегодня ровно один путь) **не может ни на что
повлиять** — этот список читается внутри `rate_limit` (`deps.py:60`), до которого исполнение не
доходит. Предписанная триажем правка инертна по построению, и это надо было заметить до того, как её
предписали.

**`F-011-5` — `PAR-A2a-001`, P3. SSE-событие дополняется в обход собственного объявления.**
`app/core/simulator/sse_broadcast.py:616-622`: событие сериализуется
(`SimulatorTxUpdatedEvent(**evt_kwargs).model_dump(...)`), после чего в получившийся словарь
дописываются ключи `edge_patch` и `node_patch`, которых в модели нет. §8 объявляет формы SSE
защищённым контрактом. Сегодня потребители справляются — отсюда P3, — но валидация ответа против
модели их отбросит. Файл сам объявляет строгую политику сериализации (`:415`: «always
`model_dump(mode="json", by_alias=True)`»), и эти две строки её нарушают.

**`F-011-6` — `T716(а)` программы 007, P3. `503` объявлен в каноне и отсутствует в схеме.**
Канон объявляет `'503'` в пяти местах (`api/openapi.yaml:51`, `:108`, `:156`, `:1692`, `:1728`).
Маршруты, которые этот статус реально отдают, его не декларируют:
`app/api/v1/simulator.py:2585` (`/runs/{run_id}/metrics`) и `:2604` (`/runs/{run_id}/bottlenecks`) —
`@router.get(..., response_model=...)` без `responses={503: ...}`. Расхождение **сознательное и
залоченное** ратчетом `ERROR_RESPONSE_DRIFT_SHA256` (`tests/contract/test_openapi_contract.py:73`).

Контраст, доказывающий, что это упущение, а не стиль: корневой `/health` (`app/main.py:551-556`)
декларирует `responses={503: ...}` как надо.

Цена: клиент, сгенерированный из приложения, `503` не увидит — а именно этим статусом бэкенд
отвечает, когда честно сообщает «измерений нет» вместо синтетики. Весь блок честности данных 007
заканчивается за один слой до потребителя.

## Current / Intended / Optimal

**Current.** Авторитет №1 описывает 88 операций из 100 вызываемых; из описанных 13 ответов не
описаны по содержимому; статус лимитера объявлен однажды на ~86 операций; корневые маршруты вне
гейта, а предписанная им правка инертна; SSE-событие дополняется мимо своего объявления; `503`
объявлен в каноне и отсутствует в схеме.

**Intended.** Канон описывает всё, что приложение отдаёт: каждую вызываемую операцию, содержимое
каждого 2xx-ответа, каждый статус, который операция способна вернуть. Расхождение канона и схемы либо
отсутствует, либо объявлено датированным решением с причиной.

**Optimal.** Разрыв закрывается **сверху вниз, а не поштучно**: контрактный гейт получает проверки,
делающие новое расхождение невозможным — на непрозрачный 2xx, на маршрут вне канона, на статус,
который код отдаёт, а декларация нет. Иначе тринадцать станут четырнадцатью, и это уже происходило:
класс ловился четырежды разными заходами.

## Non-goals

- **Не менять поведение эндпоинтов.** Программа про объявление. Единственное исключение — `F-011-5`,
  где правка сериализации и есть приведение к объявлению.
- **Не описывать восемь скрытых маршрутов «потому что нашлись».** Сначала решение: публиковать или
  оставить скрытыми с датированной причиной.
- **Не трогать денежное ядро и предикаты Interact Mode** — 010 и 009.
- **Не снимать ратчет `ERROR_RESPONSE_DRIFT_SHA256`** ради зелёного гейта. Здесь нужна точность:
  ратчет **не измеряет уменьшение**. Он требует точного совпадения и количества записей, и SHA
  (`tests/contract/test_openapi_contract.py:315-329`), а у SHA нет отношения порядка. Более того,
  комментарий над ним говорит, что обе операции уже расходятся по другим причинам и `count` останется
  **84** (`:69-76`). Поэтому правило формулируется так: **доказать по normalized diff, что исчезла
  именно canonical-only запись про `503`, и лишь затем намеренно обновить digest.** Обновление digest
  без такого доказательства запрещено. Поправка внешнего ревью 2026-08-21.
- **Не вводить генерацию канона из кода.** Канон — авторитет по §8, а не производная. Смена
  направления генерации — архитектурное решение владельца, а не побочный эффект этой работы.

## Verification plan

**1. Репродьюсеры, обязанные падать на текущем коде.**

| ID | Что обязан показать | Гейт |
|---|---|---|
| `RT-011-1` | контрактный тест обнаруживает непрозрачный 2xx **рекурсивно**: верхний уровень, элементы массивов, вложенные объекты и цели `$ref`. Сегодня верхнеуровневых **13**, плюс как минимум один вложенный (`/admin/participants`, `api/openapi.yaml:332-337`). Гард обязан считать пустой `properties: {}` непрозрачностью — иначе он останется зелёным на подделке | контрактный |
| `RT-011-2` | контрактный тест сверяет **вызываемые** операции с каноном: сегодня 100 против 88, расхождение 12 (8 Interact Mode + 4 корневых) обязано быть предъявлено поимённо. Тест **обязан фиксировать окружение** (`SIMULATOR_ACTIONS_ENABLE`, `METRICS_ENABLED`) и падать при его изменении, а не молча менять ожидаемое число | контрактный |
| `RT-011-3` | операция под лимитером возвращает `429`, которого нет в её объявлении — доказывается на реальном ответе, а не чтением | дефолтный |
| `RT-011-4` | корневой маршрут **проходит через общий policy-гейт**: сегодня `rate_limit` для него не вызывается ни разу, и тест это фиксирует как красный. Формулировка исправлена по внешнему ревью: прежняя («внесение пути в `_RATE_LIMIT_EXEMPT_PATHS` ничего не меняет») описывала тест, обязанный быть **зелёным** на текущем коде, то есть не репродьюсер. Инертность exempt-списка доказывается отдельной **контрпроверкой**, а не репродьюсером | дефолтный |
| `RT-011-5` | **ПЕРЕПИСАН 2026-08-22 — прежняя формулировка была зелёным репродьюсером.** Было: «валидация отбрасывает `edge_patch`/`node_patch`». Проверено исполнением: модель объявлена `extra="allow"` (`app/schemas/simulator.py:170`), конструктор их принимает, `model_dump(by_alias=True)` сохраняет — то есть тест был бы зелёным на текущем коде, ровно тот дефект, из-за которого уже правился `RT-011-4`. **Разрыв идёт в обратную сторону:** канон объявляет оба поля типизированно (`api/openapi.yaml:4215-4224`, `$ref` на `SimulatorGraphNodePatch`/`SimulatorGraphLinkPatch`), а отстаёт **pydantic-модель**. Репродьюсер обязан показывать, что клиент, сгенерированный из схемы приложения, этих полей не знает | дефолтный |
| `RT-011-6` | клиент, сгенерированный из схемы приложения, не знает о `503` для `/runs/{run_id}/metrics` и `/bottlenecks` | контрактный |

**2. Инварианты, обязанные выжить, и контрпроверки.**

- Ни одна вызываемая операция не отсутствует в каноне без датированного решения — контрпроверка:
  добавление нового маршрута без записи в канон обязано ронять `RT-011-2`.
- Ни один 2xx не объявлен непрозрачным объектом — контрпроверка: возврат любого из тринадцати к
  прежней форме обязан ронять `RT-011-1`.
- Существующие потребители продолжают читать те же поля: правка канона **не переименовывает** ничего.
  Контрпроверка — существующие декодеры фронта остаются зелёными без правок.
- Ратчет `ERROR_RESPONSE_DRIFT_SHA256` только уменьшается — контрпроверка: попытка зафиксировать им
  **новое** расхождение обязана падать.

  **Уточнено 2026-08-23 по внешнему ревью (`VERDICT-RATCHET: COVERAGE_GROWTH`), потому что буквальная
  формулировка запрещала бы саму `T1101`.** Монотонность действует **при неизменном наборе
  сравниваемых операций**. Ратчет обходит только операции, уже объявленные в каноне
  (`tests/contract/test_openapi_contract.py:204-208`), — именно поэтому денежная поверхность могла
  расходиться неизмеряемой. Расширение знаменателя допускает рост **только** при поимённом
  доказательстве, что (а) ни одна старая запись не ухудшилась и (б) каждая новая точно воспроизводит
  ранее измеренный класс. Для `T1101` это доказано попарно: восемь операций,
  `TRANSPORT_HEADER 59 → 67`, `SECURITY 59 → 67`, `ERROR_RESPONSE 83 → 91`, при неизменных
  `SUCCESS 71`, `REQUEST 13`, `PARAMETER 22`. **Это не разрешение будущих повышений.**

**3. Существующие селекторы, обязанные остаться зелёными.** Поимённо:

- `tests/contract/test_openapi_contract.py` — основной гейт программы
- `tests/integration/test_integrity_endpoints.py`
- `tests/integration/test_simulator_sse_smoke.py`
- `tests/integration/test_simulator_artifacts_events_ndjson.py`
- `tests/unit/test_interact_actions_backend_p1.py` — восемь скрытых маршрутов
- `npm --prefix simulator-ui/v2 run test:unit` и `npm --prefix admin-ui run test` — потребители канона; команды названы целиком, а не «unit-набор»

Тиры целиком: дефолтный backend; (замерено 2026-08-21).

**4. Запрещённые способы доказательства.**

- **Запрещено** засчитывать «канон и схема совпали» как доказательство описанности: обе могут не
  описывать содержимого. `RT-011-1` проверяет **наличие `properties`**, а не совпадение документов.
- **Запрещено** доказывать `F-011-3` чтением `router.py`. Нужен реальный `429` от реальной операции:
  §9 anti-vacuum, и ровно на этом классе 008 уже ошибалась.
- **Запрещено** закрывать `F-011-4` внесением путей в `_RATE_LIMIT_EXEMPT_PATHS` — это и есть
  инертная правка, которую находка описывает. Требуется `RT-011-4`, доказывающий инертность, **до**
  выбора настоящего решения.
- **Запрещено** приводить схему к канону подавлением: `include_in_schema=True` без решения о
  публикации маршрута меняет публичную поверхность и является продуктовым выбором.

### `F-011-N` — `POST /trustlines` не объявляет 409. Внесена 2026-08-22 программой 009.

`api/openapi.yaml:2131-2150` перечисляет для этого маршрута только `201`, `400` и `401`, тогда как
`TrustLineService.create` поднимает `ConflictException` (HTTP 409) на трёх сайтах:
предварительная проверка живой линии, конфликт уникального индекса на `flush()` и он же на `commit()`.
Расхождение **не внесено волной 009** — 409 существовал и до неё, — но волна добавила два новых его
источника, поэтому запись делается сейчас, а не задним числом.

**Уточнено 2026-08-22: маршрутов три, а не один, и форм у 409 две.** Запись 009 про отказ
`TRUSTLINE_CLOSED` адресована `TRUST_LINE_UPDATE` и `TRUST_LINE_CLOSE`, то есть
`PATCH /trustlines/{id}` (`api/openapi.yaml:2169-2190`) и `DELETE /trustlines/{id}` (`:2192-2219`) —
оба объявляют только `200/400/401/404` и 409 тоже не объявляют, хотя теперь его честно отдают. Фраза
«правка — одна строка» относилась к одному месту из трёх. Плюс у самого 409 **три** формы: предпроверка
(`app/core/trustlines/service.py:182`) поднимает `ConflictException` **без** `details`, а конфликтные
сайты `create` (`:214`, `:266`) — с `details.reason`, а `update` (`:293`) и `close` (`:420`) несут
**ещё и `trustline_id`**. Канон обязан описать все три, иначе `details` окажется объявлен обязательным
там, где его нет, либо неполным там, где он есть.

Ровно предмет этой программы: канон описывает не то, что маршрут возвращает. Шаблон `$ref` на 409
в спеке уже применён в пяти других местах (`api/openapi.yaml:599`, `:1363`, `:1578`, `:1622`, `:2372`),
поэтому правка — одна строка, и её цена целиком в том, чтобы не забыть контрактный гейт.

Сопутствующие объявленные исходы, добавленные волной 009 и подлежащие описанию здесь же: новое
**штатное успешное** состояние повторного `TRUST_LINE_CREATE` после закрытия (новая линия с новым
`id`, не конфликт); отказ `TRUSTLINE_CLOSED` на `TRUST_LINE_UPDATE`/`TRUST_LINE_CLOSE`;
`CONCURRENT_TRUSTLINE_CREATE` как `details.reason` при гонке.

### `F-011-N2` — `RUN_PERIMETER_UNAVAILABLE`. Внесена 2026-08-22 программой 010.

Новый объявленный отказ на **пяти** маршрутах Interact Mode: `trustline-create`,
`trustline-update`, `trustline-close`, `payment-real` и `clearing-real` — HTTP 503, код
`RUN_PERIMETER_UNAVAILABLE`.

**Исправлено 2026-08-22 по внешнему ревью:** первая редакция этой записи называла только два
маршрута из пяти, потому что писалась, когда проверка стояла на двух. Это ровно тот дефект, который
программа 011 и каталогизирует, — передача в канон, описывающая не то, что маршруты возвращают.

**Задача этой записи — `T1107`, а не `T1105`.** Ремедиационное ревью предположило, что объявить новый
503 должна `T1105`; это не так — `T1105` относится к `F-011-6` (`/runs/{id}/metrics` и
`/runs/{id}/bottlenecks`, наследство долга `T716(а)` программы 007), и общего у них только число 503.
Существенная часть замечания при этом верна: **задачи под эту запись не было вообще**, и она заведена
в `T1107`. Появился при закрытии `F-010-3` и заводится здесь, потому что
объявление кода отказа — поверхность этой программы.

Почему он понадобился: периметр рана вычисляется fail-closed, и «ран пуст» неотличимо от «снапшот
не строится» — обе ситуации дают пустое множество. Для авторизации это верно, для **отчёта** нет:
маршрут отвечал `200` с нулём очищенных циклов, то есть неудавшееся измерение прав выглядело как
выполненная пустая работа. Отдельный код отличает «не смогли установить периметр» от «в периметре
нечего делать».

### Сверка спеки с HEAD, 2026-08-22 — механика ратчета и восемь поправок

Спека писалась 2026-08-21; HEAD с тех пор ушёл, волны 009 и 010 закрыты и обе дописали сюда записи.
Сверка проведена отдельным проходом, **оба ратчета воспроизведены исполнением, а не чтением**:
`ERROR_RESPONSE_DRIFT` — 84 записи, digest совпал с записанным; `SUCCESS_SCHEMA_DRIFT` — 71, digest
совпал. То есть модель ратчета, на которой строится вся программа, проверена, а не предположена.

**Механика ратчета — то, чего в спеке не было и без чего программа пошла бы не туда.**

`ERROR_RESPONSE_DRIFT_SHA256` хеширует **не файл и не список кодов**, а словарь расхождений: ключ —
`"МЕТОД канонический_путь"`, значение — пара нормализованных наборов ответов (канон / генерируемая
схема), отфильтрованных до не-2xx. Нормализация оставляет **только `content`**, разыменовывает `$ref`
и выбрасывает `description`/`example`/`title`. Запись попадает в словарь, **только если стороны
различаются**. Порядка на digest нет: «расхождение уменьшилось» — это не зелёный исход, а другой
digest.

Отсюда порядок шагов для любой правки объявлений, и нарушать его нельзя:
1. снять нормализованный diff **до** правки;
2. править код и/или канон так, чтобы схемы **после нормализации совпали** — иначе статус не исчезнет
   из расхождения, а лишь переедет из «canonical-only» в «одинаковый статус, разная схема»: digest
   сменится, а долг останется;
3. убедиться, что множества 2xx не тронуты — этот assert **жёсткий**, не ратчетный;
4. пересчитать diff и предъявить дельту поимённо;
5. только теперь обновить константу, с комментарием в формате прецедента (дата, задача, что именно
   исчезло, почему `count` мог не измениться).

**Восемь поправок, внесённых по сверке:**

1. **`F-011-5` перевёрнута** — см. таблицу репродьюсеров: канон объявляет поля, отстаёт модель, а
   прежний `RT-011-5` был бы зелёным.
2. **`F-011-6` — три маршрута**, добавлен `GET /health/db` с требованием править его вместе с корневым
   близнецом.
3. **`T1103` расформирована**: в ней были склеены описание (`429` в объявлениях) и **изменение
   поведения** (корневые маршруты под лимитер). Второе запрещено собственными `## Non-goals` этой
   спеки и вынесено владельцу.
4. **`T1107` — четыре кода 503**, а не один.
5. **`F-011-N` — три маршрута и две формы 409.**
6. **`F-011-1` — класс шире заявленного.** Рекурсивный предикат даёт **34 или 36** операций против «13 и как
   минимум одна» — разница в том, заходит ли предикат внутрь `additionalProperties: {схема}`: без захода 34, с заходом 36 (добавляются `GET /integrity/status` и `POST /integrity/verify`). Предикат `RT-011-1` обязан этот выбор **объявить**, иначе класс не воспроизводим; формулировка «нижняя граница» была верна, но масштаб работы другой. Все 13 уже сидят
   в `SUCCESS_SCHEMA_DRIFT`, поэтому их описание сдвинет и digest, и, возможно, `count`.
7. **Стоимость `T1103a` по ратчету измерена:** `count 84 → 88` и новый digest. Это перезапись ратчета,
   а не инкремент, и спека обязана это называть до начала работы.
8. **Сместившиеся якоря обновлены** (`simulator.py:357-358` → `:362-363`; `:2585` → `:2758`;
   `:2604` → `:2777`; `admin.py:817-851` → `:840-874`, `:894` → `:919`, `:911` → `:936`,
   `:901-905` → `:929`).

**Отдельный риск для гейтов, найденный сверкой.** `include_in_schema=_actions_enabled()` вычисляется
на **импорте модуля**, поэтому `monkeypatch.setenv` на схему не влияет. Если процесс стартует с
`SIMULATOR_ACTIONS_ENABLE=1` — а `scripts/run_full_stack.ps1` именно это и делает — контрактный тест
`assert generated_versioned_paths == canonical_paths` **падает уже сегодня**, до всяких правок:
проверено исполнением 2026-08-23 — при выключенном флаге 77 путей против 77 канонических (равенство
держится), при `SIMULATOR_ACTIONS_ENABLE=1` 85 против 77. Считать надо **ключи путей**: в операциях это
88 против 96, и смешивать эти два счёта нельзя. Это
одновременно доказательство необходимости фиксировать окружение в `RT-011-2` и риск для `T1108`/`T1109`.

## Tasks

Легенда: `[x]` выполнено · `[ ]` в работе · `[!]` заблокировано или не авторизовано.

| ID | Задача | Статус |
|---|---|---|
| T1100 | **ЗАКРЫТА 2026-08-23: существуют все шесть.** `RT-011-1` (`test_p011_success_responses_describe_their_content.py`, гард-список, укус проверен мутацией), `RT-011-2` (`test_p011_callable_surface_matches_canon.py`, красный до `T1101`, предъявлял восемь поимённо), `RT-011-3` (`test_p011_rate_limit_status_is_declared.py`, красный на 93 операциях + реальный `429` от реальной операции), `RT-011-4` (`test_p011_root_routes_bypass_the_policy_gate.py`, контрпроверка с anti-vacuum, укус проверен мутацией), `RT-011-5` (`test_p011_sse_event_declares_patches.py`, красный до `T1104`), `RT-011-6` (был). **Три из шести переформулированы против буквы спеки**, и каждый раз по одной причине: заявленная демонстрация была недостижима или зелена на текущем коде. Прежняя запись: **Из шести репродьюсеров существует один** — `RT-011-6` (`tests/contract/test_p011_declared_status_reachability.py`), и он уже гард, а не красный: добавлен тем же коммитом, что и правка `T1105`, отдельного красного состояния в истории нет. `RT-011-1`…`RT-011-5` **не написаны**, контрпроверок раздела «Инварианты» тоже нет. ~~`RT-011-1`…`RT-011-6` — красные на текущем коде; невоспроизводимое получает вердикт `REFUTED` с записью | `[x]` 2026-08-23 |
| T1101 | **`F-011-2`: РЕШЕНИЕ ПРИНЯТО 2026-08-23 — публиковать.** Вопрос вынесен владельцу; владелец делегировал его внешнему ревьюеру (Codex, `codex-cli 0.144.0-alpha.4`, `-m gpt-5.6-sol`, `--sandbox read-only`, exit `0`, маркер `VERDICT-DONE`), вердикт `VERDICT-T1101: PUBLISH`. Обоснование, перепроверенное оркестратором: сокрытие не даёт контроля доступа (гард независим, все восемь зарегистрированы всегда), непоследовательно относительно `tx-once`/`clearing-once` и **уже противоречит существующему потребителю** — `simulator-ui/v2` вызывает все восемь вручную (`simulator-ui/v2/src/api/simulatorApi.ts:246-371`), в `admin-ui` ссылок нет. Условие ревьюера, принятое как обязательное: **публиковать атомарно с полными отрицательными исходами**, иначе клиент получит методы без ветвей на `ACTIONS_DISABLED`, отказ доступа, конфликты, `429` и четыре разных `503`. То есть `T1101` и `T1107` — один слайс. **ВЫПОЛНЕНА 2026-08-23** вместе с `T1107` | `[x]` 2026-08-23 |
| T1102 | **Пересчёт выполнен и подтверждён 2026-08-23; описание содержимого — не начато.** Числа спеки (`34`/`36`) воспроизведены точно, и различие ровно то, которое спека назвала: заход внутрь `additionalProperties: {схема}` добавляет `GET /integrity/status` и `POST /integrity/verify`. **Предикат обязан объявить своё определение, и вот оно:** объект считается неописанным, когда у него нет `properties`, нет `allOf`/`oneOf`/`anyOf` **и** значения свободной формы (`additionalProperties: true` либо отсутствует). **Типизированная карта (`additionalProperties: {схема}`) неописанной не считается** — она заявляет тип значения. Это не мелочь определения: первая редакция предиката здесь ошиблась и дала `41` вместо `34`, посчитав неописанными семь операций, у которых карта типизирована (четыре графовых, две integrity и `clearing-real`). Публикация восьми маршрутов класс не расширила. Разбор причин: 37 операций несут действительно свободную форму. `F-011-1`: описать содержимое. Формы берутся из кода, а не придумываются. **Особый случай — `ban`/`unban`:** они отдают `{"pid", "status"}` через общий helper (`app/api/v1/admin.py:817-851`) без `response_model` (`:894`, `:911`), а существующий `AdminParticipantStatusResponse` допускает только `active|suspended` (`app/schemas/admin.py:57-61`), тогда как `ban` ставит `deleted` (`:901-905`). Переиспользовать его вслепую нельзя — нужна отдельная модель, **без изменения фактического ответа** | `[ ]` |
| T1103 | **РАСФОРМИРОВАНА 2026-08-22 — в ней были склеены описание и изменение поведения.** Часть «объявить `429`» осталась как `T1103a`; часть «корневые маршруты под общий гейт» вынесена в `T1103b` как решение владельца, а не работа | `[x]` расформирована |
| T1103a | **ВЫПОЛНЕНА 2026-08-23, и цена оказалась обратной предсказанной: ратчет не вырос, а упал.** `429` объявлен с **обеих** сторон — в каноне и в генерируемой схеме — ровно для тех **95** операций, которым лимитер может ответить. Набор выводится из таблицы маршрутов (наличие `deps.rate_limit` в дереве зависимостей минус `_RATE_LIMIT_EXEMPT_PATHS`), а не перечисляется списком, поэтому он не может разойтись с тем, как смонтированы роутеры. `/simulator/session/ensure` исключён — объявить ему `429` значило бы совершить ровно ту ошибку, которую программа каталогизирует. На стороне приложения правка сделана **в одном месте** — `_declare_rate_limit_status` в `app/main.py`, — а не в 95 декораторах. **Оценка спеки пересмотрена:** она предполагала объявление только в приложении и потому предсказывала рост (`83 → 88`, generated-only `429`). Объявление с обеих сторон делает строки совпадающими. `F-011-3`: объявить `429` там, где он достижим. **Стоимость по ратчету измерена и она не инкрементальная:** правка задевает все 88 канонических операций (у 87 появится generated-only `429`, у `POST /payments` исчезнет canonical-only), меняются все 84 записи расхождения плюс четыре ранее совпадавшие → `ERROR_RESPONSE_DRIFT_COUNT` **83 → 88** и полностью новый digest (база сдвинулась на 83 закрытием `T1105`; исходная оценка «84 → 88» устарела). Оценка «плюс правка `tests/contract/test_openapi_contract.py:809`» **пересмотрена**: тест (теперь `:825-836`) читает **только канон**, поэтому от объявления `429` в декларациях приложения он не упадёт — упадёт лишь если менять канон для `POST /payments` | `[x]` 2026-08-23 |
| T1103b | **РЕШЕНИЕ ПРИНЯТО 2026-08-23 — оставить как есть (`VERDICT-T1103b: KEEP_AS_IS`, тот же прогон).** Довод ревьюера, которого не было в спеке: лимитер держит **одну корзину на IP, а не на путь** (`app/api/deps.py:66`), поэтому подключение проб и scrape'а означает, что мониторинг сможет исчерпать бюджет пользовательских запросов и наоборот. `/health/db` действительно исполняет `SELECT 1` и abuse-cost имеет, но защищается отдельной ingress/network policy, а не общим клиентским гейтом. Работа 011 сводится к фиксации текущего поведения плюс контрпроверка инертности; ревьюер требует добавить **anti-vacuum контрпроверку**, что `rate_limit` всё ещё реально вызывается на версионированном маршруте, — иначе тест «зависимости нет» останется зелёным и после поломки лимитера. Прежняя запись: **`F-011-4` — НЕ РАБОТА 011, а решение владельца.** `RT-011-4` требует, чтобы `rate_limit` реально исполнялся на `/metrics`, `/health`, `/healthz`, `/health/db`, то есть чтобы они начали отдавать `429`. Это **изменение поведения эндпоинтов**, прямо запрещённое разделом `## Non-goals` этой же спеки, и оно операционно значимо: `/health` и `/healthz` — цели liveness/readiness-проб, `/metrics` — цель scrape'а Prometheus; лимитер по IP превратит частый опрос в отказ. Программа описывает поведение, а не меняет его, поэтому здесь фиксируется **текущее** поведение (корень вне лимитера) плюс инертность `_RATE_LIMIT_EXEMPT_PATHS` контрпроверкой. Вопрос «ставить ли корень под лимитер» выносится владельцу отдельно | `[x]` решено 2026-08-23: `KEEP_AS_IS` |
| T1104 | **`F-011-5` ЗАКРЫТА 2026-08-23.** Оба поля объявлены в `SimulatorTxUpdatedEvent` (`app/schemas/simulator.py:145-152`), эмиттер передаёт их конструктором и исключает из дампа, когда их нет (`app/core/simulator/sse_broadcast.py:616-629`), — приписывание ключей к готовому словарю убрано, политика `:415` больше не нарушается. Проволока доказана неизменной на пяти входах, включая пустой список. Репродьюсер `RT-011-5` красный до правки, зелёный после; **его формулировка исправлена — см. запись ниже**, потому что заявленная спекой демонстрация недостижима | `[x]` 2026-08-23 |
| T1105 | **`F-011-6` ЗАКРЫТА 2026-08-22.** Три маршрута объявили `503` в собственной схеме приложения: два симуляторных через `ErrorEnvelope` (форма канонического `ServiceUnavailable`) и `/health/db` — инлайновой формой `{status: error}`, вместе с корневым близнецом в `app/main.py` одним коммитом. Ратчет: `count 84 → 83`, digest пересчитан на **реальной** правке. Репродьюсер `RT-011-6` был красным; плюс пин на каждый из трёх маршрутов отдельно | `[x]` 2026-08-22 |
| T1106 | **Выполнена 2026-08-23.** Пять гардов, каждый проверен мутацией: `RT-011-1` (новый непрозрачный `2xx` невозможен), `RT-011-2` (вызываемая операция вне канона невозможна), `RT-011-4` (корень вне лимитера + anti-vacuum), `test_p011_nullable_needs_a_sibling_type` (`nullable` без соседнего `type` невозможен) и `test_p011_reachable_statuses_are_declared` — **общая форма**: достижимый статус обязан быть объявлен, выводится из таблицы маршрутов и графа зависимостей, `429` делегирован функции приложения. Оба списка гарда пусты: 136 пар канона и 127 пар генерируемой схемы закрыты. Гарды в контрактном гейте, делающие новое расхождение невозможным (`Optimal`) | `[x]` выполнена |
| T1107 | **ЗАБЛОКИРОВАНА `T1101`, и спека этого не называла:** объявить эти коды в `api/openapi.yaml` нельзя, не добавив туда сами восемь скрытых путей, а это решение `T1101`. Принять записи от 009 (`T907`) **и от 010**: новые коды ответов объявляются здесь. **Пересчитано 2026-08-22: на маршрутах Interact Mode четыре разных 503, а не один** — `RUN_PERIMETER_UNAVAILABLE` (5 маршрутов), `TRUSTLINE_USED_UNAVAILABLE` (3), `SEEDING_FAILED` (достижим на 7 из 8), `ENGINE_TIMEOUT` (1). Объявить один из четырёх значило бы воспроизвести внутри этой программы ровно тот дефект, который она каталогизирует. Конверт у всех — плоский `SimulatorActionError`, **не** `ErrorEnvelope`. От 010 — `RUN_PERIMETER_UNAVAILABLE`, HTTP 503, на **пяти** маршрутах Interact Mode: `trustline-create`, `trustline-update`, `trustline-close`, `payment-real`, `clearing-real`. **Не путать с `T1105`** — та про `F-011-6`, наследство долга 007 на `/runs/{id}/metrics` и `/runs/{id}/bottlenecks`; совпадает только слово «503». Внешнее ревью 2026-08-22 эти две записи спутало именно так, поэтому различие зафиксировано здесь. **ВЫПОЛНЕНА 2026-08-23** одним слайсом с `T1101`: все четыре кода названы **поимённо и по маршрутам** в описаниях `503` (`RUN_PERIMETER_UNAVAILABLE, TRUSTLINE_USED_UNAVAILABLE или SEEDING_FAILED` — три trustline-маршрута; `RUN_PERIMETER_UNAVAILABLE, SEEDING_FAILED или ENGINE_TIMEOUT` — `payment-real`; `RUN_PERIMETER_UNAVAILABLE или SEEDING_FAILED` — `clearing-real`; `SEEDING_FAILED` — `trustlines-list` и `payment-targets`; у `participants-list` `503` недостижим и не объявлен). Состав подтверждён обходом AST по восьми функциям-обработчикам с раскрытием хелперов, а не чтением | `[x]` 2026-08-23 |
| T1108 | **Выполнена 2026-08-23.** Внутренний adversarial review субагентом на замороженном `b6d2254..5416065`, read-only, ревьюер собственные находки не чинит. Десять находок, все подтверждены и закрыты. Две подрывали тезис волны: `F-011-10` была закрыта только в узкой синтаксической форме, и **двенадцать операций отдавали тела, которые канон отвергает**, при зелёном гейте; в репозитории **ничто не валидировало ответы против канона**. Оба закрыты. Прошлая передача считала задачу заблокированной окружением — блокировка снята. Внутренний adversarial review (§16.4 `AGENTS.md`) | `[x]` выполнена |
| T1109 | **Оба круга проведены; первый — 2026-08-23 на диапазоне `31a2a80..d4dbd54`** в замороженном credential-free клоне (`codex exec review --base review-base`, `codex-cli 0.144.0-alpha.4`, `-m gpt-5.6-sol`, `--sandbox read-only`, exit `0`). **Три P2, все подтверждены исполнением и исправлены** — см. запись ниже. **Круг 2 по fix-delta `d4dbd54..9f898ff` пройден и чист** (замороженный клон на `9f898ff`, exit `0`, «No actionable regressions were identified»). Встроенный режим `review` собственного маркера вердикта не печатает — таргет в нём закреплён механически, и завершённость судится по exit code и непустому `-o`-файлу. **Независимое внешнее ревью и публикация evidence на точном HEAD** — §15: программа меняет защищённый контракт из §8 (OpenAPI, SSE shapes) и механизм проверки (`tests/contract`) | `[ ]` |

### `T1105` — первая закрытая задача 011, и что она показала о механике

`F-011-6` закрыта: ни одна операция больше не объявляет `503` только в каноне. Три маршрута, а не два —
третий (`GET /health/db`) нашёлся при сверке и правился вместе с корневым близнецом, иначе падает тест
на равенство их нормализованных ответов.

**Измеренная дельта ратчета, а не предсказанная.** Сверка спеки заранее оценивала исход как «`count`
останется 84, digest такой-то» — оценка делалась на модели из двух маршрутов. Реальность: **`84 → 83`**
и другой digest. Причина ровно та, которую описывает механика: операция покидает словарь расхождений
только когда решается её **последнее** различие. У двух симуляторных маршрутов остались
canonical-only `400/401/404` против generated-only `422`, поэтому их записи уцелели; у `/health/db`
`503` был единственным различием, и запись исчезла.

Это подтверждает и правило порядка: **пересчитывать после правки, а не доверять предсказанному
digest**. Предсказание было добросовестным и всё равно неверным, потому что моделировало не тот
состав.

**Что осталось невыясненным и записано как таковое:** оба симуляторных маршрута продолжают расходиться
по `400/401/404` (канон объявляет, декларация молчит) и по `422` (FastAPI добавляет, канон молчит).
Это не предмет `T1105`, но это тот же класс, и он попадает в `T1103a`/`T1102`.

### `T1104` — закрыта, и почему `RT-011-5` пришлось переформулировать второй раз

`F-011-5` закрыта: `SimulatorTxUpdatedEvent` объявляет `node_patch` и `edge_patch`, эмиттер передаёт
их конструктором, и приписывание ключей к уже сериализованному словарю
(`sse_broadcast.py`, прежние строки `619-622`) убрано. Политика самого файла — «always
`model_dump(mode="json", by_alias=True)`» (`:415`) — больше не нарушается собственным кодом.

**Проволока не изменилась, и это проверено, а не заявлено.** Наивное объявление полей заставило бы
`model_dump` писать `"edge_patch": null` в каждое событие. Поэтому отсутствующие поля исключаются из
дампа (`exclude=`), и старый и новый пути сравнены на пяти входах — оба `None`, оба `[]`, только
`edge_patch`, только `node_patch`, оба заполнены — результат идентичен во всех пяти.

**Items оставлены `Dict[str, Any]` намеренно, и причина записана здесь, чтобы её не «исправили».**
Канон объявляет элементы типизированно (`$ref` на `SimulatorGraphNodePatch`/`SimulatorGraphLinkPatch`),
и соблазн повторить это в модели велик. Делать этого нельзя: типизированный элемент заставит
`model_dump` материализовать **каждый** необязательный ключ патча как явный `null` внутри каждого
элемента — то есть изменит проволоку внутри массива. Ровно тот же выбор уже сделан у соседа,
`SimulatorClearingDoneEvent` (`app/schemas/simulator.py:222-223`), и теперь он согласован.

**Демонстрация, заявленная спекой, недостижима — и это правка посылки, а не формулировки.**
`RT-011-5` требовал показать, что «клиент, сгенерированный из схемы приложения, этих полей не знает».
Проверено исполнением 2026-08-23: `app.openapi()["components"]["schemas"]` не содержит **ни одного**
из четырёх имён — ни `SimulatorTxUpdatedEvent`, ни `SimulatorClearingDoneEvent`, ни
`SimulatorGraphNodePatch`, ни `SimulatorGraphLinkPatch`, — тогда как канон объявляет все четыре.
Причина: эти модели нигде не служат `response_model`, SSE-маршруты возвращают `StreamingResponse`,
поэтому в генерируемую схему они не попадают **вовсе**. Следствие: сгенерированный клиент не знает не
двух полей, а всего семейства событий, и `T1104` этого не меняет и изменить не может. Тест,
написанный по букве спеки, был бы красным по причине, которую задача не чинит, — тот же дефект, из-за
которого уже переписывались `RT-011-4` и первая редакция `RT-011-5`.

Поэтому `RT-011-5` проверяет ту половину находки, которую `T1104` действительно закрывает: **на
проволоку не попадает ключ, которого модель не объявляет**. Тест
(`tests/unit/test_p011_sse_event_declares_patches.py`) гоняет **производственный эмиттер** через
перехватывающий транспорт, а не копию его логики. Красный до правки:
`AssertionError: tx.updated emits keys its model does not declare: ['edge_patch', 'node_patch']`,
exit `1`. Зелёный после. Второй тест файла — контрпроверка на неизменность проволоки, зелёная по обе
стороны правки, как контрпроверке и положено.

**Остаток долга, записанный как таковой.** Разрыв «канон объявляет формы SSE-событий, генерируемая
схема не знает о них вовсе» находкой 011 не покрыт и `T1104` не закрывается. Это тот же класс, что и
вся программа, но другого масштаба: он не про поле, а про то, что защищённый §8 контракт SSE
существует только в каноне. Кандидат в 012/013 либо в отдельную запись; здесь фиксируется, чтобы не
потеряться.

### `T1101` + `T1107` — восемь маршрутов опубликованы, и что это стоило

**Решение принято не оркестратором.** Развилка вынесена владельцу; владелец делегировал её внешнему
ревьюеру, вердикт `VERDICT-T1101: PUBLISH`. Довод, которого не было ни в спеке, ни в передаче:
`simulator-ui/v2` **уже вызывает все восемь маршрутов вручную**
(`simulator-ui/v2/src/api/simulatorApi.ts:246-371`), в `admin-ui` ссылок нет. То есть канон молчал о
контракте, от которого зависит собственный UI проекта. Плюс подтверждено исполнением, что сокрытие не
даёт контроля доступа: маршруты зарегистрированы при любом значении флага, гард отвечает `403
ACTIONS_DISABLED` независимо от `include_in_schema`, а без учётных данных раньше него срабатывает
`401` (`app/api/deps.py:256`).

**Что сделано.** Условие `include_in_schema=_actions_enabled()` снято (восемь сайтов), у каждого
маршрута появились `responses=`, восемь операций описаны в `api/openapi.yaml` **вручную, в стиле
документа** (3.0.3, `nullable:`, `$ref` на переиспользуемые компоненты). Счёт по файлу: `77 → 85`
путей, `112 → 130` схем, `5 → 10` переиспользуемых ответов. Флаг сохранил свою настоящую работу: исполнение по-прежнему отказывает.

**Первая попытка была неверной и выброшена.** Канон был сгенерирован выгрузкой из `app.openapi()` —
получился документ с `operationId`, `title:` и 3.1-формой `anyOf: [..., 'null']`, то есть производная
от кода. Это прямо запрещено `## Non-goals` («Не вводить генерацию канона из кода»). Переписано руками.

**Состав отказов взят обходом AST, а не чтением.** Для каждой из восьми функций собраны вызовы
`_action_error` с раскрытием хелперов на три уровня. Все четыре заявленных `503` подтвердились
поимённо: `RUN_PERIMETER_UNAVAILABLE` — 5 маршрутов, `TRUSTLINE_USED_UNAVAILABLE` — 3,
`SEEDING_FAILED` — 7 из 8, `ENGINE_TIMEOUT` — 1. Дополнительно вскрылось то, чего в спеке не было:
`500 CLEARING_FAILED` на `clearing-real` и `409` из трёх разных источников (`RUN_TERMINAL`,
`CONFLICT`, `PAYMENT_REJECTED`).

**Цена по ратчетам — измерена, а не предсказана, и её оказалось три, а не две.**

| Ратчет | Было → стало | Причина |
|---|---|---|
| `TRANSPORT_HEADER_DRIFT` | `59 → 67` | канон не объявляет `X-Admin-Token`/`X-Simulator-Owner` для восьми |
| `SECURITY_DRIFT` | `59 → 67` | `_normalized_security` включает те же заголовки |
| `ERROR_RESPONSE_DRIFT` | `83 → 91` | общие `ErrorEnvelope`/`SimulatorActionError` расходятся канон-vs-схема |
| `SUCCESS_SCHEMA_DRIFT` | `71` | не изменился |
| `REQUEST_SCHEMA_DRIFT` | `13` | не изменился |
| `PARAMETER_SCHEMA_DRIFT` | `22` | не изменился |

**`SECURITY_DRIFT` оркестратор пропустил, и способ, которым он пропустил, стоит записать.** Прогон
контрактного набора показывал два падения, и это было принято за полный список. Ассерт на security
стоит **после** ассерта на error-ответы в той же тест-функции, поэтому до него исполнение не доходило:
красный на одном ратчете скрывает состояние следующих. Пропуск нашло внешнее ревью; число перепроверено
прямым счётом теми же нормализаторами.

**Три ратчета выросли, и это разрешено не «потому что иначе не получалось».** Формулировка инварианта
уточнена выше по тексту: монотонность действует при неизменном наборе сравниваемых операций, а здесь
вырос знаменатель — до публикации ратчет не обходил эти восемь ключей вовсе
(`tests/contract/test_openapi_contract.py:204-208`). Каждая новая запись точно воспроизводит класс, уже
измеренный на старых операциях (`ErrorEnvelope` — у 8 прежних операций, `SimulatorActionError` — у 2).
Всё, что можно было привести к совпадению честно, приведено: `default: true` у `ok`, `minimum` у
`cleared_cycles`/`hops`, границы `max_depth` — поэтому `SUCCESS`, `REQUEST` и `PARAMETER` не сдвинулись.

**Чего сделать не удалось, и почему это не обход.** Объявить транспортные заголовки в каноне
невозможно без противоречия: гард `test_canonical_admin_headers_follow_production_policy`
(`tests/contract/test_openapi_contract.py:949-967`) требует `required: true` у всякого объявленного
`X-Admin-Token`, а на этих маршрутах он честно необязателен — один из трёх транспортов идентичности.
Попытка была сделана и откачена; подгонять гард под правку не стали.

**Остаток долга, названный явно.** Глобальное согласование `SimulatorActionError`/`ErrorEnvelope`
между каноном и схемой **снизило бы** `ERROR_RESPONSE_DRIFT` разом, но это отдельный слайс: он трогает
все операции и требует собственного измерения. Внешнее ревью это суждение подтвердило.

**Репродьюсер.** `RT-011-2` написан (`tests/contract/test_p011_callable_surface_matches_canon.py`) и
показан красным до правки: он предъявляет все восемь операций **поимённо**, как и требовала таблица.
Окружение зафиксировано отдельным тестом. Четыре корневых маршрута внесены в список именованных
исключений со ссылкой на решение `T1103b`, и список защищён от протухания: если корневые когда-нибудь
попадут в канон, тест потребует их оттуда убрать.

### `T1103a` — `429` объявлен, и ратчет вернулся туда, откуда начал

**Что сделано.** `429` объявлен с обеих сторон для **95** операций: в каноне — `$ref` на новый
`TooManyRequests`, в схеме приложения — постобработкой `_declare_rate_limit_status` (`app/main.py`),
которая берёт набор из таблицы маршрутов и уважает `_RATE_LIMIT_EXEMPT_PATHS`. Одно место вместо 95
декораторов, и набор невозможно рассинхронизировать с монтированием роутеров.

**Заодно исправлена неточность канона, а не только умолчание.** `ErrorEnvelope.error.details` и
`SimulatorActionError.details` объявлялись как `additionalProperties: true` **без** `nullable`. Модели
объявляют `Optional[Dict[str, Any]] = None` (`app/schemas/common.py:15`), то есть канон был неверен
насчёт nullability и избыточен насчёт `additionalProperties`, которое в OpenAPI и так по умолчанию
`true`. Приведено к тому, что код действительно отдаёт.

**Измеренная дельта.**

| Ратчет | Начало сессии | После публикации восьми | После `T1103a` |
|---|---|---|---|
| `ERROR_RESPONSE_DRIFT` | `83` | `91` | **`83`** |
| `TRANSPORT_HEADER_DRIFT` | `59` | `67` | `67` |
| `SECURITY_DRIFT` | `59` | `67` | `67` |
| `SUCCESS_SCHEMA_DRIFT` | `71` | `71` | `71` |
| `REQUEST_SCHEMA_DRIFT` | `13` | `13` | `13` |

То есть `ERROR_RESPONSE_DRIFT` вернулся к исходному числу, **описывая при этом на восемь операций
больше**, чем в начале. Рост от расширения знаменателя оплачен полностью — не зачётом, а
исправлением двух настоящих расхождений.

**Оценка спеки была неверной, и причина поучительна.** Спека предсказывала `83 → 88` и объясняла это
тем, что «у 87 появится **generated-only** `429`». Предсказание исходило из того, что `429` объявят
только в приложении. Но канон — авторитет №1, и вся программа про то, чтобы он описывал отдаваемое;
объявление с обеих сторон и корректнее по существу, и делает строки совпадающими вместо расходящихся.
Это второй случай в программе, когда предсказанная дельта ратчета не сошлась с измеренной (первый —
`T1105`), и оба раза по одной причине: моделировался не тот состав правки.

**Сломался один гард, и он сломался правдой.**
`test_root_health_and_versioned_api_are_explicitly_classified` требовал, чтобы нормализованные ответы
корневого и версионированного health-близнецов совпадали. Теперь они различаются ровно на `429`:
версионированный смонтирован под лимитером и этот статус отдать может, корневой объявлен через
`@app.get`, зависимостей роутера не наследует и отдать не может (`F-011-4`), а `T1103b` решила эту
асимметрию сохранить. Сравнение уточнено: `429` обязан присутствовать у версионированного, обязан
отсутствовать у корневого, всё остальное обязано совпадать по-прежнему. Гард не ослаблен — он стал
проверять на одно настоящее утверждение больше.

**Репродьюсер.** `RT-011-3` (`tests/contract/test_p011_rate_limit_status_is_declared.py`) до правки
показывал **93** операции, которые `429` отдать могут и не объявляют. Anti-vacuum соблюдён так, как
требует спека: отдельный тест **доводит реальную операцию до реального `429`** (лимит понижается до
трёх, счётчики чистятся до и после) и проверяет код `E009` на ответе, а не читает `router.py`.
Третий тест — контрпроверка, что единственное исключение из лимитера действительно единственное.

### `RT-011-4` — контрпроверка написана, и её укус проверен мутацией

Файл — `tests/contract/test_p011_root_routes_bypass_the_policy_gate.py`. Он **не репродьюсер**, и это
записано прямо в нём: первая редакция `RT-011-4` была зелёной на текущем коде, а после решения
`T1103b` («оставить как есть») красному варианту взяться неоткуда — фиксировать надо текущее поведение
и инертность, а не требовать изменения, которое решено не делать.

Три утверждения, по прескрипции внешнего ревью:

1. четыре корневых маршрута не проходят через `deps.rate_limit`;
2. внесение их путей в `_RATE_LIMIT_EXEMPT_PATHS` ничего не меняет — плюс отдельная проверка, что
   список вообще читается **внутри** `rate_limit`, потому что весь довод об инертности стоит на этом;
3. **anti-vacuum:** лимитер по-прежнему реально навешен на версионированный маршрут, и за ним стоит
   больше 50 маршрутов. Без этого первые два утверждения остались бы зелёными на приложении, из
   которого лимитер убрали целиком.

**Укус проверен, а не предположен.** В `_RATE_LIMIT_EXEMPT_PATHS` временно добавлен `/health`; тест
упал на нужном утверждении с нужным сообщением, мутация откачена. Зелёная контрпроверка, которую не
пробовали сломать, — это то, на чём волна уже обжигалась.

### `RT-011-1` — гард на непрозрачные 2xx, и почему он список, а не запрет

Файл — `tests/contract/test_p011_success_responses_describe_their_content.py`.

**Гард сделан ратчетом-списком, а не красным тестом, и это осознанный выбор.** Класс сегодня — 36
операций; тест, требующий их отсутствия, был бы красным до конца `T1102` и в наборе жить не может.
Поэтому он делает то, что `## Optimal` и просит: **новое расхождение невозможно**. Список может
только сокращаться, и второй тест падает на протухших записях — описал операцию, обязан вычеркнуть.

**Предикат объявляет своё определение — иначе класс невоспроизводим.** Объект неописан, когда нет
`properties`, нет композиции и значения свободной формы. Пустой `properties: {}` считается
неописанным (иначе гард зеленеет на подделке). Типизированная карта неописанной **не** считается, а
вот **внутрь** её значения предикат заходит — этот выбор и есть разница между 34 и 36, и добавляет он
ровно `GET /integrity/status` и `POST /integrity/verify`. Выбран более строгий вариант: карта
неописанных объектов не описывает ничего.

**Состав важнее числа.** Среди 36 — `POST /payments`, `GET /payments`, `POST /trustlines`,
`PATCH /trustlines/{id}`, `GET /admin/liquidity/summary`, `GET /admin/clearing/cycles`. То есть авторитет
№1 не описывает содержимое ответов денежных операций. Это то, ради чего `T1102` стоит делать.

**Укус проверен мутацией:** `SimulatorActionParticipantItem` временно лишён `properties`; тест упал,
предъявив `GET /simulator/runs/{run_id}/actions/participants-list`, — то есть нашёл непрозрачность
через `$ref` **внутри элемента массива**, ровно ту вложенную форму, из-за которой находка и требовала
рекурсии. Мутация откачена. Третий тест файла проверяет предикат на восьми синтетических формах,
включая обе стороны различия «типизированная карта против карты неописанных объектов».

### `T1109`, круг 1 — три P2 на только что опубликованном контракте

Прогон: замороженный credential-free клон на `d4dbd54`, диапазон `31a2a80..d4dbd54`,
`codex exec review --base review-base`, `codex-cli 0.144.0-alpha.4`, `-m gpt-5.6-sol`,
`--sandbox read-only`, exit `0`. Все три находки **воспроизведены оркестратором исполнением**, а не
приняты на слово, и все три касаются работы этой же сессии.

**1. `403` на восьми маршрутах имеет две формы, а объявлена была одна.** Гард внутри обработчика
отвечает плоским `SimulatorActionError`, но `require_simulator_actor` поднимает `ForbiddenException`
**до** обработчика — неверный `X-Admin-Token`, неактивный участник, непройденная CSRF-проверка, — и
глобальный обработчик заворачивает это в `ErrorEnvelope`. Проверено ответами: неверный админ-токен даёт
`{"error": {"code": "E006", …}}`, выключенный флаг — `{"code": "ACTIONS_DISABLED", …}`. Объявить одну
из двух форм значило бы совершить ровно ту ошибку, которую программа каталогизирует. Обе объявлены
через `oneOf`, одной константой `_ACTION_FORBIDDEN_RESPONSE`, чтобы восемь деклараций не разъехались.

**2. `clearing-real` не объявлял `400`, который отдаёт.** Обработчик `RequestValidationError`
(`app/main.py:503`) распознаёт пути с `/actions/` и возвращает **`400`** плоским
`SimulatorActionError`, а не `422`. Проверено: `max_depth: 99` даёт `400 INVALID_REQUEST`. Это был
единственный action-POST без `400`. **Уточнение, которого в находке не было:** у `payment-targets`
в пути нет `/actions/`, поэтому его валидация уходит в общую ветку и отдаёт `422 ErrorEnvelope` —
проверено, объявлено верно. Для `participants-list` и `trustlines-list` `400` не объявлен сознательно:
у них нет тела, а все параметры строковые, то есть ошибке валидации взяться неоткуда.

**3. Рёбра клиринга публиковались как произвольная строковая карта.** `SimulatorActionEdgeRef` —
модель с ровно двумя полями `from`/`to`, но `@model_serializer(mode="plain")` был аннотирован
`Dict[str, str]`, и pydantic вывел схему сериализации **из аннотации**, потеряв оба ключа. То есть
защищённые §8 wire-ключи не были описаны ни в схеме приложения, ни в каноне, куда это и переехало.
Исправлено переопределением `__get_pydantic_json_schema__`. **Две попытки до этого отвергнуты
измерением:** `serialize_by_alias=True` ломает проволоку (`model_dump()` начинает отдавать `from_`),
а `TypedDict` в аннотации возвращает верную схему, но протаскивает в публичный контракт
служебный компонент `_SimulatorActionEdgeRefWire`. Итог проверен: проволока
`{"from": …, "to": …}` не изменилась, схема точна, служебных имён в контракте нет.

**Ратчеты не сдвинулись ни на единицу.** Все три правки внесены синхронно в приложение и канон,
поэтому строки совпадают: `ERROR 83`, `SUCCESS 71`, `SECURITY 67`, `TRANSPORT 67`, `REQUEST 13`,
контрактный набор `39 passed`.

**Своя находка, придержанная до конца ревью, чтобы не двигать цель под ревьюером.**
`_declare_rate_limit_status` присваивал `429` через `setdefault("responses", {})["429"] = …`, то есть
затёр бы объявление уровня маршрута. Сегодня таких нет — дефект латентный, — но исправлено на
`setdefault` по статусу.

### `T1102` пересобрана 2026-08-23 — «36 неописанных тел» оказались не тем, чем их считали

Инвентаризация роздана четырём агентам по роутерам; **двое независимо опровергли посылку задания**,
и оркестратор перепроверил это сам. Правильная картина:

| Что | Сколько | Что делать |
|---|---|---|
| тело **целиком** не описано (`schema` — голый объект) | **13** | описать форму по коду |
| тело описано, непрозрачен **вложенный узел** | **23** | описать узел — либо честно объявить свободную форму |

Двадцать три вложенных случая сводятся не к 23 работам, а к горстке **общих схем**:
`ParticipantProfile.contacts` (6 операций), `TrustLine.policy` (4), `Equivalent.metadata` (4),
`PaymentError.details` (3), `InvariantResult.details` (2), `AdminAuditLogItem.before_state/after_state`,
`IntegrityAuditLogItem.after_state`, `IntegrityChecksumResponse.invariants_status`.

**Отсюда следует то, чего находка не предполагала: часть класса — не дефект.** `details` у ошибок,
`before_state`/`after_state` у аудита, `metadata` у эквивалентов — это содержимое, которое по
построению открыто. Описать их выдуманной формой значит заставить канон врать, а это хуже, чем
не описать: спека прямо требует брать формы **из кода, а не придумывать**. Правильный исход для
таких узлов — **объявленная** свободная форма с записанной причиной, а не молчаливая. Классификация
«стабильная / перечислимая / свободная» вынесена отдельным заданием.

**Что уже установлено по коду и подтверждено оркестратором:**

- `POST /admin/participants/{pid}/ban` и `unban` отдают ровно `{"pid", "status"}`
  (`app/api/v1/admin.py:859`, возврат без изменений `:876`); `ban` всегда `deleted` (`:929`),
  `unban` всегда `active` (`:946`). Оговорка `T1102` про непригодность
  `AdminParticipantStatusResponse` подтверждена: он допускает только `active|suspended`.
  Общая с `freeze`/`unfreeze` форма покрывается enum `active|suspended|deleted`.
- Деньги на проволоке — строки везде в проверенных срезах, но у `TrustLine` это следствие
  **поведения pydantic** (`Decimal` → строка в json-режиме), а не явной аннотации `str`, как в
  `payment.py`. Хрупкое место: `ConfigDict(ser_json_decimal=...)` или ручная сериализация молча
  сломают контракт. Записано как риск.
- `/admin/graph/snapshot` и `/ego` объявляют `incidents`, `audit_log`, `transactions` как
  `list[Any]`, поэтому у элементов **нет схемы вовсе**; наполняются они только при
  соответствующем токене в query-параметре `include`.

### `F-011-7` — `GET /simulator/events/poll` объявлен как поток событий, а возвращает пустой массив

Внесена 2026-08-23. Обработчик — `app/api/v1/simulator.py:2560-2566` — состоит из комментария
`# MVP: no replay buffer.` и `return []`. Ни `equivalent`, ни `after` в теле не читаются.
Канон при этом обещает `array` из `SimulatorEvent` — `oneOf` по шести типам событий
(`api/openapi.yaml:1150-1158`).

Это тот же разрыв, что и вся программа, но в **обратную** сторону: не «канон молчит о том, что код
отдаёт», а «канон описывает то, чего код не отдаёт никогда». Класс тот же — авторитет №1 не
соответствует поведению, — и цена та же: клиент пишет разбор шести вариантов события под эндпоинт,
который всегда пуст.

**Это не работа `T1102` и не описание формы.** Выбор между «описать пустоту» и «реализовать буфер
повтора» — продуктовый, и по §5 он выносится владельцу. Здесь фиксируется находка, а не решение.
`RT-011-1` держит операцию в списке до тех пор, пока решение не принято.

### `T1102`, слайс 1 — дыра в собственном гарде, пилот и классификация свободных форм

**Сначала закрыта дыра в гарде, который я же и поставил.** Внешнее ревью нашло anti-vacuum дефект:
предикат `RT-011-1` открывал объектную ветку только при наличии `type`/`properties`/
`additionalProperties`, поэтому **пустую схему `{}` он не видел вовсе**, а `_measure` молча
пропускал ответ без `content` и без ключа `schema`. То есть гард был слеп ровно на самом пустом
случае. Проверено: `is_undescribed({})` возвращал `False`, и **`GET /admin/health/db` уже сидел в
этой дыре**, объявляя `schema: {}` и проходя проверку. Закрыто; список вырос `34 → 35` — **список
вырос оттого, что гард стал зорче, а не канон хуже**, и это записано в самом файле.

**Пилот на `ban`/`unban` — и механика, которую первая попытка поняла неверно.** Описание **одного
канона запись из дрейфа не убирает**: обе операции уже были в `SUCCESS_SCHEMA_DRIFT` и остались в
нём с другой канонической половиной, потому что у обработчиков нет `response_model` и генерируемая
сторона пуста. Запись уходит, только когда **обе** стороны заявляют одну форму. Поэтому приложение
объявляет форму тоже — через `responses={200: {"model": …}}`, **никогда** через `response_model=`.
Итог: `SUCCESS_SCHEMA_DRIFT 71 → 69`, список `35 → 33`.

**Различие двух написаний — несущее, и оно проверено, а не принято на веру.** `responses=` только
документирует; `response_model=` заставляет FastAPI **фильтровать** ответ до объявленных полей, то
есть молча менять проволоку. Проверено изолированно: с `responses=` ключ, которого в модели нет,
доходит до клиента, с `response_model=` — исчезает. Внешнее ревью подтвердило это же по исходникам
библиотеки (`routing.py:479` против `:508`, и `openapi/utils.py:336`). Отсюда гард
`test_p011_documentation_models_do_not_filter.py`: он держит `response_model is None` на
документированных маршрутах и **сам проверяет, что различие между написаниями ещё существует** —
иначе после апгрейда FastAPI все утверждения остались бы зелёными при изменившейся проволоке. Укус
проверен мутацией: `responses=` заменено на `response_model=`, тест упал с нужным сообщением.

**Классификация свободных форм — потому что описать можно не всё, и врать нельзя.**

| Узел | Вердикт |
|---|---|
| `IntegrityChecksumResponse.invariants_status` | **STABLE** — 9 ключей, единственный писатель |
| `IntegrityAuditLogItem.after_state` | **STABLE** — 9 ключей, синтезируется в обработчике |
| `InvariantResult.details` | **ENUMERABLE** — 3 варианта по `invariant` |
| `SimulatorActionError.details` | **ENUMERABLE** — 18 фиксированных форм по `code`, 4 кода пробрасывают чужое |
| `AdminAuditLogItem.before_state`/`after_state` | **ENUMERABLE** по `action` (10 путей) |
| `PaymentError.details` | ENUMERABLE с открытым хвостом — 4 формы плюс `{}` |
| `TrustLine.policy` | ключи закрыты на публичном пути, но см. находку ниже |
| `ParticipantProfile.contacts` | **FREE_FORM** — неваллидируемый клиентский JSON |
| `Equivalent.metadata` | **FREE_FORM** — валидатор существует, но из `app/` не вызывается |

Для `FREE_FORM` честный исход — **объявленная** свобода с записанной причиной, а не выдуманные
`properties`. Для узлов, читаемых из JSON-колонок, `additionalProperties: true` остаётся обязательным:
исторические строки могут нести формы, которых сегодняшние писатели уже не производят.

### `F-011-8` — политика trust-линии валидируется на публичном пути и не валидируется на симуляторном

Внесена 2026-08-23. `POST`/`PATCH /trustlines` пропускают `policy` через
`validate_trustline_policy` (`app/utils/validation.py:187-224`): закрытый набор из пяти ключей,
неизвестный ключ — `400`. Но `POST /simulator/scenarios` принимает сценарий, у которого
`policy` объявлена как `additionalProperties: true`
(`fixtures/simulator/scenario.schema.json:232`), и сидер пишет её в ту же колонку **без вызова
валидатора** (`app/core/simulator/real_scenario_seeder.py:224`, значение из `:213-215`). Строки
попадают в ту же таблицу и отдаются обычными ответами trust-линий.

Для 011 отсюда следует узкое: **канон не вправе объявлять `additionalProperties: false` у `policy`**,
даже описав пять ключей, — иначе он будет описывать не то, что отдаётся. Сам обход валидации —
поведение, а не объявление, поэтому это находка для отдельного владельца, не работа этой программы.

### `T1102`, слайс 2 — пять операций, которые приложение описывало, а канон нет

`GET /admin/clearing/cycles`, `/admin/incidents`, `/admin/participants`, `/admin/participants/stats`,
`GET /simulator/runs/active`. У всех пяти в приложении есть `response_model`, то есть форма была
известна — молчал только авторитет №1. Формы перенесены в канон **вручную, в стиле документа**;
выгрузка из `app.openapi()` запрещена `## Non-goals` и уже была отброшена в этой же сессии.

Результат измерен: `SUCCESS_SCHEMA_DRIFT 69 → 64` (ровно −5, как и предсказало внешнее ревью),
список непрозрачных `35 → 30`. Ни одной новой записи.

**Попутно поймана собственная ошибка — и поймал её гард, поставленный этой же сессией.** Правка
канона делалась по номерам строк, и на `/admin/clearing/cycles` она **съела соседний ключ `'429':`**,
оставив его `$ref` висеть под `200`. YAML при этом остался валидным, схема ответа выглядела
правильной, и глазами это не видно. Упал `RT-011-3` — «каждая операция под лимитером обязана
объявлять `429`» — и предъявил маршрут поимённо. Восстановлено; диффом проверено, что других
удалённых строк, кроме самих непрозрачных схем, нет.

Вывод, который стоит держать: **правка YAML по номерам строк небезопасна даже при точных номерах** —
вставка схем выше по файлу сдвинула их. Дальше в этой программе якорем должен быть текст, а не номер.

### `T1102`, слайс 3 — объявленная свобода перестала считаться непрозрачностью

**Предикат считал дефектом то, что дефектом не является.** Узел, у которого содержимое по построению
открыто, и узел, про который просто забыли, для первой редакции `RT-011-1` выглядели одинаково.
Между тем описать `contacts` или `metadata` выдуманными `properties` значит заставить канон
обещать то, чего служба не гарантирует, — а спека прямо требует брать формы из кода и не
придумывать.

Введён `ACCEPTED_FREE_FORM` — allowlist пар «схема, свойство» с **записанной причиной по каждой**:

- `ParticipantProfile.contacts` — клиентский JSON, отдаётся обратно без изменений; писатели только
  create/update (`app/core/participants/service.py:65`, `:175-178`), валидатора нет;
- `Equivalent.metadata` и `StoredEquivalent.metadata` — админский JSON, хранится дословно
  (`app/api/v1/admin.py:1150`, `:1236`). Валидатор `validate_equivalent_metadata` **существует, но
  из `app/` не вызывается ни разу** — только из `scripts/seed_db.py` и тестов. Проверено
  оркестратором отдельно, потому что на этом факте держится весь вердикт.

**Почему allowlist, а не правило «есть описание — значит объявлено».** Правило такой формы
выполняется дописыванием прозы. Строка в allowlist — это утверждение о коде, и его проверяет
контрпроверка `test_accepted_free_form_nodes_are_still_free_form`: узел обязан существовать,
обязан по-прежнему не иметь `properties`, и обязан быть объявлен открытым (`additionalProperties`
не `false`). Описали узел — обязаны убрать строку. **Укус проверен мутацией:** `contacts` выдано
свойство `email`, контрпроверка упала с требованием убрать исключение; мутация откачена.

Итог: список непрозрачных `30 → 20`. Ратчеты не двигались — канон в этом слайсе не менялся вовсе,
изменилось только то, что гард считает дефектом.

### `T1102`, слайс 4 — перечислимые `details`, и денежный wire-тест перед ними

Порядок задан внешним ревью и соблюдён: **сначала гард на деньги, потом описание денежных тел.**

**Денежный wire-тест** (`tests/integration/test_p011_money_is_a_decimal_string_on_the_wire.py`).
Причина, по которой он нужен именно на trust-линиях: `payment.py` объявляет `amount: str` — тип и
есть контракт, — а `trustline.py` объявляет `limit`/`used`/`available` как `Decimal`, и строками они
становятся **только из-за поведения pydantic** в json-режиме. Смена конфигурации сериализации или
апгрейд библиотеки превратят `"100.50"` в `100.5`, не тронув ни одной аннотации. Все утверждения
читают сырой JSON: тест на модели пережил бы поломку молча.

**Укус проверен настоящей регрессией, а не синтетикой:** `TrustLine.limit` временно объявлен `float`,
модуль упал на трёх маршрутах с сообщением «reached the wire as a JSON NUMBER (100.5)» — видно ровно
ту потерю разряда. Плюс контроль обеих сторон у самого чекера: сегодняшнее объявление обязано давать
строки, а `str(float(Decimal("100.50")))`, `"1E-8"`, `"nan"`, `True` и голые числа обязаны
отвергаться. Случай `"100.5"` — тот, который проверка `isinstance` пропустила бы.

**Непустота утверждается, а не предполагается:** фикстура строит реальную линию и реальный платёж и
требует `COMMITTED`, а денежные проверки требуют `used > 0` — то есть что платёж действительно
подвинул долг. Пустой `items: []` иначе удовлетворил бы всем утверждениям, ничего не проверив.

**Описаны `InvariantResult.details` и `PaymentError.details`.** Три варианта нарушений вынесены в
общий `InvariantViolationDetails`: их возвращают две разные поверхности из одних и тех же
производителей. Асимметрия сохранена — `violations` у zero-sum это **объект**, у двух других
**массив**; это свойство производителей, а не описка.

**Две ловушки, снятые проверкой, а не вкусом.** `equivalent` в drift-payload объявлен простой
строкой, **не** `$ref: EquivalentCode`: производитель подставляет UUID, когда код эквивалента не
читается, и паттерн кода отверг бы реальный ответ. И объединение сделано через `anyOf`, а не
`oneOf`: `oneOf` с общей веткой-хвостом отвергал бы **каждый** документированный payload — он
совпал бы с двумя ветками сразу.

**Уточнение предиката, которое стоит держать в голове:** `additionalProperties: false` без
`properties` — это не молчание, а полное описание объекта, который не может нести ключей. Ровно так
выглядит `details` у обычного aborta. Раньше предикат считал это непрозрачностью.

**Открытый хвост записан честно.** `PaymentError.details` внесён в `ACCEPTED_FREE_FORM` с причиной:
оба пути abort'а пробрасывают `.details` любого 4xx `GeoException`, поэтому закрыть хвост нельзя, не
сделав реальный ответ несоответствующим. Известные формы описаны, хвост объявлен открытым.

Итог: список непрозрачных `20 → 15`, `SUCCESS_SCHEMA_DRIFT` остался `64` при новом digest — пять
записей изменили содержание, ни одна не добавилась и не исчезла. Записи уцелели потому, что канон
стал **точнее** генерируемой схемы: на стороне pydantic это `Dict[str, Any]`, и ужесточать его на
денежных маршрутах значило бы менять валидацию ответа, чего программа не делает.

### `T1102`, слайс 5 — политика trust-линии и два стабильных узла integrity

**`TrustLine.policy`.** Канон называл пять разрешённых ключей **в прозе описания**, но не объявлял
их, поэтому сгенерированный клиент не получал ни одного типа. Теперь они объявлены — с типами из
самого валидатора: `max_hop_usage` и `daily_limit` принимают и число, и числовую строку, потому что
`validate_trustline_policy` парсит через `Decimal(str(value))`.

**`additionalProperties: true` оставлено намеренно**, и это прямое следствие `F-011-8`: публичный
путь ключи закрывает, а сидер сценариев симулятора пишет в ту же колонку произвольный JSON мимо
валидатора. Закрыть здесь набор значило бы описать не то, что отдаётся.

Правка задела и **request**-схемы: узел `policy` объявлен на всех трёх схемах, и оставить одну из
трёх расплывчатой было бы худшим контрактом, чем сдвинуть digest. `REQUEST_SCHEMA_DRIFT` остался
`13`, digest новый.

**Два стабильных узла integrity** — `invariants_status` и `after_state` аудита — описаны по единственному
писателю каждого. Существенные детали, которые легко потерять и которые записаны прямо в каноне:
`computed_at` заморожен в JSON как строка и потому несёт `+00:00`, а не `Z`, в отличие от всех
остальных временных меток ответа; `state_checksum_*` — колонки `NOT NULL`, поэтому «предыдущей
контрольной точки не было» выражается **пустой строкой**, а не `null`; обязательными у
`invariants_status` объявлены только пять базовых ключей, потому что блок
`status`/`checks`/`alerts`/`passed` появился позже и строки старше него его не несут.

Три варианта нарушений вынесены в общий `InvariantViolationDetails` и переиспользованы внутри
`checks` — те же формы, те же производители.

Итог: список непрозрачных `15 → 10`, `SUCCESS_SCHEMA_DRIFT` остался `64`.

**Осталось десять, и они делятся на три разных случая, а не на один:** пять админских тел, которые
надо описать по коду (`graph/snapshot`, `graph/ego`, `liquidity/summary`, `participants/{pid}/metrics`,
`trustlines`, `trustlines/bottlenecks`, `audit-log`); `GET /admin/health/db` с пустой `schema: {}`;
и `GET /simulator/events/poll`, который вообще не работа описания — это `F-011-7`, решение владельца.

### `T1102`, слайс 6 — операция, которую прятала пустая схема

`GET /admin/health/db` объявлял `schema: {}` — самое пустое описание, какое бывает, и то, которое
предикат `RT-011-1` не видел до правки anti-vacuum. Теперь описан **с обеих сторон**: канон плюс
`responses={200|503: {"model": AdminDbHealthResponse}}` — снова документационная модель, не
`response_model`, потому что обработчик отдаёт обычный dict на успехе и сырой `JSONResponse` на
отказе. Маршрут добавлен в список инвариант-гарда.

**Заодно описан `503`, у которого тела не было объявлено вовсе.** Обработчик возвращает там ту же
форму плюс `details` с текстом исключения.

**Три попытки совпасть — и все три расхождения оказались содержательными, а не косметическими.**
Первый вариант поднял оба ратчета (`SUCCESS 64 → 65`, `ERROR 83 → 84`), потому что канон стал точнее
схемы приложения; знаменатель при этом не рос, то есть по уточнённому инварианту это был бы
запрещённый рост. Пришлось сводить обе стороны:

- `latency_ms` объявлен **обязательным и nullable**, а не опциональным: обработчик его отдаёт
  всегда, `null` на пути отказа;
- `details` — nullable: на успехе ключа нет вовсе;
- `timestamp` типизирован `datetime`, чтобы схема сказала `format: date-time`, — модель
  документационная и ничего не сериализует, так что это безопасно и правдиво.

Итог, ради которого это и делалось: **ни один ратчет не сдвинулся** — `SUCCESS 64` и `ERROR 83`
остались с прежними digest, потому что операция ушла из **обоих** словарей. Список непрозрачных
`10 → 9`.

### `T1102`, слайс 7 — графовые тела, и находка о времени внутри одного ответа

`GET /admin/graph/snapshot` и `/ego` описаны. Существенное: три коллекции —
`incidents`, `audit_log`, `transactions` — объявлены в модели как `list[Any]`, то есть у элементов
**нет схемы вовсе**; их формы взяты из хелперов, которые их строят. Две из трёх легли на уже
существующие компоненты (`AdminIncidentItem`, `AdminAuditLogItem`), третья описана заново.

`viz_size` намеренно **не** закрыт до `{w, h}`. Поле типизировано `dict[str, int]`, и хотя
единственный писатель строит ровно два ключа, схема уже типа была бы обещанием, которого аннотация
не даёт. Ключи названы, карта осталась открытой — это тот же класс ошибки, что «не сужать поле,
которое код может расширить», на котором программа уже обжигалась с `EquivalentCode`.

Открытые узлы записаны в `ACCEPTED_FREE_FORM`: `AdminAuditLogItem.before_state`/`after_state` —
исторический журнал, где десять путей-писателей хранят каждый свою форму, и
`AdminGraphTransactionError.details` — тот же открытый хвост, что у `PaymentError.details`, но через
другое чтение. **Контрпроверка поймала ошибку в моей же записи:** ключ был указан как
`("AdminGraphTransactionItem", "details")`, тогда как `details` лежит внутри `error`, а не прямым
свойством. Тест упал на `assert None is not None`; `error` вынесен в отдельный компонент, ключ
исправлен. Allowlist, который не проверяется, — это список опечаток.

### `F-011-9` — одно и то же время в одном ответе отдаётся в двух форматах

Внесена 2026-08-23. `_graph_fetch_incidents` и `_graph_fetch_transactions` кладут `created_at` и
`updated_at` **сырыми значениями колонки** в поля `list[Any]` (`app/api/v1/admin.py:217`, `:252-253`),
поэтому валидаторы, привязывающие UTC, не отрабатывают. Под SQLite — а это дефолтный
`DATABASE_URL` — колонка возвращается наивной, и значение уходит **без смещения**. Та же строка,
отданная через `GET /admin/incidents`, смещение несёт: там модель отрабатывает.

То есть в одном ответе `audit_log[].timestamp` заканчивается на `Z`, а `incidents[].created_at` —
ничем. Для 011 следствие узкое и уже применено: у этих трёх полей в каноне **не** объявлен
`format: date-time`, потому что служба его не гарантирует; объявить было бы ровно тем дефектом,
который программа устраняет. Сама починка — изменение проволоки (в строке появится смещение) и
потому запрещена `## Non-goals`: это находка для отдельного владельца.

### `T1102`, слайс 8 — пять денежных админских чтений, и две общие схемы, которые они выдали

Список непрозрачных ответов `7 → 2`. Эти пять отличаются от всех предыдущих слайсов тем, что
**бесформенными они не были никогда**: у всех пяти с самого начала объявлен настоящий
`response_model` (`app/schemas/admin.py`, `app/schemas/metrics.py`), и канон просто отстал от
моделей, которые уже существовали. Отсюда следствие, которое пришлось измерить: описания одного
канона мало, и из шести операций дрейфа ушла ровно одна.

**Две общие схемы оказались неверны, и обе нашлись не сами по себе, а через описание их
потребителей.** У `TrustLine` вовсе не было `updated_at`, хотя его отдаёт каждое чтение линии
(`app/schemas/trustline.py:24`), а `required` называл шесть из десяти полей, которые модель
объявляет без значения по умолчанию. У `AdminAuditLogItem` поля `id` и `actor_id` объявлены
простыми строками, тогда как это UUID-колонки, типизированные как `UUID`. Именно эти две
неточности и держали `GET /admin/audit-log` в дрейфе — с их исправлением операция из него вышла.

Ратчет: `SUCCESS 64 → 63`, digest сдвинут; `ERROR 83` и `SECURITY 67` не изменились ни по числу,
ни по digest. Измерено дважды — на зеркальной копии канона до правки и на настоящем файле после;
оба измерения совпали.

**Агент опроверг три посылки задания, и все три опровержения были верны.** Названия двух схем в
брифе не совпадали с моделями (`AdminTrustLinesListResponse`, `AdminAuditLogListResponse`), причём
`AdminAuditLogResponse` в каноне **тоже есть** — это другая схема, ни одной операцией не
используемая, и угаданное имя молча переписало бы её. Ожидание, что `meta` попадёт в
`ACCEPTED_FREE_FORM`, не подтвердилось: писатель ровно один и кладёт ровно один ключ. И тот же
`threshold`, который на двух маршрутах уходит числом, на третьем уходит **десятичной строкой** —
это записано в каноне как есть, а не сглажено.

### `F-011-9` расширена — третий случай того же расхождения, найден внешним ревьюером

Дополнено 2026-08-23. Первая редакция находки называла два поля
(`AdminGraphTransactionItem.created_at/updated_at`). Внешний ревьюер указал на третье: графовый
ответ переиспользует `AdminIncidentItem`, у которого `format: date-time` объявлен, — но
`_graph_fetch_incidents` кладёт `tx.created_at` в словарь **сырым** (`app/api/v1/admin.py:217`), а
поле `incidents` объявлено как `list[Any]` (`app/schemas/graph.py:32`), поэтому UTC-валидатор
`AdminIncidentItem` там не запускается.

Проверено исполнением на производственных моделях: с наивным `datetime` на входе
`AdminIncidentItem` отдаёт `2026-08-23T12:00:00Z`, а тот же словарь внутри
`AdminGraphSnapshotResponse` — `2026-08-23T12:00:00`. То есть в каноне графового ответа нужен
отдельный item без `format: date-time`, а `GET /admin/incidents` обязан сохранить существующий.

### `F-011-10` — канон объявляет `null` там, где `null` не проходит валидацию

Внесена 2026-08-23. В OpenAPI 3.0.3 `nullable: true` изменяет **соседний `type`** и больше ничего.
На схеме, у которой нет `type`, а есть только `oneOf`/`anyOf`/`allOf`, этот ключ не значит ничего,
и `null` отвергается. Канон пишет ровно такую конструкцию в **24 местах**.

Проверено `openapi_schema_validator.OAS30Validator`, а не вычитано из спецификации:

| форма | `null` |
|---|---|
| `{nullable: true, oneOf: [X]}` | отвергнут — «None is not valid under any of the given schemas» |
| `{nullable: true, allOf: [X]}` | отвергнут — «None for not nullable» |
| `{type: object, nullable: true, allOf: [X]}` | отвергнут |
| `{oneOf: [X, {nullable: true, enum: [null]}]}` | принят, и неподходящий объект по-прежнему отвергается |

Это тот же класс, что и вся программа, но в третьей его разновидности: не «канон молчит» и не
«канон обещает несуществующее», а **канон запрещает то, что служба законно возвращает**.
Корректный ответ с `details: null` сегодня не проходит проверку по авторитету №1. Часть этих мест
внесена самой волной 011 — слайсами 4, 5, 7 и 8, — что и делает находку обязательной к закрытию
внутри волны, а не после неё.

### Решения владельца по `F-011-7` и `F-011-9`, принятые через делегирование

Владелец делегировал обе развилки внешнему ревьюеру (тот же порядок, что для `T1101` и `T1103b`).

**`F-011-7` — вариант A: канон описывает то, что есть.** `VERDICT-F0117: A`. Обоснование
ревьюера проверяемо: маршрут не зовёт ни `simulator-ui`, ни `admin-ui` (активный v2 работает через
SSE `/simulator/runs/{run_id}/events`), поэтому описание пустоты никого не ломает; реализация
буфера — отдельная продуктовая функция со своим жизненным циклом, а удаление операции ломало бы
уже опубликованный контракт ради предположения об отсутствии внешних клиентов. Форма — массив с
`maxItems: 0`, объявленный **с обеих сторон** (в приложении через `responses=`, никогда через
`response_model=`), плюс правка описаний `equivalent`/`after`, которые сегодня обещают семантику
курсора, а в MVP не читаются вовсе. Гард обязан признавать такой массив описанным и иметь
контрпроверку: без `maxItems: 0` те же `items: {}` снова непрозрачны.

**`F-011-9` — только описание.** `VERDICT-F0119: DESCRIBE_ONLY`. Починка добавила бы смещение в
строку на проволоке, что прямо запрещено `## Non-goals`. Поведенческий долг регистрируется вне 011
и **не** передаётся в 012: 012 владеет денежным представлением, а не временем.

**Критерий честного закрытия волны, названный ревьюером.** Пустого списка непрозрачных ответов
недостаточно. Требуется: пустой `UNDESCRIBED_SUCCESS_RESPONSES` при том, что все намеренно
открытые узлы остаются только в проверяемом `ACCEPTED_FREE_FORM`; `T1106` перестала быть частичной
— общий контроль достижимых, но не объявленных статусов, а не поимённые `429`/`503`; все ратчеты
пересчитаны свежо и у каждого остатка названа причина; `F-011-8`, поведенческая часть `F-011-9` и
разрыв generated-схемы SSE зарегистрированы вне 011, а не потеряны в тексте закрытой программы;
и на финальном SHA — полный гейт, `T1108` внутренним агентом и финальный `T1109` внешним.

### `T1102`, слайс 9 — `F-011-10` закрыта, и вместе с ней `F-011-7` и третий случай `F-011-9`

**26 мест, а не 24.** Первый подсчёт нашёл только те, что стоят рядом с ключом композиции; ещё два
— голая форма `{nullable: true}` без ничего, у `TxOnceRequestBody.seed` и `ClearingOnceRequestBody.seed`.
Она не запрещает `null`, но и не объявляет вообще ничего.

Решение по каждому месту принималось **из кода, а не из формулировки канона**: двенадцать получили
явную ветку `null`, потому что `null` там действительно бывает; одиннадцать просто потеряли ключ,
потому что не бывает никогда — три суммы `SimulatorGraphLink` не могут прийти пустыми ни по одному
валидируемому пути, а `details` интегрити-проверок пишет обычный литерал словаря, который либо
опускает ключ, либо кладёт словарь. Решить наоборот значило бы заставить канон валидировать ложь.

Доказано на настоящем теле, а не рассуждением: реальный `200` от
`GET /admin/participants/{pid}/metrics` был **невалиден с двенадцатью ошибками** до правки и валиден
после; `InvariantResult` с `details: null` — то же самое, проверено на обеих версиях файла из git.

**Изменён инструмент, и это записано отдельно от изменения контракта.** `_normalize_schema` теперь
выбрасывает `nullable` без соседнего `type` — ровно так же, как давно выбрасывает `title`. Причина
асимметричная: канону эта форма теперь запрещена новым гардом, а FastAPI продолжает её эмитить для
каждого `Optional[Any]`, и закрыть это правкой канона нельзя. Без нормализации два тела запросов
расходились бы вечно из-за ключа, который ни на одной стороне ничего не утверждает. Измерено:
с нормализацией `REQUEST` держится на 13.

**Гардов три, и у каждого проверена кусачесть.** Основной обходит весь документ, включая параметры и
тела запросов — обходчик по одним только `components/schemas/*/properties` нашёл бы 20 из 26 и
прошёл бы мимо узлов политики trust-линии. `enum: [null, "x"]` не отмывает нарушение. Второй гард не
даёт превратить санкционированную ветку в лазейку: вставить `enum: [null]` рядом с нарушителем и тем
заглушить первый гард нельзя — ветка обязана быть членом композиции. Третий держит нормализатор:
действующий `nullable` не должен схлопываться ни на какой глубине.

Вместе с этим применены `F-011-7` (массив с `maxItems: 0` с обеих сторон, оба query-параметра честно
объявлены игнорируемыми) и третий случай `F-011-9` (графовые чтения получили
`AdminGraphIncidentItem` без `format: date-time`).

**И исправлена собственная ошибка слайса 8.** Комментарий к `TrustLine` утверждал, что три поля
уходят «present and null». Wire-тест показал: `policy` уходит как `{}` и никогда как `null`, потому
что `TrustLineService.create` пишет `policy=data.policy or {}` — из чего следует, что
пятиключевой дефолт ORM-колонки для созданной через API линии **не срабатывает никогда**. Имена
участников не пусты везде, где запрос джойнит строки участников. Ошибку нашёл не читатель, а
исполнение.

### `T1102`, слайс 10 — список пуст

`GET /integrity/audit-log` был последней записью. Список `1 → 0`: **ни один `2xx` в
`api/openapi.yaml` больше не отвечает «какой-то объект»**. Файл гарда перестаёт быть сокращающимся
реестром и становится абсолютным правилом — это записано в нём самом.

У операции уже была именованная схема ответа, поэтому непрозрачность сидела внутри: три листа
`after_state`. **Ни один из трёх не оказался свободной формой** — все три собираются семью
писателями из литералов, поэтому в `ACCEPTED_FREE_FORM` не добавилось ничего; список свободных узлов
остался ровно таким, каким был.

Три решения стоит записать. `affected_participants` описан плоско, а не как `oneOf` из шести ветвей:
чтобы ветви различались, двум из них понадобился бы `additionalProperties: false`, а это закрывает
историческую JSON-колонку против уже записанных строк. `error_details` — наоборот, честный `oneOf`:
его четыре варианта различимы по одним только обязательным ключам. И `required` у
`IntegrityAuditLogItem` был неверен: на проволоке всегда шесть ключей, `actor_id` **захардкожен в
`None`** — интегрити-след никогда не указывает актора, и колонки для него нет.

Ратчет держится на 62, digest сдвинут: у `after_state` в pydantic — `Dict[str, Any]`, поэтому канон
стал точнее генерируемой схемы, а не равен ей. Та же причина, по которой держали счёт слайсы 4 и 5.

### `T1106` закрыта — общий гард на достижимые статусы, и 263 пары закрыты за ним

До этой сессии «код отдаёт статус, которого нет в декларации» ловилось только для `429` и одного
поимённого `503`. Общая форма выведена **из таблицы маршрутов и графа зависимостей**, а не
перечислена: пять сигналов, каждый доказан исполнением.

| Сигнал | Что даёт | Что было бы, если ошибиться |
|---|---|---|
| `S1` | фальсифицируемый параметр или тело → `422` | простой `str` в пути **не** фальсифицируем: он не бывает пропущен и приводить нечего |
| `S1b` | то же под `/simulator/runs/*/actions/*` → `400`, а не `422` — обработчик валидации ветвится по пути | пропустить значило бы записать в канон 7 ложных `422` |
| `S2` | `401` от трёх аутентифицирующих зависимостей | `require_admin` **не** из них: ветки `401` у него нет вовсе, и отсутствие токена даёт `403`. Включить его значило бы выдумать 29 недостижимых `401` |
| `S3` | `403` от тех трёх плюс `require_admin` | — |
| `S4` | `422` от кривого `X-Simulator-Owner` — **без единого фальсифицируемого параметра** | так `422` достижим на десяти операциях, у которых из входов только два необязательных заголовка |
| `S5` | `429` | не переписан: гард отдаёт пробный документ функции приложения и берёт, что вернулось. Своего мнения об exempt-списке у него нет |

Измерено: **136** достижимых-но-необъявленных пар против канона и **127** против схемы приложения.
Списка два, а не один: стороны расходятся по разным причинам, и один список позволил бы прогрессу
одной стороны маскировать регресс другой.

**Гард односторонний, и это записано.** `GET /admin/health/db` объявляет `403` правильно, а правило
его не видит — обработчик зовёт `require_admin` в собственном теле, а не через `Depends`.
Утверждается «достижим ⇒ объявлен»; симметричное утверждение упало бы на верной декларации.

**Затем обе стороны закрыты одним изменением.** Канон получил 136 объявлений через три новых
компонента ответа; приложение — `_declare_auth_statuses(document)` рядом с
`_declare_rate_limit_status`, выводящую набор операций из таблицы маршрутов, а не из списка.
Обе половины обязаны были лечь вместе: закрытие одной сдвигает `ERROR_RESPONSE_DRIFT` по
большинству из 78 операций, и ратчет пришлось бы благословлять дважды по нечитаемой дельте.

**`ERROR_RESPONSE_DRIFT` 83 → 53. Тридцать операций ушли, ни одна не вошла.** Это самый большой
сдвиг этого реестра и первый, случившийся оттого, что исправлены **оба** документа, а не сведён
один к другому. `SUCCESS` и `SECURITY` не изменились ни по числу, ни по digest — измерены напрямую,
потому что ассерт на ошибках падает раньше обоих (та же ловушка, что записана в `T1103a`).

**У оставшихся 53 ровно две названные причины.** Сорок шесть — канон-only `4xx`, которых
генерируемая сторона узнать не может: `404`, `400`, `409`, `500`, `504` поднимаются на три-четыре
кадра вглубь `app/core/**`. Это сигнал `S6`, отвергнутый обзором как невыводимый: правило,
достаточно свободное чтобы его вывести, потребовало бы этих статусов по всей поверхности и записало
бы в авторитет №1 то, чего служба не возвращает. Тринадцать — генерируемый-only `422`, который
**недостижим**: FastAPI штампует его на любой операции с любым flat-параметром, а там единственный
параметр — необязательный `X-Admin-Token`. Проверено исполнением. Они оставлены намеренно: канону
их копировать нельзя, а вычищать их из публикуемой схемы — отдельное решение.

**Третий компонент ответа существует ровно по причине этой программы.** Десять операций доходят до
`422` без единого фальсифицируемого параметра — только через кривой `X-Simulator-Owner`. Описать их
как «request validation failed» значило бы внести в канон новое ложное утверждение, поэтому у них
свой компонент, который говорит, что именно они могут ответить и почему это их единственный `422`.

**Найдено попутно и не исправлено: канон противоречит сам себе о форме `401`.** На маршрутах с
участниковой аутентификацией отсутствие заголовка `Authorization` даёт `{"detail": "Not
authenticated"}` прямо от `OAuth2PasswordBearer`, а не конверт; с `Bearer junk` — конверт. Канон
знает об этом **в одном месте**: `POST /payments` объявляет `401` как `oneOf[ErrorEnvelope, {detail}]`.
Остальные тридцать говорят только `ErrorEnvelope`. Честная правка — расширить тридцать тел, но это
изменение тел, а не объявление достижимых статусов, и оно вернуло бы тридцать операций обратно в
дрейф. Находка записана отдельно.

### `T1108` — внутренний adversarial review, десять находок, две из них подрывали тезис волны

Выполнена 2026-08-23 субагентом на замороженном `b6d2254..5416065`, read-only, по §16 `AGENTS.md`
(ревьюер не чинит собственные находки до подтверждения оркестратором). Прошлая передача считала
задачу заблокированной окружением; блокировка снята, и задача выполнена по-настоящему.

**Находка первая, и она отменяла заявленное закрытие `F-011-10`.** Название находки — «канон
запрещал `null`, который служба возвращает» — а закрыта была только узкая синтаксическая форма
(`nullable` без соседнего `type`). Общая форма жила: `nullable: true` рядом с `enum`, в котором
`null` не перечислен, **всё равно отвергает `null`**, потому что enum — более узкое ограничение и
побеждает. Такие места гард пропускал именно потому, что соседний `type` у них есть. Второй облик:
поле, которое бывает `null`, объявленное голым `$ref` или голой типизированной схемой — там нечему
быть неверным, и оба гарда слепы.

**Двенадцать операций отдавали тела, которые канон отвергает, при полностью зелёном гейте.**
Показательнее всего `GET /admin/graph/snapshot`: `_attach_net_viz` выходит рано, если
`?equivalent=` не задан, поэтому **вызов по умолчанию** возвращал тело, которое канон отвергал на
каждом участнике. И `PaymentResult.error` был голым `$ref`, хотя обычный **успешный** платёж шлёт
туда `null` — слайс 4 переписал `PaymentError.details` и перемерил все три платёжные операции, не
заметив, что свойство, указывающее на эту схему, запрещает `null`, который успех возвращает всегда.

**Находка вторая: в репозитории ничто не валидировало ни одного ответа против канона.** Сорок
восемь схем, добавленных волной, держались на транскрипции; доказательства конформности жили в
сообщениях коммитов как разовые прогоны, ни один из которых не был закоммичен как тест. Измерение,
установившее находку, не могло обнаружить её регресс. Это и есть вопрос §9, и волна отвечала на
него «нет».

Ответ — постоянная проверка: обёртка HTTP-клиента на всю сессию валидирует **каждое** 2xx-тело
против схемы, объявленной каноном для этой операции и этого статуса. 71 операция, 780 тел, и
неохваченные операции называются, а не считаются пройденными. Зависимость закреплена в
`requirements-dev.txt`, а не спрятана за `importorskip`: проверка, которая умеет молча
пропуститься, ничего не доказывает — и существовавший `importorskip` в гарде `F-011-10` снят по той
же причине.

**Тринадцатое место, которого не было ни в одном списке.** `Participant.profile`: `create_participant`
передаёт `profile=None` явно, минуя дефолт колонки, и ни одна фикстура в наборе профиль не
опускала — поэтому его не видели ни оба статических гарда, ни харнесс, пока не был написан тест,
производящий это тело.

**`TrustLine.policy` оказался не дефектом канона.** `null` там производили две юнит-фикстуры,
вставлявшие `policy=None` прямо через ORM в обход Python-дефолта и строившие строку, которую ни
один писатель `app/` написать не может. Объявить поле nullable значило бы ослабить верное
утверждение ради несуществующей строки. Фикстуры исправлены, реестр известных несоответствий пуст,
остаток (nullable-колонка без server default) записан в `specs/BACKLOG.md` как гигиена схемы.

**Четыре мутации главного гарда, которые обязаны были его сломать и не ломали.** Правка внешнего
ревью — `if not schema: return True` — ловила только буквально пустой словарь, то есть была шириной
в один ключ-аннотацию. Проходили: `{description: …}` (а `description` — ровно тот ключ, который
человек и допишет рядом со схемой, которую собирался написать позже), `{type: array}` без `items`,
`oneOf: []` и компонент, выпотрошенный до аннотации. Правило переписано с «пуст ли словарь» на
«есть ли здесь ограничение вообще». **Сразу же нашлась настоящая операция:** `GET /admin/config`
объявлял `value` как `description: Any JSON value` — и свободной формой это никогда не было:
значение есть `getattr(settings, key)` по закрытому списку из двенадцати настроек, измеренному как
шесть булевых, пять целых и одна строка.

**Allowlist свободных форм протекал по имени свойства.** `component` наследовался вглубь, поэтому
непрозрачный узел с именем `details` или `before_state` **на любой глубине** под освобождённым
компонентом освобождался тоже — самый дешёвый способ сделать гард зелёным, оставив канон немым.
Наследование убрано.

**Ещё пять находок помельче, все подтверждены и закрыты:** `RT-011-3` не видел одну из 96 операций,
которые декларировал (конвертер `{pid:path}`, и промах молча пропускался); нормализатор мог стирать
нулевость, которую сам синтезировал; причина тринадцати недостижимых `422` была названа неверно в
трёх местах (дело в строковости всех параметров, а не в необязательности одного заголовка); пять
якорей из шестнадцати проверенных указывали не туда; арифметика `F-011-10` не сходилась ни в одной
из трёх формулировок — измеренный раскол `15 + 11`, и **18 из 26 мест написала сама эта волна**.

**Две схемы графа противоречили друг другу о той же колонке.** `AdminGraphIncidentItem` объявлял
`created_at` необязательным и nullable, `AdminGraphTransactionItem` — обязательным и не-nullable,
при том что оба питаются от `Transaction.created_at` через одинаково написанные хелперы. Права
была строгая: колонка NOT NULL с server default, ключ пишется всегда. Заодно `state` перестал быть
голой строкой — запрос фильтрует до шести состояний из девяти.

**Чего ревью не нашло, и это тоже записано.** `ACCEPTED_FREE_FORM` проверен весь: ни одного ложного
освобождения, счёт «десяти писателей» для `before_state` сходится точно. Ратчет-реестр назван самым
точным артефактом волны: `ERROR 83 → 53` перемерен независимо — тридцать уходят, ни одна не входит,
и остаток раскладывается на две названные причины без третьей.

## Changelog

- **2026-08-21.** Программа заведена по маршруту 008. Все шесть якорей и три знаменателя
  перепроверены на HEAD `de4dd08`.

  **Два уточнения против записанных находок, оба в сторону расширения:**

  1. `C-C2-010` называла «четыре» непрозрачных ответа — разбор канона парсером YAML даёт **13**, и
     среди них денежные поверхности (`/admin/trustlines`, `/admin/liquidity/summary`,
     `/admin/clearing/cycles`, `/admin/participants/{pid}/metrics`), а не только графовые.
  2. Знаменатель 100/92/88 воспроизведён напрямую (`app.routes`, `app.openapi()`), и впервые
     установлено, **какие именно** восемь маршрутов скрыты: это денежные действия Interact Mode
     (`payment-real`, `clearing-real`, `trustline-create/update/close`) плюс три чтения. На одном из
     них — `trustline-create` — лежит подтверждённый P1 `F-009-4` программы 009. Дефект и
     невидимость сошлись на одном маршруте.

  **2026-08-21, внешнее ревью Codex — вердикт `REFUTED`, семь правок приняты.** Клон на `cc3c9ab`,
  `codex-cli 0.144.0-alpha.4`, `--sandbox read-only`, exit `0`, маркер `VERDICT-DONE`.

  Устояли: все три знаменателя (ревьюер пересчитал канон вручную — 54 GET + 27 POST + 5 PATCH +
  2 DELETE = 88 — и подтвердил список восьми скрытых маршрутов дословно), список тринадцати
  непрозрачных ответов, цепочка инертности `F-011-4` целиком (включая проверку, что HTTP-middleware
  лимитер не вызывает), и возможность описать ответы без изменения поведения.

  Принято и исправлено:
  1. **Числа зависят от окружения.** Восемь маршрутов скрыты не литералом, а
     `include_in_schema=_actions_enabled()` (`simulator.py:357-358`); `/metrics` условен по
     `METRICS_ENABLED`. Окружение среза зафиксировано в `## Problem`.
  2. **13 — только верхний уровень.** `/admin/participants` объявляет `properties`, но его `items`
     непрозрачны (`api/openapi.yaml:332-337`). Гард обязан быть рекурсивным, 13 — нижняя граница.
  3. **«Ратчет уменьшается» — неверно.** `_assert_drift_ratchet` требует точного совпадения count и
     SHA; отношения порядка у SHA нет, а `count` останется 84 по собственному комментарию ратчета.
     Правило переписано на «доказать исчезновение записи по normalized diff, затем обновить digest».
  4. **`RT-011-4` не был репродьюсером.** Тест «ничего не меняется» обязан быть зелёным на текущем
     коде — это контрпроверка, а не red. Переформулирован на «корневой маршрут проходит через общий
     policy-гейт», что красное сегодня.
  5. `RT-011-1` усилен до рекурсивного и обязан считать пустой `properties: {}` непрозрачностью.
  6. `RT-011-2` обязан фиксировать окружение и падать при его изменении.
  7. Селекторы UI названы командами целиком (`AGENTS.md:398`), а не «unit-набор».

  Плюс `ban`/`unban` вынесены в `T1102` отдельной оговоркой: `response_model` у них нет, а
  существующий `AdminParticipantStatusResponse` не допускает статус `deleted`, который `ban` ставит.

  **Инертность `F-011-4` подтверждена механизмом, а не пересказом:** корневые маршруты объявлены
  через `@app.get` (`main.py:543,551,569,574`) и `dependencies` роутера не наследуют, поэтому
  `rate_limit` для них не вызывается, а `_RATE_LIMIT_EXEMPT_PATHS` читается **внутри** него
  (`deps.py:60`). Список, до которого исполнение не доходит, повлиять ни на что не может.

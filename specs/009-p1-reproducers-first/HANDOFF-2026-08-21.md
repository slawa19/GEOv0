# HANDOFF 2026-08-21 — передача программы 009 новой сессии оркестратора

- **Date:** 2026-08-21
- **Назначение:** ввод новой сессии в состояние работ без перечитывания всей истории. Каждое
  утверждение снабжено якорем `path:line` или именем коммита; утверждений без якоря нет.
- **Чисел прохождения тестов здесь нет сознательно:** `AGENTS.md:55` запрещает хранить в документах
  протухающие значения. Владелец значения — прогон на HEAD.

## Где работа

- **Ветка:** `codex/009-reproducers` (`git branch --show-current`).
- **Последний коммит:** `1e86c18` — «fix(009): remediate the third review — integrity checksum and
  history». Приземлился 2026-08-22 14:24 и закрывает шесть блокеров **третьего** внешнего ревью (клон
  на `8c7cc6e`, `codex-cli 0.144.0-alpha.4`, `--sandbox read-only`, exit `0`, `VERDICT-DONE`,
  вердикт `REFUTED`). Содержание по телу коммита `1e86c18`:

  1. **контрольная сумма целостности была недетерминированной** — `app/core/integrity.py` сортировал
     только по паре `(from, to)`, а после миграции `019` закрытая и живая инкарнации делят эту пару,
     то есть порядок отдавал планировщик; добавлен тай-брейк по `id`. Это тот самый checkpoint, на
     котором строится программа 015;
  2. **закрытую линию можно было переписать** — `update()` грузил линию по `id` без проверки статуса,
     поэтому лимит закрытой инкарнации оставался патчабельным навсегда; протокол §5.2 требует
     `status=active`, теперь отказ;
  3. **классификатор конфликта был всё ещё неточен** — ревью дало четыре исполняемых контрпробы
     (двухколоночная уникальность чужого индекса, слово `unique` в другом контексте, двухуровневая
     вложенность `__cause__`, asyncpg без `constraint_name`); обходится вся цепочка причин,
     используются `sqlstate 23505` и `table_name`, текстовый fallback требует все три колонки;
  4. **барьер в тесте на гонку стоял не там** — он был на чек-поинте целостности, который вызывается
     **до** гарда; перенесён на `flush()`, ровно между гардом и `INSERT`; проверено мутацией;
  5. **fail-open периметра не был обоснован** — `_ensure_run_seeded` вызывается до вычисления
     периметра (`app/api/v1/simulator.py:959`, `:1189`, `:1351`, `:1512`), значит после сидинга пустой
     снапшот означает пустой ран; пустой периметр стал fail-closed, `None` остался только для случая
     «снапшот не строится вовсе»;
  6. заведена новая находка `clearing-real` (см. раздел «Записи для других программ»).

- **Предыдущий коммит:** `8c7cc6e` — «fix(009): close three P1 findings and remediate the second
  review»: закрыты `F-009-1` и `F-009-2` (`F-009-3`/`F-009-4` закрыты пачкой раньше) и исправлены три
  блокера второго внешнего ревью — непокрытые потребители прежней уникальности
  (`app/core/admin/metrics.py`, `edge_patch_builder.py`, `viz_patch_helper.py`), ложноположительный
  классификатор и контрпробы к нему в `tests/unit/test_trustline_conflict_identity.py`.
- **Не слито в `main`.** `git log --oneline main..codex/009-reproducers` даёт пять коммитов:
  `1e86c18`; `8c7cc6e`; `46e6c7f` — ремедиация первого ревью пачки T903 (семь непромигрированных
  потребителей тройки плюс сломанная SQLite-ветка миграции); `918cd14` — миграция
  `019_trust_lines_partial_unique_live`, закрытие `B-A3-004`; `4a995e9` — запись авторизации владельца
  на реализацию 009–014. **Причина не-слияния:** гейты merge'а не отработаны — `T908`, `T909`, `T910`
  в статусе `[!]` (`specs/009-p1-reproducers-first/spec.md:276-278`). Отдельно: владелец 2026-08-21
  потребовал **сливать с `main` чаще**, назвав накопление коммитов в одной ветке дефектом процесса
  (`specs/README.md:215-217`) — пять коммитов в отрыве уже идут против этого требования.
- **Рабочее дерево чистое**, кроме самого этого файла (`git status --porcelain` даёт только
  `?? specs/009-p1-reproducers-first/HANDOFF-2026-08-21.md`).
- **Долг документации, оставшийся после `1e86c18`:** единственная правка `spec.md` в этом коммите —
  запись про `clearing-real` (`git show 1e86c18 -- specs/009-p1-reproducers-first/spec.md`). В
  `## Changelog` пакет третьего ревью **не описан**, и раздел «известный остаток» по-прежнему
  утверждает `return pids or None` и открытую дыру периметра (`spec.md:433-442`, дословно `:435`),
  хотя в коде это уже fail-closed. Спека отстала от кода — закрыть первым делом.

## Состояние программы 009

| Находка | Реестр 008 / sev | Статус | Одной строкой | Якорь |
|---|---|---|---|---|
| `F-009-1` | `C-A1a-003`, P1 | **закрыта** (T901 `[x]`) | `RT-009-1` показал асимметрию дословно: `participants-list` скрывает участника вне рана, а `trustline-create` с ним же отдаёт `200`; резолвер сделан осведомлённым о ране, намеренности глобального резолва не обнаружено | `spec.md:54-59`, `:269`, `:421-426` |
| `F-009-2` | `B-A2b-001`, P1 | **закрыта** (T902 `[x]`) | `RT-009-2` показал уничтожение работы владельца (`1 → 0`) с последующим ложным `on_commit`; правка — SAVEPOINT по образцу, уже применённому срезом T715 к соседней `write_tick_metrics` | `spec.md:61-79`, `:270`, `:428-431` |
| `F-009-3` | `B-A3-004`, P1 | **закрыта** (T903 `[x]`) | Диагноз был неверен и переделан: строже протокола была **база**; миграция `019_trust_lines_partial_unique_live` сузила уникальность до живых строк `WHERE status <> 'closed'`, гарды возвращены к протокольному предикату, гонка ловится fail-closed | `spec.md:81-108`, `:271`, `:387-404` |
| `F-009-4` | `B-A1a-016`, наследует sev `F-009-3` | **закрыта** (T903 `[x]`) | Второй, независимый call-site: маршрут Interact Mode собирает `TrustLine` сам и ронял `IntegrityError` мимо обработки; `RT-009-4` доказал независимость, правка симметрична сервисной | `spec.md:110-115`, `:271`, `:366-368` |
| `F-009-5` | `B-B3-2-003` | **`REFUTED`** (T904 `[x]`) | Вердикт сужен внешним ревью до варианта `closed`: закрытая линия создание не блокирует (`docs/ru/02-protocol-spec.md:333`), значит active-only подсказка верна; вариант `frozen` остаётся дефектом, но это P2 честности данных и владелец — 013 | `spec.md:117-134`, `:272` |
| `F-009-6` | `PAR-A1a-001`, P2 | **заблокирована** (T905 `[ ]`) | Три пары `commit()`→`refresh()` подтверждены чтением, но `RT-009-5` не построен; правка не начинается раньше красного теста | `spec.md:155-160`, `:273`, `:444-451` |
| `F-009-7` | `C-A4-4-013`, P2 | **закрыта** (T906 `[x]`) | Обход в тесте снят: тест проходит настоящий путь create → close → create тем же тройником; промежуточная запись «задача недостижима» отменена вместе с неверным вариантом | `spec.md:142-153`, `:274` |

Итог таблицы `## Tasks` (`spec.md:266-278`): `[x]` — 6, `[ ]` — 2, `[!]` — 3, всего 11 строк.

## Что осталось в 009

Открытые (`[ ]`):

- **`T900` — репродьюсеры** (`spec.md:268`). `RT-009-1`, `RT-009-2`, `RT-009-3`, `RT-009-4`,
  `RT-009-6` написаны, были красными и закрыты правками; `RT-009-7` (гонка) добавлен по внешнему
  ревью. Задача открыта **только** из-за непостроенного `RT-009-5`.
- **`T905` — `F-009-6`, заблокирована отсутствием репродьюсера** (`spec.md:273`). Детали долга —
  `spec.md:444-451`: требуется настоящий отказ после durable-коммита (обрыв соединения либо
  конкурентное удаление строки); попытка сделать его детерминированным через `after_commit`-слушатель
  не удалась — у тестовой session-фабрики нет `sync_session_class`, а вешать слушателя на глобальный
  `Session` значит перехватывать чужие коммиты; стенд удалён, а не оставлен полуработающим.
  `monkeypatch` `refresh` **запрещён** разделом «Запрещённые способы доказательства»
  (`spec.md:256-260`): он доказал бы существование ветки `except`, а не достижимость отказа.

Заблокированные (`[!]`) — все три процедурные и все три являются предусловием merge'а:

- **`T908`** — гейты матрицы `008/plan.md` §6 после каждого пакета; milestone `verify_local.ps1` плюс
  Postgres-тир перед merge (`spec.md:276`).
- **`T909`** — внутренний adversarial review субагентом (§16.4 `AGENTS.md`), обязателен всегда и идёт
  **до** внешнего (`spec.md:277`).
- **`T910`** — независимое внешнее ревью и публикация evidence на точном HEAD по
  `docs/external-review-runbook.md` §3; при сбое ревьюера вердикт `UNVERIFIED`, а не «находок нет»
  (`spec.md:278`). Два прогона такого ревью уже были и оба дали `REFUTED` (коммиты `46e6c7f`,
  `8c7cc6e`), но на **текущем** состоянии дерева ревью не проводилось.

**Известный остаток по периметру рана** (`spec.md:433-442`): `_run_scoped_pids_or_none` возвращал
`None` и когда снапшот построить нельзя, и когда он **пуст**; оба случая трактовались как «периметр
не установлен», то есть мутация не запрещалась. Для непросеянного рана это обязательно — иначе
ломается ленивый сидинг, который `test_action_trustline_close_seeds_when_run_paused` пинит как
намеренное поведение; но у рана с честно пустым снапшотом дыра оставалась. **Внимание: в коде остаток
уже закрыт, а в спеке — нет.** Коммит `1e86c18` перевёл пустой периметр в fail-closed на том
основании, что `_ensure_run_seeded` вызывается до вычисления периметра (`app/api/v1/simulator.py:959`,
`:1189`, `:1351`, `:1512`), то есть аргумент про ленивый сидинг не держится. Текст `spec.md:433-442`
этого не отражает и по-прежнему цитирует `return pids or None` (`:435`).

## Записи для других программ

Раздел `spec.md:280-309`:

- **011 (канон).** Новых кодов **отказа** не появилось — используется существующий
  `TRUSTLINE_EXISTS`/`ConflictException`. Появилось новое **успешное** состояние: повторный
  `TRUST_LINE_CREATE` после закрытия возвращает новую линию с новым `id`; канон обязан описать это как
  штатный исход, а не как конфликт (`spec.md:282-285`).
- **013 (фронтенд), запись первая.** `app/api/v1/simulator.py:1974` зашивает `status="active"` в
  элементы `trustlines-list`, поэтому UI не отличает `active` от `frozen` (`spec.md:286-287`).
- **013 (фронтенд), запись вторая.** `frozen`-линия блокирует создание, но ни один слой UI её не
  показывает (`simulator.py:1912-1917`, `:1974`, `simulator-ui/v2/src/utils/status.ts:7-11`);
  пользователь получает 409 на «свободного» получателя, P2 честности данных (`spec.md:301-304`).
- **Новая находка без назначенного владельца: `clearing-real` обходит периметр рана целиком.**
  `app/api/v1/simulator.py:1681-1749` передаёт в `ClearingService` только эквивалент, а SQL ищет циклы
  по всему эквиваленту без фильтра по участникам рана (`app/core/clearing/service.py:348-400`):
  действие Interact Mode одного рана способно провести клиринг цикла из участников **другого** рана.
  Не регрессия пачки, тот же класс, что `C-A1a-003`, внутри среза не чинится (`spec.md:288-299`);
  заведена коммитом `1e86c18`, программы-владельца у неё пока нет.
- **Документация — исправлено здесь, а не отложено.** `docs/ru/03-architecture.md` объявлял
  `UNIQUE(from, to, equivalent)` безусловно и противоречил §5.1 протокола; приведён в соответствие в
  этом же срезе. EN/PL-копии не каноничны и остаются расходящимися — записано, а не молча оставлено
  (`spec.md:305-309`).

Вне раздела, но касается 009: программа 007 до сих пор держит `T717(а)` открытым долгом с якорем
`app/core/simulator/storage.py:621` (`specs/007-simulator-analytics-surface/spec.md:220`), тогда как
именно этот сайт закрыт задачей `T902` программы 009 (`spec.md:270`).

## Состояние волны 009–014

Счёт по строкам таблиц `## Tasks` каждой спеки (`awk '/^\| *T[0-9]+ *\|/'`):

| # | Спека заведена | Строк задач | `[x]` | `[ ]` | `[!]` | Реализация |
|---|---|---|---|---|---|---|
| 009 | да, `specs/009-p1-reproducers-first/spec.md` | 11 (`:268-278`) | 6 | 2 | 3 | **начата**: три P1 закрыты, работа в ветке `codex/009-reproducers` |
| 010 | да, `specs/010-money-core-fail-closed/spec.md` (плюс `measure_swallowed_exceptions.py`) | 8 (`:252-259`) | 0 | 0 | 8 | не начата |
| 011 | да, `specs/011-canon-describes-what-it-returns/spec.md` | 10 (`:241-250`) | 0 | 0 | 10 | не начата |
| 012 | да, `specs/012-money-precision-and-representation/spec.md` | 12 (`:310-321`) | 0 | 0 | 12 | не начата |
| 013 | да, `specs/013-frontend-data-honesty/spec.md` | 9 (`:260-268`) | 1 | 0 | 8 | не начата; единственный `[x]` — `T1300`, перепроверка находок внешним ревью |
| 014 | да, `specs/014-false-green-guards/spec.md` | 12 (`:270-281`) | 0 | 0 | 12 | не начата |

Реестр программ (`specs/README.md:32-37`) для всех шести всё ещё пишет «реализация ждёт отдельной
авторизации», хотя авторизация владельцем дана 2026-08-21 по всей волне (`specs/README.md:114`) и
записана коммитом `4a995e9`. **Реестр отстал от факта;** строка 009 дополнительно не отражает, что три
P1 закрыты.

Порядок исполнения зафиксирован номером: 009 → 010 (общий гейт Postgres, идут подряд) → 011 → 012 →
013 → 014 (`specs/README.md:79-91`), обоснование первой пары — `specs/README.md:96-99`. Программа 015
— бриф на исследование, начинается **после** закрытия волны и её не блокирует
(`specs/README.md:123-127`).

Вход 015, который волна обязана не сломать: заявленная защита нулевой суммы структурно не способна
упасть — `_compute_imbalance` (`app/core/invariants.py:62-80`) даёт тождественный ноль при любых
`amount` и ловит только висячий `creditor_id`/`debtor_id`, а `GET /integrity/status` публикует это как
пройденную проверку (`specs/015-financial-core-verification/research-brief.md:16-29`); вывод брифа —
исполняемого доказательства соответствия денежного результата протоколу сегодня нет (`:35-37`).
Второе установленное — расхождение подписанной и записанной суммы, **наблюдённое на живой Postgres**,
а не выведенное из кода: валидация принимает scale до 18 (`app/utils/validation.py:91`), хранилище —
`Numeric(20, 8)`, и Postgres **округляет в обе стороны**, вплоть до «сохранено больше поданного» и
исчезновения суммы (`research-brief.md:39-57`).

Связь 009 с 015 уже не гипотетическая: миграция `019` заставила закрытую и живую инкарнации делить
пару `(from, to)`, из-за чего чек-поинт целостности стал недетерминированным, и это чинилось внутри
009 (`app/core/integrity.py`, коммит `1e86c18`). То есть волна способна менять именно тот измеритель,
на котором 015 собирается строить доказательство, — при передаче в 015 это надо назвать явно.

## Что делать дальше

1. **Догнать спекой код — до всего остального.** Коммит `1e86c18` изменил поведение в пяти местах, а
   в `spec.md` попала одна запись про `clearing-real`. Нужны: запись в `## Changelog` о третьем ревью
   и его шести блокерах, явное закрытие «известного остатка» по периметру (`spec.md:433-442`) и
   решение, меняет ли гард `TRUST_LINE_UPDATE` запись для 011 (`spec.md:282-285`) — это новый
   объявленный отказ на поверхности, для которой программа уже утверждала, что новых кодов отказа
   нет. Незаписанная правка — ровно тот класс расхождения, который программа каталогизирует.
2. **Прогнать `T909` (внутренний adversarial), затем `T910` (внешнее ревью) на HEAD `1e86c18`.** Три
   прежних прогона дали `REFUTED` подряд и каждый нашёл настоящий дефект пачки: непромигрированных
   потребителей уникальности (`46e6c7f`), ложноположительный классификатор (`8c7cc6e`),
   недетерминированную чек-сумму целостности и патчабельную закрытую линию (`1e86c18`). Кривая ещё не
   вышла на ноль, срез трогает transaction boundaries и целостность — ревью обязательно по §15
   `AGENTS.md` (`spec.md:278`).
3. **Закрыть `T908` и слить в `main`.** Владелец прямо потребовал сливать чаще и назвал накопление в
   ветке дефектом процесса (`specs/README.md:215-217`); в отрыве уже пять коммитов, включая миграцию
   `019`, смену семантики уникальности и правку чек-суммы целостности.
4. **Принять решение по `T905`/`RT-009-5` явно, а не оставлять в `[ ]`.** Либо построить настоящий
   стенд (обрыв соединения или конкурентное удаление между `commit()` и `refresh()`), либо предъявить
   владельцу вывод, что достижимость отказа недоказуема имеющимися средствами, и вынести `F-009-6`
   отдельно. `monkeypatch` закрыт правилом (`spec.md:256-260`) и обходить его нельзя.
5. **Привести реестр в соответствие и открывать 010.** Обновить `specs/README.md:32-37`, закрыть долг
   007 `T717(а)` ссылкой на `T902` (`specs/007-simulator-analytics-surface/spec.md:220`), направить
   владельцу за диспозицией находку `clearing-real` (`spec.md:288-299`) — программы у неё нет — и
   начинать 010, следующую по очереди и делящую с 009 гейт Postgres (`specs/README.md:96-99`).

# 009 — Три P1: репродьюсер первым

- **Date:** 2026-08-21
- **Status:** AUTHORIZED, **реализация авторизована владельцем 2026-08-21**. В работе: `F-009-3` и
  `F-009-4` закрыты, их репродьюсеры зелёные. Якоря находок перепроверены на HEAD `35ef1f2` 2026-08-21.
- **Status authority:** метка описательная; завершённость устанавливают success criteria и записанные
  evidence, а не поле статуса.
- **Owner surface:** `app/api/v1/simulator.py`, `app/core/simulator/storage.py`,
  `app/core/trustlines/`, `app/db/models/trustline.py` (чтение), `simulator-ui/v2/src` — только
  подсказка существования trustline. **Запрещено:** `app/core/payments/`, `app/core/clearing/`,
  `app/core/recovery.py` — их владелец программа 010.
- **Origin:** маршрутизация класса «в спеку» программы 008 (`008/tasks.md`, раздел «Маршрутизация
  класса „в спеку“ в программы 009–014»), авторизованная владельцем 2026-08-21.
- **Почему это одна программа:** не «три P1 в мешке». Три из шести находок лежат в **одном файле** —
  `app/api/v1/simulator.py`: `C-A1a-003` (`:611-632`), `B-A1a-016` (`:983-1015`), `PAR-A1a-001`
  (`:1015`, `:1199`, `:1361`). Разрезав их на отдельные программы, мы трижды открываем один файл, а
  раздел «Пересечение по файлу» в `specs/README.md` одновременные волны по одному файлу запрещает.
  Общего у набора ещё два свойства: единый гейт (Postgres) и единый метод — **падающий репродьюсер
  раньше правки**, потому что у всех трёх P1 механизм доказан чтением, а эффект не наблюдался.

## Problem

Программа 008 прочитала весь продуктовый код и подняла до P1 ровно три находки. Ни у одной из них
**нет падающего теста**. Это худшее из возможных состояний реестра: severity назначена, цена заявлена,
а доказательство держится на чтении кода. По §5 `docs/codex-orchestrator-rule.md` расхождение формы без
воспроизведённого эффекта не является подтверждённым P1 — то есть сегодня три строки реестра либо
завышены, либо описывают реальный вред, который никто не измерил. Оба исхода стоят дорого: первый
обесценивает реестр, второй оставляет вред в проде.

Дополнительно: одна из трёх находок за время фазы I **сменила сайт**, и запись об этом в реестре не
появилась (см. `F-009-2`). Реестр, чьи якоря не перепроверяются, деградирует молча — ровно тот класс,
который 008 каталогизирует на тестах.

## Owner surface

Разрешено править: три сайта `app/api/v1/simulator.py`, один сайт `app/core/simulator/storage.py`,
гард и обработку конфликта в `app/core/trustlines/`, подсказку существования линии в
`simulator-ui/v2/src`.

Запрещено править: `app/core/payments/`, `app/core/clearing/`, `app/core/recovery.py`. Эти поверхности
остаются read-only и после закрытия 008 — их владелец программа 010. Если репродьюсер 009 упрётся в
них, это **находка для 010**, а не расширение скоупа.

Запрещено менять `api/openapi.yaml` в части графовых схем и необъявленных статусов — владелец
программа 011. Пересечение реально: правка `F-009-3` меняет код ответа на конфликт, а объявление
этого кода в каноне — работа 011. Порядок разрешения: 009 фиксирует **поведение** и заводит запись
для 011, 011 приводит канон.

## Findings

Все якоря перепроверены по коду 2026-08-21 на HEAD `35ef1f2`. Дата проверки указана у каждой,
потому что часть за время фазы I сместилась.

**`F-009-1` — `C-A1a-003`, P1. Мутирующие interact-действия резолвят участника вне скоупа рана.**
`app/api/v1/simulator.py:611-632`, `_resolve_participant_or_error`:
`select(Participant).where(Participant.pid == pid_s)` — глобальная таблица, без привязки к рану.
Read-эндпоинты того же семейства scoped по снапшоту рана. Асимметрия read/write внутри одного
семейства — дефект, а не решение. Проверено 2026-08-21: якорь сместился с `:608-629`, механизм не
изменился. Эффект (кросс-ран мутация) **не воспроизведён** — это первая задача программы.

**`F-009-2` — `B-A2b-001`, P1. Откат чужой сессии тика с последующим ложным `on_commit`.**
**Якорь изменился, и это надо было заметить раньше.** Волна 3 записала
`app/core/simulator/storage.py:395-407` и `:491-501`. На HEAD ни один из них дефекта не описывает:
`write_tick_metrics` получила SAVEPOINT срезом T715 программы 007 (`storage.py:320`, комментарий
`:333`, реализация `:514-548`), а второй якорь указывает на путь, где сессия **своя** (`:496-498`,
комментарий «This session is ours»).

Живой сайт один — `write_tick_bottlenecks`, `app/core/simulator/storage.py:635-643`: на любом
исключении делается `logger.exception`, затем `await session.rollback()`, причём отказ самого
`rollback()` накрыт `except Exception: pass`, и функция возвращает управление молча.

`session` здесь — сессия тика, переданная владельцем. Откат уничтожает весь незакоммиченный хвост
тика, после чего владелец коммитит и запускает `on_commit` (`real_tick_persistence.py:138`), то есть
**сообщает об успехе**. Это ровно то, что программа 007 записала как незакрытый долг `T717(а)` и
сознательно не чинила, чтобы не забирать решение у владельца.

Замечание, важное для 010: тот же фрагмент несёт **и** паттерн `T809-B1` — проглоченный отказ
`rollback()`. Здесь он вне денежного ядра и чинится в 009; в `payments/engine.py:538-541` — в 010.
Одна форма, два владельца, и это не дублирование: денежное ядро имеет собственный гейт.

**`F-009-3` — `B-A3-004`, P1. ЗАКРЫТА 2026-08-21. Дефект оказался в констрейнте, а не в гарде.**

`app/core/trustlines/service.py`: гард дубликата отбирал `TrustLine.status != 'closed'`, `close()`
строку не удалял, а ставил `status='closed'`, и уникальность была объявлена **без** `status`
(`app/db/models/trustline.py`, применённая миграция `001_initial_schema.py:103-108`). `IntegrityError`
в `app/core/trustlines/` не перехватывался, в `main.py` зарегистрированы только `GeoException` и
`RequestValidationError` → HTTP 500.

**Первая редакция спеки поставила диагноз неверно, и это исправлено внешним разбором.** Раздел
`Intended` предписывал «объявленный конфликт вместо 500», то есть предлагал привести **гард** к
констрейнту. Проверка протокола показала обратное:

- предусловие `TRUST_LINE_CREATE` — «Не существует **активной** линии `(from, to, equivalent)`»
  (`docs/ru/02-protocol-spec.md:333`), то есть закрытая инкарнация создание **не блокирует**;
- `TRUST_LINE_CLOSE` **устанавливает** `status='closed'` и сохраняет строку (`:379`), то есть закрытая
  линия — это история, а не занятый слот;
- операции `REOPEN` в §5 нет, и она не нужна: новая линия — это новая инкарнация с новым `id`.

Значит **строже протокола была база**, а исходный предикат гарда был верен. `docs/ru/03-architecture.md:500`
объявляет `UNIQUE(from, to, equivalent)` безусловно и противоречит §5.1 протокола.

**Правка.** Миграция `019_trust_lines_partial_unique_live` заменяет безусловный констрейнт частичным
уникальным индексом `WHERE status <> 'closed'`. Гарды возвращены к протокольной форме, а гонка
«два параллельных create» ловится как `IntegrityError` и отдаётся объявленным 409, а не улетает наружу.

Проверено на PostgreSQL 16: индекс создан как `WHERE ((status)::text <> 'closed'::text)`; повторное
создание даёт **новую инкарнацию с новым `id`**, закрытая строка сохраняет свой `id`, живая линия
ровно одна.

**`F-009-4` — `B-A1a-016`, наследует severity `F-009-3`. ЗАКРЫТА 2026-08-21.**
`app/api/v1/simulator.py` — второй, независимый call-site: маршрут Interact Mode собирает `TrustLine`
сам, в сервис не заходит, и потому правка сервиса его не закрывала. Репродьюсер `RT-009-4` доказал
это отдельно: маршрут ронял `IntegrityError` мимо всякой обработки.

Правка симметрична сервисной: протокольный предикат плюс перехват `IntegrityError` → объявленный 409.

**`F-009-5` — `B-B3-2-003`. ВЕРДИКТ `REFUTED` 2026-08-21. Находка растворилась вместе с ошибкой диагноза.**

Формулировка была: подсказка `' (exists)'` показывает только незакрытые линии, поэтому после закрытия
получатель «выглядит свободным» — и это подводит пользователя к пути `F-009-3`.

**Вердикт сужен до варианта `closed` по внешнему ревью пачки — blanket-снятие было неточным.**

- Для **закрытой** линии подсказка верна: закрытая линия создание не блокирует
  (`docs/ru/02-protocol-spec.md:333`), значит получатель действительно свободен. В этой части
  находка существовала только внутри неверного предположения о необратимости — `REFUTED`.
- Для **`frozen`** подсказка **неверна и сегодня**: индекс и оба гарда считают `frozen` живой, то есть
  она создание **блокирует** с 409, а UI её не показывает — `app/api/v1/simulator.py:1912-1917`
  отбрасывает всё, кроме литерального `active`, и `simulator-ui/v2/src/utils/status.ts:7-11` считает
  активным только его. Это **P2 честности данных**, а не целостности; владелец — программа 013, и он
  зарегистрирован в разделе «Записи для других программ».

Проверено на HEAD: `simulator-ui/v2/src/components/TrustlineManagementPanel.vue:226-260` строит
подсказку через `isActiveStatus` (`src/utils/status.ts:7-11`).

**Отдельное наблюдение, найденное при проверке и НЕ подменяющее снятую находку.**
`app/api/v1/simulator.py:1974` — `action_trustlines_list` **зашивает `status="active"`** в каждый
возвращаемый элемент. Следствие: UI не способен отличить `active` от `frozen` ни при каких данных, а
`frozen`-линия создание блокирует. Это другой дефект, другой поверхности и другого владельца —
записан в `## Записи для других программ`, а не зачтён как закрытие `F-009-5`.

**`F-009-7` — `C-A4-4-013`, P2. Набор знает про `F-009-3` и маршрутизирован в обход.**
`tests/unit/test_interact_actions_backend_p1.py:376-377` дословно: «use a different equivalent code
to avoid DB-level UNIQUE constraint (trust_lines is unique on from/to/equivalent **regardless of
status**)». Это формулировка самого дефекта, записанная в тесте как обходной манёвр. Соседний тест
`:227-240` покрывает симметричную **безопасную** ветку (создание поверх активной → чистый 409),
из-за чего сьют выглядит покрывающим оба исхода.

Находка входит в 009, а не в 012, куда её отправлял первый вариант маршрута в составе неделимой
группы `S`: она описывает **предусловие именно этого P1**, и после правки `F-009-3`/`F-009-4`
обходной комментарий обязан исчезнуть вместе с обходом. Оставить её в 012 значит запланировать окно,
в котором дефект исправлен, а тест по-прежнему обходит несуществующее ограничение. Основание для
переназначения — внешнее ревью Codex 2026-08-21, перепроверено по коду.

**`F-009-6` — `PAR-A1a-001`, P2. `commit()` → `refresh()`: ошибка по уже зафиксированной операции.**
`app/api/v1/simulator.py:1015-1016`, `:1199-1200`, `:1361-1362` — три мутации на одном шаблоне.
Отказ `refresh` после успешного коммита отдаёт клиенту ошибку по операции, которая **уже
произошла**; повтор не идемпотентен. Проверено 2026-08-21: все три пары на месте — реестр 008
называл один сайт «и два соседних», теперь они перечислены поимённо. Эффект чтением доказан,
наблюдением — нет; гейт Postgres.

**ИСПРАВЛЕНО 2026-08-22: пар не три, а восемь.** Внутренний adversarial-скан на слитом `main`
перечислил их поимённо, и счёт «три» был занижен ровно потому, что унаследован из реестра 008 без
собственной инвентаризации. Полный список: `app/api/v1/simulator.py:1087`, `:1276`, `:1443`;
`app/core/trustlines/service.py:246`, `:372`; `app/core/participants/service.py:76`, `:172`;
`app/core/auth/service.py:45`. Обработчиков на `refresh`-отказ нет ни у одной (`main.py` регистрирует
только `GeoException`/`RequestValidationError`), поэтому наружу это HTTP 500 по durable-операции.
Первые три отягощены: после них не выполняется инвалидация кэша роутера и синхронизация снапшота
рана, то есть расхождение БД и рантайма **постоянное**, а не транзиентное. Контрпроверка правки
обязана покрыть все восемь, а не три.

## Current / Intended / Optimal

**Current.** Мутирующее interact-действие резолвит участника глобально; отказ best-effort писателя
bottlenecks откатывает чужой тик и рапортует успех; повторное создание закрытой trustline даёт 500 в
двух независимых местах, а фронтенд показывает получателя свободным; отказ `refresh` после коммита
выглядит как отказ операции.

**Intended.** Мутация видит ровно тот периметр, что и чтение того же семейства. Best-effort писатель
не имеет права трогать чужую транзакцию: он либо изолирован savepoint'ом, либо отказывается, но не
рапортует успех за владельца. Повторное создание закрытой линии — объявленный конфликт, а не 500, и
пользователь видит, что линия существует. Клиент получает ошибку только о том, что не произошло.

**Optimal.** Правка каждой позиции **следует** за тестом, который на текущем коде красный. `F-009-2`
закрывается тем же приёмом, что уже применён рядом в T715 (SAVEPOINT), а не изобретением второго
механизма: соседний путь в том же файле уже показывает форму решения, принятую владельцем.

## Non-goals

- **Не сплошная правка класса.** Программа чинит перечисленные сайты. 82 голых `except` в `app/` и
  46 из них в денежном и recovery-ядре — знаменатель, принадлежащий 010; §4 п. 5 и п. 8 `AGENTS.md`
  сплошную правку запрещают.
- **Не правка денежного ядра.** Ни одного файла из `payments/`, `clearing/`, `recovery.py`.
- **Не приведение канона.** Новые коды ответов объявляет в `openapi.yaml` программа 011; 009
  оставляет для неё явную запись (T906).
- **Не решение о run boundary как продуктовой границе.** `F-009-1` фиксирует асимметрию read/write.
  Если репродьюсер покажет, что глобальный резолв **намеренный**, программа обязана предъявить это
  владельцу как продуктовое решение, а не тихо переписать поведение.
- **Не расширение на группу `Q` и на остаток группы `S`.** Группа `S` **делится по владельцам**, а не
  уезжает целиком: `C-A4-4-013` — здесь (`F-009-7`), `C-B4-3-002`, `C-B4-3-009` и
  `MISSED-B4-NUMFMT-FIXED2` — в 012 вместе с денежным форматированием, которое они закрепляют,
  `B-A4-5-001` и `C-A4-5-005` — в 014. Тесты, кодирующие старое денежное форматирование, 009 не
  трогает.

## Verification plan

**1. Репродьюсеры, обязанные падать на текущем коде.** Ни одна правка не начинается раньше своего
красного теста. Зелёный до исправления тест здесь не доказывает ничего — именно из-за его отсутствия
три P1 сегодня держатся на чтении.

| ID | Что обязан показать | Гейт |
|---|---|---|
| `RT-009-1` (= `T803-RT3`) | два рана на разных сценариях; мутирующее действие с чужим `pid` наблюдаемо меняет состояние вне своего рана | Postgres |
| `RT-009-2` | отказ внутри `write_tick_bottlenecks` на **чужой** сессии: хвост тика исчезает, а `on_commit` владельца всё равно исполняется | Postgres |
| `RT-009-3` | create → close → create тем же тройником через `app/core/trustlines/` даёт 500 и неустранимое через API состояние | Postgres |
| `RT-009-4` | тот же путь целиком внутри Interact Mode (`POST …/actions/trustline-create`) — вторая копия | Postgres |
| `RT-009-5` | отказ `refresh` после успешного `commit` отдаёт клиенту ошибку по зафиксированной операции; повтор не идемпотентен. **Механизм отказа обязан быть настоящим** — обрыв соединения или конкурентное удаление строки между `commit()` и `refresh()`; подмена `refresh` исключением доказывает искусственную ветку, а не дефект | Postgres |
| `RT-009-6` | после правки `F-009-3`/`F-009-4` обходной манёвр в `tests/unit/test_interact_actions_backend_p1.py:376-377` перестаёт быть нужен: тест, поданный тем же тройником без смены `equivalent`, обязан пройти | Postgres |

**2. Инварианты, обязанные выжить, и контрпроверки.** У каждого правила, что-либо исключающего,
обязана быть контрпроверка (§9 anti-vacuum):

- Мутация и чтение одного семейства видят один периметр — контрпроверка: тест обязан падать, если
  скоуп снят с **любой** из двух сторон, а не только с мутирующей.
- Владелец сессии тика остаётся единственным, кто её коммитит и откатывает — контрпроверка: подмена
  savepoint'а на прямой `session.rollback()` обязана ронять тест.
- Успех тика не публикуется, если его хвост откачен — контрпроверка: тест обязан падать при
  возвращении `return` без сигнала владельцу.
- Закрытая trustline остаётся видимой как существующая и на бэкенде, и в подсказке UI —
  контрпроверка: правка только бэкенда обязана оставить тест UI красным.
- **Исправлены все восемь пар `commit()`/`refresh()`, а не одна** — контрпроверка: инвариант
  проверяет каждую отдельным случаем, и возврат **любой** из них к прежней форме обязан ронять тест.
  Без этого правка одного сайта выглядит закрытием `F-009-6`. Счёт исправлен 2026-08-22 с трёх на
  восемь — см. запись в `F-009-6`; прежние три были унаследованы из реестра 008 без пересчёта.

**3. Существующие селекторы, обязанные остаться зелёными.** Поимённо — тир не является селектором:

- `tests/integration/test_concurrent_prepare_routes_bottleneck_postgres.py::test_concurrent_payments_shared_bottleneck_commit_once_postgres`
- `tests/integration/test_concurrent_clearing_payment_lost_update_postgres.py::test_concurrent_payment_and_clearing_same_trustline_preserve_effects_postgres`
- `tests/integration/test_payment_idempotency_postgres.py::test_concurrent_duplicate_payment_request_never_regresses_terminal_state_postgres`
- `tests/unit/test_interact_actions_backend_p1.py` — прямая поверхность `F-009-4` и `F-009-7`
- `tests/integration/test_trustlines_get_by_id.py`, `tests/integration/test_trustline_negative_constraints.py`,
  `tests/integration/test_trustline_cache_invalidation.py`, `tests/unit/test_trustline_audit_fail_closed.py`
  — поверхность `F-009-3`
- `tests/contract/test_openapi_contract.py` — контракт не должен разъехаться до работы 011
- `simulator-ui/v2/src/components/TrustlineManagementPanel.*` unit-покрытие — поверхность `F-009-5`

Тиры целиком остаются гейтом, но **не** заменяют список выше: дефолтный backend, Postgres marker-тир,
матрица конкурентности Phase 2, `npm --prefix simulator-ui/v2 run test:unit`,
`npm --prefix admin-ui run test`.

Числа прохождения здесь сознательно **не приводятся**: `AGENTS.md:55` запрещает хранить в документах
протухающие значения. Владелец значения — сама команда; базовая линия снимается прогоном на HEAD
перед началом работ.

**4. Запрещённые способы доказательства.**

- **Запрещено** синтетически инжектировать `IntegrityError`/`DBAPIError` вместо реального повторного
  создания закрытой линии: уникальный индекс объявлен в применённой миграции, значит эффект обязан
  приходить от настоящей БД. Прецедент запрета — программа 003.
- **Запрещено** доказывать `F-009-2` тест-даблом сессии. Дефект в том, **чья** сессия и кто её
  коммитит; дабл, у которого владелец один, воспроизводит не тот механизм. 008 уже ловила находку,
  замаскированную недостоверным даблом (`F-003-4`).
- **Запрещено** засчитывать SQLite за гейт по всем пяти RT: предикат ретрая и serialization failures
  на нём не воспроизводятся (`008/plan.md` §5).
- **Запрещено** закрывать `F-009-5` правкой только бэкенда — см. контрпроверку выше.
- **Запрещено** доказывать `F-009-6` подменой `refresh` исключением через monkeypatch. Такой тест
  красный на текущем коде и потому выглядит валидным репродьюсером, но доказывает существование
  ветки `except`, а не то, что отказ **достижим после уже зафиксированного коммита**. Требуется
  настоящее расписание — обрыв соединения или конкурентное удаление строки. Это тот же запрет, что
  003 наложила на синтетическую инъекцию DBAPI-ошибок.

## Tasks

Легенда: `[x]` выполнено · `[ ]` в работе · `[!]` заблокировано или не авторизовано.

| ID | Задача | Статус |
|---|---|---|
| T900 | Репродьюсеры. `RT-009-1`, `RT-009-2`, `RT-009-3`, `RT-009-4`, `RT-009-6` — написаны, были красными, закрыты правками. `RT-009-7` (гонка) добавлен по внешнему ревью. **`RT-009-5` построен 2026-08-22** (`tests/integration/test_p1_commit_then_refresh_postgres.py`) — отказ достижим, а не выведен из кода. Добавлены `RT-009-8` (ложный успех записи тика) и `RT-009-9` (протухшая схема SQLite) | `[x]` 2026-08-22 |
| T901 | `F-009-1`: мутирующие действия резолвят участника в периметре рана, как и чтение того же семейства. Намеренности не обнаружено: читающий эндпоинт скоупится, мутирующий нет — асимметрия внутри одного семейства | `[x]` 2026-08-21 |
| T902 | `F-009-2`: `write_tick_bottlenecks` изолирует свой отказ SAVEPOINT'ом по образцу T715 и больше не откатывает сессию тика. **Дополнено 2026-08-22:** SAVEPOINT закрыл побочный ущерб, но не то, чем находка названа — ложный рапорт об успехе. Оба писателя возвращают исход, оба вызывающих помечают тик записанным только при успехе; репродьюсер `RT-009-8`, проверен мутацией | `[x]` 2026-08-22 |
| T903 | `F-009-3` + `F-009-4`: оба call-site предиката `status != 'closed'` и обработка конфликта уникального индекса как объявленного отказа | `[x]` 2026-08-21 |
| T904 | `F-009-5`: **снята, вердикт `REFUTED`.** При правильном прочтении протокола active-only подсказка верна — закрытая линия создание не блокирует. Правки нет и не требуется | `[x]` 2026-08-21 |
| T905 | `F-009-6`: **ЗАКРЫТА 2026-08-22.** `RT-009-5` построен, отказ показан настоящим; правка — одно правило на все восемь пар: успех полностью материализуется внутри незакоммиченной транзакции, после коммита обязательных чтений нет. Контрпроверка двойная: Postgres-стенд на create-пути и структурный инвариант по всем восьми сайтам, оба проверены мутацией | `[x]` 2026-08-22 |
| T906 | `F-009-7`: обход в `tests/unit/test_interact_actions_backend_p1.py` **снят**. Тест больше не переключается на другой эквивалент, а проходит настоящий путь create → close → create тем же тройником. Промежуточная запись «задача недостижима» была следствием неверного варианта и отменена | `[x]` 2026-08-21 |
| T907 | Записи для других программ готовы — см. раздел «Записи для других программ». Новых кодов **отказа** не появилось; появилось новое штатное **успешное** состояние повторного создания | `[x]` 2026-08-21 |
| T908 | Гейты матрицы `008/plan.md` §6; milestone `verify_local.ps1` + Postgres-тир. **Пройдены 2026-08-22 на слитом `main` (`72b8030`)**: backend-тир, единственная голова Alembic `019`, lint/typecheck/тесты/сборка обоих UI, Postgres 16.9 со схемой на голове миграций. Сборка `simulator-ui/v2` прогнана отдельным шагом — `pwsh` на машине нет, дефект окружения, а не кода (`prebuild` зовёт его, волна этот путь не трогала). Отдельно проверено, что все три Postgres-репродьюсера волны **отбираются** маркером, а не отсеиваются молча. **Повторить после последней правки** — гейты закрывают состояние, а не намерение | `[x]` 2026-08-22 |
| T909 | **Внутренний adversarial review** субагентом. **Проведён 2026-08-22 на `72b8030`** двумя непересекающимися срезами: (A) потребители снятого инварианта, миграция `019`, расхождения docs/runtime; (B) транзакционные границы, периметр рана, гарды закрытой линии, классификатор конфликта, ложнозелёные тесты. Заданием был список **форм** дефекта, а не просьба оценить качество. Итог: девять находок в срезе A, десять в срезе B; все несущие перепроверены мной по коду до интеграции | `[x]` 2026-08-22 |
| T910 | **Независимое внешнее ревью и публикация evidence на точном HEAD** по `docs/external-review-runbook.md` §3. Обязательно: программа трогает transaction boundaries и целостность (§15 `AGENTS.md`), образцы — T307 в 003, T408 в 004. При сбое ревьюера — вердикт `UNVERIFIED`, а не «находок нет» | `[!]` |

## Записи для других программ

- **011 (канон).** Три записи, а не одна — исправлено 2026-08-21, прежняя формулировка «новых кодов
  отказа не появилось» устарела в тот же день:
  1. **Новое штатное успешное состояние:** повторный `TRUST_LINE_CREATE` после закрытия возвращает
     новую линию с новым `id`. Канон обязан описывать это как норму, а не как конфликт.
  2. **Новый объявленный отказ `TRUSTLINE_CLOSED`** (HTTP 409): `TRUST_LINE_UPDATE` и
     `TRUST_LINE_CLOSE` отвергают закрытую линию — `app/core/trustlines/service.py`, гарды в
     `update()` и `close()`. Протокол §5.2 этого требовал, реализация не проверяла.
  3. **`CONCURRENT_TRUSTLINE_CREATE`** как `details.reason` при гонке — тот же HTTP 409.
- **013 (фронтенд).** `app/api/v1/simulator.py:1974` зашивает `status="active"` в элементы
  `trustlines-list`, поэтому UI не отличает `active` от `frozen`. Найдено при снятии `F-009-5`.
- **НОВАЯ НАХОДКА, не чинится здесь: `clearing-real` обходит периметр рана целиком.**
  `app/api/v1/simulator.py:1681-1749` передаёт в `ClearingService` только эквивалент, а SQL ищет
  циклы по **всему** эквиваленту без фильтра по участникам рана
  (`app/core/clearing/service.py:348-400`). То есть действие Interact Mode одного рана способно
  провести клиринг цикла, состоящего из участников **другого** рана.

  Найдено внешним ревью 2026-08-21 при проверке правки `F-009-1` и перепроверено по коду. Это **не**
  регрессия текущей пачки — маршрут был таким и до неё; правка периметра резолвера его не касается,
  потому что он резолвер не использует. Заводится как отдельная находка того же класса, что
  `C-A1a-003`, и **не чинится внутри этого среза**: денежная операция по чужому периметру требует
  собственного репродьюсера и собственного гейта, а расширять срез на неё значит повторить ошибку,
  от которой предостерегает §4 `AGENTS.md`.

- **013 (фронтенд), вторая запись.** `frozen`-линия блокирует создание, но ни один слой UI её не
  показывает: `app/api/v1/simulator.py:1912-1917` отбрасывает не-`active`, `:1974` зашивает
  `status="active"`, `simulator-ui/v2/src/utils/status.ts:7-11` считает активным только литерал.
  Пользователь получает 409 на «свободного» получателя. P2 честности данных.
- **Документация — исправлено здесь, а не отложено.** `docs/ru/03-architecture.md` объявлял
  `UNIQUE(from, to, equivalent)` безусловно и противоречил §5.1 протокола. Приведён в соответствие в
  этом же срезе: правка схемы без правки описывающего её документа была бы тем же расхождением, с
  которого начиналась находка. EN/PL-копии (`docs/en/03-architecture.md`, `docs/pl/03-architecture.md`)
  не каноничны и остаются расходящимися — записано, а не молча оставлено.

### Дополнено 2026-08-22 по итогам внутреннего adversarial review

- **`clearing-real` — диспозиция принята: срез программы 010, а не новая программа и не бэклог.**
  Находка подтверждена дважды и независимо: внутренним скан-агентом и консультацией Codex
  (`--sandbox read-only`, `VERDICT-DONE`). Severity **P1**, тот же класс, что `C-A1a-003`: владелец
  одного рана инициирует денежную мутацию чужого периметра. Эффект durable, а не косметический —
  обязательства цикла уменьшаются, нулевые `Debt` удаляются, создаётся `COMMITTED`-транзакция
  клиринга (`app/core/clearing/service.py:1309-1314`, `:1440-1466`, `:1474-1484`). Пользователь рана
  A видит успешный Clearing Preview и `clearing.done` в своём SSE по рёбрам чужого цикла.
  **Почему 010, а не 016:** 010 ещё не начата, уже держит `clearing/service.py` в своей поверхности
  и делит с 009 гейт Postgres. Расширение должно быть **явным** — сегодня 010 разрешает четыре
  core-файла и запрещает `app/api/**`, поэтому в её спеку нужно внести `action_clearing_real`,
  контракт run-scope, репродьюсер и Postgres-гейт. Просто запись в `BACKLOG.md` недостаточна:
  §5 `codex-orchestrator-rule.md` требует явно принятого follow-up для несвязанного P1.
  **Развилки владельца нет:** маршрут вида `/runs/{run_id}/...` уже проверяет конкретный ран, а
  пользовательский контракт Interact Mode привязан к рану; выбор между allowlist-параметром,
  scoped SQL и отдельным методом сервиса — техника.

- **010, вторая запись: периметр не доходит до денежного пути.** `action_payment_real` скоупит
  `from_pid`/`to_pid` (`app/api/v1/simulator.py:1521`, `:1526`), но `PaymentRouter` строит граф по
  всему эквиваленту, поэтому платёж внутри рана A маршрутизируется через промежуточного участника
  рана B: его `debts` меняются, его доверие расходуется, а маршрут возвращается в SSE рана A, раскрывая
  чужие pid. P2. Тот же класс, что `clearing-real`, и чинить их нужно одним правилом, а не по
  отдельности — иначе периметр снова окажется закрытым только на входе.

- **013, третья запись: непроскоупленные чтения того же семейства.**
  `GET /runs/{run_id}/payment-targets` (`app/api/v1/simulator.py:2082`) периметр не вычисляет вовсе и
  перечисляет всех достижимых участников глобального графа — та же асимметрия, что `F-009-1`, только
  наоборот: читающий эндпоинт раскрывает то, что `participants-list` скрывает. P2. Мельче, но того же
  рода: `trustlines-list` с фильтром `participant_pid` отдаёт `200` + пусто для чужого существующего
  pid и `404` для несуществующего, то есть работает оракулом существования (P3).

- **011, четвёртая запись: `POST /trustlines` не объявляет 409.** `api/openapi.yaml:2131-2150`
  перечисляет только `201`, `400`, `401`, тогда как `ConflictException` поднимается на трёх сайтах
  сервиса. Расхождение не внесено этой волной — 409 существовал и до неё, — но волна добавила два
  новых его источника. Шаблон `$ref` на 409 в спеке уже есть в пяти других местах; это ровно предмет
  011, и в её срезе правка стоит одну строку.

- **Долги, сознательно не закрытые здесь** (P3, волну не расширяют, но названы, а не спрятаны):
  1. `_dedupe_trustline_rows` применяется в `admin_graph_ego` **после** фильтра по статусу
     (`app/api/v1/admin.py:2031-2039`), поэтому оператор, запросивший `status=closed`, видит одну
     инкарнацию тройки вместо всей истории закрытий. Дедупликация решает настоящую задачу — не
     задваивать `used`/`available`, — и менять её ради P3-отображения значит рисковать денежным
     экраном; нужен отдельный аккуратный предикат «закрытую скрываем только если есть живая».
  2. `_get_used_amount` возвращает `0` для закрытой линии (`app/core/trustlines/service.py`), из-за
     чего `GET /trustlines?status=closed` рисует мёртвую линию с полным доступным лимитом. Прежнее
     поведение (долг преемника) тоже было неверным; верное — `available=0`, и это выбор для 013.
  3. Ветка `commit=True` в `write_tick_bottlenecks` не имеет SAVEPOINT, тогда как у соседа
     `write_tick_metrics` он есть. Сегодня безвредно, потому что вызывающий коммитит поэквивалентно,
     но асимметрия — ловушка для следующего вызывающего.
  4. Коммит-уровневый `except IntegrityError` в `create()` недостижим: непереносимый уникальный
     индекс не бывает `DEFERRABLE`, поэтому проигравший `INSERT` блокируется и падает на `flush()`,
     а не на `commit()`. Ветка оставлена как defence-in-depth, но её объяснение было неверным.
  5. SQLite-ветка миграции `019` недостижима (`migrations/env.py:26-30` запрещает не-PostgreSQL, и
     это запинено тестом). Настоящий вывод из этого — не «ветка лишняя», а то, что у SQLite-схемы
     **нет пути апгрейда вообще**; он закрыт отдельно, см. `RT-009-9`.

### `F-009-6` — целевое поведение и почему это не развилка владельца

Первый разбор оставлял две несовместимые трактовки — вернуть успех с деградированным телом или
завести новый объявленный отказ, — и это выглядело продуктовой развилкой: разные контракты для
клиента, разные последствия. Консультация с Codex по коду показала, что развилки нет, потому что
исходная посылка ложна.

**Посылка была: чтение после коммита неизбежно.** Оно избежно. `expire_on_commit=False`
(`app/db/session.py:78`), поэтому коммит не истекает уже загруженные атрибуты, а всё, ради чего
делался `refresh()`, — серверные `created_at`/`updated_at` — доступно сразу после `flush()`, то есть
**внутри** транзакции. У трёх маршрутов симулятора и у challenge ответ вообще не содержит полей,
которые даёт `refresh()`.

**Отсюда единственное целевое поведение:** если коммит прошёл durable, клиент получает обычный полный
успех существующей формы. Это ровно то, что программа уже записала как intended behavior — сообщать
об ошибке разрешено только про то, что действительно не произошло.

Почему отвергнуты остальные:
- **деградированное тело** потребовало бы новых частичных response-моделей и правки wire-контракта:
  ответы `Participant` и `TrustLine` объявляют обязательными timestamps, pid'ы, `used` и `available`,
  а гидратация trustline делает отдельные запросы уже после `refresh()`;
- **новый объявленный отказ** сообщил бы универсальному HTTP-клиенту «операция неуспешна», хотя
  доказано обратное; 409 и 500 оба означают неуспех, а текст «не повторять» помогает только
  специально обновлённому клиенту. Плюс объявление нового кода — поверхность 011, которой запрещено
  менять поведение эндпоинтов, а не только описывать его;
- **ретрай `refresh` на новом соединении** — вероятностное смягчение: при окончательном отказе
  остаётся тот же выбор, а runtime-эффекты за `refresh()` всё равно не выполнятся.

**Правило одно на все восемь**, но реализация распадается на четыре механические группы: три маршрута
симулятора (явный `flush`, затем чтение, затем коммит, затем проекция в рантайм и инвалидация кэша);
два сервиса trustlines (перенести `refresh` и гидратацию перед коммитом); два сервиса participants;
challenge. Это не восемь продуктовых решений — различается только способ получить уже утверждённое
полное тело.

**Отдельно зафиксировано, что правилом НЕ закрыто:** доказанный срез — отказ на `SELECT` после
**подтверждённого** коммита. Неопределённость, когда теряется подтверждение самого `COMMIT`, этим
правилом не решена и решённой не объявляется.

**Побочная правка, которую потребовал перенос.** В `action_trustline_create` `refresh()` требует
persistent-объекта, поэтому понадобился явный `flush()` — и он положен **внутрь** того же
`except IntegrityError`. Иначе конфликт уникальности, который на PostgreSQL всплывает именно на
`flush()`, перестал бы быть объявленным 409 и стал бы необработанным 500.

## Changelog

- **2026-08-21.** Программа заведена по маршруту 008, авторизация владельца того же дня. Все шесть
  якорей перепроверены на HEAD `35ef1f2`. Зафиксированы три расхождения с реестром 008:
  `C-A1a-003` сместилась с `:608-629` на `:611-632`; `B-A2b-001` **сменила сайт** — оба записанных
  волной 3 якоря на HEAD дефекта не описывают, живой сайт один и совпадает с долгом `T717(а)`
  программы 007; `PAR-A1a-001` подтверждена на всех трёх парах `commit()`/`refresh()`, а не на одной.

- **2026-08-21, внешнее ревью Codex — вердикт `REFUTED`, шесть правок приняты.** Прогон по
  `docs/external-review-runbook.md` §3: замороженный credential-free клон на `a4d9c58` (0 untracked,
  0 файлов `.env`), `codex-cli 0.144.0-alpha.4`, `--sandbox read-only`, флаг `-m` не передавался —
  фиксируется как дефолт CLI, то есть непроверенный выбор модели. Exit `0`, маркер `VERDICT-DONE`
  получен, вывод полный.

  **Подтверждено ревьюером:** все шесть якорей (раздел A), исключение группы `X` (раздел C) и
  состоятельность границы с денежным ядром (раздел D) — то есть три утверждения, которые я и просил
  опровергнуть, устояли.

  **Принято и исправлено:**
  1. Группа `S` была назначена в 012 **неделимым целым**, и это ошибка: `C-A4-4-013` описывает
     предусловие именно `F-009-3`. Заведена `F-009-7`, добавлены `RT-009-6` и T906. Остаток `S`
     расщеплён между 012 и 014 — правка в маршруте 008.
  2. `RT-009-5` не называл механизм отказа `refresh()`; monkeypatch дал бы красный тест, доказывающий
     искусственную ветку. Механизм задан, а подмена внесена в раздел «запрещено».
  3. У `F-009-6` не было контрпроверки на **все три** пары — добавлена.
  4. Селекторы были перечислены тирами, а не поимённо (`AGENTS.md:397`) — перечислены поимённо.
  5. У таблицы `## Tasks` отсутствовала легенда `[x]/[ ]/[!]` (`AGENTS.md:387`) — добавлена.
  6. Не было отдельной задачи внешнего ревью, обязательной по §15 для программы, трогающей
     transaction boundaries (`AGENTS.md:401`) — заведены T909 (внутренний adversarial) и T910.

  **Уточнено против ревьюера.** Codex перечислил четыре «брата» паттерна проглоченного `rollback()`:
  `storage.py:503-508`, `:544-551`, `real_tick_persistence.py:234-239`,
  `real_tick_orchestrator.py:482-493`. Прочитано мной: три из четырёх после проглатывания делают
  `raise`, то есть отказ **распространяется**, и только `real_tick_orchestrator.py:483-494` логирует
  предупреждение и продолжает. Это градация класса, а не четыре экземпляра одного дефекта; в
  знаменатель 010 они идут с этим различием, а не списком.

  **Не принято к исполнению здесь:** замечание, что заявленные числа passing/deselected ревьюер
  проверить не смог. Они получены прогоном на этой машине 2026-08-21 и записаны в merge-commit
  `03cf6ea`; ревьюер работал `--sandbox read-only` и запускать их не мог по построению.

- **2026-08-21, начало реализации. `F-009-3` и `F-009-4` закрыты; P1 воспроизведён исполняемо
  впервые.** До этого дня находка `B-A3-004` держалась на чтении кода всю программу 008.

  **Репродьюсер `RT-009-3`** (`tests/integration/test_p1_trustline_reopen_postgres.py`, гейт
  Postgres) на текущем коде давал:

  ```
  asyncpg.exceptions.UniqueViolationError: duplicate key value violates unique constraint
  "uq_trust_lines_from_to_equivalent"
  ```

  — необработанный `IntegrityError` из `app/core/trustlines/service.py:105` (`flush`), то есть
  HTTP 500 на двух обычных вызовах одного пользователя.

  **Репродьюсер `RT-009-4`** (добавлен в `tests/unit/test_interact_actions_backend_p1.py`) доказал,
  что второй call-site поражён **независимо**: маршрут Interact Mode собирает `TrustLine` сам и
  роняет `IntegrityError` мимо всякой обработки. Правка сервиса его не закрывает.

  **Правка.** Оба гарда приведены в соответствие с фактическим констрейнтом (он не включает
  `status`), закрытая линия получила собственный код отказа. Форма выбрана как **пересечение всех
  вариантов открытой развилки** — см. раздел «Открытый вопрос владельцу».

  **Поправка к собственной задаче, а не тихое исполнение.** `T906` предписывала снять обходной манёвр
  в тесте. Это оказалось **недостижимо при выбранном поведении**: обход существует ради *успешного*
  создания, которое по-прежнему невозможно. Задача переформулирована на правку комментария; молча
  «выполненной» не объявляется.

  **Гейты:** `RT-009-3` — Postgres marker-тир, зелёный; `tests/unit/test_interact_actions_backend_p1.py`
  — весь файл зелёный; дефолтный backend-тир по трём каталогам — зелёный.

  **Расхождение счёта разобрано, а не списано.** Дефолтный тир дал на 1 passed меньше и на
  1 deselected больше ожидаемого. Причина установлена счётом: селектор `tests/unit,tests/integration,tests/contract`
  не захватывает `tests/test_participants_me_and_auth_payloads.py` — ровно **2** теста. С учётом двух
  добавленных мной числа сходятся точно.

- **2026-08-21, вечер: диагноз `F-009-3` был неверен и переделан. Записываю как ошибку, а не как
  уточнение.**

  Первая правка привела **гард** к констрейнту и объявила повторное создание конфликтом. Внешний
  разбор Codex (клон на `1046c57`, `--sandbox read-only`, маркер `ADVICE-DONE`) показал две вещи,
  обе подтверждённые мной по коду:

  1. **Читать надо было предусловие создания, а не описание закрытия.**
     `docs/ru/02-protocol-spec.md:333` требует отсутствия **активной** линии. Закрытая инкарнация
     создание не блокирует. Значит строже протокола была **база**, и моя правка зацементировала
     противоречие вместо его снятия.
  2. **Спека противоречила сама себе.** `Intended` и `T903` требовали 409, а `RT-009-6` требовал,
     чтобы создание тем же тройником прошло. Acceptance-критерии требовали противоположных исходов, и
     реализовать их одновременно было нельзя.

  **Переделано по варианту E:** миграция `019_trust_lines_partial_unique_live` сужает уникальность до
  живых строк (`status <> 'closed'`), гарды возвращены к протокольному предикату, гонка ловится
  fail-closed. Закрытая линия остаётся историей со своим `id`, новая — новая инкарнация.

  **Следствия, которые пришлось признать:**
  - `F-009-5` получила вердикт **`REFUTED`**: находка существовала только внутри неверного
    предположения о необратимости.
  - `T906` снова достижима, обход в тесте снят; промежуточная запись «недостижимо» отменена.
  - Открытый вопрос владельцу об обратимости **снят**: протокол на него уже отвечает, продуктового
    выбора здесь нет. Вынесение его владельцу было бы ошибкой того же рода — §5
    `codex-orchestrator-rule.md` запрещает поднимать наверх то, что решается чтением канона.

  **Гейты после переделки:** дефолтный backend-тир — `1209 passed, 2 skipped, 54 deselected`,
  exit `0`; Postgres marker-тир — `50 passed, 152 deselected`, exit `0`; pinned Ruff по `app` и
  `migrations` — чисто; единственная голова миграций — `019`. Прирост против базы объясняется счётом:
  +1 unit-тест (`RT-009-4`) и +1 postgres-тест (`RT-009-3`).

- **2026-08-21, продолжение: три P1 закрыты, `RT-009-5` признан непостроенным.**

  **`F-009-1` (`C-A1a-003`) воспроизведён и закрыт.** `RT-009-1` показал асимметрию дословно:
  `participants-list` скрывает участника `mallory`, которого нет в ране, а `trustline-create` с тем же
  `mallory` возвращает `200` и создаёт линию. Намеренности в глобальном резолве не обнаружено —
  читающий эндпоинт того же семейства скоупится по снапшоту, мутирующий не скоупился вовсе. Резолвер
  стал осведомлённым о ране; ленивый сидинг сохранён (перимeтр «не установлен» и «пуст» различаются, и
  первое не запрещает мутацию — иначе сломался бы `test_action_trustline_close_seeds_when_run_paused`).

  **`F-009-2` (`B-A2b-001`) воспроизведён и закрыт.** `RT-009-2` показал уничтожение работы владельца:
  тик складывает свою запись, `write_tick_bottlenecks` падает — и записи больше нет (`1 → 0`), после
  чего владелец коммитит и рапортует успех. Правка — SAVEPOINT по образцу, который срез T715 уже
  применил к соседней `write_tick_metrics`; форма не изобретена, а взята из того же файла.

  **Остаток периметра — ЗАКРЫТ ПОЛНОСТЬЮ 2026-08-22.** История этой записи стоит того, чтобы её
  сохранить целиком, потому что она про метод, а не про строчку кода.

  1. Изначально `_run_scoped_pids_or_none` возвращал `None` и при «нет снапшота», и при «снапшот
     пуст», а `None` **отключает** ограничение. Я оправдывал это ленивым сидингом.
  2. Третье внешнее ревью (2026-08-21) сочло оправдание неверным и предложило своё: `_ensure_run_seeded`
     вызывается **до** вычисления периметра (`app/api/v1/simulator.py:959`, `:1189`, `:1351`, `:1512`),
     значит после сидинга пустой снапшот означает пустой ран. Пустой периметр стал fail-closed. Я
     принял этот довод и записал его сюда.
  3. **Довод ревью тоже был неверен, и это выяснил внутренний adversarial-скан 2026-08-22.** Сидинг к
     значению периметра отношения не имеет вообще: узлы снапшота строятся из `run._scenario_raw`
     функцией `scenario_to_snapshot` (`app/core/simulator/snapshot_builder.py:65-66`), а при пустом
     `equivalent` шаг обогащения из БД выходит сразу (`:88-90`). Вывод был верен, обоснование — нет.
     Настоящая причина проще: `_scenario_raw` проставляется при создании рана, поэтому список
     участников у рана есть всегда, а пустой означает пустой ран.
  4. Оставалась третья ветка, и она была **fail-open**: `except Exception: return None`. Периметр —
     это P1-гард авторизации, и он выключался исключением на единственном пути без покрытия. С
     2026-08-22 функция возвращает пустое множество: неустановимый периметр не впускает никого.

  **Урок, который тут важнее правки.** Верный вывод, стоящий на неверной посылке, — это не «почти
  правильно». Записанная в спеку неверная посылка приглашает следующего редактора вернуть
  разрешающую ветку, и три круга ревью её не заметили, потому что вывод выглядел правильным.

  **`RT-009-5` не построен, и это записано как долг.** Требуется настоящий отказ после durable-коммита
  — обрыв соединения либо конкурентное удаление. Попытка сделать его детерминированным через
  `after_commit`-слушатель не удалась: у тестовой session-фабрики нет `sync_session_class`, а вешать
  слушателя на глобальный `Session` значит перехватывать чужие коммиты. Стенд удалён, а не оставлен
  полуработающим. Monkeypatch `refresh` запрещён разделом «Запрещённые способы доказательства» — он
  доказал бы существование ветки `except`, а не достижимость отказа. Поэтому `T905` **заблокирована
  отсутствием репродьюсера**: правило «правка следует за красным тестом» применяется и к самой
  программе, иначе оно ничего не стоит.

- **2026-08-21, третий круг ревью и независимый скан.**

  **Шесть блокеров третьего ревью исправлены**, два из них про целостность: сортировка чек-суммы
  (`app/core/integrity.py`) была недетерминированной при `closed + live`, а `update()` позволял
  переписать закрытую историческую линию. Плюс уточнён классификатор конфликта (12 контрпроб вместо 8),
  барьер в тесте на гонку перенесён на границу `flush` и проверен мутацией, периметр стал fail-closed.

  **Независимый скан субагентом нашёл то, чего не увидели ни я, ни три круга ревью.** Это стоит
  записи: мой собственный скан трижды объявлял поиск законченным, и трижды это было неправдой.
  Найдено и исправлено:
  - `app/api/v1/admin.py` — граф-снапшот и ego-граф отдавали **два ребра** на тройку и приклеивали
    один и тот же `Debt` к обеим инкарнациям, то есть **задваивали `used`/`available`** на админском
    экране. Восстановлена форма «одно ребро на тройку»: детерминированный порядок с приоритетом
    живой строки плюс дедупликация. Закрытые пары при этом не скрываются;
  - `close()` не имел гарда на закрытую линию, тогда как `update()` его получил — асимметрия
    непреднамеренная; повторное закрытие писало бы свежую запись аудита для давней истории;
  - `_get_used_amount`/`_get_reverse_used_amount` ключуются парой, а не `id`, поэтому **закрытая
    линия показывала долг своего живого преемника** и могла отдать отрицательный `available`.

  **Метод, который сработал:** независимая проверка утверждения «я всё нашёл» стоит дороже самого
  утверждения. Скан велся отдельным агентом с прямым заданием искать шесть конкретных форм
  предположения «одна строка на тройку», а не «посмотри, всё ли хорошо».

- **2026-08-22, четвёртый круг — внутренний adversarial review на слитом `main`, и он снова нашёл
  настоящее.**

  Кривая находок на ноль не вышла и в этот раз. Два непересекающихся среза субагентов на `72b8030`
  дали девятнадцать находок; несущие перепроверены мной по коду до интеграции, как требует §15.
  Закрыто в этом круге:

  1. **Три непокрытых потребителя снятого инварианта.** `scripts/seed_db.py` — два сайта: лукап по
     тройке ронял `MultipleResultsFound` и валил весь сид, а множество «уже существующих троек»
     считало закрытую инкарнацию присутствующей и **молча не создавало** живую линию из фикстуры.
     Это документированный и автоматизированный путь (`README.md`, `docs/en/05-deployment.md`,
     `scripts/run_full_stack.ps1`), спроектированный как повторно запускаемый. Плюс
     `scripts/check_sqlite_db.py` — два джойна без предиката статуса, задваивавшие счётчик нарушений,
     и пагинация trustlines без уникального тай-брейка.

  2. **`F-009-2` был закрыт наполовину.** SAVEPOINT из `T902` остановил разрушение чужой работы, но
     находка названа не этим, а ложным рапортом об успехе. Оба писателя глотали свой отказ и
     возвращали `None`, неотличимое от успеха, после чего тик помечался записанным — а путь повтора
     `flush_pending_storage` выходит рано, если маркер уже на этом тике. То есть транзиентная ошибка
     записи превращалась в **безвозвратную потерю** со следом в одну строку лога. Репродьюсер
     `RT-009-8`; мутация подтвердила, что тест ловит именно контракт, а не обвязку.

  3. **P1-гард периметра был выключен во всех тестах, которые его «доказывали».**
     `_run_scoped_pids_or_none` идёт к рану через реальный реестр рантайма, а фикстура подменяла
     только `runtime.get_run`. Снапшот падал, периметр уходил в `except`, ветка возвращала `None` —
     и `None` **отключает** скоупинг. Единственный тест, который выглядел доказательством, подменял
     `build_graph_snapshot` целиком, то есть продовый путь не трогал вовсе. Из четырёх заскоупленных
     сайтов покрыт был один: снятие `scoped_pids` с трёх остальных оставляло весь файл зелёным.
     Ассерт был `!= 200` — его удовлетворял и HTTP 500 от сломанного периметра, то есть обратный
     желаемому исход. Теперь фикстура регистрирует настоящий ран, периметр строится продовым кодом,
     покрыты все четыре сайта, ассерт проверяет объявленный контракт `404 PARTICIPANT_NOT_FOUND`, а
     ветка `except` стала fail-closed. Мутация: снятие `scoped_pids` роняет все четыре случая.

  4. **P1 не был закрыт на документированном локальном контуре.** Alembic отказывается работать не на
     PostgreSQL, а `create_all` — `checkfirst` и существующую таблицу не трогает, поэтому `local.db`,
     созданный до волны, навсегда сохранял старый безусловный `UNIQUE`. На нём create → close →
     create проходит гард сервиса, спотыкается о **старое** ограничение, и классификатор читает
     трёхколоночное сообщение SQLite как конфликт живой тройки — пользователю отвечают
     «Active trustline already exists» про пару, у которой активной линии нет и уже быть не может.
     P1 там не пережил правку — он **сменил маскировку**. Закрыто перестройкой таблицы
     (`RT-009-9`); лаунчер теперь зовёт скрипт и при существующей БД, иначе правка не срабатывала бы
     ни у кого, кроме тех, у кого проблемы нет.

  5. **Ложноотрицательный в классификаторе конфликта.** Факты о конфликте лежат на **разных** звеньях
     цепочки причин: обёртка SQLAlchemy для asyncpg несёт только `pgcode`/`sqlstate`, а `table_name`
     и `detail` остаются на исходном исключении. Чтение с первого звена с sqlstate давало пустую
     таблицу и сообщение без DETAIL → `False` → HTTP 500 ровно там, где классификатор существует,
     чтобы этого не было. Форма правильного обхода уже есть в этом же репозитории
     (`ClearingService._postgres_error_codes`).

  6. **`RT-009-5` построен** — единственный именованный долг программы. Отказ достижим: настоящее
     убийство бэкенда вторым соединением, `InterfaceError` на `SELECT` рефреша, строка durable, повтор
     конфликтует. Заодно выяснилось, что пар не три, а восемь.

  **Процессная находка, которую стоит записать отдельно.** В дереве лежал байткод
  `tests/integration/__pycache__/test_p1_commit_then_refresh_postgres...pyc` без исходника и без
  коммита в любой ветке. Он доказывает, что файл существовал и был **собран pytest** 2026-08-22 13:35
  — не то, что тест проходил, и это согласуется с записью «стенд не заработал и был удалён». Но
  нетракаемый артефакт в дереве — сам по себе дефект гигиены (`AGENTS.md` §12/§14); удалён.

  **Урок круга.** Три предыдущих круга внешнего ревью и один независимый скан находили настоящий
  дефект каждый раз, и четвёртый не стал исключением. Существеннее другое: в этом круге дважды
  оказалось, что **неверным было обоснование, а не вывод** — и по периметру, и по недостижимой ветке
  `except`. Записанная в спеку неверная посылка переживает ревью именно потому, что вывод выглядит
  правильным, и приглашает следующего редактора «вернуть как было».

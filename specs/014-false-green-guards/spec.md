# 014 — Ложное зелёное: гарды и тесты, не способные упасть

- **Date:** 2026-08-21
- **Status:** 🟢 **РЕАЛИЗАЦИЯ АВТОРИЗОВАНА** — та же запись владельца от 2026-08-21, что и у всей
  волны 009–014 (`specs/README.md`, «Реализация АВТОРИЗОВАНА владельцем 2026-08-21 — по всей волне
  009–014»). 014 названа в этой волне поимённо, в очереди работ и в обосновании порядка.

  **Строка выше говорила «реализация не авторизована» с 2026-08-21 и до 2026-09-11, и по ней стояли
  `[!]` у всех тринадцати задач.** Это в точности тот же дефект, который волной раньше стоил
  программе 013 восьми строк из девяти, и запись о нём лежит в реестре рядом. Дополнительное
  свидетельство, что это описка, а не сознательная граница: реестр явно исключает из той же
  авторизации программу 015 («заведена позже и в той волне не значилась») — исключение, имеющее
  смысл только потому, что 014 в ней **была**.

  **Ирония, которую стоит назвать вслух, а не сгладить:** программа про ложное зелёное была
  заблокирована ложным красным в собственной шапке, и её же строка `Status authority` ниже заранее
  описывает этот механизм. Найдено разведкой перед началом работ 2026-09-11.

  Якоря перепроверены на HEAD `b3c5976` 2026-08-21 — **и с тех пор поехали**; разбор в разделе
  разведки.
- **Status authority:** метка описательная — и здесь это не формальность, а предмет программы.
  Ровно та подмена, которую 014 разбирает: «зелёное» не равно «проверено».
- **Owner surface:** `tests/**`, гарды и валидаторы в `scripts/`, `.github/workflows/quality.yml` в
  части объявляемых проверок, и **весь путь снятия ложного утверждения** — `app/core/invariants.py`,
  `app/api/v1/integrity.py`, `app/core/integrity.py`, `app/schemas/integrity.py`, вызов с платёжного
  пути, потребитель в `admin-ui/src/pages/IntegrityPage.vue` и текст
  `docs/ru/02-protocol-spec.md:1769-1796`. Границы и основание — в `## Owner surface`.
  **Запрещено:** денежное поведение (010, 012), канон (011), поведение фронтенда (013) — и
  **построение замены** тавтологичного инварианта (015).
- **Origin:** маршрут `008/tasks.md`: группы `P`, `R`, `T`, валидатор из группы `U`, остаток группы
  `AA`, `B-A4-5-001` и `C-A4-5-005` из группы `S`, строка 9 триажа (`B-A3-001`); слепая зона
  `useSimulatorApp` из `evidence-index.md`.
- **Depends on:** 009–013. Причина в построении класса, а не в удобстве: пока поведение не
  исправлено, «ассерт структурно не может упасть» и «ассерт закрепляет подтверждённый дефект» по коду
  неразличимы. Программа, идущая раньше, будет чинить не то.
- **Почему это одна программа:** предмет один — **проверка, которая выглядит проверкой и ею не
  является**. Он не делится по поверхностям: инвариант в `app/`, ассерт в `tests/`, гард в CI и
  непокрытый шов во фронте — четыре формы одного дефекта, и цена у них одна: ложная уверенность.

## Problem

Программа 008 нашла этот класс на всех трёх поверхностях и в собственных гейтах. Его цена отличается
от цены обычного дефекта: обычный дефект ломает систему, а этот **отменяет способность узнать, что
система сломана**.

Канонический пример — прямо в ядре целостности. `app/core/invariants.py:22-85`, `check_zero_sum`:
суммы кредитов по кредиторам и долгов по должникам считаются **по одним и тем же строкам `Debt`**,
поэтому каждая строка даёт `+amount` своему кредитору и `−amount` своему должнику, а итог
тождественно нулевой **при любом согласованном снимке строк с конечными `amount`** — формулировка
сужена 2026-08-21 по внешнему ревью. Прежнее «при любых значениях» было неточным в одну сторону: два
агрегата выполняются **отдельными** `SELECT` (`app/core/invariants.py:63-75`), поэтому конкурентная
запись между ними теоретически способна дать ненулевой результат. Это **ложное срабатывание**, а не
обнаружение повреждения, и потому класс находки не меняет. Комментарий в самом коде это
признаёт: «expected to be algebraically zero; this check serves as a smoke-test».

Его тест устроен так же: `tests/unit/test_invariants.py:23-35` создаёт один здоровый долг и
утверждает `check_zero_sum(...) == {}`. Утверждение истинно при любой сумме — тест не способен
покраснеть по построению.

И то же самое публикуется наружу как пройденная проверка — причём **шире, чем два API-сайта**:
`app/api/v1/integrity.py:104-108` и `:209-216` кладут `zero_sum: passed=True` в ответ
`/integrity/status`; тот же результат пишется в checkpoint (`app/core/integrity.py:69-76`); и он же
вызывается **с платёжного пути** (`app/core/payments/engine.py:1242-1253`). Протокол предписывает эту
проверку «после каждой транзакции + каждые 5 минут» (`docs/ru/02-protocol-spec.md:1769-1796`).

Три слоя ложной уверенности на одном механизме: инвариант, его тест и его публикация.

## Owner surface

Разрешено: `tests/**`, гарды и валидаторы, объявления проверок в CI, и **весь путь публикации**
тавтологичной проверки — то есть право перестать публиковать её как пройденную.

**Граница с 015 переопределена 2026-08-21 по внешнему ревью — прежняя была несостоятельна.**
Первая редакция разрешала 014 править «только статус в `app/core/invariants.py`». Проверено: статус
формируется **не там** — в `app/api/v1/integrity.py:104-108`, `:209-216` и `app/core/integrity.py:69-76`;
решение 008 требует ещё и снять вызов с платёжного пути (`008/tasks.md:305,311-321`). Кроме того:

- **Admin UI трактует отсутствие ключа как провал:** `admin-ui/src/pages/IntegrityPage.vue:466`
  печатает `passed ? passed : failed`, поэтому исчезнувший `zero_sum` станет красным «failed», а не
  «не проверяется».
- **Промежуточное состояние сегодня невыразимо:** `InvariantResult.passed` объявлен обязательным
  `bool` (`app/schemas/integrity.py:15-19`), значит «unverified» требует правки схемы.
- **Владение пересекалось:** черновик 015 назначает удаление ложного zero-sum себе
  (`015/research-2026-08-21-codex.md`, раздел «Проект спеки 015», `T1507`).

**Действующее разграничение, по работе а не по файлу.** 014 владеет **снятием ложного утверждения
целиком** — включая `api/v1/integrity.py`, `core/integrity.py`, `app/schemas/integrity.py` в части
выражения «не проверяется», вызов с платёжного пути, потребителя в `admin-ui` и текст
`docs/ru/02-protocol-spec.md:1769-1796`. 015 владеет **построением замены** — попарной сверкой
`debts` с журналом. Пересечение снято: `T1507` черновика 015 передаётся сюда, и это фиксируется при
заведении спеки 015.

Запрещено: строить замену тавтологичного инварианта. Это 015.

Запрещено: править денежное поведение (010, 012), канон (011), поведение фронтенда (013). Если гард
падает потому, что поведение неверно, — это находка для владельца поведения.

### Исключение для `T1402`, внесено 2026-09-11

**Две строки выше противоречили друг другу, и противоречие было обнаружено не чтением, а попыткой
выполнить `T1402`.** Раздел владения даёт 014 «снятие ложного утверждения **целиком**», а запрет —
`канон (011)` и `поведение фронтенда (013)`. Снять утверждение, не тронув канон, невозможно:
`api/openapi.yaml:5857` держит `zero_sum` в `required`, `:5960` — в `enum`, `:6111` объявляет
`InvariantResult.passed` обязательным `bool`, а `app/schemas/integrity.py:15-19` — тем же. **Состояние
«не проверяется» сегодня невыразимо**, и запрет запрещал ровно то, что владение предписывало.

Разрешено, и не шире:

> Для `T1402` программа 014 владеет изменениями проволочного контракта целостности и его
> потребителей **строго в объёме, необходимом, чтобы представить zero-sum как непроверяемый и снять
> его публикацию как пройденной проверки**. Прочие изменения канона и фронтенда остаются
> исключёнными; построение замены остаётся за 015.

Сюда входят затронутые схемы ответа, чекпойнта и аудита, их описания и примеры, сериализация, типы
потребителя и контрактные тесты. **Каждая правка обязана прослеживаться** до снятия утверждения либо
до совместимости с уже сохранёнными историческими результатами. Не входят: посторонние исправления
контракта, финансовые алгоритмы, замена инварианта.

**Закрытие 011 само по себе полномочием не является** и здесь ни на что не ссылается: разрешение
даёт раздел владения 014, а не отсутствие владельца у канона. Прецедент ограниченной согласованной
правки канона при нерешённом общем владении уже есть — 012, запись в `BACKLOG.md`; общего
размораживания он не создавал и здесь не создаёт.

**Основание:** внешняя консультация 2026-09-11 (Codex `gpt-6-astra`, `medium`, read-only),
маркеры `CANON-EDIT-BY-014: YES`, `WIRE-SHAPE: C`, `FRONTEND-EDIT-ALLOWED: YES`,
`T1402-BEFORE-T1403: YES`. Правка фронтенда признана частью именно этого снятия — нарисовать снятую
проверку нейтральным «не проверяется» и не дать ей ни стать обнаруженным отказом, ни внести вклад в
утверждение об успешной проверке.

## Findings

Проверено по коду 2026-08-21 на HEAD `b3c5976`. Часть находок унаследована из реестра 008 без
построчной перепроверки — помечено явно.

**`F-014-1` — строка 9 триажа, `B-A3-001`, P2. ПЕРЕПРОВЕРЕНО. Тавтологичный инвариант публикуется как
пройденная проверка.** Механизм — в `## Problem`. Что именно скрывает тождество, проверено в 008:
**ничего**. Zero-sum структурно гарантирован моделью Debt-рёбер, поэтому тавтологичность не прячет
реального повреждения — она **завышает заявленную защиту**. Это ложное зелёное (§9 anti-vacuum), а не
потеря денег.

Правку требует и документ: `docs/ru/02-protocol-spec.md:1769-1796` учит тому же тождеству и приводит
SQL с той же структурой.

**Разграничение с 015** — в `## Owner surface`: 014 снимает ложное утверждение целиком (инвариант,
его публикация во всех точках, его тест и текст документа), 015 строит замену. Граница проходит по
работе, а не по файлу.

**`F-014-2` — группа `R` + тест из `F-014-1`, P2. ПЕРЕПРОВЕРЕНО. Ассерт, истинный при любых данных.**
`tests/unit/test_invariants.py:23-35` — эталонный экземпляр: один здоровый долг, `assert ... == {}`.
Соседи по группе — `C-A4-2-001`, `C-A4-4-003`; исследование 015 добавило к ним
`tests/unit/test_integrity_checkpoints.py:62-92` и `tests/integration/test_integrity_endpoints.py:57-81`.

Отдельно назван приём проверки, которым эта группа закрывается и который уже применялся в T705:
**контрпроба обязательна**. Гард, не падающий на внесённом повреждении, неотличим от своего
отсутствия.

**`F-014-3` — группа `S` (часть), `B-A4-5-001`. P2. Унаследовано, якорь сверен.**
Единственный тест, доводящий тик до гарда порога повторных отказов, **выключает гард** (`limit = 0`)
и закрепляет выключение ассертом: его `fail_run` — это
`raise AssertionError("failure threshold is disabled")`. Продакшен прочитан в 008: `0` действительно
означает «выключено» (`app/core/simulator/real_tick_orchestrator.py:566-568`). Замер 008:
`git grep -c "REAL_MODE_TICK_FAILED_REPEATED" -- tests/` → **exit 1**, терминальный код гарда не
встречается во всём наборе ни разу.

**`F-014-4` — группа `S` (часть), `C-A4-5-005`. P3. Унаследовано.** Инвертированное покрытие:
`$defs/trustline` несёт `additionalProperties: False` и не имеет ключа `status`, а тесты подают
форму, которую продакшен произвести не может. Ценность находки — в объяснении, **почему**
подтверждённая `B-A2b-002` не была поймана.

**`F-014-5` — группа `T`, P2** (первая редакция ставила P3 при том, что два члена группы приняты в
008 как P2 — `evidence-index.md:1643-1644`; исправлено). **ПЕРЕПРОВЕРЕНО, и хуже, чем описывалось.**

`tests/unit/test_clearing_plan_edges_extraction.py` не «копирует продакшен-логику» — он **объявляет
её у себя**: `extract_cycle_edges_for_ui` определена прямо в теле файла (`:6-24`), единственный
импорт — `pytest` (`:3`). То есть тест проверяет собственную функцию, а не продакшен.

**ПЕРЕЯКОРЕНО 2026-09-11 перед `T1401`, и все три продакшен-якоря первой редакции оказались
мёртвыми.** Проверено построчно на сегодняшнем дереве:

| Якорь первой редакции | Что там сегодня |
|---|---|
| `real_clearing_engine.py:339-350` | приоритизация циклов и сбор `amount` в `Decimal` — извлечения рёбер нет |
| `real_clearing_engine.py:545-575` | обновление квантилей и `compute_node_patches` — не связано |
| `simulator.py:1681-1695` | **создание платежа** `PaymentService.create_payment_internal` — посторонний продакшен-код |

**Это не косметика.** `RT-014-3` предписывал «удалить оба продакшен-пути извлечения» по этим самым
координатам; выполненный буквально, он удалил бы создание платежа. Репродьюсер переписан ниже.

**Живые пути найдены и оказались другими, чем описано:**

- `app/core/simulator/real_clearing_engine.py:428-436` — накопление `touched_edges` как
  **`(creditor_pid, debtor_pid)`**;
- `app/core/simulator/real_clearing_engine.py:623-650` — авторитетное для визуализации построение
  `done_cycle_edges`. Ориентация здесь **не фиксирована**: каждое ребро разрешается поиском в кэше
  топологии прогона (`(a,b)` либо `(b,a)`), и только при отсутствии кэша берётся `(creditor, debtor)`
  как fallback. Комментарий на `:628-632` прямо признаёт, что часть вызывающих и старые данные
  считают ребро `(debtor → creditor)`;
- `app/api/v1/simulator.py:1920-1928` — `SimulatorActionEdgeRef(from_=creditor, to=debtor)`, с
  комментарием «Trustline direction is creditor -> debtor».

**Отсюда находка сильнее, чем «копия не наблюдает продакшен».** Копия строит
`{"from": debtor, "to": creditor}` **безусловно** — то есть является точным инверсом задокументи-
рованного fallback и не моделирует разрешение ориентации по топологии, которое и составляет всю
трудность живого кода. Копия никогда не совпадала ни с одним продакшен-путём, и тест утверждает одну
из двух ориентаций как правильную, не наблюдая ни одной.

**Второй тест файла реплицирует форму, которой в репозитории нет вовсе.**
`test_clearing_plan_steps_include_highlight_edges` строит шаги плана с ключами `highlight_edges`,
`particles_edges`, `flash`. Грепом по всему дереву: `highlight_edges` и `particles_edges` не
встречаются **ни в `app/`, ни в `simulator-ui/v2/src`, ни в `admin-ui/src`** — ни одного вхождения.
Тест зелен относительно структуры, которую никто не производит и не потребляет.

Диспозиция по обеим копиям принимается в `T1405` отдельно, как требует `Non-goals`.

**Атрибуция «пересекает границу языков» была неверной — исправлено.** Этот экземпляр целиком
Python-овый. Настоящий cross-language член группы — `tests/unit/test_topology_changed_no_empty_payload.py:229-260`,
где Python-класс `TestFrontendContractEmptyPayload` реплицирует логику `isEmptyPayload`/`hasPatches`
фронтенда.

**Состав группы восстановлен.** Первая редакция якорила только `C-A4-4-001`. Полный набор по
`008/tasks.md:201`: `C-A4-4-001`, **`B-A4-6-001`**, **`B-A4-6-002`** (оба P2) и безымянный экземпляр
— им и является `test_topology_changed_no_empty_payload.py:229-260`.

Правка **не codemod**: для каждой копии выбор между «свериться с оригиналом» и «удалить копию»
делается отдельно.

**`F-014-6` — группа `P`, P3. ПЕРЕПРОВЕРЕНО по коду 2026-09-11 (`T1400`).** Гард политики или CI,
притворяющийся покрытием. **Пять подтверждены, одна переформулирована, ни одна не опровергнута.**

| ID | Вердикт | Живой якорь | Что именно проходит вхолостую |
|---|---|---|---|
| `C-A4-1-007` | `CONFIRMED` | `tests/unit/test_backend_marker_policy.py:1609-1610` | 2914 строк маркерного гарда связаны с реальным раннером **двумя подстроками**; `scripts/verify_local.ps1` читается из **одной** строки всего набора (`:1592`) |
| `C-A4-1-001` | `CONFIRMED` | `tests/unit/test_postgres_test_taxonomy.py:155-181` | `_executable_command` (`:114-120`) отбрасывает ровно тот шаг CI, у которого есть file-allowlist (`quality.yml:206-216`), поэтому `assert ".py" not in command` смотрит только на законопослушный шаг |
| `C-C4-005` | **`REFRAMED`** | `admin-ui/src/i18n/i18n.statsPunctuation.test.ts:8-10,15-17` | гард зелен после удаления собственной цели — но **по другой причине**, чем записано; см. ниже |
| `R-C4-002` | `CONFIRMED` | `admin-ui/src/api/adminMutationIntegrity.contract.test.ts:293-320` | файл назван контрактом mock↔real, импортирует обе стороны и **закрепляет расхождение как ожидаемое**: моку предписаны нули (`mockApi.ts:388,954,965`), реалу — ненулевые значения; сравнения сторон нет |
| `C-A4-3-011` | `CONFIRMED` | `tests/integration/test_simulator_super_smoke.py:592`, `:986` | единственная проверка «прогон не закончился в `error`» спрятана за `if st.status_code == 200`: отказ статус-эндпоинта **удаляет проверку**, а не роняет тест |
| `C-A4-6-009` | `CONFIRMED` | `tests/unit/test_warmup_and_capacity.py:394-399` | единственный ассерт границы A→B исполняется под фильтром, способным дать ноль элементов; единственная страховка `:391` требует непустоты **всего** плана |

**Четыре дефекта в самой записи, найденные при перепроверке. Их нужно исправить вместе с кодом:**

1. **`C-A4-3-011` не имеет якоря нигде** — ни в `evidence-index.md`, ни в `t810-collation.md:83`
   (там пять якорей на шесть ID, и пропущен именно он). Сайт выше **реконструирован поиском по коду**
   и проверен мной построчно; атрибуция наша, а не исходной записи. Признак, что это дефект, а не
   стиль: в том же файле все остальные проверки статуса написаны громкой обратной формой
   (`if resp.status_code != 200: raise`) — `:473`, `:515`, `:532`, `:545`, `:929`. Тихая
   положительная форма применена ровно к худшему исходу.
2. **`C-C4-005` приписан двум разным файлам** двумя источниками: `t810-collation.md:82` и
   `d2-deletion-list.md:163` дают `i18n.statsPunctuation.test.ts`, а `d3-cross-duplicates.md:545` —
   `i18n.hardcodedUiStrings.test.ts`. Проверена та версия, на которой сходятся два независимых
   источника.
3. **`R-C4-002` тоже приписан двум файлам:** `adminMutationIntegrity.contract.test.ts` в
   `evidence-index.md` и `api.contract.test.ts` в `t810-collation.md:83`. Проверены оба; класс
   присутствует в обоих, три названных в находке ассерта — в первом.
4. **Две цифры протухли, и обе в сторону усиления находки:** «49 файлов можно отрезать молча» — это
   было при 64 файлах в `tests/integration`; сегодня файлов **88**, помеченных `postgres`/`slow` —
   31, то есть в дефолтном тире **57**. «3 ключа из 671» — сегодня из **675**.

**Переформулировка `C-C4-005`.** Записано было: «гард остаётся зелёным при удалении собственных
ключей **из-за `fallbackLocale: 'en'`**». Механизм назван неверно. Ассерт — **отрицательный**
(`expect(t(key)).not.toMatch(/:\s*$/)`), а `vue-i18n` на отсутствующий ключ возвращает **сам ключ**,
который под регэксп не подходит. Поэтому удаление ключа **из обеих локалей** оставляет все шесть
ассертов истинными — и это следствие «missing key → key», а не фоллбэка. `fallbackLocale: 'en'`
(`admin-ui/src/i18n/index.ts:26`) даёт **другую**, тоже настоящую дыру: при удалении ключа только из
`ru.ts` русский тест молча проверяет **английскую** строку, то есть русская пунктуация не охраняется
никогда. Исходная запись назвала одну дыру и приписала ей механизм другой. **Класс группы `P`
сохраняется, P2 остаётся** — гард действительно зелен после удаления своей цели.

**`T1409` разблокирована:** запрет `Non-goals` «не править находки, не прошедшие `T1400`» для
`F-014-6` снят.

**`F-014-7` — валидатор из группы `U`, P3. ПЕРЕПРОВЕРЕНО частично.** Изменяемая БД в корне
репозитория вопреки §7 и §12. Пять тестовых модулей закрываются классом `fix now` внутри 008;
**валидатор** — здесь. Сверено: `tests/integration/test_simulator_adaptive_clearing_integration.py:41`
и `tests/integration/test_simulator_clearing_no_deadlock.py:47` объявляют
`_TEST_DB_PATH = ".pytest_adaptive_int_test.db"` / `".pytest_deadlock_test.db"` — относительные пути,
то есть файлы создаются в рабочем каталоге прогона. Задача 014 — гард, делающий появление такого
файла **красным**, а не разовая уборка.

**`F-014-8` — слепая зона `useSimulatorApp`, позиция 3. P2. ПЕРЕПРОВЕРЕНО.**
`grep -rl onEdgeClick --include=*.test.ts simulator-ui/v2/src` → **0 файлов**. Шов
`useSimulatorApp.ts:1423` (`onEdgeClick`, объявлен опциональным на
`useAppCanvasInteractionsWiring.ts:16`) не упомянут ни одним тестом ни на одном уровне; удаление
проходит `typecheck`. Симптом отказа: клик по ребру на канве молча не открывает окно детали —
основное взаимодействие с канвой, радиус больше, чем у закрытых позиций 1 и 2.

Приём закрытия известен и проверен на позициях 1 и 2 (T705 программы 007): исходный гард читает
файл, вырезает блок опций нужного вызова, снимает комментарии и требует наличия **исходного
выражения**, а не ключа — потому что подмена значения при живом ключе вероятнее удаления.

**`F-014-9` — долг: `useSimulatorApp` не инстанцируется в тестах вовсе. P2. ПЕРЕПРОВЕРЕНО в 007.**
Composable инстанцируется в дереве ровно один раз — в `SimulatorAppRoot.vue:224`, а единственный
тест, монтирующий корень, его **мокает**. Следствие: весь код внутри composable невидим для набора.
Измерено 007: удаление строки `getLayoutLinks` оставило 844 теста зелёными и `typecheck` чистым.

**Исходный гард этого не заменяет** и записан 007 явно как долг: он не исполняет код и не проверяет
поведение. Полноценное инстанцирование — существенно более крупная работа, и она принадлежит этой
программе как **отдельная задача с собственной оценкой**, а не как побочный эффект `F-014-8`.

**Оговорка внешнего ревью принята:** «отдельная оценка» сама по себе задачей не является. `T1408`
не может быть закрыт, пока у него нет названного селектора, наблюдаемого эффекта и контрпробы через
внесённое повреждение. До их появления задача остаётся открытой, а не «оценённой».

**`F-014-10` — остаток группы `AA`. ПЕРЕПРОВЕРЕНО по коду 2026-09-11 (`T1400`): три из трёх
подтверждены.** Позиции предъявлены внешним ревьюером волны 5 и до сегодня были входом в триаж, а не
подтверждёнными находками (§1 `AGENTS.md` эти вещи различает). **В отличие от `F-014-5`, все якоря
этой группы живы и номера строк не сдвинулись.**

| ID | Вердикт | Живой якорь | Что проходит вхолостую |
|---|---|---|---|
| `MISSED-B5-SCENE-B` | `CONFIRMED`, **и вырожден уже сегодня** | `simulator-ui/v2/e2e/scenes.spec.ts:20-24` | комментарий теста обещает карточку узла, а deep-link только присваивает `selectedNodeId` (`useSceneState.ts:209-210`); карточка открывается лишь из окна window-manager (`SimulatorAppRoot.vue:1411`) двумя вызовами, которых спека не делает |
| `MISSED-1` | `CONFIRMED`, вырождение **латентное** | `tests/unit/test_flow_and_periodicity.py:604-609`, `:633-638` | reciprocity проверяется в цикле по `a_to_b`, а единственная страховка (`:598`, `:627`) требует непустоты **всего** `planned`, который наполняется и одним направлением B→A |
| `MISSED-2` | `CONFIRMED`, вырождение **латентное** | `tests/unit/test_admin_graph_ego.py:112-113` | оба `all(...)` над незащищённым `pf['trustlines']`: если фильтрация `equivalent`/`status` (`app/api/v1/admin.py:2115-2118`) отбросит **всё**, ассерты истинны вхолостую |

**`MISSED-B5-SCENE-B` подтверждён не рассуждением, а эталоном.** Принятый снимок
`simulator-ui/v2/e2e/scenes.spec.ts-snapshots/scene-B-win32.png` открыт и просмотрен: граф,
режимная панель, кнопки `SINGLE TX` / `RUN CLEARING` — **карточки узла на нём нет**. Сравнение
закреплено против картинки, в которой обещанного элемента не существует, поэтому любая регрессия
карточки оставляет снимок зелёным.

**Найдено там же и в записи отсутствует:** `scene-A-win32.png` и `scene-C-win32.png` **побайтово
одинаковы** (`md5 ef653d3ffa00da533f1c1ce245a3f44b` у обоих). Снимок сцены C не способен отличить
сцену C от сцены A. Это тот же класс и тот же файл; принято в `F-014-10` как четвёртая позиция.

**Охват смягчает, но не отменяет.** `scenes.spec.ts` не лежит на блокирующем пути: полный
`test:e2e` идёт только в джобе `simulator-super-smoke` по расписанию и вручную
(`.github/workflows/quality.yml:773-775`), а блокирующий шаг запускает `test:e2e:smoke`, то есть
`playwright test console-guard`. `MISSED-1` и `MISSED-2` — обычный юнит-тир, блокирующий.

**Латентное ≠ безвредное, и это измерено, а не предположено.** Сегодня `MISSED-1` даёт
`planned=50, a_to_b=25`, то есть цикл исполняется; при обнулении прямой пропускной способности A→B —
`planned=50, a_to_b=0`, и `assert len(planned) > 0` остаётся истинным на 50 действиях B→A, пока тело
reciprocity не исполняется ни разу. Соседний тест того же файла (`:553`) страховку имеет
(`:572`, `assert len(a_to_b) > 0`) — то есть пропуск виден внутри одного класса.

`MISSED-3` из этой группы **исключён**: разбор 008 уже перевёл его в группу `Q`, то есть в
программу 012 (дедупликация выполнена в маршруте 2026-08-21). **Оговорка по учёту:** ни строка
`MISSED-3`, ни `test_trustlines_list_filters_pagination` не встречаются нигде под
`specs/012-money-precision-and-representation/` — передача заявлена в 008 и 014, но получение в 012
не видно. Записано в `BACKLOG.md`; передача без получателя — это долг, а не закрытие.

**`T1409` разблокирована и для `F-014-10`.**

**`F-014-11` — внесена волной 012 2026-08-25, P2. Закрыта `T1412` 2026-09-11.**

**Заголовок при заведении звучал так: «вердикт восьми тестов есть функция ширины консоли того, кто
их запускает». Обе половины измерением не подтвердились** — их не восемь, и ширина не единственная и
даже не сработавшая ось. Точная формулировка: **вердикт двенадцати ассертов есть функция канала
вывода, ширины консоли и локали хоста, и ни одна из трёх не находится под контролем теста.** Разбор
расхождений — ниже, исходная редакция сохранена целиком.

Найдена не чтением, а расхождением двух прогонов. При закрытии волны 012 соседняя сессия получила в
backend-тире `1858 passed / 1 failed` и классифицировала падение как baseline, воспроизведя его на
`origin/main`. Классификация верна, но недооценивает механизм: **этот же тест на том же дереве и том
же коммите у меня прошёл зелёным несколькими часами раньше** (`1809 passed, 2 skipped`, ноль
падений), а при повторном прогоне отдельным файлом — упал.

Причина измерена: `tests/unit/test_run_full_stack_database_url_redaction.py`,
`test_fail_on_conflict_checks_all_services_before_any_stop` утверждал
`"not stopped" in result.stdout`, а `powershell.exe` переносит консольный вывод по ширине терминала.
Перенос ложится **между словами**:

```
... the listener was not \nstopped.
```

Отсюда точная формулировка, и она сильнее, чем «хрупкий тест»: **вердикт не несёт информации о коде
ни когда красный, ни когда зелёный** — он измеряет окружение. Зелёный не доказывает, что лаунчер
сказал то, что должен; красный не означает регрессии. Это единственное утверждение исходной находки,
которое пересчёт 2026-09-11 подтвердил целиком.

**Приписка при заведении — «при `columns=80` перенос ложится между словами» — измерением
опровергнута:** именно при 80 эта строка проходит, как и при 100. Красный был при **120**. Ширина,
названная в находке как причина, была в ней же названа неверно; см. пункт 2 ниже.

**Это класс, а не экземпляр — и пересчёт 2026-09-11 при выполнении `T1412` изменил в этом абзаце
всё, кроме слова «класс».** Ниже — что говорилось при заведении находки и что оказалось на самом
деле. Исходная редакция сохранена как датированная история, потому что ошиблась она поучительно.

**Говорилось (2026-08-25):** экспонированы **восемь** ассертов — те, что содержат пробел; самый
экспонированный — 57-символьный `owned PID 101 is alive but is not listening on port 18000`,
держащийся «только текущей шириной»; чинить — нормализацией, а пин ставить **на узкую консоль**.

**Измерено (2026-09-11), три расхождения:**

1. **Экспозицию определяет канал вывода, а не длина фразы.** Хост-форматтер PowerShell переносит то,
   что рендерит сам: `Write-Warning`, error record, verbose, debug. `[Console]::Out.Write` идёт мимо
   форматтера и **не переносится никогда**. Поэтому 57-символьная фраза — заголовочный пример
   находки — **неуязвима**: её тест печатает причину через
   `[Console]::Out.Write($state.Reason)` (`tests/unit/test_run_full_stack_database_url_redaction.py`,
   `test_exact_live_non_listener_has_an_actionable_conflict_reason`). Длина была не той мерой.
   Полная инвентаризация по каналу: **77 подстрочных ассертов на выводе процесса, из них
   экспонированы 12, различных диагностик 9** — а не 8.

2. **Зависимость от ширины немонотонна — это пила, а не порог.** Живой красный
   (`test_fail_on_conflict_checks_all_services_before_any_stop`) — при ширине **120**; та же строка
   при **100** и при **80** проходит, потому что граница ложится в другое место. Отсюда прямое
   следствие: **предписанный находкой пин «прогон на искусственно узкой консоли остаётся зелёным»
   был бы зелёным на том самом дефекте, ради которого он писался.** Он не слабый — он не тот.

3. **Сработала не ширина, а локаль, и этой оси в находке нет вовсе.** Прогон, давший красный, шёл
   **вообще без консоли**: PowerShell берёт фиксированный запасной размер 120. Префикс предупреждения
   в Windows PowerShell локализован — `WARNING: ` (9 символов) против `ПРЕДУПРЕЖДЕНИЕ: ` (16). Эти
   семь колонок и есть вся разница: `ПРЕДУПРЕЖДЕНИЕ: Admin UI: … The listener was not stopped.` —
   123 символа, перенос после `not `; та же строка с английским префиксом — 116, в одну строку.
   **Один и тот же коммит красен на русской Windows и зелен на английской при одинаковой ширине.**
   GitHub Actions — en-US и без консоли, поэтому **этот экземпляр** CI не видел. Это не то же самое,
   что «CI не способен увидеть класс»: другой текст сообщения или другой префикс дадут другую границу
   и при 120.

**Четвёртый дефект нашёлся внутри самой находки.** Ассерт
`assert "not stopped" in result.stdout or "not proven" in result.stdout` (четыре параметризации)
удовлетворяется **суммарным** предупреждением в одиночку, поэтому **ни в одном из четырёх случаев
не проверял причину конфликта вовсе**: все четыре прошли бы с удалённым текстом причины. Это
`F-014-2` внутри `F-014-11`, и чинилось здесь же.

**Что сделано (`T1412`).** Не «нормализация в восьми местах», а:

- `_console_text()` — снятие раскладки перед сравнением, только для **содержания диагностики**;
  точные сравнения, проверки редактирования секретов и структурные ассерты остаются на сыром потоке,
  потому что для них пробелы — часть контракта
  (`tests/unit/test_run_full_stack_database_url_redaction.py:34`);
- **все двенадцать** экспонированных ассертов переписаны с двухсловных фрагментов на **полные
  предложения диагностики**. Причина не косметическая: схлопывание пробелов не отличает перенос от
  границы строк, поэтому двухсловный фрагмент собирается из двух несвязанных строк, ни одна из
  которых его не содержала. Полное предложение так не собирается
  (`test_collapsing_whitespace_joins_across_line_boundaries`);
- **пин — не ширина, а инвариантность.** Для каждой из девяти диагностик и **обоих** префиксов
  перебираются **все** позиции переноса (`\r\n`, `\n`, перенос с отступом продолжения, и вариант с
  переносом на каждом пробеле сразу); ассерт обязан выдержать любую
  (`test_console_text_survives_every_host_wrap_position`, `:2246`). Пин детерминирован, не требует ни
  консоли, ни локали, ни установленного PowerShell — и потому исполняется в том числе в CI, который
  этого экземпляра не видел;
- **контрпроба на невакуумность** (`test_the_wrap_sweep_is_not_vacuous`, `:2260`): свип обязан ломать
  строку **в каждой** заявленной позиции. Первая редакция контрпробы требовала лишь «хоть один
  сломанный вариант», и мутация, удалявшая весь позиционный свип, её пережила — исправлено после
  мутационного прогона, а не до него;
- **негативные контроли** (`test_normalization_does_not_rescue_a_changed_diagnostic`): удаление
  **любого** одного слова из диагностики обязано оставить ассерт красным, иначе нормализация стала бы
  вторым ложным зелёным;
- **гард класса** (`test_no_launcher_output_assertion_skips_console_normalization`) — AST-обход файла:
  ни один `in`/`not in` на выводе процесса не проходит мимо `_console_text`, кроме явно объявленных
  raw-сайтов, каждый с названным каналом. У гарда есть **свой контртест**
  (`test_the_guard_detects_an_unnormalized_assertion`);
- пин на тексты в лаунчерах (`test_pinned_diagnostics_are_still_the_text_the_launchers_emit`): правка
  формулировки в `run_full_stack.ps1`/`run_local.ps1` краснит пин, а не осиротляет его молча.

**Мутационная проверка (2026-09-11).** `M1` нормализация снята → `34 failed`, включая исходный живой
красный; `M2` один ассерт возвращён к фрагменту → гард красный; `M3` сообщение лаунчера
переформулировано → пин текстов красный; `M4` raw-сайт убран из объявленного списка → гард красный;
`M5` свип урезан (двумя способами) → контрпроба красная. Ни одна мутация не выжила.

**Почему это записано сюда, а не в 012.** Программа 012 слита; предмет находки — тест, чей зелёный
не связан с проверяемым поведением, то есть буквально `## Problem` этой программы.


### `T1402` и `T1403` — снятие ложного утверждения, 2026-09-11

**Порядок изменён по внешней консультации, и это не перестановка ради удобства.** Разведка ставила
`T1402` последней из-за коллизий; консультация 2026-09-11 (`T1402-BEFORE-T1403: YES`) показала
зависимость: правильная замена ассерта `assert check_zero_sum(...) == {}` — не «переписать тавтологию
поаккуратнее», а **проверить снятие утверждения**, и проверять там нечего, пока утверждение не снято.
Поэтому обе задачи выполнены одним срезом.

**Форма на проводе — вариант `C` из четырёх рассмотренных.** Ключ `zero_sum` остаётся, но вердикта не
несёт:

```json
"zero_sum": { "status": "not_verified", "reason": "check_withdrawn" }
```

Отклонены: (a) выкинуть ключ совсем — отсутствие ключа читается как «в этой сборке такой проверки
нет», а у потребителя админки отсутствующий `passed` рисуется словом `FAILED`; (b) сделать `passed`
опциональным на `InvariantResult` — это ослабило бы **две работающие** проверки ради третьей и
оставило бы «не проверяется» выразимым как «пройдено». Поэтому введён **отдельный тип**
`InvariantWithdrawn`, которому `passed` **запрещён** схемой (`extra="forbid"`, `additionalProperties:
false` в каноне): опциональный булев позволил бы позже вернуть утверждение, и ни канон, ни декодер
этого бы не заметили.

**Совокупный статус сужен до фактически выполненных проверок, и это названо вслух.** Без этого ложная
уверенность просто переехала бы из строки в сводку: `status: healthy` читалось бы как «все три
проверены и всё хорошо». Добавлено поле `unverified` — список имён без вердикта, **выводимый из
значений**, а не захардкоженный, чтобы он опустел сам, когда 015 построит настоящую проверку.

**Все четыре call-site перечислены и закрыты** (§16.1), включая тот, что 008 требовала снять отдельно:

| Call-site | Что сделано |
|---|---|
| `app/api/v1/integrity.py:105` (`GET /status`) | вызов снят, публикуется `InvariantWithdrawn()` |
| `app/api/v1/integrity.py:210` (`POST /verify`) | то же |
| `app/core/integrity.py:80` (чекпойнт) | то же, плюс `unverified` в `invariants_status` |
| `app/core/payments/engine.py:1252` (**платёжный путь**) | вызов удалён. Он сканировал весь эквивалент на каждом платеже и не мог прервать ни один — прервать его мог только результат, которого эта проверка не способна дать |

Гард `test_no_production_path_calls_check_zero_sum` — обход AST по всему `app/` (проверено: файлов
больше ста, пустой поиск за чистый не сойдёт), а не греп: вызов, разнесённый на две строки, тоже
считается.

**Что сломалось от снятия, и почему это правильно сломалось.** Три теста доказывали, что отказ
проверяющего пробрасывается наружу, — и делали это, патча **`check_zero_sum`**, потому что он шёл
первым. После снятия зонд перестал срабатывать, и все три **стали зелёными ни на чём**: они прошли
бы и с удалённым пробросом. Перенацелены на `check_trust_limits`, предмет не изменился:
`tests/unit/test_integrity_checkpoints.py:25,57` и `tests/unit/test_trustline_audit_fail_closed.py:55`.
Последний нашёлся только полным прогоном тира — через `TrustLineService.create`, которая считает
чекпойнт для своего аудита.

**`T1403` — три сайта, а не пять.** `C-A4-2-001` и `C-A4-4-003` оказались не соседями по группе, а
**теми же двумя ассертами, названными вторично**: первый — строка внутри
`tests/integration/test_integrity_endpoints.py`, второй — внутри `tests/unit/test_integrity_checkpoints.py`.
Список в этой спеке перечислял их как отдельные позиции. Совпало независимо у двух проверяющих.

| Сайт | Почему не мог упасть | Чем заменён |
|---|---|---|
| `tests/unit/test_invariants.py:35` | **тождество** — `_compute_imbalance` телескопируется, `{}` был единственным достижимым возвратом | направленность рёберной модели (`_calculate_net_position` = `-100`/`+100`) и правило «нет trustline — лимит ноль», которое на этой же фикстуре **реально нарушается** |
| `tests/unit/test_integrity_checkpoints.py:90` | **литерал против литерала** через один переход: `except` рядом недостижим | форма снятия; плюс не проверявшиеся ранее `debts_count`, `trustlines_count`, `debts_non_negative`, **детерминизм checksum** и его **чувствительность** к раздутию на копейку |
| `tests/integration/test_integrity_endpoints.py:79` | **вакуум поверх тождества** — долгов в тесте не было вовсе, все три проверки истинны на пустом множестве | посеян долг без trustline: `trust_limits` теперь **находит нарушение**, `status` — `critical`, и это первые здесь ассерты, которые вообще способны покраснеть |

Ни один тест не удалён: `Non-goals` это запрещает, и правильно — покрытие ушло бы вместе с ложной
уверенностью. Каждый **заменён** на утверждение о том, что код и так делает, выбранное так, чтобы оно
было опровержимым.

**Мутационная проверка — семь из семи красных.** `M1` чекпойнт снова публикует `passed: True`;
`M2` оба эндпойнта возвращают `InvariantResult(passed=True, value="0")`; `M3` вызов возвращён на
платёжный путь → красен AST-гард; `M4` снят `extra="forbid"` с `InvariantWithdrawn`; `M5` `unverified`
всегда пуст; `M6` фикстура мока снова показывает `passed`; `M7` декодер теряет вариант снятия.

**Первая редакция мутационного скрипта была недействительна, и это записано, а не заглажено.** Она
откатывала мутацию через `git checkout --`, а правки под проверкой не были застейджены — поэтому
откат сносил **их вместе с мутацией**, и каждая следующая мутация шла по всё более старому дереву.
Признак: `M5` «не нашла якорь» — якоря не было, потому что правку уже снесли. Переделано с
побайтовым сохранением и проверкой восстановления. Потери нет: правки восстановлены теми же
скриптами и застейджены до повтора. Это второй за день случай, когда `read_text()`/`git checkout`
на CRLF-дереве дали не то, что обещали, — первый описан в `T1400`.

**Канон правился в границах исключения, записанного выше**, и ратчет `SUCCESS_SCHEMA_DRIFT_SHA256`
сдвинут **с ревью, а не вслепую**: множество дрейфующих операций посчитано на обоих деревьях
(HEAD в отдельном worktree против рабочего) — **63 до и 63 после, ни одна не вошла и не вышла**.
Заявление сознательно узкое: ратчет **записывает** расхождение и хеширует его, поэтому «ни одна
запись не стала хуже» он показать не способен — на этом волна 013 уже ошиблась, и поправка стоит
рядом в том же файле.

**Мок приведён в соответствие с сервером.** `admin-ui/public/admin-fixtures/v1/datasets/integrity-status.json`
публиковал `zero_sum: passed` во всех трёх эквивалентах. Оставить это значило бы создать ровно
расхождение `R-C4-002`, подтверждённое `T1400` в тот же день: оператор в mock-режиме видел бы
зелёную метку, которой сервис больше не утверждает.

**Строгий декодер админки сработал так, как предсказывала непринятая находка 013.**
`InvariantResultSchema` объявлен `.strict()`, поэтому новая запись `{status, reason}` была бы
**отвергнута**, и страница целостности не загрузилась бы против исправного сервера. Это `P3` из
`BACKLOG.md` без получателя — «строгий декодер превращает аддитивное изменение сервера в отказ
страницы», — встретившийся впервые не как гипотеза, а как настоящее аддитивное изменение. Декодер
расширен тем же срезом; сама политика декодирования по-прежнему без владельца.

**Локальные гейты после последнего изменения, с командами и exit code:**

- `scripts/verify_local.ps1 -TaskSlug p014-t1402gate -BackendOnly` — exit `0`,
  **`1932 passed, 2 skipped, 137 deselected`** (до среза было `1927`; `+5` — новый модуль снятия);
- `scripts/verify_local.ps1 -TaskSlug p014-t1402pg -BackendOnly -BackendMarker postgres
  -BackendSelector tests/integration` — exit `0`, **`133 passed, 322 deselected`**,
  `Test database guard passed (backend=postgresql, database='geov0_test_ci')`. **Это канонический
  Postgres-гейт, а не голый `pytest`:** волне 013 пришлось оговаривать, что её postgres-цифра —
  debug path; здесь оговорки не требуется;
- Admin UI: `npm test` — **`43 files / 389 passed`**; `npm run lint` — `0 errors, 117 warnings`;
  `npm run build` — собралась.

**Не запускалось и потому не утверждается:** Playwright ни в каком объёме; симуляторный тир этим
срезом не затронут и не перепрогонялся; CI на этом дереве ещё не шёл.

### Внешнее ревью среза `T1402`/`T1403` — две находки P2, обе настоящие, 2026-09-11

Ревьюер: Codex `codex-cli 0.154.0`, `-m gpt-6-astra`, `model_reasoning_effort=medium`,
`--sandbox read-only`, замороженный credential-free клон на `3abf479`, база сравнения `056e720`,
exit `0`. Клон без remote'ов, без `.venv`, `node_modules` и `.local-run`; совпадения по шаблону
токена оказались примерами в документации (`docs/*/04-api-reference.md`, `app/api/deps.py:267`).
Вердикт ревью: **гейты не запускались, ревью read-only.**

Это **единственный разрешённый §15 круг по fix-delta** (`AGENTS.md:597`). Обе находки исправлены
ниже; сама fix-delta пойдёт на тот же круг ещё раз, потому что правило допускает ровно один такой
повтор, и он ещё не израсходован.

**P2-1 — правилась производная копия, а источников было шесть.** Отредактирован
`admin-ui/public/admin-fixtures/v1/datasets/integrity-status.json`, но и `npm run dev`, и
`npm run build` выполняют `sync:fixtures`, который делает `fs.cp(..., force: true)` поверх
`public/` из `admin-fixtures/v1`. **Воспроизведено:** один `npm run sync:fixtures` вернул
`"passed": true` на место — то есть новый фикстурный тест покраснел бы на следующей же сборке, а
mock-режим снова показал бы зелёную метку. Позади стояли: канонический датасет, **два пакета
сценариев** (`greenfield-village-100-v2`, `riverside-town-50-v2`) и **два генератора**
(`admin-fixtures/tools/adminlib.py:87`, `generate_admin_fixtures.py:337`). Все шесть приведены в
соответствие; генераторы теперь выводят и `unverified`. Проверено регенерацией:
`python admin-fixtures/tools/generate_admin_fixtures.py --out-v1 <tmp> --force-canonical` даёт
файл, **идентичный** закоммиченному, и после настоящей `npm run build` публичная копия остаётся
снятой.

**Это ровно §16.1, и я его нарушил:** закрыт один call-site из шести. Формулировка «перечислите все
call-sites» существует потому, что интуиция закрывает тот, который трогала.

**P2-2 — закрыта половина находки, и снова та же половина.** `GET /integrity/checksum/{equivalent}`
отдаёт **сохранённый** `invariants_status` дословно. Строки, записанные до снятия, несут
`zero_sum: {"passed": true}` и могут нести `alerts: ["zero_sum"]`. Историческая совместимость была
добавлена в `IntegrityAuditLogCheckResult` и **не** добавлена в `IntegrityInvariantChecks` — то есть
канон объявил достижимый ответ невозможным, зеркало того дефекта, ради которого заведена 011.
Исправлено: `zero_sum` описан как `oneOf` двух вариантов, и `zero_sum` возвращён в enum `alerts`
чекпойнта с оговоркой «только для сохранённых строк».

**И внутри этого исправления немедленно нашёлся третий дефект — мой.** Legacy-ветка объявлена
`additionalProperties: true`, поэтому запись `{status: not_verified, reason: check_withdrawn,
passed: true}` проходила по ней как «legacy-вердикт с лишними ключами»: снятое утверждение
возвращалось через дверь, открытую для описания старых строк. Поймано **собственным ассертом**
`fused entry must not validate`, написанным в том же коммите. Закрыто `not: {required: [status]}`.
Это четвёртый за две программы случай, когда худший дефект круга сидел внутри починки предыдущего;
ассерт на слияние вариантов написан именно поэтому, а не после факта.

**Добавлено два теста, которых требовало ревью:**

- `test_no_checked_in_fixture_publishes_a_zero_sum_verdict` — обход всего дерева по
  `integrity-status.json` (с проверкой, что найден хоть один: пустой поиск за чистый не сойдёт),
  чтобы седьмой источник не появился тихо;
- `test_a_checkpoint_stored_before_the_withdrawal_still_conforms_to_the_canon` — валидация
  `OAS30Validator` по канону для трёх форм сразу: исторический провал, исторический успех и
  сегодняшнее снятие, плюс отрицательный контроль на слитую запись.

**Ратчет сдвинут второй раз за день**, с тем же узким заявлением: **63 дрейфующие операции до и 63
после**, ни одна не вошла и не вышла; посчитано на обоих деревьях.

**Гейты после fix-delta:** дефолтный тир `1933 passed, 2 skipped, 137 deselected`
(`+1` — новый фикстурный обход; `test_a_checkpoint...` попал в тот же модуль);
`tests/contract` — `125 passed`; Admin UI продакшен-сборка — собралась, и публичная фикстура после
неё осталась снятой.

**Круг по fix-delta израсходован и чист.** Замороженный клон на `8b18ed3` против базы `3abf479`,
тот же ревьюер и та же конфигурация, exit `0`:

> No actionable regressions were identified in the fix-delta. The fixture sources consistently
> publish the withdrawn check, and the schema accommodates historical checkpoints while rejecting
> fused legacy/withdrawn entries. Tests and canonical gates were not run in this read-only review.

Оговорку про гейты воспроизвожу дословно: **ревью не запускало ни одного гейта ни в одном из двух
кругов**, все приведённые цифры — локальные. Единственный разрешённый §15 круг по fix-delta
(`AGENTS.md:597`) на этом израсходован; дальнейшие правки этого среза внешне проверены не будут.

## Current / Intended / Optimal

**Current.** Ядро целостности публикует как пройденную проверку утверждение, истинное при любых
данных; его тест не способен покраснеть; документ учит тому же тождеству. Гарды политики объявлены
покрытием, не будучи им. Тесты закрепляют выключенные гарды и недостижимые формы. Основное
взаимодействие с канвой не упомянуто ни одним тестом.

**Intended.** Проверка либо способна упасть на внесённом повреждении, либо не публикуется как
проверка. Отсутствие покрытия видно как отсутствие, а не как зелёное.

**Optimal.** Каждая правка сопровождается **контрпробой**: внесённым повреждением, от которого новая
проверка обязана покраснеть. Без контрпробы работа не отличается от той, которую исправляет, — и
этот урок уже оплачен: первая версия гарда T705 проходила на удалении, потому что матчила слово
внутри собственного комментария.

## Non-goals

- **Не строить замену zero-sum** — это 015. 014 снимает ложное утверждение, не создавая нового.
- **Не чинить поведение**, вскрытое покраснением гарда: находка уходит владельцу поведения.
- **Не codemod по группе `T`.** Для каждой копии решение принимается отдельно.
- **Не удалять тесты ради зелёного.** Тест, неспособный упасть, заменяется способным, а не
  вычёркивается — иначе покрытие уменьшается вместе с ложной уверенностью.
- **Не править находки, не прошедшие `T1400`.** **Снято 2026-09-11:** `T1400` выполнена, обе
  группы перепроверены по коду (8 `CONFIRMED`, 1 `REFRAMED`, 0 `REFUTED`), и `T1409` открыта.
  Запрет остаётся в силе для любой будущей находки, входящей в программу без перепроверки.

## Verification plan

**1. Репродьюсеры, обязанные падать на текущем коде.** Здесь они имеют особую форму: репродьюсер —
это **внесённое повреждение**, от которого существующая проверка обязана была покраснеть и не краснеет.

| ID | Внесённое повреждение | Что обязано произойти сегодня | Гейт |
|---|---|---|---|
| `RT-014-1` | увеличить `Debt.amount` одной строки на `0.00000001` **при заведомом запасе trust limit** (иначе повреждение поймает `check_trust_limits`, и репродьюсер докажет не то) | `check_zero_sum` остаётся зелёным, `/integrity/status` публикует `passed=True` | Postgres |
| `RT-014-2` | одинаково увеличить все рёбра замкнутого цикла **минимум из трёх рёбер и с запасом лимитов** — двусторонний цикл поймает debt-symmetry | zero-sum и participant-net остаются зелёными | Postgres |
| `RT-014-3` | **переписан дважды.** Первая форма («изменить формат `find_cycles`») ложна: изменение ключей `debtor`/`creditor` ловят существующие продакшен-тесты (`tests/unit/test_clearing_sql_cycle_detection.py:48,89,170`, `test_clearing_additional_cases.py:195,252-253`). Вторая форма — «удалить оба продакшен-пути извлечения (`real_clearing_engine.py:339-350`, `simulator.py:1681-1695`)» — **опасна: все три координаты мертвы, и вторая сегодня указывает на создание платежа**; выполненный буквально, репродьюсер удалил бы посторонний продакшен-код. Действующая форма, переякорено 2026-09-11: **инвертировать ориентацию ребра во всех трёх местах, где она проставляется** — `app/core/simulator/real_clearing_engine.py:643`, `:645` (ветка разрешения по топологии — поменять местами) и `:650` (fallback — `{"from": a, "to": b}` → `{"from": b, "to": a}`), **и** в `app/api/v1/simulator.py:1927` (`from_=creditor, to=debtor` → `from_=debtor, to=creditor`). Инвертировать только fallback недостаточно: он берётся лишь при отсутствии кэша топологии — `test_clearing_plan_edges_extraction` обязан остаться зелёным, чем и доказывается, что он не наблюдает продакшен. Инверсия выбрана вместо удаления намеренно: удаление ловится компилятором и соседними тестами, а разворот ребра — ровно тот дефект, который эта копия обязана была бы поймать и не ловит. **ИСПОЛНЕНО 2026-09-11, и результат уточняет сам репродьюсер:** под повреждением копия зелёная (`2 passed`), `test_clearing_sql_cycle_detection` и `test_clearing_additional_cases` зелёные; но разворот **в движке** ловит один посторонний тест — `tests/unit/test_real_clearing_engine_partial_failure.py::test_partial_clearing_is_finalized_before_failure_propagates[geo]`. Разворот же в `app/api/v1/simulator.py:1927` не замечает **никто**: весь дефолтный тир `1927 passed, 2 skipped, 137 deselected`, exit `0`, и `SimulatorActionEdgeRef` не упомянут ни одним тестом. Поэтому чистая форма репродьюсера — **только сайт API**; форма «движок + API» тоже годится, но её красный приходит не от проверяемой копии, и утверждать «набор остался зелёным» про неё нельзя | дефолтный |
| `RT-014-4` | удалить `onEdgeClick` из проводки `useSimulatorApp.ts:1423` | набор остаётся зелёным, `typecheck` чист | дефолтный |
| `RT-014-5` | **переписан по внешнему ревью.** Прежняя форма репродьюсером не была: включение защиты — не «внесённое повреждение», и план сам ожидал покраснения теста. Действующая форма: **удалить терминальный код гарда `REAL_MODE_TICK_FAILED_REPEATED` из продакшена** — набор обязан остаться зелёным, что и доказывает нулевое покрытие гарда (замер 008: `git grep -c` по тестам → exit 1) | дефолтный |
| `RT-014-6` | оставить `.pytest_*.db` в рабочем каталоге и выполнить **названную команду**: `scripts/verify_local.ps1` (milestone-прогон) | ни один шаг не краснеет; корень репозитория не сканируется (`verify_local.ps1:81-113`), а `*.db` глобально в `.gitignore:35-44`, поэтому и `git status` молчит | дефолтный |

### `T1401` — исполнение шести репродьюсеров, 2026-09-11

**Шесть из шести исполнены и все шесть показали сегодняшнее ложное зелёное**; три из них при этом
уточнили собственную формулировку — репродьюсер, который не запускали, описан неверно чаще, чем
кажется.

**`RT-014-1` и `RT-014-2` — канонический Postgres-гейт, не debug path.**
`scripts/verify_local.ps1 -TaskSlug p014-rt1412pg -BackendOnly -BackendMarker postgres
-BackendSelector <модуль>`, exit `0`, `3 passed`, `Test database guard passed
(backend=postgresql, database='geov0_test_ci')`. Схема мигрированная
(`GEO_TEST_USE_MIGRATED_SCHEMA=1`). Модуль был **временным** и удалён тем же коммитом: он утверждает,
что проверки остаются зелёными на внесённом повреждении, а тест, закрепляющий дефект, — ровно тот
класс, который эта программа снимает. Постоянной формой это станет в 015, когда появится замена.

Стенд: направленный цикл `A→B→C→A` из трёх рёбер, лимиты доверия `1000` при долгах `100` — то есть
запас `900`, чтобы повреждение поймал не `check_trust_limits`; цикл направленный, поэтому взаимных
долгов нет и `check_debt_symmetry` нечего сказать. Оба условия требует спека, иначе репродьюсер
доказывает не ту проверку.

| | `zero_sum` | `trust_limits` | `debt_symmetry` | нетто-позиции `A/B/C` |
|---|---|---|---|---|
| до повреждения | `PASSED` | `PASSED` | `PASSED` | `0 / 0 / 0` |
| `RT-014-1`: одна строка `+0.00000001` | `PASSED` | `PASSED` | `PASSED` | **`-1E-8 / +1E-8 / 0`** |
| `RT-014-2`: все три ребра `+7.00000000` | `PASSED` | `PASSED` | `PASSED` | **`0 / 0 / 0`** |

**`RT-014-1` подтверждён.** Повреждение реально — оно видно в нетто-позициях — и `check_zero_sum`,
которая публикуется наружу как пройденная проверка целостности, его не видит.

**`RT-014-2` оказался сильнее, чем записано.** Спека ожидала «zero-sum и participant-net остаются
зелёными». Измерено: равномерное раздувание замкнутого цикла оставляет нетто-позиции **побитово
теми же**, `0 / 0 / 0`. Валовый долг при этом вырос на `21.00000000` (три ребра по `+7`), и **ни
одна из трёх опубликованных проверок целостности, ни нетто-позиции** это состояние от исходного не
отличают. Ложное зелёное здесь не «проверка слабая», а «у этих четырёх наблюдателей его нет».

**Две правки в этом абзаце внесены по внешней консультации, и обе — мои ошибки, а не уточнения.**
Первая редакция говорила «300 единиц долга возникли из ничего»: это число не выведено ниоткуда,
арифметика даёт `3 × 7 = 21`. Вторая говорила «**ни одна** проверка системы», без квантора: измерение
покрывает `check_zero_sum`, `check_trust_limits`, `check_debt_symmetry` и `_calculate_net_position` —
и не даёт права утверждать, что состояние не различимо **нигде** в системе. Сохраняю обе поправки
здесь, а не переписываю молча: сфабрикованная ячейка в доказательстве уже стоила волне 013 отдельного
круга ревью, и это второй случай того же класса за две программы.

**«Не способна упасть» — относительно какой модели отказа.** Различение, которого в находке нет:
`check_zero_sum` **способна** покраснеть на мутации собственной реализации (например, если
`_compute_imbalance` перестанет вычитать долги) — и **не способна** на повреждении данных, ради
обнаружения которого она публикуется. Дальше в программе имеется в виду второе. Отдельная оговорка:
телескопирование точно в ноль предполагает согласованный снимок и точную арифметику — два агрегата
выполняются разными `SELECT` (`app/core/invariants.py:63-75`), так что конкурентная запись между ними
теоретически способна дать ненулевой результат. Это ложное срабатывание, а не обнаружение, и класс
находки оно не меняет; формулировка сужена ещё 2026-08-21 по внешнему ревью.

**Алгебра, стоящая за обоими, проверена отдельно, а не пересказана.** `_compute_imbalance`
(`app/core/invariants.py:62-85`) суммирует **одни и те же** строки `Debt` дважды — группировкой по
кредитору и группировкой по должнику — и возвращает разность. Каждая строка даёт `+amount` своему
кредитору и `−amount` своему должнику, поэтому сумма телескопируется в ноль при **любом** наборе
строк с конечными `amount`. Прогнано на трёх произвольных наборах сумм, включая `0.00000001` и
`999.99999999`: `imbalance == 0` во всех.

**`RT-014-3` — исполнен, форма исправлена дважды; разбор в теле `F-014-5` выше.** Под повреждением
копия зелёная, а сайт `app/api/v1/simulator.py:1927` не наблюдает **никто**: дефолтный тир
`1927 passed, 2 skipped, 137 deselected`, exit `0`.

**`RT-014-4` — исполнен, подтверждён.** Проводка `onEdgeClick` (19 строк,
`simulator-ui/v2/src/composables/useSimulatorApp.ts:1423`) удалена целиком:
`npm run typecheck` exit `0`, `npm run test:unit` — **`117 files / 1069 passed`, ровно столько же,
сколько без повреждения**. Основное взаимодействие с канвой удаляется, и набор этого не замечает.

**`RT-014-5` — исполнен, подтверждён.** Терминальный гард `REAL_MODE_TICK_FAILED_REPEATED`
(`app/core/simulator/real_tick_orchestrator.py:566-571`) удалён из продакшена: дефолтный тир
`1927 passed, 2 skipped, 137 deselected`, exit `0`. Якорь, в отличие от якорей `F-014-5`, жив и
точен.

**`RT-014-6` — исполнен именованной командой, подтверждён.** В корень репозитория положены три
изменяемых SQLite-файла (`.pytest_adaptive_int_test.db`, `.pytest_deadlock_test.db` и
`.pytest_stray_rt0146.db` — последний не совпадает ни с одним именем, которое набор создаёт сам, и
потому пережил прогон как чистый свидетель). Затем выполнена `scripts/verify_local.ps1`. **Ни один
шаг не покраснел и ни один файл не заметил:**

| Шаг | Результат при лежащем в корне `.db` |
|---|---|
| `Test database safety guard` | `passed` — валидирует **URL**, файловую систему не смотрит вовсе |
| `Backend tests (pytest)` | `1927 passed, 2 skipped, 137 deselected` |
| `Single Alembic migration head` | `passed`, `019_trust_lines_partial_unique_live` |
| `Admin UI lint` | `0 errors` |
| `Admin UI unit tests` | `42 files / 383 passed` |
| `Admin UI production build` | собралась |
| `Simulator UI v2 correctness lint`, `typecheck`, `unit tests` | пройдены |
| `Simulator UI v2 production build` | `✓ built in 5.18s`, exit `0` |

`git status` при этом **молчит**: `.gitignore:47-48` покрывает `.pytest_*.db` и `-journal`. То есть
изменяемая БД в рабочем каталоге не видна ни гейту, ни VCS.

**Оговорка о том, как получен последний шаг, потому что без неё запись читается шире, чем есть.**
Сборка симулятора на этой машине не запускается напрямую: её `prebuild` зовёт `pwsh` (PowerShell 7),
которого здесь нет — стоит только Windows PowerShell 5.1. Это **машинное условие, а не дефект ветки
и не находка**: оно записано в заметках тулчейна с 2026-08-20 вместе с обходом. Шаг выполнен
задокументированным обходом — шим `pwsh.cmd` → `powershell.exe` на `PATH`, вне репозитория. По той же
причине **зелёный `verify_local.ps1` на этой машине всегда композит двух прогонов**, и здесь он тоже
композит: бэкенд и админка — из общего прогона без шима, сборка симулятора — отдельным запуском с
шимом. Одним прогоном это дерево зелёным не бывает **ни при какой ветке**.

**Попутно измерено и к находке не относится:** первая попытка этого прогона упала на
`Admin UI production build`, и падение оказалось артефактом **моего вызова**, а не сборки —
`2>&1` на нативном `node` в PowerShell 5.1 оборачивает каждую строку stderr в `NativeCommandError`
и роняет шаг при exit `0`. Записано здесь потому, что это ровно предмет `F-014-11`: вердикт,
произведённый аппаратом наблюдения, а не кодом. Инструментальная ошибка, не находка программы.


**2. Инварианты, обязанные выжить, и контрпроверки.**

- Каждая новая проверка сопровождается контрпробой: **повреждение внесено — проверка красная**;
  **повреждение снято — зелёная**. Обе половины обязательны; одна половина — это и есть ложное
  зелёное.
- Исходный гард (`F-014-8`) проверяет **исходное выражение**, а не наличие ключа — контрпроверка:
  подмена значения при живом ключе обязана ронять гард. Прецедент проверен на T705: подмена
  `computed(() => real.runStatus)` на `computed(() => null)` роняет.
- Стриппер комментариев в исходном гарде действительно работает — контрпроверка: гард обязан падать,
  если искомое слово оставлено только внутри комментария. Прецедент: первая версия гарда T705 именно
  так и проходила.
- Снятие публикации zero-sum **не уменьшает** объём проверок: контрпроверка — `/integrity/status`
  обязан продолжать сообщать trust-limit, symmetry и clearing-neutrality, которые падать способны
  (`app/core/invariants.py:87-235`, `:260-289`).

**3. Существующие селекторы, обязанные остаться зелёными.** Поимённо:

- `tests/unit/test_invariants.py` — поверхность `F-014-1`/`F-014-2`; после правки часть его
  утверждений **обязана измениться**, и это ожидаемо
- `tests/unit/test_integrity_checkpoints.py`, `tests/integration/test_integrity_endpoints.py`
- `tests/integration/test_integrity_repairs_atomicity.py`
- `tests/unit/test_clearing_plan_edges_extraction.py` — поверхность `F-014-5`
- `npm --prefix simulator-ui/v2 run test:unit` и `npm --prefix simulator-ui/v2 run typecheck`
- Тиры целиком: дефолтный backend и Postgres marker-тир

Числа прохождения здесь сознательно **не приводятся**: `AGENTS.md:55` запрещает хранить в документах
протухающие значения. Владелец значения — сама команда.

**4. Запрещённые способы доказательства.**

- **Запрещено** засчитывать «тест зелёный» как доказательство работоспособности гарда. Единственное
  доказательство — **красный на внесённом повреждении**. Это правило программы, а не пожелание.
- **Запрещено** закрывать `F-014-8` тем же приёмом, что позиции 1 и 2, **без контрпробы на подмену**:
  гард на наличие ключа слабее гарда на выражение, и разница уже оплачена.
- **Запрещено** объявлять `F-014-9` закрытым исходным гардом. 007 записала это явно: гард не
  исполняет код и покрытия не заменяет.
- **Запрещено** считать `N passed` доказательством чего-либо без осмотра артефактов — прямая
  формулировка из исследования 015, применимая ко всей программе.
- **Запрещено** править `F-014-6` и `F-014-10` до `T1400`. **`T1400` выполнена 2026-09-11 —
  запрет снят.**

## Tasks

Легенда: `[x]` выполнено · `[ ]` авторизовано, в работе или ещё не начато · `[!]` заблокировано
или не авторизовано.

**Все `[!]` сняты 2026-09-11.** Они стояли по неверной строке `Status` выше, а не по реальному
блокеру: реализация авторизована владельцем 2026-08-21 вместе со всей волной 009–014. Зависимость
`Depends on: 009–013` удовлетворена по её же собственному основанию — «пока поведение не исправлено»:
009–012 закрыты, реализация 013 выполнена и слита. То, что закрытие 013 ждёт датированного принятия
риска, — артефакт закрытия 013, а не блокер 014.

| ID | Задача | Статус |
|---|---|---|
| T1400 | **Перепроверка по коду** шести экземпляров группы `P` (`F-014-6`) и трёх позиций `AA` (`F-014-10`). Исход по каждой: подтверждена с якорем / `REFUTED` / переформулирована. **Выполнено 2026-09-11: 8 `CONFIRMED`, 1 `REFRAMED` (`C-C4-005` — механизм был назван неверно), 0 `REFUTED`.** Попутно найдены четыре дефекта самой записи (`C-A4-3-011` без якоря вообще; `C-C4-005` и `R-C4-002` приписаны каждый двум разным файлам; две протухшие цифры) и одна новая позиция того же класса — побайтово одинаковые эталоны `scene-A`/`scene-C`. `T1409` разблокирована | `[x]` |
| T1401 | `RT-014-1`…`RT-014-6` — все шесть обязаны показать сегодняшнее ложное зелёное. **Исполнены 2026-09-11: шесть из шести показали.** `RT-014-1`/`RT-014-2` — каноническим Postgres-гейтом, exit `0`; `RT-014-2` оказался сильнее записанного (нетто-позиции тоже не меняются); `RT-014-3` потребовал переякорения и уточнения формы. Измерения — в разделе `T1401` выше | `[x]` |
| T1402 | `F-014-1`: zero-sum перестаёт публиковаться как пройденной проверкой; документ `02-protocol-spec.md` перестаёт учить тавтологии. **Замену не вводить** — 015. **Выполнено 2026-09-11** вариантом `C`: ключ остаётся, вердикта не несёт, `passed` запрещён схемой; совокупный статус сужен до выполненных проверок и разрыв назван в `unverified`. Все четыре call-site закрыты, включая платёжный путь. Разбор — в разделе `T1402`/`T1403` выше | `[x]` |
| T1403 | `F-014-2`: ассерты, истинные при любых данных, заменяются способными упасть — каждый с контрпробой. **Выполнено 2026-09-11** вместе с `T1402`, от которого зависит. **Сайтов оказалось три, а не пять:** `C-A4-2-001` и `C-A4-4-003` — те же два ассерта, названные вторично. Семь мутаций, семь красных | `[x]` |
| T1404 | `F-014-3` + `F-014-4`: тесты перестают закреплять выключенный гард и недостижимую форму | `[ ]` |
| T1405 | `F-014-5`: по каждой копии — сверка с оригиналом либо удаление, с основанием | `[ ]` |
| T1406 | `F-014-7`: гард, делающий появление изменяемой БД в рабочем каталоге красным | `[ ]` |
| T1407 | `F-014-8`: исходный гард на `onEdgeClick` с контрпробой на подмену | `[ ]` |
| T1408 | `F-014-9`: полноценное инстанцирование `useSimulatorApp` в тестах. **Отдельная оценка объёма перед началом** — работа крупная и в T1407 не входит | `[ ]` |
| T1409 | `F-014-6` и `F-014-10` — по результату `T1400`. **Разблокирована 2026-09-11:** девять подтверждённых экземпляров с живыми якорями и репродьюсерами, плюс четыре дефекта записи к исправлению вместе с кодом | `[ ]` |
| T1412 | `F-014-11`: экспонированные ассерты на выводе PowerShell — нормализация и пин. **Выполнено 2026-09-11, и пересчёт изменил условие задачи:** экспозицию определяет канал вывода, а не длина фразы, поэтому их **12, а не 8** (9 различных диагностик из 77 ассертов); заголовочная 57-символьная фраза идёт через `[Console]::Out.Write` и **неуязвима**; зависимость от ширины **немонотонна**, так что предписанный пин «узкая консоль зелёная» был бы зелёным на живом дефекте; сработавшая ось — **локаль** (`ПРЕДУПРЕЖДЕНИЕ: ` на 7 колонок длиннее `WARNING: `), а консоли в том прогоне не было вовсе. Пин заменён на **инвариантность по всем позициям переноса в обоих префиксах**, с контрпробой на невакуумность, негативными контролями и гардом класса со своим контртестом. Разбор — в теле находки | `[x]` |
| T1410 | **Внутренний adversarial review** субагентом (§16.4 `AGENTS.md`) | `[ ]` |
| T1411 | **Независимое внешнее ревью и публикация evidence на точном HEAD** — §15: программа меняет сам механизм проверки (гарды, CI, conftest), а ревьюер собственного гейта независимым не является | `[ ]` |

## Changelog

- **2026-08-21.** Программа заведена по маршруту 008 — последняя в волне 009–014.

  **Перепроверено мной по коду:** `F-014-1` (тавтология `check_zero_sum` — `invariants.py:22-85`, и
  её публикация — `integrity.py:105-108`, `:210-213`), `F-014-2`
  (`test_invariants.py:23-35` — один здоровый долг и `assert == {}`), `F-014-5`
  (`test_clearing_plan_edges_extraction.py:27-42` — литерал формата внутри теста), `F-014-7`
  (относительные `_TEST_DB_PATH` в двух интеграционных модулях), `F-014-8`
  (`grep onEdgeClick --include=*.test.ts` → 0 файлов).

  **Разграничение с 015 зафиксировано в owner surface, а не в комментарии.** 014 имеет право снять
  ложное утверждение и **не имеет права** строить замену: попарная сверка `debts` с журналом требует
  изменений модели данных и собственного внешнего ревью, и это предмет 015. Без этого разграничения
  программа неизбежно расползлась бы в переписывание ядра целостности.

  **`MISSED-3` исключён из группы `AA`** — маршрут 2026-08-21 уже перевёл его в группу `Q`, то есть
  в 012. В `AA` остаются три позиции, и все три — **без диспозиции**, а не с отложенной.

  **`F-014-9` выделен из `F-014-8` намеренно.** Исходный гард закрывает шов от молчаливого разрыва
  проводки, но покрытием не является — 007 записала это явно. Смешивать их в одной задаче значило бы
  повторить ту самую подмену «зелёное = проверено», которую программа разбирает.

- **2026-08-21, внешнее ревью Codex — вердикт `REFUTED`, шесть правок приняты.** Клон на `21be337`,
  `codex-cli 0.144.0-alpha.4`, `--sandbox read-only`, exit `0`, маркер `VERDICT-DONE`.

  Ревьюер отдельно отметил, что не смог выполнить mutation-прогоны: sandbox read-only, а debug-only
  `pytest` упал до сборки с `FileNotFoundError: No usable temporary directory found`. Вывод сделан
  статически, и это записано, а не сглажено.

  **Принято и исправлено:**
  1. **«При любых значениях» сужено** до «при любом согласованном снимке строк с конечными `amount`»:
     два агрегата — отдельные `SELECT`, поэтому конкурентная запись между ними способна дать
     ненулевой результат. Это ложное срабатывание, а не обнаружение, и класс находки не меняет.
  2. **Публикация шире двух API-сайтов:** к ним добавлены checkpoint (`core/integrity.py:69-76`) и
     вызов с платёжного пути (`payments/engine.py:1242-1253`).
  3. **Граница с 015 была несостоятельной** и переопределена по работе, а не по файлу. Снятие
     публикации невозможно «только в `invariants.py`»: статус формируется в трёх местах, `admin-ui`
     печатает отсутствующий ключ как **failed** (`IntegrityPage.vue:466`), а «не проверяется» сегодня
     невыразимо — `InvariantResult.passed` обязателен (`schemas/integrity.py:15-19`). Плюс черновик
     015 назначал ту же работу себе; пересечение снято передачей `T1507` сюда.
  4. **`F-014-5` оказалась хуже описанного и была недооценена.** `extract_cycle_edges_for_ui`
     **объявлена в самом тесте** (`:6-24`, единственный импорт — `pytest`), а продакшен извлекает
     рёбра двумя независимыми путями. Атрибуция «пересекает границу языков» неверна — настоящий
     cross-language экземпляр это `test_topology_changed_no_empty_payload.py:229-260`. Состав группы
     восстановлен (`B-A4-6-001`, `B-A4-6-002` — оба P2), severity поднята P3 → **P2**.
  5. **`RT-014-3` и `RT-014-5` были ложными репродьюсерами** и переписаны. Первый: изменение формата
     ловят существующие продакшен-тесты. Второй: включение защиты — не повреждение, а план сам ждал
     покраснения. Обе формы заменены на удаление продакшен-пути с требованием, чтобы набор **остался
     зелёным**. `RT-014-1`/`RT-014-2` получили условия, без которых их поймал бы другой инвариант.
     `RT-014-6` получил названную команду.
  6. **Протухающие числа прохождения тестов убраны** — прямой запрет `AGENTS.md:55`.

  Исключение `MISSED-3` ревьюер подтвердил как обоснованное.

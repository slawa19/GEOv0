# Продолжение программы 015 в новой сессии — передача 2026-09-14

Сессия остановлена по лимиту. Этот файл — сообщение для следующей сессии оркестратора. Всё нужное лежит в репозитории.

## Сообщение для вставки в новую сессию

> Продолжаем закрытие программы 015 `financial-core-verification` в GEOv0. Ты — оркестратор (AGENTS.md §15): работа
> через агентов, свои проверки их результата, Codex — ревьюер на ключевых точках. Прочитай сначала:
> `AGENTS.md` §19 и **§19.5** (правило остановки), в `specs/015-financial-core-verification/spec.md` раздел
> **«F. Закрытие»** (grep `F. Закрытие`) и строки `T1548`, `T1535`, `T1523`, `T1509`; затем
> `specs/015-financial-core-verification/closure-briefs/HANDOFF-2026-09-14.md` целиком.
> Шаг F1 (`T1549`) сделан и слит. Следующий — **F2 `T1548`**: запусти агента по брифу
> `closure-briefs/F2-t1548-brief.md`, проверь его результат сам, один узкий круг Codex по дельте, запиши, слей.
> Затем F3 `T1535`, F4 `T1523`, F5 `T1509` — по брифам в той же папке. Класс 2 по §19.5 — только в
> `specs/BACKLOG.md`, никаких новых задач в 015. После `T1509` предложи владельцу закрыть 015 и вернуться к
> продуктовым фазам README.

## Где мы

- **Шаг 5 и `T1544` слиты** в `main` (`dda21f3`): стоп эквивалента, baseline и критерий (а), критерий (б) и конверт
  платежа v2, реакция и удержание.
- **Закрытие задано владельцем 2026-09-14** (коммит `8e0fd45`): пять шагов F1–F5, правило §19.5, 22 задачи отложены в
  `specs/BACKLOG.md`, программа 016 не авторизована.
- **F1 `T1549` выполнен** этой передачей: тестовый движок PostgreSQL на `SERIALIZABLE` приложения, держатель и мутации,
  один дефект класса 2 (одновременное создание линии доверия → `500`) — `xfail(strict=True)` и запись в BACKLOG.
  Независимый PostgreSQL-гейт оркестратора по F1 **не прогонялся**; закрывающее ревью `T1509` его покрывает.

## Очередь и брифы

| Шаг | Задача | Бриф | Состояние брифа |
|---|---|---|---|
| F2 | `T1548` — повтор `tx_id` без отпечатка отвечает `409`, денег не двигает | `F2-t1548-brief.md` | готов; дефект подтверждён по коду: `app/core/payments/service.py:247-249` сравнивает отпечаток только при его наличии |
| F3 | `T1535` — голый `alembic upgrade head` на свежей базе | `F3-t1535-brief.md` | готов; **сначала проверить**, есть ли `version_table_column_type` в установленном Alembic — если нет, стоп |
| F4 | `T1523` — матрица ≤ 8 ячеек и один настоящий перезапуск | `F4-t1523-brief.md`, инвентаризация `F4-t1523-inventory.md` | готов, прошёл ревью Codex (три правки внесены: процесс B — второй процесс ОС; анти-вакуум перед тройкой; ячейка 5 без предположения `40001`) |
| F5 | `T1509` — закрывающее внешнее ревью по §19.5 | `F5-t1509-closing-review.md` | черновик чек-листа и промпта; `<BASE>` подтвердить при запуске (ветка несёт и работу 012) |

## Базовые числа на момент передачи (после F1)

- SQLite `scripts/verify_local.ps1 -TaskSlug <slug> -BackendOnly`: **2378 passed / 2 skipped / 280 deselected**.
- PostgreSQL, полный: **275 passed / 1 xfailed / 0 failed / 351 deselected** —
  `-BackendMarker postgres -BackendSelector tests/integration`, `GEO_TEST_USE_MIGRATED_SCHEMA=1`,
  `GEO_TEST_ALLOW_DB_RESET=1`, своя база агента на `127.0.0.1` (никогда `localhost`).
- `tests/contract`: **125 passed**. Голова миграций: `028_equivalent_integrity_hold`.

## Грабли этой недели, которые стоят времени

1. **Память машины.** 2026-09-14 кончился свободный commit памяти: ложный красный PostgreSQL-гейт
   (`connection is closed` в несвязанных файлах) и два падения Codex по памяти. Перед каждым гейтом —
   `(Get-CimInstance Win32_OperatingSystem).FreeVirtualMemory`; меньше 2 GB — красный прогон не evidence. Помогла
   перезагрузка.
2. **PostgreSQL после перезагрузки:** `Start-Process pg_ctl.exe -ArgumentList start,-D,C:\Users\admin\tools\pgdata,-l,
   C:\Users\admin\tools\pgdata\server.log -WindowStyle Hidden` **без `-Wait`**, затем `pg_isready -h 127.0.0.1 -p 5432`.
3. **Codex:** `codex exec --sandbox read-only -c model_reasoning_effort="high" - < prompt > out`; в промпт — «бюджет
   чтения» (grep и узкие диапазоны, без больших файлов целиком). Маркеры вердикта читать в **хвосте** вывода: grep по
   «последнему `VERDICT:`» однажды нашёл старый вердикт внутри прочитанного Codex файла.
4. **Агент, написавший «жду монитор», стоит навсегда** — уведомление о его завершении значит, что живых фоновых задач у
   него нет. Проверить процесс и возобновить прямым текстом «опрашивай сам».
5. **Мутация «снять гард» проверяет наличие, а не место.** У гарда с решённым местом заказывать тест, краснеющий от
   сдвига. Отказ проводить через цикл-хозяина (тик симулятора), а не звать функцию напрямую.
6. **Коммитить только свои файлы поимённо**; слияние — через worktree `D:/www/projects/2025/GEOv0-wt-base`
   (`git pull --ff-only`, `git merge --no-ff`, `git push`). `app/core/simulator/inject_executor.py` иногда виден
   изменённым без изменения содержимого — устаревший stat, не коммитить.

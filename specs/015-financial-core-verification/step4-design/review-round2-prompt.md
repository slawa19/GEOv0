You are the external technical reviewer for GEOv0, round 2 of the phase B step 4 design review. Read-only. Answer in Russian, concisely, with file:line references. REFUTE, do not approve.

Working directory is a frozen clone at `7de0a7c`. Files next to the clone (one level up): `step4_design_v2.md` (the design under review), `codex_step4_answer.md` (your round-1 answer, verdict NO-GO), `sqlalchemy-2.0.25-src/` (the installed SQLAlchemy source; the clone has no venv). Binding contract: `specs/015-financial-core-verification/spec.md`, "### Фаза B — архитектурный контракт ядра" and the `B4`/`T1525` records.

This is a follow-up round, not a fresh review (AGENTS.md §15): judge whether each of your round-1 "Обязательные изменения до кода" (1-10) is actually closed by v2, and review NEW mechanisms v2 introduced. Do not reopen accepted points without new evidence.

Answer:
1. For each round-1 mandatory change 1-10: CLOSED / PARTIAL / OPEN, with the reason and, for PARTIAL/OPEN, the concrete remaining gap.
2. The new Core-event mechanism (v2 §1): registry keyed by Core `RootTransaction` in a WeakKeyDictionary; refusal inside `commit`/`release_savepoint` events doing DB rollback before raising; `rollback_savepoint` dropping ops while keeping root poison. Find any sequence where: a Debt change becomes durable without a COMPLETED envelope; a refused commit leaves an open or half-committed DB transaction on asyncpg/PostgreSQL (the design only probed pysqlite/aiosqlite — say what asyncpg's adapter does on `do_rollback` inside the commit event and whether `AsyncAdapt_asyncpg_connection` transaction state stays consistent); the WeakKeyDictionary loses state early (RootTransaction garbage-collected or replaced while the DB transaction continues, e.g. Connection re-used via `session.connection()` after `Session.commit` with `expire_on_commit=False`, pool return, `invalidate`); private attributes `_savepoint`, `_previous_nested`, `_flushing`, `_execution_options` behave differently than claimed.
3. The runtime write guard (v2 §6.3, `before_execute` + per-flush grants + `visitors.iterate` for `UpdateBase`): bypasses? (e.g. `Session.execute` with `insert(Debt).values()` when the grant is still valid during a flush from an event hook; `connection.execute(Debt.__table__.update())` from inside `after_flush` of another listener; `executemany` via `insertmanyvalues`; `returning`; ORM `update()` with `synchronize_session`). Is refusing in `before_execute` sufficient without a DB rollback (it fires before SQL)?
4. T1525 (v2 §3): is the app-level reproduction credible from the code (`PaymentEngine.commit` on SQLite, `engine.py` ~1040-1300, `_apply_flow` savepoint ~1429)? Is the fix recipe right for aiosqlite 0.20 + SQLAlchemy 2.0.25 (`isolation_level=None` on the adapted connection vs the underlying sqlite3 connection; `begin` event `exec_driver_sql("BEGIN")`), and what does it break (WAL pragma placement, `database is locked`, the adaptive clearing background session, `test_simulator_clearing_no_deadlock`)? Is P1 severity justified? Is deferred BEGIN the right choice?
5. Money precision rule (v2 §4): is the 2^26 domain claim correct for SQLAlchemy's SQLite Numeric processors? Is a per-dialect round-trip storability refusal in production acceptable, or does it create a divergence between SQLite dev and PostgreSQL behaviour that itself breaks tests?
6. Schema (v2 §5) and the step-5 split: anything that blocks step 5, or CHECKs that are wrong/unportable to SQLite (e.g. `length()` on CHAR, partial index `sqlite_where`)?
7. Counterexamples C1-C22 and §10: remaining vacuous or proxy tests; is C6 now non-vacuous and does it truly pass the existing barriers (verify `check_payment_delta` and `check_trust_limits` in `app/core/payments/engine.py`); is §10.2 acceptable as written text rather than code under the contract's "Отложенные позиции не превращаются в пропускаемые зелёные тесты"?
8. Codemod rules R1-R6: sufficient? Is trace equality (R4) implementable and not itself a source of false green?
9. Forks R2-1..R2-14: AGREE/DISAGREE each, one sentence.
10. Sequencing: can T1525 start now independently of the rest? Anything in T1525's scope that must wait for this design?

Output: verdict line `STEP4-DESIGN-R2: GO | GO-WITH-CHANGES | NO-GO`, a separate line `T1525-START: YES | NO`, then the numbered answers, then "Обязательные изменения до кода" (only what is still required; each concrete and testable). No code patches.

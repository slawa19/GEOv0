You are the external technical reviewer for GEOv0 (FastAPI + SQLAlchemy 2.0.25 async; PostgreSQL SERIALIZABLE in production, SQLite default test tier via create_all; Alembic). Read-only. Answer in Russian, concisely, with file:line references. Your job is to REFUTE, not to approve: find where this design is wrong, unsafe, contradicts the contract, or will produce false green.

Programme goal: zero sum of all debts, 100% reliable and idempotent money transactions. The binding contract is `specs/015-financial-core-verification/spec.md`, section "### Фаза B — архитектурный контракт ядра" (≈ lines 575-775) — read it fully. Steps 1 and 3 are done (B3 record in the same spec). The design for step 4 below was produced by an architect agent from the current tree (HEAD `0a9b7b6`). The design document is at the end of this prompt, between the markers.

Answer:

1. For each fork F1-F17: AGREE or DISAGREE with the recommendation, one or two sentences why, and the alternative if you disagree.
2. Correctness of the transaction binding (section 3) against SQLAlchemy 2.0.25 as installed (`.venv/Lib/site-packages/sqlalchemy/orm/session.py`, `state.py`): is there any path where (a) a Debt change commits without a completed envelope; (b) an envelope/entries survive a rollback; (c) a retry reuses a stale op; (d) the root-poison is lost (e.g. poison stored in session.info cleared by a savepoint end, `session.close()`, `expire_all`, or the AsyncSession greenlet boundary); (e) `before_commit` misses a commit (e.g. `Connection`-level commit by `_commit_to_terminal` in `app/core/clearing/service.py`, `engine.begin()` blocks, the tests' `join_transaction_mode="create_savepoint"`)? Give concrete sequences.
3. The hook (section 4): is `history.deleted[0]` / `history.unchanged[0]` actually the "value before this flush" in every writer path (payments `_apply_flow` + netting in `app/core/payments/engine.py` ~1420-1505, clearing ~1976-1990, inject)? Is writing entries in `after_flush` via `session.connection()` safe under the unit of work (autoflush recursion, `_warn_on_events`), and on SQLite? Is `do_orm_execute` sufficient to refuse bulk DML on `Debt` for all app execution paths (ORM-enabled update/delete, `Debt.__table__` through Session)?
4. Concurrency: the per-equivalent head CAS at completion under SERIALIZABLE plus the equivalent owner lock (`PaymentEngine.acquire_staged_equivalent_owner_locks`), clearing's session-level lock with a fresh snapshot, and the inject owner. Any deadlock, lost seq, duplicate seq, or 40001 storm that the payment retry budget (`COMMIT_RETRY_ATTEMPTS`, `app/config.py`) would turn into user-visible failures? Does the head row write happen while every writer of that equivalent holds the owner lock — check all three writers.
5. SQLite: journal digests computed from values read back from SQLite `Numeric` (REAL affinity) — can the default tier produce a digest or continuity check that differs from PostgreSQL for the same operations and so give a false red/green? What should be the rule?
6. Tests (section 5): is `debt_fixture_setup` with kind `TEST_FIXTURE` compliant with the contract's "тест-локальный контекст, закрываемый до вызова кода приложения" and the prohibition of a global test mode / default context? Is the 53-file mechanical migration the smallest safe path, and what must be checked so the codemod does not silently change what a test asserts?
7. Counterexamples C1-C18 (section 6): which are missing that the contract's step 2 requires, which are vacuous or test a proxy, and which must be PostgreSQL rather than SQLite.
8. Scope: anything in the design outside the contract's allowed perimeter ("Граница владения расширяется") that needs an explicit amendment before code; anything that should be deferred to step 5+ to keep step 4 reviewable.

Output format: a verdict line `STEP4-DESIGN: GO | GO-WITH-CHANGES | NO-GO`, then the numbered answers, then a list "Обязательные изменения до кода" (each concrete, testable). No code patches.

=== DESIGN DOCUMENT BEGIN ===

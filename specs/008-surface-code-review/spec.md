# 008 — Сплошное код-ревью трёх поверхностей

- **Date:** 2026-08-11 (обновлено 2026-08-15)
- **Status:** AUTHORIZED — фаза I; волна 1 закрыта 2026-08-11 на `ea9cde9`; план скорректирован
  2026-08-12 по `plan-audit-2026-08-12.md`; **волны 2, 3 и 4 закрыты 2026-08-14, волна 5 закрыта
  2026-08-15**; волна 6 не начата, frozen HEAD — `7cb5149eb03c902977ac0d740d73a933a54e372e`
- **Status authority:** метка описательная; завершённость устанавливают success criteria и записанные evidence, а не поле статуса.
- **Owner surface:** фаза I — весь продуктовый код `app/`, `admin-ui/src/`, `simulator-ui/v2/src/` и `tests/` **read-only**. Фаза II (правки) — только `clean`/`delete`-класс вне денежного ядра; см. «Owner surface» ниже.
- **Origin:** запрос владельца 2026-08-11 и четыре его решения того же дня (глубина, режим денежного ядра, темп, судьба тестов) — раздел «Решения владельца».
- **Почему одна программа:** предмет один — качество кода как таковое, а не поведение отдельной подсистемы. Разрезать по поверхностям нельзя: половина находок (дубли mock/real, разошедшиеся форматтеры, контракт, увиденный тремя реализациями по-разному) видна только при сопоставлении реестров разных треков.

## Problem

Программа 001 закрыта, 002–006 специфицированы. Все они искали **дефекты поведения**: деньги,
конкурентность, периметр, честность гейтов. **Ни одна не читала код сплошняком.** Качество самого
кода — мёртвое, дублируемое, непереиспользуемое, размазанное по God-файлам — известно только
точечно, и то как побочный улов чужих аудитов.

Измеренный масштаб непокрытого (2026-08-11, HEAD `ea9cde9`, продуктовый код без тестов):

| Поверхность | Файлы | LOC |
|---|---|---|
| `app/` | 123 | 30 914 |
| `simulator-ui/v2/src` | 266 | 29 742 |
| `admin-ui/src` | 111 | 21 671 |
| **Итого** | **500** | **≈ 82 300** |

Плюс 31 224 строки тестов двух UI и 165 тестовых файлов бэкенда.

**Наблюдаемый вред, а не «хотелось бы чище».** Находки этого класса уже подтверждены — но найдены
попутно, поиском их никто не занимался, поэтому неизвестно, сколько их ещё:

- два параллельных источника правды одного контракта расходятся семантически:
  порог bottleneck считается во float в SQL и точным decimal в моке — KPI на границе различается
  между режимами (`specs/BACKLOG.md`, «Bottleneck-порог»);
- ~240 строк политики вместимости продублированы в `engine.prepare` и `engine.prepare_routes`
  (`app/core/payments/engine.py:359-580` против `:582-822`);
- мёртвые экспорты живут в обеих поверхностях (`router.find_paths`, `restartRun`,
  `bestEffortTotal`);
- одноимённые функции с разными телами в трёх лаунчерах.

Стоимость измеряется в синхронных касаниях: правка одного правила требует найти и изменить N мест,
и цена ошибки — молчаливое расхождение режимов, а не падение.

## Owner surface

**Фаза I — разведка, read-only.** Читается всё: `app/**`, `admin-ui/src/**`,
`simulator-ui/v2/src/**`, `tests/**`. Ни одной правки.

**Фаза II — правки, разрешено только после триажа владельца:**

- разрешено: `clean` и `delete` вне денежного ядра — мёртвый код, дубли внутри файла, удаления
  после reference scan;
- **запрещено полностью:** `app/core/payments/`, `app/core/clearing/`, `app/core/recovery.py` —
  денежное ядро остаётся read-only по решению владельца 2026-08-11. Программы 002–006 с тех пор
  **закрыты** (см. `specs/README.md`), поэтому находки идут в `specs/BACKLOG.md`, а не в их спеки;
- вне периметра: `simulator-ui/v1/`, любые `archive/`, `_audit/`, `_audit_xfer/`, применённые
  migrations, generated fixture copies, `plans/`;
- `merge` и `rewrite` требуют отдельного решения владельца по каждому случаю — они не входят в
  «дешёвые правки».

### Границы соседних поверхностей (аудит A5, 2026-08-12)

Чтобы исключённое отличалось от забытого:

| Поверхность | Статус для 008 |
|---|---|
| Применённые migrations | исключены намеренно (защищены §8 AGENTS.md; Alembic-head гейт канонический) |
| `tests/conftest.py`, семантика маркеров `pytest.ini` | **в scope** — волна 5 (A4) |
| `scripts/`, `.github/workflows/` | вне scope; исторический owner — 006. Находки о гейтах, всплывшие попутно, передаются в BACKLOG с пометкой owner surface |
| `docker/`, container parity | вне scope; исторический owner — 005; та же передача через BACKLOG |
| `app/config.py`, `app/main.py` | **в scope** — слайс A1c (добавлен по F-PA-2) |
| Канонические fixtures/generators/seeds | вне scope инвентаря; проверяются только как call-sites; находки — в BACKLOG |
| Playwright configs/suites | **в scope** — слайсы B5/C5 волны 5 (read-only; browser-запуск только для подтверждения runtime-находки) |
| OpenAPI (`api/openapi.yaml`) | **в scope** — контрактная ось R2 и триангуляция D1 |
| Threat model / pentest / dependency / crypto audit | не является целью: это code review security-релевантного кода, не security-аудит |
| Продуктовые `docs/ru/` | источник intended behavior, не поверхность инвентаря |

## Findings

Реестры находок ведутся по волнам в
`plans/review-2026-08-11-three-surfaces/` (untracked evidence, см. `plan.md` §5).
**Подтверждённые** находки сводятся сюда таблицей по мере закрытия волн.

**Волна 1 закрыта 2026-08-11.** Прочитано **48** файлов / 11 149 строк (A1a 3/3449, A1b **28**/4322,
C1 17/3378). Полнота проверена пофайловым сопоставлением с `git ls-files`: файлов без строки
вердикта — ноль (счёт A1b исправлен 2026-08-14 с 26 на 28 — арифметическая описка в шапке реестра,
не пробел чтения; разбор — `evidence-index.md`). Вердикты: `keep` **29**, `clean` **18**, `delete` 1,
`merge` 0, **`rewrite` 0**.
Находок 40: **P2 — 7**, P3 — 33. Плюс 10 пунктов помечены `known` (BACKLOG, 005, 006) и
31 гипотеза убита агентами на самопроверке.

Волны 2–6 не запускались. Для них состояние — «не измерено», а не «дефектов нет» (AGENTS.md §1).

**P2 волны 1** — каждый перепроверен оркестратором по коду вручную. Таблица — исторический реестр
на `ea9cde9`; **текущие статусы после re-baseline 2026-08-12** (аудит A1, 212 коммитов спустя):
5 × LIVE, `C-C1-002` — FIXED_EXTERNALLY (`84ca396`), `C-A1a-003` — UNVERIFIED;
`C-A1a-001` дополнительно подтверждён исполняемо (контрактный селектор: без флага `23 passed`
exit 0, с `SIMULATOR_ACTIONS_ENABLE=1` — `1 failed` exit 1). Сводно — `evidence-index.md`.
Аудит также нашёл восьмую находку `C-C1-012` (см. последнюю строку).

| ID | path:line | Класс | Sev | Суть | Вердикт перепроверки | Триаж |
|---|---|---|---|---|---|---|
| `C-A1b-001` | `app/schemas/simulator.py:95,110` | C | P2 | `serialize_by_alias=True` — no-op на закреплённом `pydantic==2.5.3` (ключ введён в 2.11). Защита алиасов `from`/`to` (§8) инертна; wire цел только потому, что все 6 продюсеров в `sse_broadcast.py` передают `by_alias=True` руками | **CONFIRMED интроспекцией:** `model_dump()` и `model_dump(mode="json")` дают `{'from_': …}`; `'serialize_by_alias' in ConfigDict.__annotations__` → `False`. Других сериализаторов этих моделей в `app/` нет | fix now |
| `C-A1a-001` | `app/api/v1/simulator.py:891,1088,1257,1415,1587,1732,1784,1940` | C | P2 | 8 живых interact-маршрутов отсутствуют в `openapi.yaml`, а гард паритета путей проходит вхолостую: `include_in_schema=_actions_enabled()` читает env на импорте | **CONFIRMED:** путей в каноне нет; `tx-once`/`clearing-once` того же семейства — есть (`openapi.yaml:1480,1524`), исключение непоследовательно; гард — `assert generated_versioned_paths == canonical_paths` (`tests/contract/test_openapi_contract.py:1003`), зелёный зависит от переменной окружения | fix now |
| `C-C1-001` | `admin-ui/src/api/realApi.ts:884-886` | C | P2 | `graphSnapshot` не шлёт `include`, поэтому `incidents`/`audit_log`/`transactions` в real-режиме пусты всегда (гейт `app/api/v1/admin.py:1643-1662`), а в моке полны | **CONFIRMED** чтением обеих сторон | **чинить в паре с `C-C1-012`** (сцепка с `C-C1-002` устранена внешним фиксом) |
| `C-C1-002` | `admin-ui/src/api/realApi.ts:155-170` | B | P2 | `AuditLogEntrySchema` требует `actor_id: z.string()`, бэкенд всегда пишет `actor_id=None` (`admin.py:277`) | **CONFIRMED, сцеплено:** `GraphSnapshotSchema` содержит `audit_log: z.array(AuditLogEntrySchema)` (`realApi.ts:195`). Починка `C-C1-001` в одиночку уронит Zod-разбор **всего** снапшота → страница Graph умрёт целиком | **FIXED_EXTERNALLY 2026-08-12:** коммит `84ca396` сделал `actor_id` и sibling-поля `.nullable().optional()` с контрпроверками malformed-значений. Закрыта |
| `C-C1-012` | `app/api/v1/admin.py:219-231` против `admin-ui/src/api/realApi.ts:181` | B | P2 | **Найдена аудитом 2026-08-12** — брат `C-C1-002`: `_graph_fetch_transactions` собирает транзакцию **без** ключа `payload` (использует его только для извлечения `equivalent`), а `TransactionSchema` требует `payload: z.record(...)` без `.optional()`; `useGraphAnalytics.ts:626-636` читает из него `from`/`to`/`edges` | Включение `include=transactions` (починка `C-C1-001`) уронит Zod-разбор всего снапшота — та же механика, что уже сработала у `C-C1-002`. Падающего runtime-репродьюсера пока нет — статически подтверждено | **CONFIRMED** оркестратором чтением обеих сторон 2026-08-12 | fix now, **в паре с `C-C1-001`** |
| `B-A1b-003` | `app/api/v1/admin.py:2085-2088` | B | P2 | `except Exception → raw_cycles = []` без разбора класса отказа и без лога; ветка не покрыта тестами | **CONFIRMED:** соседний `app/api/v1/clearing.py:26` зовёт тот же `find_cycles` без глушителя. Оператор видит «циклов нет» там, где «поиск упал» | fix now |
| `B-A1b-002` | `app/api/v1/auth.py:81` | B | P2 | Сырой `X-Request-ID` в аудит логина мимо `validate_request_id`/`request_id_var`; блок в `except Exception: rollback` без лога | **CONFIRMED структурно:** соседний писатель `admin.py:271-274` валидирует и подставляет `new_request_id()`. Гипотеза о `DataError` на PG (`String(64)`) **не воспроизводилась** | fix now (валидация), PG-гипотеза — отдельно |
| `C-A1a-003` | `app/api/v1/simulator.py:608-629` | C | P2 | Мутирующие interact-действия резолвят участников по глобальной таблице `Participant`, тогда как read-эндпоинты того же семейства scoped по снапшоту рана | **CONFIRMED как код** (`select(Participant).where(pid==…)` без скоупа), **эффект не воспроизведён**: нужен прогон двух ранов на разных сценариях | **решение владельца о границе**, не автоматическая правка |

**Волна 2 закрыта 2026-08-14** на frozen HEAD `7cb5149` (рабочее дерево `f008890`; продуктовый scope
байт-в-байт равен frozen HEAD, проверено `git diff` — регенерация манифеста и re-baseline P2 волны 1
не потребовались). Прочитано 79 файлов / 19 555 строк (A1c 29/2381, A2a 17/7157, A2b 24/5907,
B1 9/4110). Вердикты: `keep` 46, `clean` 30, `merge` 2, **`rewrite` 0**, `delete` 1.
Находок 64: **P2 — 15** в слайсах плюс одна на поверхности закрытого A1a (ниже), P3 — 48;
36 гипотез убито агентами на самопроверке. Каждый P2 перепроверен оркестратором по коду вручную;
пофайловые таблицы, исправленные формулировки и список непроверенного —
[`evidence-index.md`](evidence-index.md).

| ID | path:line | Класс | Sev | Суть | Вердикт перепроверки | Триаж |
|---|---|---|---|---|---|---|
| `B-A1a-015` | `app/api/v1/simulator.py:2101-2128` | B | ~~P2~~ → **P3** | Fallback-ветка SSE (`status_evt is None`) теряет уже снятые с очереди события `prefetched` (отдаются только в ветке успеха `:2137`) и выдаёт `event_id=f"evt_init_{token_hex(6)}"` вне последовательности продюсера | Механика **CONFIRMED**, но **severity понижена внешним ревью 2026-08-14 и перепроверена оркестратором**: ветка **недостижима на production-пути**. `sse_broadcast.py:359-364` резервирует слот очереди под bootstrap-статус и падает `SseReplayUnavailable`, если replay не влезает; `:366-382` под одним локом кладёт replay и статус с **последовательным** id `evt_{run_id}_{seq:06d}` в очередь и только затем публикует подписчика в `run._subs`. Все три входа потока (`simulator.py:2072,2284,2491`) идут через `runtime.subscribe_with_status`, поэтому статус уже в очереди до начала дренажа и таймаут 1.0 с не наступает. Ошибка первичной перепроверки оркестратора: проверены внутренности ветки, **не проверена её достижимость** | защитная ветка, внутренне противоречащая инварианту соседнего модуля; в пакет P3, не «fix now» |

**Волна 3 закрыта 2026-08-14** на frozen HEAD `7cb5149` (рабочее дерево `f008890`; продуктовый scope
байт-в-байт равен frozen HEAD, проверено `git diff` и `git status`). Прочитано 57 файлов /
18 705 непустых строк — 21 176 физических (A3 22/7 254, B2 11/4 509, C2 24/6 942). Вердикты:
`keep` 31, `clean` 19, `delete` 7, `merge` 0, **`rewrite` 0**. Находок 48: **P2 — 18**, P3 — 30;
40 гипотез убито агентами на самопроверке. Впервые применён метод обязательных таблиц паритета
(`plan.md` §3): закрыты все шесть осей, знаменатели и расхождения — в
[`evidence-index.md`](evidence-index.md). Каждый из 18 P2 перепроверен оркестратором по коду вручную;
пофайловые таблицы и список непроверенного — там же.

Две находки волны 3 вынесены владельцу как **кандидаты на повышение до P1** (оркестратор severity сам
не меняет — `docs/codex-orchestrator-rule.md` §5):

| ID | path:line | Класс | Sev | Суть | Вердикт перепроверки | Триаж |
|---|---|---|---|---|---|---|
| `B-A3-001` | `app/core/invariants.py:22-85` | B | P2 → **кандидат P1** | `check_zero_sum` тождественно истинен: обе выборки идут по одной таблице `debts`, каждая строка даёт `+A` одному кредитору и `−A` одному должнику, сумма ≡ 0 при любых данных | **CONFIRMED как тождество.** Исключение не возбуждается никогда, `verification_passed: true` пишется в каждый checkpoint (`engine.py:1252`, `core/integrity.py:71`, `api/v1/integrity.py:105,210`). Гард, проходящий вхолостую (§9 anti-vacuum). Осложнение: `docs/ru/02-protocol-spec.md:1769-1795` даёт тот же SQL-эталон — Current == Intended | решение владельца: дефект в конструкции инварианта, в документе или нужен другой инвариант. Денежное ядро read-only → BACKLOG |
| `B-A3-004` | `app/core/trustlines/service.py:81-92` | B | P2 → **кандидат P1** | Гард дубликата отбирает `status != 'closed'`, а уникальный индекс `uq_trust_lines_from_to_equivalent` статус не включает | **CONFIRMED полностью, включая непроверенное агентом:** индекс без `status` и в модели (`db/models/trustline.py:31`), и в применённой миграции (`001_initial_schema.py:104-108`); `close()` ставит `status='closed'`, строку не удаляет (`:288`); `IntegrityError` в `app/core/trustlines/` не ловится; в `main.py` только `GeoException`/`RequestValidationError` → 500. Пути обратно нет: `status='active'` присваивается лишь при создании (`:101`) | закрыть линию и создать заново тем же тройником — два обычных вызова, 500, состояние необратимо через API. Решение владельца дважды: о severity и о допустимости исключения из read-only |

Классы: **B** баг · **C** расхождение с контрактом/идеей проекта · **D** мёртвый код ·
**R** грязное/дублируемое/непереиспользуемое · **P** упущенная оптимизация с измеримой стоимостью.
Severity: **P1** деньги/целостность/безопасность или сломанный пользовательский путь ·
**P2** нарушение контракта или достижимый дефект · **P3** качество кода.

## Current / Intended / Optimal

**Current.** Сплошного покрытия нет. Качество кода известно точечно, из побочного улова аудитов
поведения. Ни для одного файла из 500 нет записи «прочитан, вердикт такой-то», поэтому
непрочитанный файл неотличим от чистого.

**Intended.** AGENTS.md §2 требует high-leverage изменений и прямо запрещает overengineering:
новая абстракция допустима только против подтверждённого дублирования, не «на будущее». §14
запрещает удалять файл по одному пустому grep. §11 задаёт качество тестов. То есть контракт на
чистку уже написан — не выполнена именно инвентаризация.

**Optimal.** Каждый файл трёх поверхностей имеет явный вердикт `keep`/`clean`/`merge`/`rewrite`/
`delete` с датой и HEAD. Дешёвые правки применены пакетами по одному классу. Дорогие — оценены и
лежат в BACKLOG с ценой, а не с эмоцией. **Ни одной новой абстракции не введено.**

## Решения владельца (2026-08-11)

1. **Глубина:** реестр находок + дешёвые безболезненные правки. Всё, что требует продуктового
   решения или трогает контракты, — в триаж.
2. **Денежное ядро:** read-only, находки уходят в 002/003/004 и BACKLOG. *(Уточнение 2026-08-12:
   программы 002–006 закрыты, read-only сохраняется, маршрут находок — только BACKLOG.)*
3. **Темп:** два трека параллельно начиная с волны 1.
4. **Тесты входят** в обзор отдельной волной 5.

## Non-goals

- Переписывание архитектуры, новые слои, service layer, outbox, generic-обёртки, DI-контейнеры,
  «единый API-клиент ради симметрии». Это не банковское enterprise-приложение.
- Долг Ruff/Black — он принадлежит T608 в 006 и в этот обзор не втягивается (AGENTS.md §6).
- Переоткрытие закрытых программ 001–006 и правки денежного ядра (read-only решением владельца).
- Threat model, pentest, dependency- и crypto-аудит: 008 — code review security-релевантного кода,
  не security-аудит (граница из A5).
- Сплошные codemod'ы, включая массовую замену `??` — запрещено разбором M20 в BACKLOG.
- Переоткрытие решений из раздела «принято» в BACKLOG (AGENTS.md §17).
- Новые фичи, стилевые правки, тюнинг под отдельный fixture или тест.

## Verification plan

**Разведка (фаза I) проверяется полнотой, а не прогоном.**

1. **Полнота покрытия — машинно проверяемый критерий.** Owner surface слайса задаёт
   [`manifest.md`](manifest.md): каждый tracked-файл scope назначен ровно одному слайсу
   (0 пропусков, 0 двойных назначений — проверка падает при обоих). Инвентарь волны обязан
   содержать строку на каждый файл своего слайса из манифеста; итог волны фиксируется в tracked
   [`evidence-index.md`](evidence-index.md). Файл без строки вердикта считается непрочитанным.
2. **Каждая находка проходит adversarial-опровержение** (AGENTS.md §16.4) отдельным агентом с
   чистым контекстом: `CONFIRMED` / `REFUTED` (с контр-evidence) / `NEEDS-RUNTIME`. Оркестратор
   вручную воспроизводит каждый P1/P2 по коду до внесения в реестр.
3. **Внешнее независимое ревью** батчем по поверхности перед закрытием фазы I: оркестратор —
   Claude Code, значит ревьюер — Codex (`docs/external-review-runbook.md` §3). Подаётся реестр
   находок с вопросом «что ложное, что пропущено», а не «посмотри код».

**Правки (фаза II) проверяются репродьюсером.**

1. **Репродьюсер, падающий на `ea9cde9`,** обязателен для каждой находки класса **B** и **C** до
   её починки. Зелёный до правки тест ничего не доказывает.
2. **Класс D (удаление) доказывается иначе:** reference scan по AGENTS.md §14 (package scripts,
   dynamic imports, docs, generators, runtime entrypoints, `git log -- <path>`), затем контрпроверка
   — targeted gates зелёные **после** удаления. Пустой grep сам по себе доказательством не является.
3. **Класс R (дубли):** правка обязана убрать дублирование, а не спрятать его за абстракцией.
   Контрпроверка — число синхронных касаний при гипотетической правке правила стало меньше,
   и это показано перечислением call-sites до/после.
4. **Инварианты, обязанные выжить:** защищённые контракты §8 AGENTS.md — `api/openapi.yaml`, SSE
   имена и wire shapes, алиасы `from`/`to`, decimal-семантика денег, направление trustline
   `creditor -> debtor`, идемпотентность по `tx_id`.
5. **Существующие селекторы, обязанные остаться зелёными** (называются поимённо в каждом слайсе):
   `.\scripts\verify_local.ps1 -TaskSlug <slug> -BackendOnly`,
   `npm --prefix admin-ui run test`, `npm --prefix admin-ui run build`,
   `npm --prefix simulator-ui/v2 run typecheck`, `npm --prefix simulator-ui/v2 run test:unit`,
   `npm --prefix simulator-ui/v2 run build`.

**Явно запрещённые способы проверки** (дают ложное доказательство):

- считать утверждение агента «файл прочитан» покрытием без строки инвентаря;
- принимать находку по памяти агента без `path:line`, существующего на замороженном HEAD;
- удалять код по одному пустому `git grep`;
- засчитывать «0 находок» там, где волна не запускалась;
- чинить дубль введением новой абстракции и называть это упрощением.

## Tasks

Волновой уровень. Слайсы и их статусы — `tasks.md`.

| ID | Задача | Статус |
|---|---|---|
| T800 | Спека, план, реестр задач, регистрация, аудит плана и его применение | ЗАКРЫТ 2026-08-12 |
| T801 | Волна 1: A1a + A1b + C1 | ЗАКРЫТ 2026-08-11 |
| T802 | Сведение волны 1, re-baseline P2 на HEAD аудита | ЗАКРЫТ 2026-08-12 |
| T803 | Волна 2: A1c + A2a + A2b + B1; отдельные RT-задачи NEEDS-RUNTIME | ЗАКРЫТ 2026-08-14; RT5–RT8 заведены |
| T804 | Волна 3: A3 (read-only) + B2 + C2 | ЗАКРЫТ 2026-08-14; RT9–RT13 заведены |
| T805 | Волна 4: B3 + C3 | ЗАКРЫТ 2026-08-14; RT14 заведена |
| T806 | Волна 5 suite-wide: A4 + B4 + C4 + B5 + C5 (Playwright) | ЗАКРЫТ 2026-08-15 |
| T807 | Волна 6: триангуляция контрактов, список удаления, дубли | НЕ НАЧАТ |
| T808 | Единый fresh-context adversarial-проход по сведённым реестрам | НЕ НАЧАТ |
| T809 | Внешнее ревью другой системой, `UNVERIFIED` при сбое | НЕ НАЧАТ |
| T810 | Триаж: fix now / в спеку / удалить / принято | В РАБОТЕ: делегирован владельцем 2026-08-14, вопросы 1–14 волн 1–3 решены; открыт для волн 4–6 |
| T811 | Фаза II: дешёвые правки пакетами + targeted gates | ЗАБЛОКИРОВАН: ждёт завершения фазы I и пересмотра T810 |
| T812 | Перенос итога в BACKLOG, закрытие программы | НЕ НАЧАТ |

Гранулярные статусы слайсов — [`tasks.md`](tasks.md); он выигрывает при расхождении.

## Changelog

- **2026-08-11.** Владелец запросил сплошное ревью трёх поверхностей волнами с перепроверкой
  гипотез внешним агентом. Составлен метод (P0–P4), разбиение на блоки, приняты четыре решения
  владельца. Волна 1 (A1a, A1b, C1) запущена как read-only разведка на HEAD `ea9cde9`
  **до** написания этой спеки; спека написана следом, в тот же день, по факту запуска — записано
  как есть, без ретроспективной подгонки (AGENTS.md §13). Продуктовый код на момент заморозки
  чист: `git status --short -- app admin-ui simulator-ui api tests` пуст.
- **2026-08-11.** Волна 1 закрыта: A1a, A1b, C1 — 46 файлов / 11 149 строк, 40 находок (7×P2),
  0 кандидатов на `rewrite`. Полнота подтверждена пофайловым сопоставлением с `git ls-files`.
  Каждый P2 перепроверен оркестратором вручную; `C-A1b-001` — интроспекцией на закреплённой
  версии pydantic. **Правок не вносилось**, дерево продуктового кода после волны чисто.
  Работа приостановлена по требованию владельца (T802-STOP). Evidence —
  `plans/review-2026-08-11-three-surfaces/{backend,admin-ui}/`.
- **2026-08-12.** Аудит плана (`plan-audit-2026-08-12.md`, A1–A8, 11 дефектов F-PA-1…F-PA-11)
  и авторизованное владельцем применение его подтверждённых исправлений: (1) манифест
  «файл → ровно один слайс» — 710 файлов, 0 пропусков против 129 у прежнего разбиения, 0 двойных
  назначений против 87; новые слайсы A1c (каркас backend) и B5/C5 (Playwright); (2) tracked
  `evidence-index.md` — восстановимость больше не зависит от ignored `plans/` (F-PA-7);
  (3) re-baseline P2: 5 × LIVE, `C-C1-002` FIXED_EXTERNALLY (`84ca396`), `C-A1a-003` UNVERIFIED;
  `C-A1a-001` подтверждён исполняемо в обоих env-состояниях; (4) новая находка `C-C1-012`
  (transaction `payload`), перепроверена оркестратором; (5) новый frozen HEAD волн 2–6 `7cb5149`,
  `ea9cde9` сохранён историческим baseline волны 1; (6) материальные тесты читаются вместе с
  source-слайсом, волна 5 — suite-wide (F-PA-4/5/6); (7) проходы переименованы в R0–R4, статусы
  задач разведены (НЕ НАЧАТ/ПРИОСТАНОВЛЕН/ЗАБЛОКИРОВАН), пороги объявлены эвристиками,
  предрешённые вердикты заменены нейтральными вопросами (F-PA-9/10/11); (8) программа
  зарегистрирована в `specs/README.md`. Сверка статусов с реальностью: программы 002–006 закрыты
  (README реестр), денежное ядро остаётся read-only, маршрут находок — BACKLOG.
- **2026-08-14.** Волна 2 закрыта: A1c, A2a, A2b, B1 — 79 файлов / 19 555 строк, 64 находки
  (15×P2 в слайсах + `B-A1a-015` на поверхности закрытого A1a), 0 кандидатов на `rewrite`.
  Слайсы шли двумя параллельными треками (A1c+A2a, затем A2b+B1). Полнота подтверждена машинно:
  строк инвентаря = числу файлов слайса в каждом реестре, сумма вердиктов = числу файлов, ID
  инвентаря и findings взаимно покрыты. Перед стартом сверена заморозка: HEAD ушёл вперёд на три
  docs-коммита, но `git diff 7cb5149..f008890 -- app admin-ui simulator-ui api tests` пуст, поэтому
  манифест не перегенерировался; он перепроверен на текущем дереве (710 = 710, 0 UNASSIGNED,
  0 двойных назначений). Каждый P2 перепроверен оркестратором по коду вручную; две находки
  подтверждены интроспекцией на закреплённом интерпретаторе (`Decimal('NaN') < 0`,
  `uvicorn.config.LOGGING_CONFIG` без ключа `root`). Оркестратор внёс три коррекции в результаты
  агентов: исправлена фактическая ошибка в `B-A2b-002` (утверждение «status нет ни в одной фикстуре»
  опровергнуто — `trust_drift_decay_regression.json` его несёт, что усиливает находку), понижена
  диспозиция `D-A1c-005` с `delete` до «verify first» (таблица `config` живёт в применённых
  миграциях и связана с неперсистентностью `/admin/config`), снята собственная переданная гипотеза
  о двух формах `edge_patch` (фронт принимает обе). **Правок продуктового кода не вносилось**,
  тесты не запускались. Заведены RT5–RT8. Evidence — `evidence-index.md` и
  `plans/review-2026-08-11-three-surfaces/`.
- **2026-08-14.** Волна 3 закрыта: A3, B2, C2 — 57 файлов / 18 705 непустых строк (21 176 физических),
  48 находок (18×P2), 0 кандидатов на `rewrite`. Слайсы шли двумя треками (A3+C2, затем B2). Перед
  стартом сверена заморозка: HEAD ушёл вперёд на три docs-коммита, но `git diff 7cb5149..f008890 --
  app admin-ui simulator-ui api tests` пуст, поэтому манифест не перегенерировался; он перепроверен
  **регенерацией по собственным 17 правилам** из `git ls-files` с пофайловым сравнением (710 = 710,
  0 UNASSIGNED, 0 расхождений слайса, 0 stale). Впервые применён метод обязательных таблиц паритета,
  введённый в `plan.md` §3 по evidence волны 2: закрыты шесть осей, из них «границы транзакций» дала
  **измеренный отрицательный результат** (братьев `B-A2b-001` в денежном ядре нет: `rollback()` чужой
  сессии — ноль, частичный commit при retry — ноль, проглоченный `40001` — ноль), а «mock ↔ real»
  в C2 — четыре расхождения из четырёх методов. Все 18 P2 перепроверены оркестратором по коду вручную;
  он же закрыл дыру агента A3, который честно записал, что не читал `migrations/`: применённая
  миграция 001 подтвердила безусловный уникальный индекс и сняла ослабление `B-A3-004`. Две находки
  вынесены владельцу как кандидаты в P1 (`B-A3-001`, `B-A3-004`) — severity оркестратором не менялась.
  Установлено, что запись BACKLOG о дублировании ~240 строк в `engine.prepare`/`prepare_routes`
  устарела (закрыто общим `_get_segment_capacity_and_reserved_usage`), а `router.find_paths` уже
  удалён — правка BACKLOG отнесена в T812. **Правок продуктового кода не вносилось**, тесты не
  запускались; единственные исполнения — `ruff check` по A3 и три read-only интроспекции. Заведены
  RT9–RT13. Evidence — `evidence-index.md` и `plans/review-2026-08-11-three-surfaces/`.
- **2026-08-14.** Исправлен счёт файлов слайса A1b: **26 → 28**, вердикты 15 + 11 → 18 + 10. Историческая
  запись волны 1 ниже сохранена как есть (§13 AGENTS.md — исторические документы не переписываются);
  здесь фиксируется факт. Расхождение выглядело как возможный пробел покрытия фазы I и проверено
  пофайлово: все 28 путей A1b манифеста имеют строку инвентаря, непрочитанных ноль, тальи вердиктов
  дают 28, а между `ea9cde9` и frozen HEAD в `app/api/**`/`app/schemas/**` ни одного файла не
  добавлялось. Источник — арифметическая описка в шапке локального реестра, противоречащая собственной
  разбивке в той же строке. Итог волны 1 по файлам — **48**, а не 46. Разбор — `evidence-index.md`.
- **2026-08-14.** Переписан §1.1 `docs/codex-orchestrator-rule.md` (бюджет контекста) по указанию
  владельца: пороги-проценты убраны, считается **только собственный контекст оркестратора** (расход
  субагентов не суммируется — у них свои окна), выдумывать процент при отсутствии счётчика запрещено,
  заполнение контекста **перестало быть самостоятельной причиной остановки** — причиной остаются
  наблюдаемые признаки деградации. Уведомление о состоянии контекста даётся **по завершении этапа**
  и является уведомлением, а не запросом разрешения. Повод — инцидент в этой же программе: оркестратор
  объявил «85 %» без счётчика и предложил передачу сессии, хотя ни один признак деградации не наступил.
- **2026-08-14.** Волна 4 закрыта: B3 + C3 — 185 файлов / 30 478 непустых строк (34 920 физических),
  107 находок (**21×P2**, P1 — 0), 0 кандидатов на `rewrite`. Волна больше волн 2 и 3 вместе, поэтому
  прошла **шестью исполнительскими под-слайсами** по когезии слоя (правила 14 и 17 манифеста не
  менялись, единицей учёта остались B3 и C3); объединение под-слайсов равно слайсу манифеста
  (146 = 146, 39 = 39). Перед стартом сверена заморозка и манифест перепроверен регенерацией по его
  собственным 17 правилам (710 = 710, 0 UNASSIGNED, 0 расхождений, 0 stale). Все 21 P2 перепроверены
  оркестратором по коду вручную; две находки подтверждены его собственными измерениями — интроспекцией
  на закреплённом `pydantic 2.5.3` (`Decimal('0E-8')` уходит на провод строкой `"0E-8"`) и разбором
  поставляемой фикстуры (100 узлов несут атомы, 0 несут мажорную величину). Оркестратор внёс четыре
  коррекции в результаты агентов: два понижения severity (`C-B3-1-002`, `B-C3-2-005` — оба по одной
  причине: проверена достижимость состояния, но не вызывающего), уточнение набора правки у
  подтверждённой `C-B3-2-002` (приватная копия форматтера вместо импорта) и исправление якоря продюсера
  у `B-C3-2-001`. Отдельно **принят брошенный слайс C3-2**: его агент записал оба артефакта, но процесс
  завершился до возврата сводки — слайс принят по артефактам с независимой регенерацией состава,
  а не по стенограмме. Денежная ось волны сошлась в одну структурную причину и получила окончательный
  знаменатель (13 форматтеров, 4 нарушают §8; 47 вызовов, 9 искажают печатаемую величину; источников
  `precision` во фронте симулятора — **ноль**). Два кандидата в P1 вынесены в T810 — severity
  оркестратором не назначалась. Найден третий независимый механизм невидимости класса «precision ≠ 2».
  **Правок продуктового кода не вносилось**, тесты и гейты не запускались. Заведена T805-RT14.
  Evidence — `evidence-index.md` и `plans/review-2026-08-11-three-surfaces/`.
- **2026-08-15.** Закрыт метод-пробел волны 3: предъявлена обязательная ось **«деньги» слайса C2**,
  не закрытая при сдаче волны (`plan.md` §3 — ось без таблицы считается непроверенной). Знаменатель —
  **222 денежные величины, 63 расхождения**; счёт вёлся **по вызовам, а не по строкам** — ловушка, на
  которой волна 4 занизила собственный знаменатель. Перепроверка оркестратором изменила счёт находок:
  из пяти заявленных новой оказалась **одна** (`C-A3-023`, P3, на поверхности закрытого слайса A3 —
  `metrics.py:259` строит `net` без `quantize`, тогда как `:436` в том же модуле квантует), а обе
  заявленные P2 понижены до «усиления уже подтверждённых» `B-C3-2-001` и `C-C3-2-002`. Отдельно
  **исправлен знаменатель подтверждённой `C-C2-002`: не «24 места рендера», а 47 вызовов / 25 в живом
  коде**. Найден **четвёртый** механизм невидимости класса «precision ≠ 2»: все денежные значения
  admin-фикстур несут ровно 2 знака и ни одного в экспоненте.
- **2026-08-15.** **Волна 5 закрыта:** A4 + B4 + C4 + B5 + C5 — **341 файл = 341 по манифесту**,
  70 052 непустых / 82 077 физических строк, **117 находок (30×P2, P1 — 0)**, `delete`-кандидат —
  **один** на всю волну, `merge`/`rewrite` — 0. Волна крупнее всех предыдущих вместе и прошла
  одиннадцатью исполнительскими под-слайсами двумя треками; правила 1–5 манифеста не менялись.
  Перед стартом сверена заморозка и манифест перепроверен **регенерацией по его собственным 17
  правилам** (710 = 710, 0 UNASSIGNED, 0 расхождений, 0 stale). Все 30 P2 перепроверены оркестратором
  по коду вручную.
  **Уникальный результат волны — не список находок, а измеренный ответ на вопрос «что набор ловит».**
  По девяти подтверждённым дефектам предъявлены контрпроверки: backend-набор не ловит **ни одного**,
  включая обе P1; simulator-ui впервые дал 4 «да» из 9. При этом обнаружен класс, которого программа
  раньше не знала: **набор кодирует дефект как ожидаемое поведение** — тавтологичный ассерт на
  вхолостую проходящем инварианте (4 экземпляра на 3 независимых путях), тест, **обходящий P1
  комментарием, дословно описывающим её механику** (`C-A4-4-013`), гард, **выключенный и закреплённый
  ассертом на выключение** (`B-A4-5-001`), тест, закрепляющий 10-секундное окно устаревания как
  контракт (`C-B4-2-003`), и **инвертированное покрытие** — тесты подают форму данных, которую
  продакшен произвести не может, из-за чего кандидат в P1 `B-B3-4-001` ненаблюдаем принципиально
  (`C-B4-3-002`, `C-A4-5-005`). Отдельно измерено: **пять тестовых модулей создают SQLite-БД в корне
  репозитория** мимо `-TaskSlug`, четыре из них в блокирующем tier, а `scripts/validate_test_database_url.py:53`
  этот паттерн **объявляет безопасным** вопреки §7 и §12; обязательный `npm run lint` **не читает ни
  одного из 100** тестовых файлов simulator-ui; 55 % и 27 % объёма двух интеграционных под-слайсов
  неспособны уронить PR.
  **Методическое.** Полный знаменатель класса «тест обходит дефект» **грепом недостижим** — доказано
  эмпирически: третий экземпляр (`test_real_payments_ordered_journal.py:166`) не содержит ни одного
  ключевого слова. Введён и подтверждён приём «валидировать сканер на известном позитиве прежде, чем
  заявлять ноль» — у слайса B4-3 он опрокинул его же первый вывод с «ноль» на «два подтверждённых».
  Один под-слайс (A4-6) прошёл **дважды**: первый прогон был прерван, записав findings без инвентаря,
  и **не был принят** — покрытие без приложения непроверяемо; при перезапуске прежний файл выдан
  агенту как список зацепок, а не как evidence.
  **Оркестратор внёс семь коррекций, три из них — против собственных утверждений** (ошибочный греп по
  `decimal.ts`, переобобщение про `ui-smoke`, неверное назначение оси снапшотов), и принял три
  коррекции агентов против своих входных данных. **Правок продуктового кода не вносилось, тесты и
  гейты не запускались.** Evidence — `evidence-index.md` и `plans/review-2026-08-11-three-surfaces/`.
- **2026-08-15.** **Внешнее независимое ревью реестра волны 5** силами Codex (`0.147.0-alpha.6.6`,
  `gpt-5.6-sol`, `model_reasoning_effort=high`, `--sandbox read-only`, три параллельных среза,
  exit 0/0/0, маркеры на месте). **T809 не закрывает** — он подаётся батчем по поверхности перед
  закрытием фазы I, а волна 6 не пройдена; прогон **не credential-free**, риск принят владельцем
  датированно. Итог: **разобрано 49, CONFIRMED 35, REFUTED 11, UNCERTAIN 3, MISSED 7** — самый
  результативный прогон программы против прежних 2, 0 и 2 опровержений.
  **Счёт P2 волны 5: 30 → 27** (восемь понижений, пять повышений). **Три понижения опровергают
  вердикты оркестратора,** и каждое перепроверено им по коду: механизм, который тест якобы проверял
  «на неподходящем движке», оказался `version_id_col` SQLAlchemy и **backend-независим**; тест,
  объявленный закрепляющим дефект, закрепляет обычное поведение TTL и починку не блокирует;
  «контракт `geo:interact-esc`» **не существует** — грепом он встречается только в самом тесте.
  Ещё одно понижение — возражение на **консистентность**: `skipif`, честно объявляющий свою границу,
  есть отсутствие покрытия, а не притворяющийся гард, — тем же аргументом оркестратор ранее сам
  оставил соседнюю находку на P3.
  **Знаменатель оркестратора оказался неверен дважды.** Утверждение «обязательный `npm run lint`
  не читает ни одного из 100 тестовых файлов simulator-ui» **ложно**: `eslint.config.js:37-40` несёт
  отрицание, возвращающее тесты `windowManager` в область проверки, — линтуются **3 из 99**; ошибка
  возникла от сложения трёх локальных нулей под-слайсов в suite-wide ноль без проверки конфига.
  Счёт ассертов исправлен (**A4-3: 447 → 367**, A4-2 626 → 625, A4-6 533 → 532) — независимый
  подсчёт `ast.Assert` оркестратором воспроизвёл числа ревью **до единицы**. Знаменатель денежной
  оси simulator-ui занижен **третий раз подряд в ту же сторону** (`fmtAmt`: 6 → 8 вызовов), а итог
  оси «деньги» C2 (222 → ≥ 224) переведён в статус **нижней границы**, поскольку его числитель
  смешивал единицы счёта.
  **Ревью независимо подтвердило `B-B3-4-001` как P1** — второе независимое подтверждение; решается
  в T810, оркестратором severity не назначалась. Подтверждены как верные три его решения: понижение
  `R-C4-006` и переклассификация `B-C2-030`/`B-C2-031` в усиления уже подтверждённых находок.
  Из семи MISSED две перепроверены оркестратором и подтверждены — в том числе **настоящий** член
  класса «тест закрепляет дефект как контракт» (`numberFormat.test.ts:14-18` жёстко закрепляет два
  знака у `formatAmount2`), заменивший два опровергнутых; остальные пять идут в T810 входами.
- **2026-08-14.** Владелец делегировал триаж T810 оркестратору. Решены все 14 открытых вопросов волн
  1–3 — таблица в `tasks.md`, раздел «T810 — триаж». Приняты повышения до P1 по `C-A1a-003`,
  `B-A2b-001` и `B-A3-004`; понижения до P3 по `C-A1b-001` и `C-A2b-006`; понижение `R-A1c-003`
  **отклонено** с записанной причиной (довод внешнего ревью отвечал про публичность доступа, а не про
  проверенную фактом rate-limit-асимметрию). `B-A3-001` оставлен P2, а не поднят: тавтологичность
  проверки завышает заявленную защиту, но не прячет повреждения — zero-sum структурно гарантирован
  моделью Debt-рёбер. Два решения развернулись фактами, добытыми в триаже: удаление корневых health
  (`R-A1c-003`) **отменено** — их потребляют `docker/Dockerfile:35` и `.github/workflows/quality.yml`,
  вместо удаления решено убрать дублирование и объявить исключение rate-limit явно; удаление шести
  вкладок графа и `graphUiOptions.ts` (`D-C2-001`, `D-C2-013`), наоборот, **подтверждено** reference
  scan'ом и `git log` до safe delete. Зафиксировано уточнение scope: `app/core/trustlines/` не входит
  в замороженный список спеки, промпт слайса A3 был строже спеки. **Фаза II не открыта:** T811 остаётся
  заблокированным до конца чтения волн 4–6, потому что волна 3 трижды нашла братьев уже закрытых
  находок и правка до полного реестра оплачивается дважды.

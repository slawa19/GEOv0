# ARCHIVED: Documentation roadmap (Real Mode + Monorepo)

Этот документ перенесён в архив, чтобы не отвлекать от реализации.

Актуальный вход для разработки:
- `docs/ru/simulator/backend/pre-implementation-readiness-report.md`

Оригинальное содержимое сохранено ниже без правок.

---

# Дорожная карта документации для старта работ по **Real Mode + Monorepo Architecture**

Этот документ фиксирует **исчерпывающий перечень документации/артефактов**, которые необходимо подготовить до начала (и по ходу) реализации плана:

- `plans/real-mode-monorepo-architecture.md`

Цель — убрать неопределённость в контрактах (schema/API/протокол), зависимости между компонентами (backend ↔ simulator-ui ↔ fixtures), и заранее зафиксировать критерии готовности.

> Основано на анализе: `docs/ru/simulator/backend/archive/Анализ плана Real Mode Monorepo Architecture Documentation plan.md`.

---

## 0. Термины и границы

### 0.1. Что мы документируем

Мы документируем:

1) **Контракты данных** (JSON Schema сценария, типы доменных событий)
2) **Контракты интеграции** (OpenAPI, WebSocket/SSE протокол)
3) **Исполнительную модель** (tick/runner алгоритм, детерминизм, метрики)
4) **Артефакты воспроизводимости** (fixtures/scenarios, конвертер)
5) **Критерии приёмки и план тестов** (до того, как писать код)

### 0.2. Что намеренно не входит в эту дорожную карту

- Полная продуктовая спецификация UI/визуализации (она уже есть в `docs/ru/simulator/frontend/...`).
- Подробная спецификация домена GEO (payments/clearing) — используем существующие API/код как источник.

---

## 1. Источники (source of truth)

Ниже перечислены ключевые документы/файлы в репозитории, от которых мы отталкиваемся.

### 1.1. Планы и дорожные карты

- **План работ:** `plans/real-mode-monorepo-architecture.md`

### 1.2. Backend (концепция симулятора)

- **Концепт/требования к симулятору:** `docs/ru/simulator/backend/GEO-community-simulator-application.md`

### 1.3. Frontend (контракты визуализации)

- **UI contracts snapshot/events (текущий):** `docs/ru/simulator/frontend/docs/api.md`
- **UI/визуализация:** `docs/ru/simulator/frontend/docs/specs/GEO-game-interface-spec.md`
- **Physics layout:** `docs/ru/simulator/frontend/docs/specs/live-physics-layout-spec.md`
- **UI панели real mode (Scenario/Run/Metrics/Bottlenecks/Artifacts):** `docs/ru/simulator/frontend/docs/specs/simulator-real-mode-screens-spec.md`

### 1.4. Backend GEO Hub API

- **OpenAPI (текущее):** `api/openapi.yaml`
  - Важно: OpenAPI является **источником правды** для целевых endpoints Real Mode (`/simulator/scenarios`, `/simulator/runs`, SSE `/simulator/runs/{run_id}/events`).

- **Отчёт готовности к реализации:** `docs/ru/simulator/backend/pre-implementation-readiness-report.md`

### 1.5. Fixtures

- **Admin fixtures:** `admin-fixtures/` (datasets/scenarios/tools)

---

## 2. Принципы документации (как понять, что документ готов)

Для каждого документа ниже мы фиксируем:

- **Назначение** (зачем он нужен)
- **Аудитория** (кто читает/пишет)
- **Содержание** (структура, обязательные разделы)
- **Артефакты/форматы** (файлы, схемы, примеры)
- **Критерии готовности** (definition of done)
- **Источники** (из каких файлов/спек берём информацию)

---

## 2.1. Зафиксированные решения (2026-01-28) ✅

Чтобы backend и simulator-ui не разъехались, фиксируем решения для MVP (их считаем **source of truth** до отдельного ADR):

1) **Real-time транспорт:** `SSE + REST команды` для MVP.
  - WebSocket допускается позже как улучшение, но MVP должен быть реализуем и проверяем на SSE.

2) **События статуса:** backend эмитит `run_status` в stream:
  - при смене состояния (`start/pause/resume/stop/error`)
  - и периодически во время `running` (например раз в 1–2 секунды).
  - `GET /simulator/runs/{run_id}` остаётся как polling/восстановление состояния.

3) **Tick-событие:** `tick` не требуется для UI по умолчанию.
  - Допускается только как debug/опциональное (например `debug=1`).

4) **Загрузка сценария:** `POST /simulator/scenarios` принимает `scenario.json` как JSON body (не multipart) для MVP.
  - Ввести разумный `max_size` и понятные ошибки валидации.

5) **Bottlenecks:** отдаём `top-N` + опциональный фильтр `min_score`.

6) **Хранение/retention:**
  - `run state`/summary — хранить в Postgres (история прогонов) + обновлять периодически.
  - `events stream` — in-memory ring buffer на run (TTL по времени/кол-ву) + опционально Redis для fan-out/масштабирования.
  - `metrics/bottlenecks` — агрегаты для UI (Postgres или кэш), raw события — не обязательны для долговременного хранения.
  - `artifacts` — это **export** (скачиваемые файлы/zip), а не основной источник UI.

---

## 3. Последовательность подготовки документации (по фазам)

### ФАЗА A — Контракты (обязательное до начала реализации)

Цель фазы: зафиксировать всё, что является **интерфейсом** между компонентами.

Порядок:

1) JSON Schema сценария
2) Доменные типы симулятора (events/snapshots/metrics)
3) WebSocket/SSE протокол
4) OpenAPI контракт Simulator Backend
5) Fixtures mapping + конвертер (как производим валидные сценарии)

### ФАЗА B — Исполнительная модель (до активной разработки runner)

Цель фазы: описать **tick**, детерминизм, интеграцию с PaymentEngine и метрики.

### ФАЗА C — Качество: приёмка и тестирование

Цель фазы: критерии приёмки, тест-план, smoke/e2e сценарии.

### ФАЗА D — Операции и эксплуатация (после MVP, но лучше спроектировать заранее)

Цель фазы: режимы запуска, окружения, наблюдаемость, хранение артефактов прогонов.

---

## 4. Перечень документов и артефактов (исчерпывающе)

Ниже — полный список. Приоритеты:

- 🔴 критично (без этого нельзя безопасно писать код)
- 🟠 важно (сильно снижает риски/переписывания)
- 🟡 желательно (ускоряет онбординг/эксплуатацию)

---

### A1. **JSON Schema сценария симулятора** 🔴

**Файл(ы):**
- `fixtures/simulator/scenario.schema.json` (предпочтительно — рядом с данными)
- (опционально дублирующая ссылка/копия) `docs/ru/simulator/backend/scenario-schema.md` (как поясняющий документ)

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Формальная валидация входного сценария для backend (и для генераторов/конвертеров).
- Единый источник правды для полей: `participants`, `trustlines`, `behaviorProfiles`, `groups`, `events`.

**Аудитория:**
- Backend dev (валидатор сценария)
- Tooling dev (конвертер fixtures)
- QA (контрактные тесты)

**Содержание (минимум):**
- Версия схемы + совместимость (semver или хотя бы `schemaVersion`).
- `participants[]`:
  - `id`, `name`, `type` (`person|business|hub`), `groupId`, `behaviorProfileId`.
  - (решение) допустим ли `status` и другие поля.
- `trustlines[]`:
  - `from`, `to`, `limit`, (опц.) `equivalent`, `policy`.
- `behaviorProfiles[]`:
  - `id`, `extends?`, `props`, `rules[]`.
  - определить допустимые `trigger`, `action`, `params` как enum/oneOf.
- `groups[]`:
  - `id`, `label`.
- `events[]`:
  - `time` (формат), `type`, `effects[]`.

**Критерии готовности:**
- Схема валидирует пример **в стиле** `docs/ru/simulator/backend/GEO-community-simulator-application.md` (раздел 7.2), с добавлением обязательных заголовков `schema_version`/`scenario_id`.
- Схема валидирует хотя бы 1 «реальный» сценарий из `fixtures/simulator/*/scenario.json`.
- Есть минимальный набор negative cases (обязательные поля, enum).

**Источники:**
- `docs/ru/simulator/backend/GEO-community-simulator-application.md` (7.2)
- `admin-fixtures/v1/datasets/*.json` (structure participants/trustlines)
- Решения из `plans/real-mode-monorepo-architecture.md` (fixtures strategy)

---

### A2. **Модель данных симулятора: events/snapshots/metrics (domain types)** 🔴

**Файл:**
- `docs/ru/simulator/backend/simulator-domain-model.md`

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Зафиксировать «что такое событие» в симуляторе и какие данные нужны UI.
- Развести:
  - доменные события симуляции (tick, tx.started/tx.completed, clearing.plan, errors)
  - снапшоты (graph snapshot/state snapshot)
  - метрики (time series / агрегаты)

**Аудитория:** backend + frontend.

**Содержание:**
- Словарь типов событий (канонические `type` строковые значения).
- Базовые поля всех событий:
  - `event_id`, `ts`, `type`, `run_id`, `scenario_id?`, `equivalent?`, `ttl_ms?`.
- Отдельные payload’ы для:
  - `tick`
  - `tx.*`
  - `clearing.*`
  - `run_status`
  - `error`
- Формат snapshot’ов:
  - «latest» vs «history»
  - минимальный payload для UI (чтобы не тянуть мегабайты)
- Метрики:
  - перечень метрик для MVP (из `GEO-community-simulator-application.md` 3.1)
  - форматы выдачи (в event, в snapshot, отдельным endpoint)

**Критерии готовности:**
- Явно описаны все события, которые UI должен уметь обработать в real mode.
- Согласовано с текущей моделью `api/openapi.yaml` (там есть `SimulatorEvent` для SSE).
- Есть примеры минимум для 3 событий (JSON примеры).

**Источники:**
- `api/openapi.yaml` (раздел `SimulatorEvent*`)
- `docs/ru/simulator/frontend/docs/api.md` (ожидания UI)
- `plans/real-mode-monorepo-architecture.md` (выбор WebSocket, snapshots)

---

### A3. **WebSocket (или SSE+REST) протокол Simulator Backend** 🔴

**Файл:**
- `docs/ru/simulator/backend/ws-protocol.md`

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Контракт реального времени между simulator-ui и backend.
- Без него невозможно реализовать `admin-ui/src/api/realApi.ts` и backend endpoint без расхождений.

**Аудитория:** backend + frontend.

**Содержание:**
- Выбранный транспорт:
  - WebSocket (как в решениях плана) **или** SSE для MVP + REST команды.
  - Важно: в `plans/real-mode-monorepo-architecture.md` есть конфликт (в тексте рекомендация SSE для MVP, но в разделе 9 принято WebSocket). В протоколе должно быть закреплено итоговое.
- URL/endpoint’ы (шаблоны):
  - `/api/v1/simulator/runs/{run_id}/ws` или аналоги.
- Формат сообщений **Client → Server**:
  - `pause`, `resume`, `stop`, `set_intensity`, `trigger_action`.
- Формат сообщений **Server → Client**:
  - `tick`, `run_status`, `tx.*`, `clearing.*`, `error`.
- Heartbeat/keepalive, reconnect policy.
- Ошибки/коды ошибок (как минимум типизированные причины).
- Версионирование протокола (например `protocolVersion`).

**Критерии готовности:**
- По документу можно независимо реализовать клиента и сервер, и они сойдутся.
- Есть таблица message types + JSON examples.

**Источники:**
- `plans/real-mode-monorepo-architecture.md` (7.1)
- `docs/ru/simulator/frontend/docs/api.md`
- `api/openapi.yaml` (если SSE остаётся)

---

### A4. **OpenAPI: Simulator Backend (scenarios/runs/snapshots)** 🔴

**Файл:**
- `api/openapi.yaml` (расширить)

**Статус:** ✅ выполнено (2026-01-28)

**Назначение:**
- Формально зафиксировать REST endpoints для управления симуляцией и чтения состояния.
- Генерировать типы/клиентов в будущем (опционально), но главное — контракт.

**Аудитория:** backend + frontend + QA.

**Содержание (минимум):**
- Привести к одному миру пути:
  - либо сохранить существующее `/simulator/*` как legacy,
  - либо добавить новый namespace (`/simulator/scenarios`, `/simulator/runs`, `/simulator/snapshots`) **и явно пометить legacy**.
- Эндпоинты:
  - `GET/POST /simulator/scenarios`
  - `GET/DELETE /simulator/scenarios/{id}`
  - `POST /simulator/runs`
  - `GET /simulator/runs/{run_id}`
  - `POST /simulator/runs/{run_id}/pause|resume|stop`
  - `GET /simulator/snapshots/{run_id}/latest`
  - `GET /simulator/snapshots/{run_id}/history`
- Схемы request/response:
  - `ScenarioCreate`, `Scenario`, `SimulationStart`, `SimulationState`, `SimulationSnapshot`, `ErrorEnvelope`.
- Ссылка на JSON Schema сценария (как externalDocs или описанием).

**Критерии готовности:**
- Все endpoints имеют request/response schemas.
- Согласованы названия полей с `ws-protocol.md` и `simulator-domain-model.md`.

**Источники:**
- `plans/real-mode-monorepo-architecture.md` (2.2, 5.2)
- текущий `api/openapi.yaml` (существующие схемы)

---

### A5. **Mapping fixtures → simulator scenario + правила конвертации** 🔴

**Статус:** ✅ done (2026-01-28)

**Файл:**
- `docs/ru/simulator/backend/fixtures-mapping.md`

**Назначение:**
- Зафиксировать, как именно текущие `admin-fixtures` превращаются в валидный `fixtures/simulator/<scenario_id>/scenario.json`.

**Аудитория:** backend/tooling.

**Содержание:**
- Таблица соответствий:
  - `admin participants` → `scenario participants` (+ groupId, behaviorProfileId)
  - `admin trustlines` → `scenario trustlines` (+ policies?)
- Правила генерации:
  - как назначаются `groupId` (по типу, кластеризация, случайно)
  - как назначаются `behaviorProfileId`
  - какие базовые behaviorProfiles для MVP (3–5)
- Гарантии:
  - детерминизм генератора (seed)
  - стабильность ID

**Критерии готовности:**
- Описано так, что по документу можно поддерживать генератор seed-сценариев `scripts/generate_simulator_seed_scenarios.py`.

**Источники:**
- `admin-fixtures/v1/datasets/*`
- `plans/real-mode-monorepo-architecture.md` (7.3)
- `docs/ru/simulator/backend/GEO-community-simulator-application.md` (идея behaviorProfiles/groups/events)

---

### A6. **Артефакты сценариев (fixtures/simulator/*)** 🔴

**Статус:** ✅ done (2026-01-28)

**Файлы:**
- `fixtures/simulator/greenfield-village-100/scenario.json`
- `fixtures/simulator/riverside-town-50/scenario.json` (желательно)

**Назначение:**
- «Золотые» эталонные сценарии для разработки, CI, демо, регрессии.

**Аудитория:** все.

**Содержание:**
- Сценарий должен валидироваться `fixtures/simulator/scenario.schema.json`.
- Поведенческие профили и группы должны быть достаточны, чтобы runner генерировал события.

**Критерии готовности:**
- Валидация по JSON Schema проходит.
- Есть минимальный README по запуску (см. D1).

**Источники:**
- `admin-fixtures/v1` (данные)
- `fixtures-mapping.md` (правила)

---

### B1. **Алгоритм SimulationRunner (tick-model + детерминизм)** 🟠

**Файл:**
- `docs/ru/simulator/backend/runner-algorithm.md`

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Устранить разночтения «что такое tick» и как из behaviorProfiles рождаются действия.

**Аудитория:** backend.

**Содержание:**
- Определение времени:
  - `tick` как целое, mapping tick→time.
- Планирование действий:
  - какие `trigger` поддерживаем в MVP (`each_tick`, `each_day`, `random(p)` и т.д.).
- Интерпретация `intensity`:
  - формула: intensity→probability/actions per tick.
- Детерминизм:
  - seed, PRNG, порядок обхода.
- Ошибки и retries (минимум: как трактуем payment fail).

**Критерии готовности:**
- По документу можно написать unit-тесты для SB-08 и SB-NF-04.

**Источники:**
- `docs/ru/simulator/backend/GEO-community-simulator-application.md` (2.2, 2.3, 5)
- существующий `app/core/payments/*` (интеграция)

---

### B2. **Интеграция с PaymentEngine / GEO Core API** 🟠

**Файл:**
- `docs/ru/simulator/backend/payment-integration.md`

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Зафиксировать, как симулятор создаёт реальные платежи и что считается успехом/ошибкой.

**Аудитория:** backend.

**Содержание:**
- Какие endpoints вызываем (`POST /api/v1/payments`, возможно trustlines, clearing).
- Идемпотентность: используем ли `Idempotency-Key`.
- Таймауты, ретраи, лимиты параллельности.
- Какие доменные события эмитим на основе ответа (`tx.started`, `tx.completed`, `tx.failed`).

**Критерии готовности:**
- Ясно, как реализовать SB-09/SB-10.

**Источники:**
- `api/openapi.yaml` (Payments)
- код: `app/core/payments/` (реальная логика/ошибки)

---

### B3. **Наблюдаемость и метрики (симулятор)** 🟠

**Файл:**
- `docs/ru/simulator/backend/observability.md`

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Заранее определить: какие метрики экспортируем/сохраняем, чтобы симуляция была управляемой.

**Аудитория:** backend + devops.

**Содержание:**
- Метрики MVP (success rate, avg route len, clearing volume, bottlenecks).
- Технические метрики: tick latency, ws clients, queue size.
- Как публикуем: Prometheus (если есть), логирование, events.

**Критерии готовности:**
- Список метрик привязан к точкам измерения в runner.

**Источники:**
- `docs/ru/simulator/backend/GEO-community-simulator-application.md` (3.1)
- `app/utils/metrics.py` (существующие практики)

---

### C1. **Acceptance Criteria: Simulator Backend + UI Real Mode** 🔴

**Файл:**
- `docs/ru/simulator/backend/acceptance-criteria.md`

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Превратить план в проверяемые критерии (что значит “готово”).

**Аудитория:** PM/Tech lead/backend/frontend/QA.

**Содержание:**
- Таблица критериев уровня:
  - SB-* (backend)
  - SUI-* (frontend)
  - INT-* (интеграция)
- Методы проверки: unit/integration/contract/e2e/perf.

**Критерии готовности:**
- Покрывает минимум требования из анализа (SB-01..SB-10 и NF-01..NF-04).

**Источники:**
- `docs/ru/simulator/backend/archive/Анализ плана Real Mode Monorepo Architecture Documentation plan.md` (раздел 5)
- `plans/real-mode-monorepo-architecture.md`

---

### C2. **Test Plan (структура тестов + smoke сценарии)** 🔴

**Файл:**
- `docs/ru/simulator/backend/test-plan.md`

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Описать тестовую пирамиду и минимальный E2E smoke сценарий.

**Аудитория:** QA/backend/frontend.

**Содержание:**
- Структура тестов (unit/integration/contract/performance).
- Smoke test (gherkin) + привязка к Playwright (или другому инструменту).
- Тестовые данные: какие сценарии используем.

**Критерии готовности:**
- Есть минимум 1 smoke сценарий “UI получает tick + tx.*”.

**Источники:**
- Анализ (раздел 4, 4. Smoke Test)
- текущая структура `tests/`

---

### C3. **API Examples (curl/ws) для ручной проверки** 🟡

**Файл:**
- `docs/ru/simulator/backend/api-examples.md`

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Ускорить ручную проверку и отладку без UI.

**Аудитория:** dev/QA.

**Содержание:**
- Примеры:
  - загрузка сценария
  - старт run
  - pause/resume/stop
  - подключение к WS (пример на JS/python)
  - получение snapshots

**Критерии готовности:**
- Примеры соответствуют OpenAPI + ws-protocol.

**Источники:**
- `api/openapi.yaml`
- `ws-protocol.md`

---

### D1. **README: как запускать Real Mode симулятор в monorepo** 🟠

**Файл:**
- `docs/ru/simulator/backend/real-mode-runbook.md`

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Онбординг разработчика: быстро поднять backend + simulator-ui + зависимости.

**Аудитория:** dev.

**Содержание:**
- Режимы:
  - fixtures mode
  - real mode
- Запуск на Windows/WSL2:
  - какие скрипты (`scripts/run_local.ps1`, `scripts/run_simulator_ui.ps1`)
  - env vars
- Запуск через Docker (staging/QNAP): что включить.
- Troubleshooting.

Примечание про UX артефактов:
- В браузере невозможно надёжно реализовать «Открыть папку run».
- Поэтому нужно заранее договориться: либо отдаём ссылки/zip на скачивание, либо показываем `artifact_path` только как текст/копируемую строку.

**Критерии готовности:**
- Новый разработчик может поднять окружение по инструкции.

**Источники:**
- `plans/real-mode-monorepo-architecture.md` (3)
- `README.md`, `scripts/*`, `docker-compose*.yml`

---

### D2. **Хранение состояния/артефактов прогонов (in-memory/redis/postgres)** 🟡

**Файл:**
- `docs/ru/simulator/backend/run-storage.md`

**Статус:** ✅ done (2026-01-28)

**Назначение:**
- Зафиксировать, что и где мы храним (state/events/snapshots/summary), и почему.

**Аудитория:** backend.

**Содержание:**
- MVP вариант (in-memory + redis pubsub? + optional postgres summary) — как в плане.
- TTL/очистка.
- Ограничения горизонтального масштабирования.

**Критерии готовности:**
- Понятно, какой вариант реализуем первым и как мигрируем.

**Источники:**
- `plans/real-mode-monorepo-architecture.md` (7.2)
- `docker-compose.yml` (redis/postgres)

---

## 5. Сводная таблица зависимостей и очередности

| № | Документ/артефакт | Приоритет | Зависит от | Даёт вход для |
|---:|---|:--:|---|---|
| A1 | `fixtures/simulator/scenario.schema.json` | 🔴 | `GEO-community-simulator-application.md` | A4, A5, A6, B1, C2 |
| A2 | `simulator-domain-model.md` | 🔴 | `api/openapi.yaml`, `docs/ru/simulator/frontend/docs/api.md` | A3, A4 |
| A3 | `ws-protocol.md` | 🔴 | A2, решения из плана | frontend realApi, backend ws |
| A4 | `api/openapi.yaml` (simulator runs/scenarios) | 🔴 | A1, A2, A3 | QA/contract, frontend |
| A5 | `fixtures-mapping.md` | 🔴 | A1, admin-fixtures | конвертер + сценарии |
| A6 | `fixtures/simulator/*/scenario.json` | 🔴 | A1, A5 | dev/test/demo |
| B1 | `runner-algorithm.md` | 🟠 | A1, A2 | реализация runner + unit tests |
| B2 | `payment-integration.md` | 🟠 | `api/openapi.yaml` (payments) | интеграция PaymentEngine |
| B3 | `observability.md` | 🟠 | `GEO-community-simulator-application.md` | метрики/мониторинг |
| C1 | `acceptance-criteria.md` | 🔴 | план + анализ | итоговая приёмка |
| C2 | `test-plan.md` | 🔴 | C1, A1, A3, A4, A6 | тесты |
| C3 | `api-examples.md` | 🟡 | A3, A4 | ручные проверки |
| D1 | `real-mode-runbook.md` | 🟠 | план + scripts/docker | онбординг |
| D2 | `run-storage.md` | 🟡 | план | реализация persistence |

---

## 6. Рекомендованный минимальный “Doc Sprint” перед кодом (оценка)

Если цель — начать реализацию **без переделок интерфейсов**, минимальный набор до кода:

1) A1 (schema)
2) A2 (domain model)
3) A3 (ws protocol)
4) A4 (openapi)
5) A5 (fixtures mapping)
6) C1 (acceptance criteria)
7) C2 (test plan)

После этого можно писать код backend/ui, не «угадывая» форматы.

---

## 7. Следующий шаг

Если ок, я могу дальше:

1) Создать шаблоны (skeleton) для перечисленных документов в `docs/ru/simulator/backend/`.
2) Или начать сразу с A1: `fixtures/simulator/scenario.schema.json` (самый критичный контракт).

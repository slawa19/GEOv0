
# Спецификация реализации: Игровой Интерфейс (Vue 3 Edition)

## 1. Визуальный стиль (Уточненный)
*   **Бизнес:** 🟩 Ярко-зеленый **Квадрат** (Emerald-500). Символ стабильности и фундамента.
*   **Человек:** 🔵 Ярко-синий **Круг** (Blue-500). Символ динамики.
*   **Связи:** Тонкие линии, видимые только при взаимодействии.

---

## 2. Замена React-библиотек на Vue-аналоги

Мы сохраняем всю логику (физика, анимации), но меняем инструменты на "родные" для Vue:

| Задача | React (было в демо) | Vue (утвержденный стек) | Почему это лучше |
| :--- | :--- | :--- | :--- |
| **Граф** | `react-force-graph-2d` | **`force-graph`** | Это та же самая библиотека (ядро), только без React-обертки. Работает во Vue быстрее и легче. |
| **Анимация UI** | `Framer Motion` | **Vue `<Transition>`** | Встроенный во Vue инструмент. Для всплывания карточек его достаточно. |
| **Сложная анимация** | `Framer Motion` | **`@motionone/vue`** | Если потребуются сложные цепочки анимаций — это прямой аналог Framer Motion от тех же разработчиков, но для Vue. |

---

## 3. Детальная реализация компонентов

### 3.1. Ядро Графа (`force-graph`)
Вместо тега `<ForceGraph2D />` мы используем инициализацию в коде. Это дает полный контроль над рисованием фигур (квадраты/круги).

**Логика отрисовки (`nodeCanvasObject`):**
Разработчик реализует функцию, которая смотрит на тип узла:

```javascript
// Псевдокод логики рендера
function paintNode(node, ctx) {
  if (node.type === 'business') {
    // 🟩 Рисуем КВАДРАТ
    ctx.fillStyle = '#10b981'; // Emerald 500
    ctx.fillRect(node.x - 6, node.y - 6, 12, 12); 
  } else {
    // 🔵 Рисуем КРУГ
    ctx.beginPath();
    ctx.arc(node.x, node.y, 4, 0, 2 * Math.PI); 
    ctx.fillStyle = '#3b82f6'; // Blue 500
    ctx.fill();
  }
}
```

### 3.2. Всплывающие карточки (HUD)
Используем **Tailwind CSS** для верстки. Карточки лежат слоем выше (`z-index: 10`) над `canvas`.

*   **Позиционирование:** Абсолютное, координаты обновляются при клике на узел.
*   **Стиль:**
    *   Фон: `bg-slate-900/80` (Темный полупрозрачный)
    *   Блюр: `backdrop-blur-md` (Эффект матового стекла)
    *   Бордер: `border border-white/10` (Тонкая обводка)
    *   Текст: `text-white` с неоновым акцентом на цифрах.

### 3.3. Линии транзакций и "Вспышка"
*   **Частицы в линиях:** Встроены в `force-graph`. При клиринге мы запускаем частицы по линии: `linkDirectionalParticles(4)`.
*   **Вспышка на весь экран:** Обычный `div` поверх всего интерфейса, который управляется через CSS opacity.

---

## 4. Итоговый `package.json` (Минимальный)

Никаких лишних библиотек. Только то, что нужно для задачи:

```json
{
  "dependencies": {
    "force-graph": "^1.43.0",       // Ядро физики и рендера (Canvas)
    "howler": "^2.2.4",             // Звуки (клики, ambient, разряд)
    "@tsparticles/vue3": "^3.0.0",  // Фоновые звезды
    "tsparticles-slim": "^2.12.0",  // Облегченный движок частиц
    "@motionone/vue": "^10.16.4"    // (Опционально) Если нужны сложные анимации UI
  }
}
```

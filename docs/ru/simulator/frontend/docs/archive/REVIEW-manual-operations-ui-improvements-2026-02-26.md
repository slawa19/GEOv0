# Проверка реализации замечаний ревью в спецификации

> Документ создан: 2026-02-26  
> Спецификация: `manual-operations-ui-improvements-spec-2026-02-26.md`  
> Метод: ручная сверка кода + текста спецификации по каждому пункту ревью

---

## Общий вывод

Спецификация **хорошо охватывает** большинство замечаний ревью — все 26+ функциональных пунктов (MP-1..MP-6, TL-1..TL-4, CL-1, ED-1..ED-3, NC-1..NC-4, FB-1..FB-3) присутствуют с кодовыми примерами и AC. Однако в спецификации обнаружены **4 реальных дефекта** (один из них критичный для корректности NC-3), а также ряд мелких пробелов в формулировках.

---

## Статус по каждому замечанию ревью

### Раздел 2.1 — Блокирующие несостыковки

| # | Замечание ревью | Статус | Где в спецификации |
|---|-----------------|--------|--------------------|
| 2.1.1 | Tri-state модель `availableTargetIds` (undefined / known-empty / known-nonempty) | ✅ **Учтено полностью** | §2 Terms, §5.1 MP-1, MP-1a, §6 UX-4; AC-MP-7/8 |
| 2.1.2 | Опциональный `availableTargetIds` в `useParticipantsList` → риск runtime-crash | ✅ **Учтено** | §5.1 MP-1: «Вариант A: поле НЕ опциональное, допускает `undefined`; в местах без фильтрации передавать `() => undefined`» |
| 2.1.3 | Единый канонический маркер "unknown" (`undefined`) в тестах/AC | ✅ **Учтено** | AC-MP-7: `availableTargetIds=undefined`; AC-MP-8: `availableTargetIds=new Set()` — последовательно |

### Раздел 2.2 — Доменные нюансы

| # | Замечание ревью | Статус | Где в спецификации |
|---|-----------------|--------|--------------------|
| 2.2.4 | Multi-hop маршруты: direct-hop эвристика даёт false negatives | ✅ **Учтено** | §2 Terms («direct-hop approximation, точная фильтрация — Phase 3»); §7.2 опциональный endpoint; §11 Risks («скрывает multi-hop достижимые цели») |
| 2.2.5 | Close TL при reverse debt: `TrustlineInfo` не содержит `reverse_used` | ✅ **Учтено** | §5.2 TL-2: «guard best-effort»; AC-TL-9: backend вернёт ошибку при `used=0` + reverse debt; §11 Risks |
| 2.2.6 | Числовой формат vs backend (`parse_amount_decimal()`) | ✅ **Учтено** | §6 UX-8: ссылка на `parse_amount_decimal()`, требование `parseAmountStringOrNull()`, нормализация `,`→`.`, AC-MP-9/10, AC-TL-5..8 |
| 2.2.7 | Design system consistency для toast-стилей | ✅ **Учтено** | §3.8 Non-goals: «Полная унификация toast вынесена в Non-goals»; §5.6 FB-1: «допустимо оставить toast исключением» |

### Раздел 2.3 — Корректировки фаз/AC

| # | Замечание ревью | Статус | Где в спецификации |
|---|-----------------|--------|--------------------|
| 2.3.8 | Исправление `availableTargetIds` — отдельный пункт в Phase 1 | ✅ **Учтено** | §10 Phase 1: **MP-1a** вынесен в отдельную строку |
| 2.3.9 | AC на tri-state в двух ветках (loading vs known-empty) | ✅ **Учтено** | AC-MP-7 (updating/unknown) и AC-MP-8 (known-empty без fallback) |

### Раздел 3.1 — Manual Payment (кодовые несоответствия)

| # | Замечание ревью | Статус | Где в спецификации |
|---|-----------------|--------|--------------------|
| MP-1/MP-5 | Фильтрация To по `availableTargetIds`; canvas↔dropdown не согласованы | ✅ **Учтено** | §5.1 MP-1 с кодовым примером; MP-5 как автоматическое следствие MP-1 |
| MP-1a | `availableTargetIds` не проверяет `available > 0`; fallback при `size=0` | ✅ **Учтено** | §5.1 MP-1a: добавлена проверка `parseAmountNumber(tl.available) > 0`; fallback только при `trustlinesLoading === true` |
| MP-2 | Capacity в dropdown отсутствует | ✅ **Учтено** | §5.1 MP-2: `capacityByToPid`, `toOptionLabel()`, wiring из SimulatorAppRoot |
| MP-4 | Inline-причина disabled Confirm | ✅ **Учтено** | §5.1 MP-4: `confirmDisabledReason` computed с 4 ветками, нормализация от `normalized` |
| MP-6 | Prefetch trustlines при старте payment flow | ✅ **Учтено** | §5.1 MP-6 (Дополнение): «`startPaymentFlow()` обязан делать best-effort prefetch `refreshTrustlines()`» |
| Формат чисел MP | `amountNum = Number(amount.value)`, raw string в action | ✅ **Учтено** | §6 UX-8; MP-4: «при отправке — `confirmPayment(normalized)`» |

### Раздел 3.2 — Trustline Management

| # | Замечание ревью | Статус | Где в спецификации |
|---|-----------------|--------|--------------------|
| TL-1 | newLimit < used — нет сообщения | ✅ **Учтено** | §5.2 TL-1: `updateLimitTooLow` computed + template |
| TL-2 | Close TL guard при debt отсутствует | ✅ **Учтено** | §5.2 TL-2: `closeBlocked` computed, disabled кнопка, inline hint |
| TL-3 | `(exists)` marker в create-flow To | ✅ **Учтено** | §5.2 TL-3: `existingToPids`, `toLabel()` |
| TL-4 | pre-fill из `props.currentLimit` вместо `effectiveLimit` | ✅ **Учтено** | §5.2 TL-4: `newLimit.value = effectiveLimit.value` |

### Раздел 3.3 — Clearing

| # | Замечание ревью | Статус | Где в спецификации |
|---|-----------------|--------|--------------------|
| CL-1 | Loading-индикатор отсутствует | ✅ **Учтено** | §5.3 CL-1: спиннер / «Running…» в кнопке |

### Раздел 3.4 — EdgeDetailPopup

| # | Замечание ревью | Статус | Где в спецификации |
|---|-----------------|--------|--------------------|
| ED-1 | Close line не проверяет `used > 0` | ✅ **Учтено** | §5.4 ED-1: `closeBlocked`, disabled кнопка, inline hint |
| ED-2 | Utilization bar отсутствует | ✅ **Учтено** | §5.4 ED-2: `utilizationPct`, `utilizationColor`, CSS-bar |
| ED-3 | Send Payment shortcut отсутствует | ✅ **Учтено** | §5.4 ED-3: кнопка `emit('sendPayment')` + wiring в SimulatorAppRoot |

### Раздел 3.5 — NodeCardOverlay

| # | Замечание ревью | Статус | Где в спецификации |
|---|-----------------|--------|--------------------|
| NC-1 | Edit для IN trustlines отсутствует | ✅ **Учтено** | §5.5 NC-1: кнопка ✏️ для IN group |
| NC-2 | Available column отсутствует | ✅ **Учтено** | §5.5 NC-2: `nco-trustline-row__avail`, расширение grid |
| NC-3 | Saturated visual отсутствует; риск `safeNum()` | ⚠️ **Учтено частично — содержит баг** | §5.5 NC-3: использует `parseAmountNumber()`, НО см. [Дефект #2] ниже |
| NC-4 | Run Clearing quick action отсутствует | ✅ **Учтено** | §5.5 NC-4: кнопка `🔄 Run Clearing`, новый prop `onInteractRunClearing` |

### Раздел 3.6 — Feedback & Discoverability

| # | Замечание ревью | Статус | Где в спецификации |
|---|-----------------|--------|--------------------|
| FB-1 | SuccessToast отсутствует | ✅ **Учтено** | §5.6 FB-1: новый `SuccessToast.vue`, триггеры из `useInteractMode`, 2500ms |
| FB-2 | Adaptive dismiss ErrorToast | ✅ **Учтено** | §5.6 FB-2: `effectiveDismissMs` по длине сообщения |
| FB-3 | ESC hint отсутствует в UI | ✅ **Учтено** | §5.6 FB-3: «Press ESC to cancel…» в ActionBar и панелях |

### Раздел 4 — Критические несоответствия

| # | Замечание | Статус |
|---|-----------|--------|
| 4.1 | Фильтрация To по `availableTargetIds` не wired | ✅ Учтено в MP-1 |
| 4.2 | Fallback «показать всех» при `size=0` | ✅ Учтено в MP-1a |
| 4.3 | Guard close при debt в TL и ED | ✅ Учтено в TL-2, ED-1 |
| 4.4 | Числовой формат vs backend | ✅ Учтено в UX-8 |

### Раздел 5 — Рекомендации

| # | Рекомендация | Статус |
|---|--------------|--------|
| 5.1.1 | Tri-state + filter `available > 0` как обязательный минимум | ✅ Phase 1 |
| 5.1.2 | Prefetch trustlines в `startPaymentFlow()` | ✅ MP-6 |
| 5.1.3 | Единый decimal helper | ✅ UX-8 |
| 5.2.1 | Phase 3 endpoint как дорогостоящий | ✅ Phase 3 — опциональный |
| 5.2.2 | SuccessToast vs history-highlight | ✅ FB-1 выбирает SuccessToast |
| 5.2.3 | Utilization bar может подождать Phase 2 | ✅ ED-2 в Phase 2 |
| 5.3.1 | Явно описать "error/unknown/known" для trustlines-кэша | ✅ §6 UX-4 |
| 5.3.2 | NC-3: "unknown available" не должен быть saturated | ⚠️ Требование правильное, реализация в spec содержит баг — см. Дефект #2 |
| 5.3.3 | TL-2: ограничение по `reverse_used` | ✅ TL-2 + AC-TL-9 + §11 Risks |

---

## Найденные дефекты в самой спецификации

### Дефект #1 (Критичный для корректности NC-3): `parseAmountNumber(null)` возвращает `0`, а не `NaN`

**Где:** §5.5 NC-3, код `tlRowClass()`.

**Проблема:** Спецификация пишет:

> «Якщо значення не парсится, `parseAmountNumber()` даст NaN; такие случаи не считаем saturated автоматически.»

Но это **неверно** по коду в `simulator-ui/v2/src/utils/numberFormat.ts`:

```typescript
export function asFiniteNumber(v: unknown): number {
  if (typeof v === 'number') return Number.isFinite(v) ? v : 0
  if (typeof v === 'string') { ... }
  return 0  // ← для null, undefined, object
}
```

`parseAmountNumber(null)` = `0` (конечное число), а не `NaN`. Следовательно:
- `availOk = Number.isFinite(0)` → `true`
- `avail <= 0` → `true`
- → **trustline с `available = null` будет помечена как saturated** — та же ошибка, что и с `safeNum()`!

Спецификация неправильно описывает поведение `parseAmountNumber()` и предлагает решение, которое проблему **не исправляет**.

**Правильное исправление для кода `tlRowClass()`:**

```typescript
function tlRowClass(tl: TrustlineInfo): Record<string, boolean> {
  // Explicit null/empty guard — parseAmountNumber() returns 0 for null/undefined,
  // which would incorrectly mark "unknown" available as saturated.
  const availRaw = tl.available
  if (availRaw == null || String(availRaw).trim() === '') {
    // Unknown available → no visual class
    return {}
  }
  const avail = parseAmountNumber(availRaw)
  const limit = parseAmountNumber(tl.limit)
  const limitOk = Number.isFinite(limit) && limit > 0
  return {
    'nco-trustline-row--saturated': avail <= 0,
    'nco-trustline-row--warning': avail > 0 && limitOk && avail / limit < 0.15,
  }
}
```

**Необходимое изменение в спецификации:** Заменить `availOk = Number.isFinite(avail)` на явную проверку исходного значения перед парсингом, добавить примечание: «`parseAmountNumber(null)` возвращает `0`, поэтому проверять исходное значение на `null`/пустую строку обязательно до вызова хелпера».

---

### Дефект #2 (Важный): `parseAmountStringOrNull()` не нормализует запятую, но спека это требует

**Где:** §6 UX-8, §5.1 MP-4.

**Проблема:** Спецификация в UX-8 требует:

> «`parseAmountStringOrNull()` обязана делать... нормализацию `,`→`.` перед отправкой»

Но текущая реализация `parseAmountStringOrNull()` в `numberFormat.ts`:

```typescript
export function parseAmountStringOrNull(v: unknown): string | null {
  if (v == null) return null
  if (typeof v === 'number') return Number.isFinite(v) ? String(v) : null
  if (typeof v === 'string') {
    const s = v.trim()
    return s ? s : null  // ← только trim(), без замены запятой на точку!
  }
  return null
}
```

AC-MP-10 требует: `parseAmountStringOrNull("1,23")` → `"1.23"`, но фактически вернёт `"1,23"`. Backend `parse_amount_decimal()` отклонит строку с запятой (regex `^\d+(?:\.\d+)?$`).

**Важно:** Спецификация нигде **явно** не указывает файл `simulator-ui/v2/src/utils/numberFormat.ts` как подлежащий изменению. Изменение `parseAmountStringOrNull()` не включено ни в одну из фаз в §10.

**Необходимые изменения в спецификации:**
1. Добавить в §10 Phase 1 отдельную строку: **`parseAmountStringOrNull()` нормализация** | `numberFormat.ts` | XS.
2. Добавить кодовый пример изменённой функции:

```typescript
export function parseAmountStringOrNull(v: unknown): string | null {
  if (v == null) return null
  if (typeof v === 'number') return Number.isFinite(v) ? String(v) : null
  if (typeof v === 'string') {
    // Normalize: trim + replace comma decimal separator with period
    const s = v.trim().replaceAll(',', '.')
    return s ? s : null
  }
  return null
}
```

3. Добавить unit-тест в `numberFormat.test.ts`: `parseAmountStringOrNull("1,23")` → `"1.23"`.

---

### Дефект #3 (Средний): `createValid` для create-flow не обновлён явно

**Где:** §5.2, §6 UX-8, AC-TL-8.

**Проблема:** Текущий код `TrustlineManagementPanel.vue`:

```typescript
// UX: don't allow creating a 0-limit trustline (no practical capacity).
const createValid = computed(() => Number.isFinite(limitNum.value) && limitNum.value > 0)
```

AC-TL-8 требует: «В create-flow limit=`" 0 "` → Create разрешён». UX-8 говорит: «trustline `limit` допускает **0**». Следовательно, `> 0` нужно заменить на `>= 0`.

Но ни в §5.2, ни в §10 нет явного указания изменить `createValid`. Разработчик должен сам догадаться из AC, что нужно менять логику. Существующий комментарий в коде `UX: don't allow creating a 0-limit trustline` противоречит требованию.

**Необходимое изменение в спецификации:** В §5.2 (TL-1 или отдельным пунктом) явно указать:

> «Изменить `createValid` в `TrustlineManagementPanel.vue`: заменить условие `limitNum.value > 0` на `limitNum.value >= 0` (0-лимит допустим согласно UX-8). Удалить комментарий "don't allow creating a 0-limit trustline".»

---

### Дефект #4 (Малый): Файл `useInteractMode.ts` отсутствует в Phase 1 для MP-6

**Где:** §10 Phase 1, строка MP-6.

**Проблема:** Текущая таблица:

| MP-6 | ManualPaymentPanel.vue, SimulatorAppRoot.vue | XS |

Но §5.1 MP-6 требует: «`startPaymentFlow()` в `useInteractMode.ts` должен делать best-effort prefetch `refreshTrustlines()`». Текущий код `startPaymentFlow()` вызывает только `refreshParticipants()`.

**Необходимое изменение в спецификации:** Добавить `useInteractMode.ts` в список файлов для MP-6.

---

## Итоговая таблица

| Дефект | Критичность | Требуется изменение в spec |
|--------|------------|---------------------------|
| **#1**: `parseAmountNumber(null)=0`, а не NaN → NC-3 saturated для null-значений | 🔴 Критичный (баг в коде-примере spec) | Исправить `tlRowClass()`, добавить null-guard |
| **#2**: `parseAmountStringOrNull()` не нормализует запятую; файл не в фазах | 🟠 Важный (AC-MP-10 не пройдёт без этого) | Добавить comma→period в функцию; добавить в Phase 1 |
| **#3**: `createValid` изменение не описано явно | 🟡 Средний (AC-TL-8 не пройдёт без этого) | Явно указать смену `> 0` → `>= 0` |
| **#4**: `useInteractMode.ts` отсутствует в файлах для MP-6 | 🔵 Малый (ошибка в таблице планирования) | Добавить файл в строку MP-6 |

---

## Пункты ревью, которые остались **вне scope** спецификации (обосновано)

Следующие замечания ревью сознательно вынесены за рамки спецификации с обоснованием в Non-goals:

| Замечание | Non-goal section |
|-----------|-----------------|
| Полная унификация toast по DS | §3.8 |
| Новый роутинг страниц | §3.1 |
| Полный редизайн панелей | §3.2 |
| Drag/reposition панелей | §3.5 |
| Keyboard shortcuts для ActionBar | §3.6 |
| History log interactivity | §3.7 |
| Изменение протокола SSE | §3.3 |
| Переписывание backend-маршрутизации | §3.4 |

Все Non-goals корректно аргументированы в спецификации.

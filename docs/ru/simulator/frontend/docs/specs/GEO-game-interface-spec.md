
# Полная спецификация: Игровой интерфейс симулятора GEO

**Дата:** 2026-01-17  
**Версия:** 1.0 Final

---

## Оглавление

1. [Общее описание](#1-общее-описание)
2. [Цели и задачи](#2-цели-и-задачи)
3. [Визуальная концепция](#3-визуальная-концепция)
4. [Функциональные требования](#4-функциональные-требования)
5. [Архитектура](#5-архитектура)
6. [Детальная техническая реализация](#6-детальная-техническая-реализация)
7. [Финальный стек технологий](#7-финальный-стек-технологий)
8. [План разработки (PoC → MVP → Full)](#8-план-разработки-poc--mvp--full)
9. [Принципы данных и производительности (как Admin UI)](#9-принципы-данных-и-производительности-как-admin-ui)
10. [Контракт API для состояния, транзакций и клиринга](#10-контракт-api-для-состояния-транзакций-и-клиринга)

---

## 1. Общее описание

### 1.1. Что это такое

**Игровой интерфейс симулятора GEO** — это визуальная надстройка над существующей системой GEO (децентрализованной кредитной сети), созданная для:
- Наглядной демонстрации сетевой структуры доверия
- Визуализации механизма клиринга (автоматического взаимозачёта долгов)
- Создания эффекта погружения через метафору "космической карты"

### 1.2. Зачем это нужно

**Проблема:** Табличные интерфейсы и графы в админке не передают динамику и "живость" системы доверия.

**Решение:** Тактический экран в стиле космических стратегий (EVE Online, Stellaris), где:
- **Участники** = звёздные системы (узлы графа)
- **Линии доверия** = гравитационные связи
- **Клиринг** = энергетический разряд, "замыкающий" цепь долгов

**Целевая аудитория:**
1. **Администраторы** — мониторинг состояния сети
2. **Демонстрации** — презентации для инвесторов и партнёров
3. **Обучение** — наглядное объяснение механики системы новым пользователям

---

## 2. Цели и задачи

### 2.1. Основные цели

| Цель | Описание |
|------|----------|
| **Визуализация графа доверия** | Отображение 50-100+ участников и их связей в режиме реального времени |
| **Демонстрация клиринга** | Анимация процесса взаимозачёта долгов с визуальными эффектами |
| **Иммерсивность** | Создание "Wow-эффекта" через звук, свет, частицы |
| **Производительность** | Плавная работа (60 FPS) даже на средних устройствах |

### 2.2. Функциональные задачи

1. **Отображение участников**
   - Визуальное различие типов (бизнес/физлицо)
   - Цветовая индикация состояния (активный/неактивный/в сделке)
  - Размер узла задаётся готовым `viz_size` (семантика на backend/в фикстурах)

2. **Интерактивность**
   - Клик по узлу → фокус камеры + всплывающая карточка с данными
   - Ховер → подсветка связей
   - Зум и панорамирование

3. **Визуализация транзакций**
   - Бегущие частицы вдоль линий при передаче долга
   - Вспышка при успешном клиринге
   - Звуковое сопровождение

4. **Атмосфера**
   - Темный космический фон с частицами-звёздами
   - Неоновая палитра (cyan/emerald/purple)
   - Эффекты свечения (glow) и размытия (blur)

---

## 3. Визуальная концепция

### 3.1. Стилистика: "Космический тактический экран"

**Референсы:**
- EVE Online (карта звёздных систем)
- Stellaris (галактическая карта)
- Cyberpunk 2077 (UI/HUD)

### 3.2. Цветовая палитра

| Элемент | Цвет | Tailwind | Назначение |
|---------|------|----------|------------|
| **Фон** | Глубокий тёмно-синий | `bg-slate-950` | Космическая пустота |
| **Бизнес-узел** | Ярко-зелёный | `emerald-500` | Квадрат (стабильность) |
| **Физлицо-узел** | Ярко-синий | `blue-500` | Круг (динамика) |
| **Связи (неактивные)** | Полупрозрачный серый | `slate-700/20` | Скрытые линии |
| **Связи (активные)** | Яркий cyan | `cyan-400` | Видимые при фокусе |
| **Транзакция** | Пульсирующий фиолетовый | `purple-500` | Бегущие частицы |
| **Вспышка клиринга** | Белый → Голубой | `white → cyan-300` | Полноэкранный эффект |

### 3.3. Типология узлов

```
🟩 БИЗНЕС (крупный квадрат)
   - Цвет: Emerald-500 (#10b981)
   - Иконка: Building (Lucide)
   - Символизм: Фундамент, узел-хаб

🔵 ФИЗЛИЦО (Круг меньшего размера)
   - Цвет: Blue-500 (#3b82f6)
   - Иконка: User (Lucide)
   - Символизм: Подвижность, конечный потребитель
```

### 3.4. Анатомия интерфейса

```
┌─────────────────────────────────────────────────────┐
│ [Верхняя панель: Статистика]        [Режим: Normal] │
├─────────────────────────────────────────────────────┤
│                                                       │
│          🟩────🔵                                    │
│           │     ╲                                     │
│           │      🔵────🟩                            │
│          🔵      /                                    │
│           ╲    🔵                                     │
│            🟩                                         │
│                                                       │
│   [Фон: Звёздная пыль (tsparticles)]               │
│                                                       │
├─────────────────────────────────────────────────────┤
│ [Нижняя панель: Кнопка "Запустить клиринг" "Одиночная Tx]        │
└─────────────────────────────────────────────────────┘

         ┌──────────────────┐
         │  КАРТОЧКА УЗЛА   │ ← Всплывает при клике
         │                  │
         │  Тип: Бизнес     │
         │  Баланс: +1200 UAH │
         │  Связей: 8       │
         └──────────────────┘
```

---

## 4. Функциональные требования

### 4.1. Базовые возможности (MVP)

| № | Функция | Описание |
|---|---------|----------|
| 1 | **Загрузка данных** | GET запрос к API `/graph/snapshot` → получение JSON с узлами и связями |
| 2 | **Отрисовка графа** | Рендер 50-100 узлов с физической симуляцией (force-directed layout) |
| 3 | **Клик по узлу** | Выделение узла → плавный зум камеры → показ карточки с данными |
| 4 | **Фильтр связей** | Показывать линии только для выбранного узла (остальные скрыты) |
| 5 | **Hover эффект** | При наведении на узел — лёгкая пульсация + изменение курсора |

### 4.2. Расширенные возможности (Full)

| № | Функция | Описание |
|---|---------|----------|
| 6 | **Анимация клиринга** | Подсветка цепи участников → бегущие частицы → вспышка → обновление балансов |
| 7 | **Звуковое сопровождение** | Фоновый ambient + звуки кликов + "разряд" при клиринге |
| 8 | **Режимы отображения** | Переключение между "Обзор" / "Фокус" / "Клиринг" |
| 9 | **Мини-карта** | Уменьшенная копия графа в углу экрана для навигации |
| 10 | **История действий** | Лента последних транзакций справа |

### 4.3. Принципы данных и UX (как мы делали граф в Admin UI)

Цель: визуализация должна быть **детерминированной**, **стабильной** и без тяжёлых вычислений на фронтенде.

1) **Ничего “семантического” не вычислять на фронтенде**
- Фронтенд НЕ считает: net-эффекты, биннинг/перцентили, “кто должник/кредитор”, “какой цвет/размер”.
- Фронтенд только:
  - рисует узлы/линии,
  - применяет `viz_*` поля как есть,
  - проигрывает анимации по готовому сценарию от backend.

2) **Дискретные ключи вместо формул и градиентов**
- Вместо “посчитай цвет по величине” используются дискретные ключи: `viz_color_key` (например `person|business|suspended|debt-0..8`).
- Это снижает сложность, упрощает кэширование и убирает расхождения между режимами (fixtures/real).

3) **Progressive disclosure: показывать меньше, но точнее**
- В режиме “Обзор” связи не должны превращаться в “шерсть”: линии тонкие и спокойные.
- Детали раскрываются:
  - при выборе узла (локальная окрестность),
  - при транзакции/клиринге (подсветка только участвующих рёбер).

4) **Камера и раскладка: предсказуемость важнее “эффектности”**
- UI-оверлеи (карточка, панель истории) не должны заставлять граф постоянно “прыгать”.
- Перецентровка/fit делается только:
  - по явному действию пользователя (например кнопка “Center”), либо
  - если панель перекрывает выбранный объект.

### 4.4. Производительность и защита от перегруза (обязательные требования)

**Бюджет кадра:** целимся в 60 FPS (≈16.7ms), деградируем плавно.

Ограничители:
- **Nodes:** целевой режим 50–150, допустимый пик 300 (с деградацией LOD).
- **Links:** в Overview рендерим все, но с LOD; в Focus “ярко” только окрестность.
- **Particles:** не более 200–400 одновременно (иначе быстро растёт стоимость draw).
- **Текст/лейблы:** по умолчанию только выбранный узел и соседи; глобальные подписи — отдельный тумблер.
- **Частота обновлений:** не дергать `.graphData()` чаще, чем нужно; анимации делать через accessors + `Graph.refresh()`.

Стратегии LOD:
- Малый зум: тонкие линии, низкая альфа, без подписей, без дорогих эффектов.
- Средний зум: подсветка окрестности выбранного узла.
- Большой зум: можно включать подписи и умеренный glow.

---

## 5. Архитектура

Цель раздела: зафиксировать границы ответственности и базовые слои так, чтобы не повторять ошибки «первой демки» (недетерминизм, смешение данных/рендера/эффектов и вычисление семантики на клиенте).

### 5.1. Принципы (обязательные)

1) **Fixture/API parity**
- UI должен одинаково работать от:
  - реального API (snapshot + события),
  - демо-фикстур (static snapshot + плейлист событий).

2) **Семантика визуализации приходит готовой**
- Клиент не вычисляет: классы должников, квантили, ранги, «кто должник/кредитор», цвет/размер/толщину/альфу.
- Клиент применяет `viz_*` как есть и проигрывает сценарии.

3) **Детерминизм**
- В runtime запрещён `Math.random()` для решений, влияющих на смысл и воспроизводимость.
- Если нужен «живой» эффект (дыхание/лёгкое мерцание) — он:
  - визуально мягкий,
  - не меняет смысл (не перекрашивает и не пересайзит семантику),
  - предпочтительно задаётся предвычисленными параметрами или seeded PRNG.

4) **Fail-fast по данным**
- При ошибке загрузки/валидации — показываем понятную ошибку, а не «тихо» генерируем случайный граф.

### 5.2. Слои визуализации (стабильный порядок)

1) **Фон** (звёздная пыль/туманность) — отдельный слой.
2) **Базовый граф** (Canvas/WebGL):
   - фоновые рёбра,
   - узлы,
   - опционально мягкое свечение (glow/bloom).
   Важно: **знак баланса кодируется цветом самого узла, не внешним свечением.**
3) **FX overlay** (второй canvas поверх графа):
   - искры транзакций,
   - клиринг по маршруту/циклу,
   - вспышки/кольца.
4) **HUD (HTML overlay)**:
   - карточка узла,
   - управляющие кнопки,
   - (опционально) debug overlay (FPS/particles).

### 5.3. Поток данных (snapshot → render → events)

1) Источник снапшота: API или fixtures (один и тот же контракт).
2) Валидация структуры снапшота/событий на входе.
3) Базовый граф обновляется редко (смена эквивалента/сцены/снапшота).
4) События `tx.*`/`clearing.*` должны работать как «оверлеи»:
   - не пересобирать весь граф на каждый шаг,
   - использовать accessors и `refresh()` для минимальных изменений отображения,
   - временные эффекты рисовать в FX overlay.

### 5.4. Границы ответственности (что где считается)

- Backend/офлайн-генератор:
  - `viz_color_key`, `viz_size`, `viz_width_key`, `viz_alpha_key` и любые классы/квантили.
  - подготовка плейлистов событий (сценарии tx/clearing).
  - политика отображения статусов (`suspended/left/deleted`): скрывать/показывать/стилизовать — решается данными, не фронтом.
- Клиент:
  - физика/раскладка (если используем force layout),
  - отрисовка, LOD, камера,
  - проигрывание событий (тайминг, эффекты),
  - но не «финансовая семантика».

### 5.5. Анти-паттерны (ошибки первой демки — не повторять)

1) Монолитный компонент, который одновременно грузит данные, крутит физику, рисует, обрабатывает ввод и эффекты.
2) Скрытый недетерминизм (рандом в рантайме, «мигания», случайная генерация графа при ошибках).
3) Семантические вычисления на фронтенде (цвет/размер/классы должников/квантили).
4) Частые полные пересборки графа на клик/шаг событий вместо `refresh()` и оверлеев.

---

## 6. Детальная техническая реализация

### 6.1. Архитектура компонента

```
SimulatorView.vue (Главный компонент)
│
├── GraphCanvas (Canvas слой)
│   └── force-graph (Ядро физики + рендер)
│
├── ParticleBackground (Фон)
│   └── tsparticles (Звёздная пыль)
│
├── HUD (Оверлей)
│   ├── TopBar (Статистика)
│   ├── NodeCard (Карточка при клике)
│   └── BottomBar (Кнопки управления)
│
└── AudioManager (Звуки)
    └── howler.js
```

### 6.2. Компонент №1: Граф (`force-graph`)

**Задача:** Отрисовка узлов и связей с физической симуляцией.

#### Почему не `react-force-graph-2d`?
- Основной стек проекта — **Vue.js 3**
- `react-force-graph-2d` — это React-обертка над библиотекой `force-graph`
- Использование React внутри Vue-проекта раздует бандл и создаст конфликты зависимостей
- **Решение:** Использовать нативную библиотеку `force-graph` (Vanilla JS) напрямую

#### Как это работает

```typescript
import ForceGraph from 'force-graph';

// В хуке onMounted:
const Graph = ForceGraph()
  (graphContainer.value) // Привязываем к div
  .graphData(data)       // Передаём { nodes: [], links: [] }
  .nodeCanvasObject((node, ctx, globalScale) => {
    // Важно: цвет/размер приходят готовыми с backend как viz_* поля.
    // UI только применяет эти ключи (без вычислений/градиентов на фронте).
    const colorKey = String((node as any).viz_color_key || node.type || 'person').toLowerCase();
    const size = (node as any).viz_size || { w: 12, h: 12 };
    const w = Math.max(6, Number(size.w) || 12);
    const h = Math.max(6, Number(size.h) || 12);

    const palette: Record<string, string> = {
      business: '#10b981', // emerald
      person: '#3b82f6',   // blue
      suspended: '#94a3b8',
      left: '#64748b',
      deleted: '#475569',
    };

    // Должники могут быть дискретно раскрашены (debt-0..8) — ключи задаёт backend.
    const fill = palette[colorKey] || (colorKey.startsWith('debt-') ? '#f59e0b' : '#3b82f6');
    ctx.fillStyle = fill;

    // Кастомная отрисовка
    if (node.type === 'business') {
      // 🟩 Рисуем КВАДРАТ
      ctx.fillRect(node.x - w / 2, node.y - h / 2, w, h);
    } else {
      // 🔵 Рисуем КРУГ
      ctx.beginPath();
      ctx.arc(node.x, node.y, Math.min(w, h) / 2, 0, 2 * Math.PI);
      ctx.fill();
    }
  })
  .linkWidth(link => link.selected ? 2 : 0) // Скрываем неактивные
  .linkColor(link => link.selected ? '#22d3ee' : '#64748b33')
  .onNodeClick(node => {
    store.selectNode(node); // Обновляем состояние через Pinia
  });
```

#### Доработка спецификации под производительность и “не перегружать экран”

Вместо бинарного `selected` вводим режимы визуализации + LOD (уровни детализации):

**Режимы:**
- `Overview` (по умолчанию):
  - узлы видны все;
  - связи — тонкие и спокойные (низкая альфа), чтобы читалась “структура”, но не было шума;
  - подписи выключены.
- `Focus` (выбор узла):
  - ярко рисуются только связи выбранного узла и ближайших соседей;
  - “фоновые” связи либо скрыты, либо почти прозрачные.
- `Clearing` (на время клиринга):
  - отображаются только участники и рёбра, задействованные в сценарии клиринга;
  - на фоне можно оставить еле заметный силуэт сети (опционально).

**LOD по зуму/нагрузке:**
- малый зум: тонкие линии, минимум эффектов, без текста;
- средний: подсветка окрестности + умеренный glow;
- большой: подписи (ограниченно) и дополнительные детали.

Критически важно: правила цвета/размера/толщины задаются не формулами на клиенте, а ключами `viz_*`.

#### Логика связей (Фильтрация)

```javascript
// По умолчанию все связи невидимы
links.forEach(link => link.selected = false);

// При клике на узел
function selectNode(nodeId) {
  links.forEach(link => {
    // Показываем только связи выбранного узла
    link.selected = (link.source.id === nodeId || link.target.id === nodeId);
  });
  Graph.graphData({ nodes, links }); // Перерисовываем
}
```

Доработка: вместо полной пересборки графа на каждый клик (что может быть дорого), рекомендовано:
- хранить `mode` и `selectedNodeId` в Pinia;
- менять только видимость/параметры через accessors (`linkWidth`, `linkColor`, `linkDirectionalParticles`) и вызывать `Graph.refresh()`.

### 6.3. Компонент №2: Карточка узла (HUD)

**Задача:** Показать детальную информацию при клике.

#### Реализация

```vue
<template>
  <Transition name="fade">
    <div v-if="selectedNode"
         class="absolute z-10 bg-slate-900/90 backdrop-blur-md border border-slate-700 rounded-xl p-4 shadow-2xl text-white"
         :style="{ top: cardY + 'px', left: cardX + 'px' }">
      
      <!-- Заголовок -->
      <div class="flex items-center gap-2 mb-3">
        <Building v-if="selectedNode.type === 'business'" class="text-emerald-400" />
        <User v-else class="text-blue-400" />
        <h3 class="font-bold">{{ selectedNode.name }}</h3>
      </div>

      <!-- Данные -->
      <div class="space-y-2 text-sm">
        <div>Баланс: <span class="text-cyan-400 font-mono">{{ selectedNode.balance }}₽</span></div>
        <div>Связей: <span class="text-slate-400">{{ selectedNode.links }}</span></div>
      </div>

      <!-- Кнопка закрытия -->
      <button @click="close" class="absolute top-2 right-2 text-slate-500 hover:text-white">
        <X :size="16" />
      </button>
    </div>
  </Transition>
</template>
```

#### Позиционирование карточки

```javascript
// Получаем экранные координаты узла
const screenCoords = Graph.graph2ScreenCoords(node.x, node.y);
cardX.value = screenCoords.x + 20; // Смещение вправо
cardY.value = screenCoords.y - 50; // Смещение вверх
```

### 6.4. Компонент №3: Анимация транзакций

**Задача:** Визуализировать движение денег по связям.

Обязательное уточнение (важно для протокола GEO и чтобы не «запереть» UI в тупик):
- транзакция может идти **по маршруту из нескольких рёбер** (multi-hop), поэтому визуальная метафора должна поддерживать список рёбер/узлов маршрута;
- клиринг — это **цикл долгов** (обычно 3+ узла, иногда 4–6+), поэтому эффекты должны уметь подсветить/проиграть цикл целиком, а не только один сегмент.

#### Вариант А: Частицы внутри графа

```javascript
// При запуске клиринга
Graph
  .linkDirectionalParticles(link => {
    return link.isClearing ? 4 : 0; // 4 частицы для активной связи
  })
  .linkDirectionalParticleSpeed(0.005)
  .linkDirectionalParticleWidth(2)
  .linkDirectionalParticleColor(() => '#a855f7'); // Purple-500
```

#### Вариант Б: Вспышка на весь экран

```vue
<template>
  <Transition name="flash">
    <div v-if="isClearingActive"
         class="fixed inset-0 bg-gradient-radial from-cyan-500/80 via-transparent to-transparent pointer-events-none z-50" />
  </Transition>
</template>

<style>
.flash-enter-active { transition: opacity 0.3s; }
.flash-leave-active { transition: opacity 0.6s; }
.flash-enter-from, .flash-leave-to { opacity: 0; }
</style>
```

### 6.5. Компонент №4: Звуковое сопровождение

**Задача:** Добавить аудио-слой для погружения.

```typescript
import { Howl } from 'howler';

// Инициализация звуков
const sounds = {
  ambient: new Howl({ src: ['/audio/space-ambient.mp3'], loop: true, volume: 0.3 }),
  click: new Howl({ src: ['/audio/ui-click.mp3'], volume: 0.5 }),
  clearing: new Howl({ src: ['/audio/electric-zap.mp3'], volume: 0.7 })
};

// Запуск фона при загрузке
onMounted(() => {
  sounds.ambient.play();
});

// Проигрывание по событиям
function onNodeClick() {
  sounds.click.play();
}

function onClearingTrigger() {
  sounds.clearing.play();
}
```

### 6.6. Компонент №5: Фоновые частицы

**Задача:** Создать эффект "космической глубины".

```vue
<template>
  <Particles
    id="tsparticles"
    :options="{
      background: { color: '#020617' },
      particles: {
        number: { value: 80 },
        color: { value: '#ffffff' },
        opacity: { value: 0.3 },
        size: { value: 1 },
        move: { speed: 0.2 }
      }
    }"
  />
</template>

<script setup>
import { loadSlim } from 'tsparticles-slim';
</script>
```

---

## 7. Финальный стек технологий

### 6.1. Принципы выбора

1. **Единство экосистемы** — использование Vue.js 3 для синхронизации с основным проектом (админка, PWA)
2. **Производительность** — Canvas вместо DOM-манипуляций для работы с 100+ объектами
3. **Минимализм зависимостей** — только критически необходимые библиотеки
4. **Атмосферность** — звук + частицы + кастомная графика

### 6.2. Сводная таблица стека

| Слой | Компонент | Технология | Обоснование |
|------|-----------|------------|-------------|
| **Framework** | Основа | **Vue.js 3** (Composition API) | Стандарт проекта. Composition API идеален для сложной логики симуляции |
| | Build Tool | **Vite** | Мгновенная сборка, HMR |
| | Язык | **TypeScript** | Строгая типизация данных графа критична для стабильности |
| | State | **Pinia** | Управление состоянием (выбранный узел, режим клиринга) |
| **Визуализация** | Граф | **force-graph** | Нативная библиотека (Vanilla JS) на Canvas. 60 FPS даже на 100+ узлах |
| | Физика | **d3-force** | Встроен в force-graph. Симуляция притяжения/отталкивания |
| **UI/UX** | Стилизация | **Tailwind CSS** | Быстрая верстка "стеклянных" панелей (backdrop-blur, glow) |
| | Иконки | **Lucide Vue** | Легкие векторные иконки |
| | Анимации | **Vue Transition** | Нативная система переходов для карточек |
| **Иммерсия** | Звук | **Howler.js** | Профессиональная работа с Web Audio API |
| | Частицы | **tsparticles** (slim) + `@tsparticles/vue3` | Фоновая "звёздная пыль" |
| **Backend** | API | **REST** (FastAPI) | Загрузка данных графа через `GET /graph/snapshot` |
| | Симуляция | **Client-side** | Физика рассчитывается в браузере |

### 6.3. Установка зависимостей

#### package.json (дополнительные зависимости)

```json
{
  "dependencies": {
    "force-graph": "^1.43.0",
    "howler": "^2.2.4",
    "@tsparticles/vue3": "^3.0.0",
    "tsparticles-slim": "^2.12.0",
    "lucide-vue-next": "^0.300.0"
  }
}
```

*Примечание: Vue 3, Vite, TypeScript, Tailwind CSS и Pinia уже присутствуют в основном проекте*

#### Команда установки

```bash
npm install force-graph howler @tsparticles/vue3 tsparticles-slim lucide-vue-next
```

### 6.4. Почему НЕ используются React-библиотеки

| React-библиотека | Почему не подходит | Vue-аналог |
|------------------|-------------------|------------|
| `react-force-graph-2d` | React-обертка, конфликт с Vue, раздутие бандла | `force-graph` (нативная версия) |
| `Framer Motion` | Работает только с React | `Vue Transition` + `@motionone/vue` (опционально) |
| `@mui/material` | Тяжелый UI-кит, React-only | `Tailwind CSS` (утилитарный подход) |

---

## 8. План разработки (PoC → MVP → Full)

### Этап 1: Proof of Concept (3-5 дней)

**Цель:** Доказать техническую осуществимость.

| Задача | Результат |
|--------|-----------|
| 1. Создать Vue-компонент `<SimulatorView />` | Пустой экран с подключенным Canvas |
| 2. Подключить `force-graph` | Отображение 20 тестовых узлов (круги) |
| 3. Реализовать кастомную отрисовку | Бизнес = квадрат, Физлицо = круг |
| 4. Добавить клик по узлу | `console.log(node.id)` |

**Критерий успеха:** Граф рисуется, узлы кликабельны, FPS > 55.

---

### Этап 2: MVP (1-2 недели)

**Цель:** Базовая работающая версия для демонстрации.

| Задача | Результат |
|--------|-----------|
| 5. Интеграция с API | Загрузка реальных данных из `/graph/snapshot` |
| 6. Всплывающая карточка | Показ данных узла при клике (Tailwind) |
| 7. Фильтрация связей | Видны только линии выбранного узла |
| 8. Фоновые частицы | Звёздный фон через `tsparticles` |
| 9. Базовые звуки | Клик + фоновый ambient |

**Критерий успеха:** Можно показать инвестору/партнёру. Есть "Вау-эффект".

---

### Этап 3: Full Version (2-3 недели)

**Цель:** Полнофункциональный модуль для продакшена.

| Задача | Результат |
|--------|-----------|
| 10. Анимация клиринга | Бегущие частицы + вспышка |
| 11. Звук разряда | Проигрывание при клиринге |
| 12. Режимы отображения | Переключатель "Обзор/Фокус/Клиринг" |
| 13. Мини-карта | Навигация по большому графу |
| 14. История действий | Лента транзакций справа |
| 15. Адаптивность | Работа на планшетах/мобильных |
| 16. Тесты | Unit-тесты на логику фильтрации |

**Критерий успеха:** Готово к внедрению в продакшн.

---

## Приложение А: Пример структуры компонента

```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue';
import ForceGraph from 'force-graph';
import { Howl } from 'howler';
import { useSimulatorStore } from '@/stores/simulator';
import { Building, User, X } from 'lucide-vue-next';

const store = useSimulatorStore();
const graphContainer = ref<HTMLElement | null>(null);
const selectedNode = ref(null);

// Звуки
const sounds = {
  click: new Howl({ src: ['/audio/click.mp3'], volume: 0.5 }),
  ambient: new Howl({ src: ['/audio/ambient.mp3'], loop: true, volume: 0.3 })
};

onMounted(async () => {
  // Загрузка данных
  const data = await store.loadGraphData();
  
  // Инициализация графа
  const Graph = ForceGraph()
    (graphContainer.value!)
    .graphData(data)
    .backgroundColor('#020617')
    .nodeCanvasObject((node, ctx) => {
      if (node.type === 'business') {
        ctx.fillStyle = '#10b981';
        ctx.fillRect(node.x - 6, node.y - 6, 12, 12);
      } else {
        ctx.beginPath();
        ctx.arc(node.x, node.y, 4, 0, 2 * Math.PI);
        ctx.fillStyle = '#3b82f6';
        ctx.fill();
      }
    })
    .onNodeClick(node => {
      sounds.click.play();
      selectedNode.value = node;
    });
  
  sounds.ambient.play();
});
</script>

<template>
  <div class="relative w-full h-screen bg-slate-950">
    <!-- Фоновые частицы -->
    <Particles id="bg" class="absolute inset-0 z-0" />
    
    <!-- Canvas графа -->
    <div ref="graphContainer" class="absolute inset-0 z-10" />
    
    <!-- Карточка узла -->
    <Transition name="fade">
      <div v-if="selectedNode" class="absolute z-20 top-20 right-4 bg-slate-900/90 backdrop-blur-md border border-slate-700 rounded-xl p-4 shadow-2xl">
        <div class="flex items-center gap-2 mb-3">
          <Building v-if="selectedNode.type === 'business'" class="text-emerald-400" />
          <User v-else class="text-blue-400" />
          <h3 class="font-bold text-white">{{ selectedNode.name }}</h3>
        </div>
        <div class="text-sm text-slate-300">
          <p>Баланс: <span class="text-cyan-400">{{ selectedNode.balance }}</span></p>
        </div>
      </div>
    </Transition>
  </div>
</template>
```

---

## Приложение Б: JSON структура данных

### Формат ответа API `/graph/snapshot`

```json
{
  "nodes": [
    {
      "id": "user_1",
      "name": "ООО Альфа",
      "type": "business",
      "balance": 1500,
      "links_count": 8,

      "viz_color_key": "business",
      "viz_size": { "w": 14, "h": 14 },
      "net_balance_atoms": "150000",
      "net_sign": 1
    },
    {
      "id": "user_2",
      "name": "Иван Петров",
      "type": "person",
      "balance": -300,
      "links_count": 3,

      "viz_color_key": "debt-3",
      "viz_size": { "w": 10, "h": 10 },
      "net_balance_atoms": "-30000",
      "net_sign": -1
    }
  ],
  "links": [
    {
      "source": "user_1",
      "target": "user_2",
      "amount": 500,
      "trust_limit": 1000,

      "viz_alpha_key": "bg",
      "viz_width_key": "thin"
    }
  ]
}
```

---

## 9. Принципы данных и производительности (как Admin UI)

### 8.1. Правило “визуальные поля приходят готовыми”

Чтобы избежать расхождений и просадок FPS:
- backend/офлайн-генератор вычисляет всё, что связано с:
  - балансом/долгом,
  - классами цвета,
  - масштабами размера,
  - приоритетами подсветки,
  - бинами/перцентилями.

Фронтенд применяет только:
- `viz_color_key` → палитра/паттерн,
- `viz_size` → размер примитива,
- `viz_*_key` для линий → ширина/альфа/цвет,
- события → анимация.

### 8.2. Анти-перегруз интерфейса

По умолчанию на экране одновременно:
- видны **без подписей** все узлы;
- активные связи (окрестность выделенного узла / событие) — заметные;
- остальные связи — “фоновые” (тонкие + низкая альфа), либо скрыты при сильном отдалении.

### 8.3. Деградация качества без лагов

При росте нагрузки снижать (в таком порядке):
1) число видимых рёбер (по LOD),
2) число частиц,
3) glow/blur,
4) подписи.

---

## 10. Контракт API для состояния, транзакций и клиринга

**Источник правды:** см. [docs/ru/simulator/frontend/docs/api.md](../api.md).

Коротко:
- Snapshot возвращает узлы/рёбра и готовые `viz_*` ключи (цвет/размер/альфа/ширина).
- Поток событий (SSE) отдаёт `tx.*` и `clearing.*` как готовые к анимации сценарии.
- Клиент не пересобирает граф на каждое событие: только overlays + `refresh()`.

---

## Заключение

Данная спецификация описывает полный цикл разработки игрового интерфейса симулятора GEO — от концепции до технической реализации. 

**Ключевые преимущества выбранного стека:**
- ✅ Единство с основным проектом (Vue.js 3)
- ✅ Высокая производительность (Canvas + нативные библиотеки)
- ✅ Минимальные зависимости (5 дополнительных пакетов)
- ✅ Иммерсивный опыт (звук + частицы + кастомная графика)

**Следующий шаг:** Запуск Proof of Concept (Этап 1).
